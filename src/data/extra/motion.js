// 进阶内容：motion 让世界动起来（motion-8/9/10）
export const lessons = [
  { id: 'motion-8', title: 'Time 与帧率' },
  { id: 'motion-9', title: '实例化 Instantiate' },
  { id: 'motion-10', title: '销毁与延迟调用' },
  { id: 'motion-11', title: '动手练：让小球滚动接金币' },
]
export const cards = {
  'motion-8': [
    { type: 'text', title: 'Time 与帧率', body: '帧率（FPS）就是「每秒钟画多少帧画面」。屏幕上的每一帧，Update 都会被调用一次——但帧率不是恒定的：手机可能掉到 30 帧，高端 PC 能稳定 144 帧。\n\n一、为什么不能在 Update 里「每帧加固定值」\n\ntransform.position += new Vector3(0.1f, 0, 0);   // 每帧走 0.1\n\n这段代码在 30 帧的机器上每秒走 3，在 60 帧的机器上每秒走 6——同一段代码，不同帧率下速度差一倍！这就是「帧率相关」的 bug。\n\n二、Time.deltaTime：把「每帧」换成「每秒」\n\ntransform.Translate(new Vector3(1f, 0, 0) * Time.deltaTime);\n\nTime.deltaTime 是「上一帧用了多少秒」。把它乘上速度，速度就变成「每秒走 1 米」：60 帧时每帧走 1/60，30 帧时每帧走 1/30，加起来每秒都是 1——无论帧率多少，世界里的运动速度一致。\n\n凡是基于时间的位移、旋转、动画，都要乘 Time.deltaTime，这是 Unity 的第一铁律。\n\n三、Update vs FixedUpdate\n· Update：每帧调用一次，间隔不固定（看帧率），适合输入响应、逻辑判断、UI 更新\n· FixedUpdate：按固定时间步长调用（默认每 0.02 秒一次，即每秒 50 次），不受帧率影响，物理引擎就是在这个频率上算的\n\n物理相关的操作（AddForce、velocity 赋值）放进 FixedUpdate 才稳定，因为物理每帧时长固定；放 Update 里给刚体加力，帧率不同力度就不同。\n\n四、Time 家族常用成员\n· Time.deltaTime —— 上一帧耗时（秒）\n· Time.fixedDeltaTime —— 固定步长（默认 0.02 秒）\n· Time.time —— 游戏开始后经过的总时间（秒），随暂停/缩放联动\n· Time.unscaledDeltaTime —— 不受缩放影响的 deltaTime\n· Time.timeScale —— 时间缩放：1 正常、0 完全暂停、0.5 半速慢动作\n\n五、Time.timeScale：暂停与慢动作\n\nTime.timeScale = 0f;   // 整个世界暂停\nTime.timeScale = 0.5f; // 半速慢动作\n\n暂停游戏（弹出菜单、结算画面）都靠它。注意：timeScale 不影响 Update 本身被调用，但 Time.deltaTime 会按缩放比例变小，所以乘了 deltaTime 的运动自然变慢或停住。想让 UI 动画无视暂停继续转，用 unscaledDeltaTime。\n\n一句话总结：Update 跟帧走、FixedUpdate 跟时间走；位移速度乘 Time.deltaTime 才与帧率无关；Time.timeScale 一键暂停慢放。' },
    { type: 'component', title: 'Time 与生命周期', items: [
      { name: 'Update', desc: '每帧调用，间隔不定，管逻辑与输入' },
      { name: 'FixedUpdate', desc: '固定步长（0.02 秒），物理运算的主场' },
      { name: 'Time.deltaTime', desc: '上一帧耗时，位移要乘它' },
      { name: 'Time.timeScale', desc: '时间缩放，0 暂停、0.5 慢放' },
      { name: 'Time.time', desc: '游戏运行总秒数' },
    ] },
    { type: 'quiz', question: '同一段移动代码，30 帧手机上每秒走 3 米，60 帧电脑上却每秒走 6 米。要让它速度一致，该怎么做？', options: ['把移动代码移到 FixedUpdate 里', '位移量乘上 Time.deltaTime', '限制游戏只能 30 帧运行'], answer: 1, tip: '乘 Time.deltaTime 后，位移量变成「每秒的米数」，帧率越高每帧走得越少、加起来每秒都一样，速度就和帧率无关了。' },
  ],
  'motion-9': [
    { type: 'text', title: '实例化 Instantiate', body: '实例化（Instantiate）就是「在游戏运行时，照着一份模板当场造出一个新物体」——模板就是 art-4 课讲的预制体（Prefab）。\n\n一、为什么需要 Instantiate\n场景里的物体是「开局就摆好的」。可子弹要打出去、敌人要一波波刷出来、金币要随机生成——这些是运行时才出现的东西，不能靠手摆，只能靠代码现场造。\n\n二、基本用法\n\npublic GameObject bulletPrefab;  // 在检查器里拖预制体进来\n\nvoid Shoot()\n{\n    Instantiate(bulletPrefab);   // 照着预制体克隆一个\n}\n\nInstantiate 返回新生成的物体，常见三种写法：\n\n// 1. 只克隆，新物体生成在世界原点，旋转归零\nInstantiate(prefab);\n\n// 2. 指定生成的位置和旋转\nInstantiate(prefab, new Vector3(0, 1, 0), Quaternion.identity);\n\n// 3. 接住返回值，还能指定父物体\nGameObject go = Instantiate(prefab, position, rotation);\n\nQuaternion.identity 是「不旋转」（朝向正方向）；想要有旋转，一般传 Quaternion.Euler(0, 90, 0) 这种。\n\n三、生成在枪口：炮口发射的完整套路\n\npublic GameObject bulletPrefab;\npublic Transform firePoint;   // 枪口空物体，挂到枪的末端\n\nvoid Shoot()\n{\n    // 在枪口位置、沿枪口朝向生成子弹\n    Instantiate(bulletPrefab, firePoint.position, firePoint.rotation);\n}\n\n让一个空物体挂在枪口当「出生点」，生成时直接用它和它的旋转——子弹自然从枪口朝正确方向飞出，这是最常用的生成姿势。\n\n四、生成后立刻改属性\n\nGameObject go = Instantiate(prefab, pos, rot);\ngo.name = \"子弹_1\";                       // 改名字\nBullet b = go.GetComponent<Bullet>();    // 拿组件\nb.speed = 30f;                           // 改速度\nDestroy(go, 3f);                         // 3 秒后自动销毁\n\n五、常见坑\n· 预制体没赋值：检查器里忘拖预制体，调用会报错。养成习惯：字段设为 public 拖进去\n· 生成位置不对：不传位置时生成在原点，别惊讶\n· 狂点生成不销毁：每帧 Instantiate 而不管理，内存暴涨。生成的东西要配套销毁\n\n一句话总结：Instantiate(预制体, 位置, 旋转) 在运行时按模板造物体——先拖预制体进字段，再指定生成点和朝向，生成后按需改属性、配销毁。' },
    { type: 'text', title: '批量生成与生命周期管理', body: '实战中很少「生成一个就不管」，更多是批量生成 + 配套管理。\n\n一、批量生成\n\nfor (int i = 0; i < 10; i++)\n{\n    Vector3 pos = new Vector3(Random.Range(-10f, 10f), 0, Random.Range(-10f, 10f));\n    Instantiate(coinPrefab, pos, Quaternion.identity);\n}\n\n一次撒 10 枚金币。注意随机生成时结合 procgen-3 的防重叠思路，别撒成一坨。\n\n二、生成在父物体下\n\nInstantiate(effectPrefab, transform);  // 特效挂到当前物体下，跟着移动\n\n掉落的装备、挂在角色身上的刀光，用这个重载，新物体自动成为子物体，父物体动它跟着动。\n\n三、生命周期要配套\n\n生成的东西必须「有始有终」：\n· 一次性特效：生成后几秒自动销毁（Destroy(go, 2f)）\n· 子弹：命中或飞出范围就销毁\n· 高频生成销毁：考虑对象池（perf-1 讲过），开局建一批、借了还\n\n四、一个常见误区\n\n每帧 Instantiate 又不销毁，内存一路涨，最后卡死——「生」和「灭」永远要配套想。\n\n一句话总结：批量生成用循环、想挂父物体用带 transform 的重载、生成后必须配销毁或回收，否则内存迟早爆掉。' },
    { type: 'quiz', question: '想让子弹从枪口飞出去、朝向也对，生成时应该怎么做？', options: ['Instantiate(bulletPrefab) 就行，Unity 自动放枪口', '用 Instantiate(prefab, 枪口.position, 枪口.rotation)，位置旋转都取自枪口', '先 Instantiate 再慢慢挪到枪口'], answer: 1, tip: '枪口是一个挂在枪末端的 Transform。位置用 firePoint.position、旋转用 firePoint.rotation，子弹一生成就在枪口、朝向也正确，一步到位。' },
  ],
  'motion-10': [
    { type: 'text', title: '销毁与延迟调用', body: '物体「活着」时会动会响，但总有「该死」的时候——子弹打中要消失、敌人死亡要消失、过场动画演完要清理。这一课把销毁和延迟调用的家底一次讲清。\n\n一、Destroy：最常用的销毁\n\nDestroy(gameObject);   // 销毁自己\n\nDestroy 的两个特点：\n· 延迟到「当前帧结束」才真正销毁 —— 这一帧内代码还能安全访问它，不会立刻崩\n· 可以在 Update、碰撞回调（OnCollisionEnter）、协程里随意调用\n\n常用的三种用法：\nDestroy(other.gameObject);        // 销毁别人（比如子弹销毁撞到的敌人）\nDestroy(gameObject, 2f);          // 2 秒后销毁自己（延迟销毁）\nDestroy(other.gameObject, 1f);    // 延迟销毁别的物体\n\n二、DestroyImmediate：立即销毁（慎用）\n\nDestroyImmediate(gameObject);\n\n它不等帧末，当场就删。运行时用它会引发各种诡异问题（正访问的组件瞬间没了），所以：\n· 运行时（游戏脚本）一律用 Destroy\n· 只有编辑器扩展代码里（Editor 文件夹、菜单工具、自定义窗口）才用 DestroyImmediate\n\n记住：游戏运行时别碰 DestroyImmediate，这是条纪律。\n\n三、Invoke：延迟调用方法\n\nInvoke(\"Explode\", 2f);          // 2 秒后调用 Explode 方法\nInvokeRepeating(\"SpawnEnemy\", 1f, 3f);  // 1 秒后开始，每 3 秒重复\n\n用法：\n· 第一个参数是方法名（字符串），方法必须写在当前脚本里\n· 方法名拼错不会编译报错，但运行时会静默失败——这是 Invoke 最大的坑\n· CancelInvoke() 取消所有；IsInvoking(\"方法名\") 查询是否在挂起中\n\nvoid Explode()\n{\n    Instantiate(boomEffect, transform.position, Quaternion.identity);\n    Destroy(gameObject);\n}\n\n四、协程 vs Invoke：谁更灵活\n\n两者都能「延迟做事」，但协程（prog-3 讲过）更强：\n· 协程能等任意条件、嵌套、中途退出，还能带参数；Invoke 只能按名字调方法\n· 协程写在代码里像流水账，可读性好\n· Invoke 的优点：写法极简，延迟一行的量\n\n规律：只延迟一下、不重复 → 用 Invoke 就够；要复杂流程（先等 1 秒再等玩家操作再继续）→ 用协程。\n\n五、一个隐蔽的坑\n\n物体被 Destroy 后，挂在它身上的协程、Invoke 全部失效——「物体没了，后面的活也别干了」是正常行为。想让某段逻辑在物体销毁后还能跑，把它挪到「活得长」的管理器（比如 GameManager）身上。\n\n一句话总结：运行时用 Destroy 等帧末销毁、编辑器才用 DestroyImmediate；延迟做事用 Invoke 图简单、用协程图灵活；生要配死，别让物体只生不死。' },
    { type: 'component', title: '销毁与延迟调用家族', items: [
      { name: 'Destroy', desc: '运行时销毁，帧末生效，可带延迟秒数' },
      { name: 'DestroyImmediate', desc: '立即销毁，编辑器专用' },
      { name: 'Invoke', desc: '延迟调用方法，按名字字符串' },
      { name: 'InvokeRepeating', desc: '延迟后按周期重复调用' },
      { name: 'CancelInvoke', desc: '取消所有挂起的延迟调用' },
      { name: '协程 WaitForSeconds', desc: '更灵活的延时，可带复杂流程' },
    ] },
    { type: 'quiz', question: '游戏脚本里想销毁一个物体，应该用哪个方法？', options: ['Destroy(gameObject)，等当前帧结束再销毁，安全', 'DestroyImmediate(gameObject)，立即销毁', '两者都一样，随便用'], answer: 0, tip: 'Destroy 延迟到帧末销毁，运行时安全；DestroyImmediate 当场删除，只该用在编辑器扩展代码里，运行时用它容易出诡异 bug。' },
  ],
  'motion-11': [
    { type: 'text', title: '动手练：让小球滚动接金币', body: '🎯 任务目标：做一个小球滚动接金币的小游戏——方向键让小球滚动，滚过金币金币就消失、分数 +1，金币由预制体批量生成。\n\n📦 前置要求：先学完物理与碰撞（motion-2）、输入控制（motion-3）、碰撞器 vs 触发器（motion-7）、Time 与帧率（motion-8）、实例化 Instantiate（motion-9）、销毁与延迟调用（motion-10），以及预制体 Prefab（art-4）。\n\n📋 动手步骤：\n\n第 1 步｜搭场景：新建 3D 场景。GameObject → 3D Object → Plane 做地面，Scale 设 (3, 1, 3)；GameObject → 3D Object → Sphere 做小球，Position 设 (0, 0.5, 0)。选中小球 Add Component → Rigidbody，它才受重力、能被推动。\n\n第 2 步｜确认地面有碰撞器：选中地面，检查器里确认有 Mesh Collider（Plane 默认自带）。有碰撞器小球才落得上去，否则直接掉穿。\n\n第 3 步｜写滚动控制脚本：新建 C# 脚本 BallController，拖到小球上。\n\npublic class BallController : MonoBehaviour\n{\n    public float speed = 10f;      // 推力大小，检查器里可调\n    private Rigidbody rb;\n\n    void Start()\n    {\n        rb = GetComponent<Rigidbody>();\n    }\n\n    void Update()\n    {\n        float moveX = Input.GetAxis(\"Horizontal\");   // A/D 或 ←/→\n        float moveZ = Input.GetAxis(\"Vertical\");     // W/S 或 ↑/↓\n        Vector3 force = new Vector3(moveX, 0f, moveZ) * speed;\n        rb.AddForce(force);                          // 每帧推一下\n    }\n}\n\n先按 Play 试一次：方向键能推动小球滚动，松键后球靠惯性滑一会儿——物理的正常手感。\n\n第 4 步｜做金币并设触发器：GameObject → 3D Object → Cube，Scale 缩成 (0.5, 0.5, 0.5) 当金币。选中它，检查器里把它身上的 Box Collider 的 Is Trigger 勾上——触发器只感应不阻挡，球才能「穿过并触发收集」。再给它建标签：检查器顶部 Tag → Add Tag → 新建 Coin 标签，把金币的 Tag 设成 Coin。\n\n第 5 步｜存成预制体：把做好的金币从层级面板拖进项目面板的 Assets 文件夹，它就变成预制体（名字带蓝色方块图标）。然后删掉场景里那个金币——场景里一个不留，后面全用代码从预制体生成。\n\n第 6 步｜写批量生成脚本：新建 C# 脚本 CoinSpawner，拖到一个空物体（GameObject → Create Empty）上。\n\npublic class CoinSpawner : MonoBehaviour\n{\n    public GameObject coinPrefab;      // 检查器里拖入金币预制体\n    public int count = 10;             // 要生成几个\n\n    void Start()\n    {\n        for (int i = 0; i < count; i++)\n        {\n            Vector3 pos = new Vector3(Random.Range(-6f, 6f), 0.5f, Random.Range(-6f, 6f));\n            Instantiate(coinPrefab, pos, Quaternion.identity);\n        }\n    }\n}\n\n把金币预制体拖进 CoinSpawner 的 coinPrefab 槽，count 填 10。\n\n第 7 步｜写收集计分脚本：新建 C# 脚本 CoinCollector，拖到小球上。\n\nusing UnityEngine;\nusing UnityEngine.UI;\n\npublic class CoinCollector : MonoBehaviour\n{\n    public int score = 0;\n    public Text scoreText;             // 拖入场景里的 UI 文字\n\n    void Start()\n    {\n        scoreText.text = \"得分: 0\";\n    }\n\n    void OnTriggerEnter(Collider other)\n    {\n        if (other.CompareTag(\"Coin\"))\n        {\n            score++;\n            scoreText.text = \"得分: \" + score;\n            Destroy(other.gameObject);   // 让这枚金币消失\n        }\n    }\n}\n\n第 8 步｜做计分 UI：GameObject → UI → Text 建文字，内容改「得分: 0」，摆到屏幕左上角；把场景里的 Text 拖进小球上 CoinCollector 的 scoreText 槽——这是「接线」，漏了会报空引用。\n\n第 9 步｜跑起来验收：按 Play，滚过金币——金币消失、分数 +1。想改金币大小/颜色？改 Assets 里的金币预制体（原版），所有已生成的副本一起变。\n\n✅ 验收清单（全部做到算过关）：\n能 □ 方向键能推动小球，小球受重力贴地滚动、不掉穿地面\n能 □ 场景里至少 10 枚金币，全是运行时从金币预制体批量生成的\n能 □ 小球滚过金币，金币消失、分数 +1\n能 □ UI 文字实时显示累计得分\n能 □ 改一下金币预制体的颜色或大小，重新 Play 后所有金币跟着变\n\n🕳 常见卡壳：\n· 滚过金币没反应 —— 八成是金币的 Is Trigger 没勾、Tag 没设成 Coin，或小球上没有 Rigidbody。触发器的触发条件：至少一方有 Rigidbody + 另一方是 Is Trigger。\n· 报 NullReferenceException —— CoinCollector 的 scoreText 没拖进 UI 文字，或 CoinSpawner 的 coinPrefab 没拖预制体。把检查器里的槽填满。\n· 生成的金币一落地就陷进地面 —— 生成位置 Y 太低。Instantiate 时位置 Y 给 0.5（Cube 半高），别直接给 0。' },
    { type: 'quiz', question: '想让小球「滚过金币时触发收集，而不会被金币弹开」，金币和小球该怎么设置？', options: ['金币勾 Is Trigger，小球带 Rigidbody，滚过时触发 OnTriggerEnter', '金币保持普通 Collider，小球带 Rigidbody，就能直接穿过去', '金币和小球都勾 Is Trigger，就能收集'], answer: 0, tip: '触发器只感应不阻挡：金币勾 Is Trigger 后球穿过去不弹开，再配合小球身上的 Rigidbody（触发条件：一方 Rigidbody + 一方 Is Trigger），滚过就触发 OnTriggerEnter 收集。' },
  ],
}
