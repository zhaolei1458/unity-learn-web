// 程序化生成（procgen）新增第 5/6/7 节：迷宫生成、波函数坍缩 WFC、分形与优化
export const lessons = [
  { id: 'procgen-5', title: '迷宫生成' },
  { id: 'procgen-6', title: '波函数坍缩 WFC' },
  { id: 'procgen-7', title: '分形与优化' },
  { id: 'procgen-8', title: '噪声进阶：Perlin/Simplex/Voronoi 与 FBM' },
  { id: 'procgen-9', title: '地形生成实战：山、水、植被与生态' },
  { id: 'procgen-10', title: '洞穴生成：算法、通道与房间' },
  { id: 'procgen-11', title: '城市生成：道路、建筑与街区' },
  { id: 'procgen-12', title: '生物群系：分区、过渡与边界' },
  { id: 'procgen-13', title: '道具与宝物分布：规则、稀有度与平衡' },
  { id: 'procgen-14', title: '对称与镜像生成' },
  { id: 'procgen-15', title: '手动与程序混合生成' },
  { id: 'procgen-16', title: '生成性能：多线程、缓存与分帧' },
  { id: 'procgen-17', title: '生成验证：可玩性、死路修复与保通关' },
  { id: 'procgen-18', title: '程序化动画：摆动与生长' },
  { id: 'procgen-19', title: '无限世界：Chunk、流式加载与种子' },
  { id: 'procgen-20', title: '程序化综合动手练：生成一座能玩的小城' },
]

export const cards = {
  'procgen-5': [
    { type: 'text', title: '迷宫生成', body: '上一课生成关卡是「撒平台」，这一课生成「连通的路」——迷宫。迷宫的本质要求：墙挡得住、路连得通、还不能有「死胡同环」。\n\n先把迷宫建模：\n· 最直观的模型：一个二维数组，每个格子存「这格有没有墙」。\n· 更好用的模型（生成算法都用它）：一个 grid，每个 cell 记「上/下/左/右四面墙哪面还在」。初始四面全有墙，算法「打洞」连通相邻格。\n\n四种经典算法，按「生成风格的差别」记：\n\n1. 深度优先回溯（Recursive Backtracker）——最常用\n\n步骤：从起点格开始 → 随机挑一个「未访问的邻居」，打掉中间的墙走过去 → 标记已访问 → 继续往前挖。挖到四面都是已访问格时，沿来路「回溯」，退到还有未访问邻居的格子继续。\n\n特点：一条路挖到底再回头，生成的迷宫「长通道多、分支绕」，有纵深感。用递归写会有爆栈风险（深挖几百层），工程上改用显式 Stack：\n\nStack<Cell> stack = new Stack<Cell>();\nCell current = grid[start];\ncurrent.visited = true;\nstack.Push(current);\n\nwhile (stack.Count > 0) {\n    Cell next = current.GetRandomUnvisitedNeighbor();\n    if (next != null) {\n        current.RemoveWallBetween(next);   // 打墙\n        next.visited = true;\n        stack.Push(next);\n        current = next;\n    } else {\n        current = stack.Pop();            // 回溯\n    }\n}\n\n2. 递归分割（Recursive Division）——反过来做\n\n把整块空地横一刀或竖一刀切成两半，墙上留一个口；再对两块各自继续切。特点：规整、像「格子间」，特别适合办公室、箱庭感地图。\n\n3. 随机 Prim（Randomized Prim）——像「水波扩散」\n\n从起点出发，把「当前连通区域边界上的墙」放进列表，随机抽一面墙，打通它对面的格。特点：分支均匀、无偏向，没有一条超长主路。\n\n4. Kruskal（并查集）——边随机排序\n\n把所有墙按随机权值排序，从「最随机的墙」开始，如果打通它不形成环（并查集判断），就打。特点：数学上完全均匀的完美迷宫。\n\n四种都生成「完美迷宫」——任意两点之间有且仅有一条路，没有环。\n\n四选一怎么选：\n· 要「探险感、主路分明」→ 深度优先回溯\n· 要「规整的室内房间感」→ 递归分割\n· 要「均匀无偏向」→ Prim 或 Kruskal\n\n起点终点怎么定（三种办法）：\n· 固定 —— 左上入口、右下出口，最简单\n· BFS 求「最远两点」—— 从任意点 BFS 算出最远点当起点，再 BFS 算离它最远的点当终点，迷宫就会「又长又好玩」\n· 指定 A、B 点后用 A*（aiadv-2 学过）检查确实连通，再放宝箱\n\n迷宫解法：\n· 左手贴墙法 —— 无脑简单，但可能绕远路\n· BFS / 洪水填充 —— 从起点逐层扩散，第一次到终点就是最短路\n· A* —— 比 BFS 更快，适合大迷宫\n\n常见坑：\n· 递归深挖爆栈 —— 用显式 Stack\n· 索引越界 —— 邻居判断前先查边界\n· 每次进游戏迷宫不一样，想复现 —— 用固定种子（procgen-1 学过 Random.InitState）\n\n一句话总结：迷宫生成 = 格子上「打墙」——深度优先回溯出长道、递归分割出格子间、Prim/Kruskal 出均匀迷宫，解法交给 BFS/A*；用种子锁复现、用 Stack 防爆栈。' },
    { type: 'quiz', question: '用深度优先回溯（Recursive Backtracker）生成的迷宫，在视觉上最显著的特点是什么？', options: ['所有通道一样长，非常均匀', '会有又长又绕的通道，偏向「一条主路挖到底再回头」的长走廊风格', '迷宫会生成很多环，有近路可抄'], answer: 1, tip: '深度优先回溯是「一路挖到死再回头」，所以主路极长、分支从主路旁生出来——这正是它和均匀的 Prim 风格最大的视觉差异。完美迷宫本身无环。' },
  ],
  'procgen-6': [
    { type: 'text', title: '波函数坍缩 WFC', body: 'Perlin 噪声能生成「连续渐变」，但生成不了「有局部规则」的东西——水管要能接上、道路要能相连、草丛旁边不能是水。这就是 WFC（Wave Function Collapse，波函数坍缩）的舞台：从一个「瓦片集 + 相邻约束」出发，生成「处处满足约束」的图案。\n\nWFC 名字吓人，其实是从量子力学的「叠加态坍缩」借来的比喻。核心概念四个：\n· 叠加（Superposition）—— 每个格子一开始「什么瓦片都有可能」，候选集是所有瓦片\n· 熵（Entropy）—— 格子的「不确定性」：候选越少，熵越低\n· 坍缩（Collapse）—— 挑一个格子，从候选里随机定一块瓦片，它的不确定没了\n· 传播（Propagation）—— 格子定了，向四周邻居「通知」：我定了这块，你们不能放跟它相邻冲突的瓦片，逐个排除\n\n完整流程（核心循环）：\n1. 找熵最低的格子（候选最少、最「差不多定局」的格子）——先处理它，矛盾概率最低\n2. 把它坍缩：按权重随机定一块瓦片\n3. 传播：把排除信息一层层传向邻居，邻居候选集变小，继续传\n4. 循环 1~3，直到所有格子都定下来——成功；或者某格候选集变空——矛盾（Contradiction），重来或换种子\n\n瓦片长什么样：每张瓦片自带「四个方向能接谁」的约束：\n\npublic class Tile {\n    public int id;\n    public float weight = 1f;         // 权重：越重越常被选中\n    public string up, right, down, left;   // 四边「接口名」\n}\n\n约束的两种来源：\n· 手工指定 —— 你写「草地的右接草、道路的右接口 road 只能接 road/路口」\n· 采样学习 —— 给一张「示例小图」（比如一张已画好的 10×10 道路图），WFC 自动统计「什么瓦片和什么瓦片相邻」的频率，转成约束。官方 WFC 工具包（如 Unity 的 WaveFunctionCollapse 资源）都支持\n\n实战：程序化生成一个「小镇街道」\n瓦片集：道路（直线、转弯、十字、丁字）、房屋、空地。\n· 约束设计好：道路的四边必须接道路或路口，房屋旁边不能是水\n· 指定「边界瓦片」：地图四周是空地，内部随机生成\n· 跑核心循环，出来的就是一张「道路连成网、房屋围街摆」的小镇地图——再根据每个格子的瓦片类型实例化 3D 模型（房屋模型、路面模型）\n\n和 Perlin 地形对比着记：\n· Perlin —— 生成「连续数值」，管地形高度、密度这类渐变\n· WFC —— 生成「离散瓦片 + 局部规则」，管道路、管道、拼接图案这类「必须接得上」的东西\n· 两者能混用：Perlin 定大格局（哪里是水、哪里是山），WFC 细化局部（山路上的路怎么接）\n\n常见坑：\n· 瓦片集不全，频繁矛盾重来 —— 检查有没有「漏掉的相邻组合」，补瓦片或放宽约束\n· 权重没设，满地都是最丑的瓦片 —— 稀有瓦片（喷泉、转角）权重调低\n· 约束方向搞反 —— up/right/down/left 约定好后，所有瓦片统一按同一套写\n· 想让同一张地图可复现 —— 用固定种子\n\n一句话总结：WFC = 每个格子「候选集」+ 最小熵优先坍缩 + 邻居约束传播——把「必须接得上」的规则交给系统，程序化生成道路、城镇、水管这类「有结构」的地图。' },
    { type: 'quiz', question: 'WFC 里，某个格子被确定放下一块「道路」瓦片后，为什么紧跟着要去更新它邻居的候选列表？', options: ['为了视觉效果更均匀', '因为这块瓦片限定了邻居能放哪些瓦片（比如道路右边必须接道路或路口），不传播后面一定会矛盾', '为了加快生成速度，可以跳过部分格子'], answer: 1, tip: '传播（Propagation）是 WFC 的关键：瓦片之间的相邻约束会逐格排除掉不可能的候选，把「某格定了」的影响传到整片区域——少了这一步，后面必然到处矛盾。' },
  ],
  'procgen-7': [
    { type: 'text', title: '分形与优化', body: '最后一课，把「自相似」玩明白——分形（Fractal）：整体和局部长得一样，山的一角放大还是山，树的一枝放大还是一棵树。分形做树木、海岸线、云，顺手把生成性能也一并解决。\n\n一、递归分形：一棵树，一个函数\n\n树的画法：主干 → 分两根枝 → 每根枝再分两根……每层都一样。用递归写：\n\nvoid DrawBranch(Vector3 pos, Vector3 dir, float len, int depth) {\n    if (depth <= 0) return;                 // 深度到顶就停\n    Vector3 end = pos + dir * len;\n    DrawLine(pos, end);                     // 画主干这段\n    Vector3 left = Quaternion.Euler(0, 0, 30) * dir;   // 左偏 30 度\n    Vector3 right = Quaternion.Euler(0, 0, -30) * dir; // 右偏 30 度\n    DrawBranch(end, left, len * 0.7f, depth - 1);      // 递归，每次短一截\n    DrawBranch(end, right, len * 0.7f, depth - 1);\n}\n\n深度就是「细节量」：depth=6 已经是一棵像样的树。加点随机（每层角度、长度乘个 0.9~1.1 的随机），每棵树都不一样。\n\n二、L 系统：字符串重写\n\nL 系统（Lindenmayer System）把「生长」写成字符串替换：\n· 公理（Axiom）—— 初始字符串，如 F（往前走一步）\n· 产生式规则（Rules）—— 每次迭代把 F 替换成更复杂的串，如 F → F+F--F+F\n· 迭代 N 次，得到一长串指令\n· 再解释执行：F 前进、+ 左转 30 度、- 右转 30 度\n\n迭代过程：\n第 0 代：F\n第 1 代：F+F--F+F\n第 2 代：F+F--F+F + F+F--F+F -- F+F--F+F + F+F--F+F\n\n字符串每代爆炸式变长，画出来就是分形几何（Koch 雪花就是它）。改变产生式，就是完全不同的植物/图案——这是程序化植物建模的经典手法。\n\n三、树木、海岸线、山脉：三样大自然\n· 树 —— 递归/L 系统生成枝条骨架，末端加叶子\n· 海岸线 —— Perlin 噪声 + 多层叠加（FBM，procgen-2 提过），把「陆地/海水」阈值线一取，就是弯弯曲曲的海岸\n· 山脉 —— FBM 噪声 + 高度阈值分层着色（低处绿、高处白）\n\n四、性能：分形一放开就爆炸\n\n分形「无限细节」既是魅力也是灾难，几个硬指标：\n· 顶点爆炸 —— 深度 10 的树有几百万条线，生成时当场卡死。限制深度、分支数，用 LOD\n· 生成时机 —— 全部放 Start/Awake 一次性生成（procgen-3 说过），绝不每帧生成\n\n五、多线程与 Job：把生成挪到后台\n\n大量顶点计算（噪声地形、分形网格）单线程太慢，用 Job System（ecs-3 学过）：\n· 把「算顶点」的纯数据计算放进 IJobParallelFor，多线程并行\n· 关键规矩：Job 里不能碰 Unity API（Instantiate、GetComponent），只能碰 NativeArray 这类原生容器——把顶点数据算完，回到主线程再一次性建 Mesh/Instantiate\n· 配合 Burst 编译，几百万顶点几秒算完\n\n六、缓存与复用：同样的东西别生成两遍\n\n· 结果缓存 —— 以「种子+区域坐标」为 key 存生成结果，玩家再次访问同一区域直接取缓存，不重新算\n· 保存资产 —— 把生成好的 Mesh 存成 .asset，编辑器里生成一次，运行时加载即可\n· 合并网格 —— 一棵树几十个 Mesh 会拖垮 Draw Call（perf-2 学过），用 Mesh.CombineMeshes 合并成一块，Draw Call 从几十降到 1\n\n七、常见坑汇总：\n· 递归爆栈/顶点爆炸 —— 限制深度和分支，加 LOD\n· 多线程里碰 Unity API 崩溃 —— 数据归数据、实例化归主线程\n· 重复生成同一区域卡顿 —— 缓存 + 种子\n\n一句话总结：分形用「自相似+递归/L 系统」做树木海岸线，但无限细节必须用「限制深度 + 多线程算数据 + 缓存结果 + 合并网格」四件套管住——否则生成本身就成了新的卡顿源。' },
    { type: 'quiz', question: 'L 系统（Lindenmayer System）里，「F → F+F--F+F」这种产生式规则的作用是什么？', options: ['每迭代一次，把字符串里的 F 替换成右侧的串，迭代次数越多指令串越复杂，画出的分形越精细', '直接把 F 画成一条线，规则没有作用', '用来控制相机视角'], answer: 0, tip: 'L 系统的核心就是字符串重写：公理 + 产生式迭代，一串变一长串，解释执行就得到自相似的分形结构（Koch 雪花就是它画的）。规则决定「长什么样」，迭代次数决定「精细度」。' },
  ],
  'procgen-8': [
    { type: 'text', title: '噪声进阶：Perlin/Simplex/Voronoi 与 FBM', body: '这一课把「噪声」这块地基打牢——上一课只用了 Mathf.PerlinNoise 一种，这一课认识三种常用噪声，再学一个把它们叠加成复杂地形的万能手法 FBM。\n\n一、三种噪声，各管一摊\n\n1. Perlin 噪声（Perlin）\n最经典，输出连续的渐变值，一片一片的云感。特点：没有明显的「格子感」，适合做地形高度、云朵、大理石。Unity 内置：Mathf.PerlinNoise(x, y)，返回 0~1 的浮点。\n\n2. Simplex 噪声（Simplex）\nPerlin 的升级版：计算更快、高维度（3D/4D）无方向性伪影，效果更均匀。Unity 没有内置 Simplex，要自己写或用插件（如 FastNoiseLite，最常用的开源库，一个 .cs 文件搞定）。\n\n3. Voronoi 噪声（细胞噪声）\n不是渐变，是「细胞」——随机撒一堆点，每个像素取「离它最近的点」的距离，形成蜂窝 / 晶格。做石头裂缝、鳞片、科幻面板、龟裂地面。FastNoiseLite 里的 Cellular 就是它。\n\n4. 还有一种常用的是 Value 噪声——纯随机值插值，更「软」的块状感，做洞穴、云层密度常配着用。\n\n二、FBM：把噪声「叠成山」\n\n单层噪声太平，现实地形是「大山包小山、小山包碎石」。FBM（Fractal Brownian Motion，分形布朗运动）就是多层噪声叠加：每层频率翻倍、振幅减半，越叠越细。\n\nC# 实现（2D 高度采样）：\n\nfloat Fbm(float x, float y, int octaves)\n{\n    float value = 0f;\n    float amp = 0.5f;      // 振幅，从 0.5 起步\n    float freq = 1f;       // 频率\n    float sum = 0f;\n    for (int i = 0; i < octaves; i++)\n    {\n        value += Mathf.PerlinNoise(x * freq, y * freq) * amp;\n        sum += amp;\n        amp *= 0.5f;       // 每层减半\n        freq *= 2f;        // 每层加倍\n    }\n    return value / sum;    // 归一化到 0~1\n}\n\noctaves 就是细节层数：4~6 层已经很好，8 层以上性价比骤降。频率乘数（lacunarity）默认 2、衰减（gain）默认 0.5，调它们能改变地形性格——lacunarity 小地形更平缓，gain 大细节更明显。\n\n三、不同噪声做不同事（对照着记）\n· 地形高度 → Perlin / Simplex + FBM\n· 云朵、雾密度 → Perlin / Value\n· 石头纹理、细胞状 → Voronoi\n· 海底沙丘、沙纹 → Simplex 多层\n\n四、常见坑\n· PerlinNoise 只有一个 2D 版本，想要 3D 体积噪声（云的立体分布）得用 Simplex / FastNoiseLite\n· FBM 忘了归一化，高度全挤在 0.3~0.5 —— 除以 sum\n· octaves 开太高，生成慢得怀疑人生 —— 4~6 层够用\n· 每次进游戏山不一样 —— 给噪声传入一个随机偏移（种子），想复现就固定它\n\n一句话总结：Perlin 渐变、Simplex 快且三维无伪影、Voronoi 出细胞；FBM 用「频率翻倍、振幅减半」的多层叠加把单层噪声堆成山峦起伏——这是程序化地形的万能起手式。' },
    { type: 'component', title: '噪声家族一览', items: [
      { name: 'Perlin', desc: '连续渐变，地形高度、云、大理石的首选' },
      { name: 'Simplex', desc: 'Perlin 升级版：快、3D 无方向伪影，需第三方库' },
      { name: 'Voronoi', desc: '细胞 / 晶格结构，石头、鳞片、科幻面板' },
      { name: 'Value', desc: '纯随机插值，更软的块状，做洞穴、云层密度' },
      { name: 'FBM', desc: '多层叠加手法：频率翻倍、振幅减半，叠出山峦' },
    ] },
    { type: 'quiz', question: 'FBM（分形布朗运动）把多层噪声叠加在一起，它的核心做法是什么？', options: ['每层频率翻倍、振幅减半，把大起伏和小细节叠加起来，越叠越细', '把同一层噪声复制几份，让地形更高一点', '用随机数直接生成高度，不用噪声'], answer: 0, tip: 'FBM = 多层噪声：频率加倍管「细节变细」、振幅减半管「大层盖小层」，叠加后归一化。单层 Perlin 太平，叠几层才有山的层次。' },
  ],
  'procgen-9': [
    { type: 'text', title: '地形生成实战：山、水、植被与生态', body: '噪声会了，这课把噪声变成「能踩的地形」——高度图 → Mesh → 按高度和湿度分生态，一气呵成。\n\n一、从高度图到 Mesh\n\n核心是「把噪声值变成顶点 Y」：\n\nint size = 64;\nVector3[] verts = new Vector3[size * size];\nfor (int z = 0; z < size; z++)\n    for (int x = 0; x < size; x++)\n    {\n        float h = Fbm(x * 0.05f, z * 0.05f, 5);   // 上一课学的 FBM\n        verts[z * size + x] = new Vector3(x, h * 40f, z);\n    }\n// 三角形索引：每格两个三角，按行列填（网格构建基本功）\n\n两个关键点：\n· 采样密度（0.05 乘数）决定「一座山占几格」，调小山峰更大\n· 高度乘数（40f）决定「山多高」\n\n二、按高度分生态（水的分割线）\n\n给每个顶点一个颜色，高度分段：\n\nfloat h = verts[i].y / maxHeight;      // 归一化 0~1\nColor c;\nif (h < 0.35f) c = water;       // 水\nelse if (h < 0.4f) c = sand;    // 沙滩\nelse if (h < 0.7f) c = grass;   // 草地\nelse if (h < 0.85f) c = rock;   // 岩石\nelse c = snow;                  // 雪顶\n\n这就是「按高度分层」：水 → 沙滩 → 草地 → 岩石 → 雪。想让岸线弯弯绕绕不像一刀切，在阈值附近加一点噪声抖动。\n\n三、按湿度分植被（生态的第二个轴）\n\n高度管「哪里是水哪里是山」，湿度管「草多还是沙多」。湿度 = 另一套 FBM（seed 不同）。\n\n按 (高度, 湿度) 查表分「生态带」（biome）：\n· 低矮 + 湿润 → 森林，密集种树\n· 低矮 + 干燥 → 草原 / 沙漠，稀疏\n· 中高 → 岩石带\n\n植被放置：遍历每个格子，按生态带给「种树概率」，用随机数 + 概率决定种不种，再加「树距」限制避免树叠树。\n\n四、性能与常见坑\n· 三角索引填错，地形出现「撕裂 / 洞」——先用小 size（比如 8）验证网格逻辑\n· 高度没归一化，全挤在一段 —— FBM 记得除以 sum\n· 水在网格上还是「凹陷」而非「平面水面」——水面单独放一个透明 Plane，把低于阈值的顶点压平或拉低\n· 大规模生成卡顿 —— 放到协程 / Job 里分帧生成（procgen-16 专门讲）\n\n一句话总结：地形 = 高度图（FBM）→ 顶点 Y → 按高度分水/沙/草/岩/雪、按湿度分植被密度——两个轴一交叉，生态就活了。' },
    { type: 'component', title: '高度分区配色表', items: [
      { name: '0~0.35 水', desc: '低于海平面，主体是水' },
      { name: '0.35~0.4 沙滩', desc: '水位线附近的过渡带' },
      { name: '0.4~0.7 草地', desc: '宜居带，植被最密' },
      { name: '0.7~0.85 岩石', desc: '高地，植被稀疏' },
      { name: '0.85~1.0 雪顶', desc: '山巅，白' },
    ] },
    { type: 'quiz', question: '要判断一个格子里是「水」还是「陆地」，最直接的做法是什么？', options: ['看它的湿度噪声值高不高', '看它的高度值有没有超过「海平面阈值」——高于阈值是陆地，低于是水', '随机抛硬币决定'], answer: 1, tip: '水陆分界本质是「高度阈值」：低于海平面是水、高于是陆地。湿度管的是同一片陆地上草多还是沙多（植被分布），管不了水陆。' },
  ],
  'procgen-10': [
    { type: 'text', title: '洞穴生成：算法、通道与房间', body: '洞穴和山不一样：山是「实心的地形」，洞穴是「挖空的洞」。这课用细胞自动机（Cellular Automata）生成自然感的洞穴，再把洞连成能玩的迷宫。\n\n一、细胞自动机：像「细胞生长」一样生成洞穴\n\n思路：先随机撒墙，再反复按规则「进化」几次，墙会聚成连片的洞穴结构。规则就两条：\n· 一个墙格，如果周围墙太少（<4），它变成洞——孤立的墙被淘汰\n· 一个洞格，如果周围墙太多（>5），它变成墙——细碎的空洞被填平\n\nC# 核心（简化）：\n\nbool[,] map = new bool[width, height];   // true = 墙\n// 1. 随机初始化：40% 概率是墙\nfor (int y = 0; y < height; y++)\n    for (int x = 0; x < width; x++)\n        map[x, y] = Random.value < 0.4f;\n\n// 2. 迭代 5 次\nfor (int it = 0; it < 5; it++)\n{\n    bool[,] next = new bool[width, height];\n    for (int y = 0; y < height; y++)\n        for (int x = 0; x < width; x++)\n        {\n            int walls = CountWalls(map, x, y);   // 统计 8 邻域墙数\n            next[x, y] = map[x, y]\n                ? (walls >= 4)          // 墙：周围墙多才留\n                : (walls >= 5);         // 洞：周围墙太多才堵\n        }\n    map = next;\n}\n\n初始墙密度（0.4）、迭代次数（5）、邻域阈值，三个旋钮决定洞穴「是开阔大厅还是蜿蜒隧道」——墙密度越低越开阔。\n\n二、保证连通：洞穴要「通」\n\n细胞自动机生成的洞经常是「几个互不相连的洞穴区」。连通是关键：\n1. 用 Flood Fill（BFS）找出所有「洞穴区」\n2. 把每块区的边界格子记下来\n3. 从一块区出发，往最近的另一块区「挖一条通道」连过去，直到全部连通\n\n挖通道的方式：从 A 区边界格到 B 区边界格，用直线扫描线（先横后竖）把经过的墙全部挖掉，简单粗暴有效。\n\n三、放入口、出口、房间\n· 入口/出口：选两个相距最远的洞穴区边界格（BFS 算最远，procgen-5 学过）\n· 房间：生成洞穴前先「挖几个大房间」，再做细胞自动机，房间自然嵌在洞里；或者生成后手动把几个指定位置的空地扩大成房间\n\n四、3D 化思路\n2D 洞穴矩阵可以竖直拉伸成「洞穴通道」，或者做真正的 3D 洞穴：用 3D 细胞自动机（bool[,,]），再 Marching Cubes（体素转网格算法）生成 Mesh。2D 先玩通，3D 是它的自然延伸。\n\n五、常见坑\n· 迭代次数太多洞穴被「磨平」——4~6 次足够\n· 忘记处理边界（地图边缘当墙），洞穴靠边全被堵死\n· 通道挖太窄（一格宽），玩家卡住 —— 至少两格宽\n\n一句话总结：洞穴 = 随机撒墙 + 细胞自动机迭代聚成洞穴，再用 Flood Fill 分区 + 扫描线挖通道保证连通，最后定出入口和房间——三个步骤顺序做，洞就活了。' },
    { type: 'quiz', question: '细胞自动机生成洞穴时，为什么要迭代好几轮而不是随机撒一次墙就完事？', options: ['迭代能让墙聚成连片的洞穴结构，去掉孤立碎块和细碎空洞，效果才自然', '迭代是为了让洞穴更小', '迭代是为了增加随机性，让每次都不重样'], answer: 0, tip: '单次随机撒墙是「噪点」，细胞自动机的迭代规则会淘汰孤立墙、填平碎洞，让结构聚集成连片的洞穴空间——这才是它和纯随机最大的区别。' },
  ],
  'procgen-11': [
    { type: 'text', title: '城市生成：道路、建筑与街区', body: '城市生成的关键词是「网格」：道路网格 → 地块 → 建筑。把大城市拆成这四步。\n\n一、道路网格（Road Grid）\n\n城市骨架是路。最简单的路网：规整的方格网（Grid），每条路横平竖直。\n· 主干道：大间距（如每 4 个街区一条宽路）\n· 次干道：小间距细分\n· 加一点「随机抖动」：每条路的位置在基线附近 ±小偏移，方格网就不再呆板\n· 想更自然：用「等距离线」（从一个中心向外扩张，间距受噪声扰动）生成环状放射路网，像真实古城\n\n二、地块（Lot）划分\n\n路网把城市切成一块块「街区」，每块再切给建筑：\n· 方块街区直接按尺寸均分，给每栋楼分一块地\n· 地块大小 = 街区大小 / (每边楼数)\n\n三、建筑放置与类型\n\n每块地「放什么」由规则决定：\n· 按地块位置：市中心高密度（高楼）、郊区低密度（矮房）\n· 按噪声分区：一块区域的「繁荣度」噪声高 → 楼高、密度大\n· 按功能分区：商业 / 住宅 / 工业，用另一张噪声或手绘地图分\n\n放置逻辑：\nfloat density = GetDensityNoise(x, z);\nif (Random.value < density)\n    SpawnBuilding(lot, height: Mathf.Lerp(2f, 30f, density));\n\n楼高也跟繁荣度走，城市天际线才有起伏。\n\n四、细节与道具\n· 路灯 / 树：沿路每隔固定间距放一个，注意避开门和路口\n· 路口：两条路交叉处放红绿灯 / 环岛模型\n· 水面 / 公园：用噪声划几块「无建筑区」放公园，城市立刻透气\n\n五、常见坑\n· 道路没对齐，建筑穿模 —— 路网先把坐标对齐到整数格再生成\n· 楼和楼重叠 —— 地块划分要保证每个 lot 独立，放置前用包围盒检查\n· 城市太平 —— 繁荣度噪声 + 楼高联动，天际线才有起伏\n· 一生成卡死 —— 分块生成 + 缓存（procgen-16）\n\n一句话总结：城市 = 道路网格定骨架（主干道+抖动）→ 路网切地块 → 按繁荣度/功能放建筑——街道对齐、楼高联动，城市就有模有样。' },
    { type: 'quiz', question: '程序化城市里，「市中心高楼林立、郊区矮屋稀落」的天际线效果，最自然的做法是什么？', options: ['给每栋楼随机一个高度，没有规律', '用一张繁荣度噪声，把楼的高度和密度都跟它挂钩——噪声高的市中心楼又高又密', '全部生成两层小楼'], answer: 1, tip: '用噪声 / 参数驱动楼高和密度，城市的天际线才有「中心高、外围矮」的自然起伏，这是程序化城市质感的关键。' },
  ],
  'procgen-12': [
    { type: 'text', title: '生物群系：分区、过渡与边界', body: '第 9 课把生态按高度分，这课更系统：用「温度 + 湿度」两个轴把整个世界切成生物群系（Biome），并处理好区与区之间的过渡。\n\n一、双轴划分（Whittaker 图）\n\n真实生态不是单一因素决定的：温度高不一定沙漠，还得看水够不够。经典做法用两张噪声：\n· 温度噪声（temperature）：纬度 / 高度影响\n· 湿度噪声（humidity）：降水 / 蒸发的模拟\n\n两个值查表：\n· 高温 + 高湿 → 热带雨林\n· 高温 + 低湿 → 沙漠\n· 低温 + 高湿 → 苔原 / 针叶林\n· 低温 + 低湿 → 冰原\n\nC# 判定（简化）：\n\nstring GetBiome(float t, float h)\n{\n    if (t > 0.7f && h > 0.6f) return \"Rainforest\";\n    if (t > 0.7f) return \"Desert\";\n    if (t < 0.3f && h > 0.5f) return \"Tundra\";\n    if (t < 0.3f) return \"Ice\";\n    return h > 0.5f ? \"Forest\" : \"Grassland\";\n}\n\n二、区域过渡：别一刀切\n\n双轴噪声直接在格子边界处跳变，会看到「森林旁突然是沙漠」的硬边界，很假。三种软化手法：\n1. 差值（Blend）：在群系边界带上，用权重在两个群系的颜色 / 植被密度之间 Lerp\n2. 渐变带：把「区域边界」也画成一张图，越靠中心越纯、越靠边越混\n3. 采样扰动：判定阈值处加一点噪声，边界线就弯弯曲曲、没那么直\n\n三、生物群系决定「长什么」\n\n每个群系配置一套「生成规则」：地面颜色、植被类型和密度、地表材质、动物种类。\n\n// 数据结构化：每个群系一个配置\npublic class BiomeConfig {\n    public string name;\n    public Color groundColor;\n    public float treeChance;\n    public GameObject treePrefab;\n    public float snowCover;    // 雪盖程度\n}\n\n遍历格子时：查格子的 (温度, 湿度) → 拿到 BiomeConfig → 按配置上色、种树。数据和生成逻辑分离，加新群系 = 加一条配置，不用改代码。\n\n四、边界处理的三条经验\n· 硬边界只适合「岩浆海 / 虚空」这种本来就要断的\n· 软过渡先改颜色 / 密度，再慢慢改植被，视觉才舒服\n· 跨大区域（雪山 ↔ 沙漠）用多层噪声过渡带，别指望一帧到位\n\n五、常见坑\n· 温度和湿度用同一个种子，导致「热的地方一定湿」——两张噪声必须独立\n· 边界抖动过大，群系糊成一团 —— 扰动幅度控制在小范围\n· 配置写死在代码里，想加新群系要改代码 —— 用配置表 / ScriptableObject\n\n一句话总结：生物群系 = 温度 + 湿度两个独立噪声查表分区，边界用差值、渐变带、噪声抖动软化，每个群系配一套生成规则——数据和逻辑分离，世界就活了。' },
    { type: 'component', title: '生物群系查表（温度 × 湿度）', items: [
      { name: '高温 + 高湿', desc: '热带雨林：树密、色深' },
      { name: '高温 + 低湿', desc: '沙漠：植被极稀疏' },
      { name: '低温 + 高湿', desc: '苔原 / 针叶林：耐寒植被' },
      { name: '低温 + 低湿', desc: '冰原：无植被、雪盖' },
      { name: '中温 + 高湿 / 低湿', desc: '森林 / 草原，过渡带主战场' },
    ] },
    { type: 'quiz', question: '为什么划分生物群系要用「温度 + 湿度」两个轴，而不是只看温度？', options: ['因为温度噪声不好生成', '因为同一个温度下，湿度高低会得到完全不同的生态——比如高温高湿是雨林，高温低湿是沙漠', '湿度只影响天气不影响生态'], answer: 1, tip: 'Whittaker 生态图的精髓就是双轴：温度定「冷热」，湿度定「干湿」，两轴交叉才分得出雨林和沙漠的区别。只凭温度会把沙漠和雨林混为一谈。' },
  ],
  'procgen-13': [
    { type: 'text', title: '道具与宝物分布：规则、稀有度与平衡', body: '有了地形、洞穴、城市，该往里面撒「宝物」了。这课解决三件事：怎么随机撒、怎么分稀有度、怎么保证平衡。\n\n一、权重随机：不是所有道具概率一样\n\n铜币满地、神器万里挑一。实现方式：给每个道具一个权重（weight），随机数落在谁的区间就选谁。\n\n// 按权重随机选一个\npublic static T PickWeighted<T>(List<(T item, float weight)> pool)\n{\n    float total = 0f;\n    foreach (var p in pool) total += p.weight;\n    float roll = Random.value * total;\n    float acc = 0f;\n    foreach (var p in pool)\n    {\n        acc += p.weight;\n        if (roll <= acc) return p.item;\n    }\n    return pool[pool.Count - 1].item;\n}\n\n调整权重的直觉：普通药水 weight=50、金币 30、稀有武器 3、神器 0.5——稀有度用「几十倍」的权重差，而不是微调。\n\n二、放置规则：不是哪儿都能放\n\n撒之前先过「放置检查」：\n· 地形检查：只能放陆地、不能放水里、不能放悬崖顶\n· 可及性：离出生点至少一定距离（新手区不放神器）、离危险区（岩浆 / 深洞）至少一格\n· 互不重叠：跟已放置的宝物保持最小间距，用网格记录已占位\n\nif (CanPlace(x, z) && grid[x, z] == 0 && Random.value < chance)\n{\n    SpawnItem(weightedPick, x, z);\n    grid[x, z] = 1;\n}\n\n三、平衡：稀有度落地要可验证\n\n权重配好了，但「世界到底有几个神器」得算得出来：\n· 期望数量 = 总放置点 × 该道具权重占比。想整张图刚好 3 个神器，就把「神器槽位」单独留出来：先保证放 3 个，其余位置填普通道具\n· 保底机制：玩家探索了 80% 还没见到稀有道具？按进度补一个——防止「脸黑一整局」\n· 检验脚本：生成后遍历统计各稀有度数量，打印日志 / 用断言，超范围就重生成\n\n四、常见坑\n· 权重全相等，神器满天飞 —— 权重拉开数量级差\n· 只写随机不放检查，宝物掉水里 / 卡墙里 —— 放置检查不能省\n· 想复现分布却变样 —— 生成统一用一个种子\n· 稀有度不可控，测试时找不到道具 —— 做「保底 + 统计」\n\n一句话总结：宝物分布 = 权重随机定「出什么」（权重差拉开稀有度）+ 放置规则定「能不能放」+ 保底与统计定「平衡性」——三件套齐了，寻宝才好玩。' },
    { type: 'quiz', question: '想让「神器」在一张地图里刚好出现 3 个、而不是靠运气随机，最稳的做法是？', options: ['把神器的权重设得极大，它就会出现很多', '把神器放进权重随机池里碰运气', '单独预留 3 个「神器槽位」固定生成神器，其余槽位走权重随机放普通道具'], answer: 2, tip: '权重随机管的是「这个位置大概率出什么」，管不了总数。要精确控制数量，就单独留固定槽位，其余位置再走权重随机——「数量保证 + 种类随机」才是平衡的做法。' },
  ],
  'procgen-14': [
    { type: 'text', title: '对称与镜像生成', body: '有些关卡设计讲究「对称」——两边一模一样，视觉整齐、难度均衡（比如竞技场、双人同图）。这课学怎么生成对称关卡，以及怎么不呆板。\n\n一、三种对称\n· 左右镜像（Mirror X）：以中心线为轴，左边和右边互为镜像\n· 上下镜像（Mirror Z）：同上，上下对称\n· 旋转对称（Rotate 180°）：旋转 180° 后与原图重合，常用于方形竞技场、头目房\n\n二、怎么生成：生成一半，复制另一半\n\n对称生成的最简单套路：只生成半张图，再「镜像复制」。\n\n// 左右镜像：左半边生成，右半边复制\nfor (int x = 0; x < halfWidth; x++)\n    for (int z = 0; z < height; z++)\n    {\n        map[x, z] = GenerateCell(x, z);\n        map[width - 1 - x, z] = map[x, z];   // 镜像复制\n    }\n\n· 宽度要是偶数，或者把中心列留成「对称轴」单独处理\n· 地图里带方向的元素（门的朝向、敌人朝向）复制时要翻转朝向\n· 旋转对称：生成左上四分之一，旋转三次复制到其它三个象限\n\n三、为什么要「打破对称」\n\n纯对称会呆板：玩家玩过一次就知道右边和左边一样，探索感没了。常见的「对称破缺」：\n· 道具分布不对称：布局对称，但宝箱、补给随机偏置，每边不一样\n· 细枝末节扰动：装饰（草、碎屑）不对称，大结构对称\n· 关键点不对称：对称关卡的出生点、Boss 可以放在非对称位置，制造「公平但有趣」\n\n四、对称的应用场景\n· 竞技场 / PvP 地图 —— 左右完全对称，公平性拉满\n· Boss 房 —— 中心对称，走位空间均衡\n· 拼图 / 解密关卡 —— 对称本身就是谜题线索\n\n五、常见坑\n· 奇数宽度镜像错位 —— 宽度设偶数，或单独处理中心列\n· 复制时没翻转朝向（门、机关）—— 复制逻辑里带上「镜像翻转」\n· 全部对称，玩三遍就腻 —— 加道具 / 装饰的不对称\n\n一句话总结：对称关卡 = 生成半边 + 镜像 / 旋转复制，配合「布局对称、内容不对称」的破缺手法——又公平又不呆板。' },
    { type: 'quiz', question: '想做一个左右对称的竞技场地图，最高效的生成方式是什么？', options: ['先随机生成整张图，再用对称工具检查哪里不对称，逐个修改', '只生成左半边，然后按中心线把格子逐一镜像复制到右半边，宽度设成偶数', '生成一张图后直接原地旋转 180°'], answer: 1, tip: '对称生成的核心是「生成一半、复制一半」：左半边算出来，右半边镜像复制，省一半计算还保证严格对称。旋转 180° 是另一种对称（中心对称），不是左右镜像。' },
  ],
  'procgen-15': [
    { type: 'text', title: '手动与程序混合生成', body: '纯随机生成的问题是「不可控」——你可能想要一个完美的开场、一个精心设计的 Boss 房、一条能讲故事的路径。这课学「手动 + 程序」混合：手搭关键结构，程序填充其余。\n\n一、为什么混合\n· 纯手动：工作量大，做不出大世界\n· 纯程序：不可控，容易做出「平平无奇」或「致命错误」的关卡\n· 混合：关键体验手动保证，冗余内容程序填充——两者优点都有\n\n二、分层设计：手绘「关键点」，程序补「血肉」\n\n把生成分成两层：\n1. 设计层（手动 / 半自动）：定出生点、Boss 房、重要路径、地标。这些用编辑器手摆，或用简单的点列表描述\n2. 填充层（程序）：围绕这些关键点，用程序生成地形、装饰、杂兵、普通房间\n\n// 手动定关键点，程序接着生成\n[SerializeField] Transform[] keyPoints;   // 编辑器中手动摆\nvoid Generate()\n{\n    foreach (var p in keyPoints)\n        CarveRoom(p.position);            // 程序：以关键点为核心挖房间\n    GenerateTunnels();                     // 程序：连接关键点\n    FillDetails();                         // 程序：填充装饰\n}\n\n三、编辑生成结果：生成完还能改\n\n生成 ≠ 一次定型。常见工作流：\n· 生成后手动修：在编辑器中生成一次，把不满意的块手动拖走 / 删掉，然后保存场景\n· 运行时编辑：玩家能改地形（挖 / 填），程序生成的格子支持运行时修改——把格子数据存起来而不是生成完就烧成网格\n\n四、把「程序生成」做成可回滚\n\n生成结果用「数据」表示（数组、格子、配置），而不是直接 Instantiate 一批 GameObject。好处：\n· 可以随时重新生成\n· 可以保存 / 加载\n· 可以手动覆盖某一格：标记 overridden，生成时跳过\n\n五、常见坑\n· 程序填充把手动设计「盖掉」——填充逻辑先检查关键点标记，绕开它们\n· 关键点之间连不通 —— 生成通道时强制保证关键点两两可达（procgen-17 验证）\n· 一改需求就得重写 —— 把关键点、填充规则都做成配置，别写死在代码里\n\n一句话总结：混合生成 = 设计层定关键点（手动、保体验）+ 填充层补细节（程序、保规模）——数据可回滚、关键点优先，既有手工的心血又有程序的规模。' },
    { type: 'quiz', question: '混合生成里，「程序填充」把开发者手动摆好的 Boss 房覆盖掉了，最稳妥的预防办法是？', options: ['把关键点标记出来，填充逻辑绕开标记区域', '填充时用随机数碰运气', '把 Boss 房换成程序生成的'], answer: 0, tip: '混合生成的铁律是「关键点优先」：填充逻辑先检查关键区域标记，绝不覆盖设计层定好的结构。程序是给设计服务的，不能反客为主。' },
  ],
  'procgen-16': [
    { type: 'text', title: '生成性能：多线程、缓存与分帧', body: '生成大世界最怕「卡一下」——玩家刚进游戏，一把生成几万格子，当场卡死。这课给三个武器：多线程、缓存、分帧。\n\n一、分帧生成：不卡顿的秘密武器\n\n把一次大生成拆成很多小段，每帧做一段，剩下的留到下一帧。Unity 用协程（Coroutine）最方便：\n\nIEnumerator GenerateChunks()\n{\n    for (int i = 0; i < chunks.Length; i++)\n    {\n        BuildChunk(chunks[i]);            // 一段小工作\n        if (i % 4 == 0) yield return null; // 每做 4 个就让出这一帧\n    }\n}\n\n关键：yield return null 会把控制权还给主循环，帧率不掉，玩家几乎无感。\n\n二、多线程 / Job：把重计算搬到后台\n\n顶点计算、噪声采样这类「纯数据计算」特别适合多线程。Job System（ecs-3 学过）：\n· IJobParallelFor 把数组切块并行算\n· 铁律：Job 里不能碰 Unity API（Instantiate、GetComponent、Mesh 属性），只能用 NativeArray / 普通数组\n· 算完回主线程一次性提交 Mesh、Instantiate\n配合 Burst 编译，几百万顶点的地形几秒算完，主线程不卡。\n\n三、缓存：同样的东西别算两遍\n\n· 以「种子 + 区块坐标」为 key，把生成结果存起来：玩家逛回来直接取缓存，不重算\nDictionary<Vector2Int, ChunkData> cache;\n\nif (!cache.TryGetValue(key, out var data))\n{\n    data = GenerateChunk(key);\n    cache[key] = data;\n}\nApply(data);\n\n· 保存到磁盘（JSON / 二进制 / ScriptableObject），跨会话复用\n\n四、对象池与合并：别反复 Instantiate\n\n· 装饰物（草、石头）用对象池复用，而不是反复创建销毁（perf-3 学过）\n· 同类静态装饰合并成一个大 Mesh（Mesh.CombineMeshes），Draw Call 从几百降到几个\n\n五、常见坑\n· 全放 Start 一次性生成 —— 必须分帧 / 多线程\n· 协程里每帧只 yield 一次，但每段工作太重 —— 每段控制在 2ms 内\n· 缓存 key 没带种子，换了种子还返回旧缓存 —— key 必须包含种子 + 坐标\n· 数据没算完就在主线程读取 —— 等 Job 的 Complete() 后再用\n\n一句话总结：生成性能三件套——分帧生成保帧率、Job 多线程扛重计算、缓存与合并防重复劳动；铁律是「重计算下放、结果缓存复用」。' },
    { type: 'quiz', question: '玩家刚进游戏，一次要生成 200 个区块，直接用 Start 全部生成会卡住。正确的做法是？', options: ['把生成逻辑写进协程，分帧逐个生成，每做几个就 yield 一次让出当前帧', '一次性生成完，反正只有一次卡顿', '把 Fixed Timestep 调大'], answer: 0, tip: '分帧生成（协程 + yield）把大任务拆成小段摊到多帧，帧率不掉、玩家无感。物理步长跟生成卡顿没关系。' },
  ],
  'procgen-17': [
    { type: 'text', title: '生成验证：可玩性、死路修复与保通关', body: '生成完的关卡「能不能玩」，跟生成算法一样重要——出口被墙堵死、出生点卡在悬崖里，玩家直接弃游。这课学生成后的「体检」与「修复」。\n\n一、连通性检测：通关的底线\n\n最核心的验证：出生点能不能到终点。用 Flood Fill（洪水填充，BFS）：\n\n// 从 start 出发，把能走到的格子全标记\nQueue<Vector2Int> q = new();\nbool[,] visited = new bool[w, h];\nq.Enqueue(start);\nvisited[start.x, start.y] = true;\nwhile (q.Count > 0)\n{\n    var p = q.Dequeue();\n    foreach (var n in Neighbors(p))\n        if (!IsWall(n) && !visited[n.x, n.y])\n        {\n            visited[n.x, n.y] = true;\n            q.Enqueue(n);\n        }\n}\nbool reachable = visited[goal.x, goal.y];\n\nBFS 也顺手算出了「从起点到每个格子的最短距离」——可以用来检验「Boss 房离出生点够远」这类设计约束。\n\n二、死路与不可达区的修复\n\n检测发现不可达，别急着重新生成整个关卡，先「修」：\n· 打通墙：从不可达区找一堵离主区最近的墙，挖掉\n· 重排关键点：把终点挪到可达区域\n· 兜底重生成：修 3 次还不行，换种子重生成（带重试上限）\n\n三、可玩性体检清单\n\n连通性只是底线，还有这些要查：\n· 跳跃能否到达：二格高的平台，角色跳得上去吗（用真实角色参数检测）\n· 关键道具可及：钥匙 / 机关是否都在可达区\n· 难度分布：敌人密度、平台间距是否在合理区间\n· 无死角区：有没有「能进不能出」的坑（跳跃距离够不够回来）\n\n四、验证时机\n· 生成时验证：每生成完一区就跑检查，不通过就修/重生成\n· 编辑器里批量验证：一键扫全图，把问题格高亮标红\n· 运行时只读验证：玩家进图前快速跑连通性，作为最后的保险\n\n五、常见坑\n· 只查「有没有路」不查「路能不能走通」——用角色真实参数（跳跃高度、移动距离）测\n· 修复挖错墙，把关键结构破坏了 —— 修复时优先挖「走廊墙」而非承重墙\n· 重生成无限循环 —— 设最大尝试次数（如 50 次）\n\n一句话总结：生成验证 = Flood Fill 查连通 + 死亡区修复 + 可玩性体检（跳跃、道具、死角）——生成完不是终点，跑得通才算完。' },
    { type: 'quiz', question: '程序生成关卡后，判断「出生点能不能走到出口」最直接的手段是？', options: ['用 Flood Fill（BFS）从出生点出发标记所有可达格，看出口有没有被标记到', '用 AddForce 把玩家推过去看会不会撞墙', '看两张地图长得像不像'], answer: 0, tip: 'Flood Fill 从出生点扩散标记可达格，出口被标记到就是连通。这也是验证一切「可达性」问题的通用底牌。' },
  ],
  'procgen-18': [
    { type: 'text', title: '程序化动画：摆动与生长', body: '程序化动画（Procedural Animation）是「不用关键帧、用公式算出来的动作」——草会自己摇、树会自己长。这课从简单到进阶，三种玩法。\n\n一、正弦摆动：草、绳、旗帜\n\n最简单的程序动画：给每个顶点一个「随时间和位置变化」的偏移，用正弦波驱动。\n\n// C# 更新顶点：草尖摇摆\nMesh mesh = GetComponent<MeshFilter>().mesh;\nVector3[] verts = mesh.vertices;\nfor (int i = 0; i < verts.Length; i++)\n{\n    float sway = Mathf.Sin(Time.time * 2f + verts[i].x * 3f);\n    verts[i].y += sway * 0.1f;   // 越靠上摆越大，可用顶点色加权\n}\nmesh.vertices = verts;\nmesh.RecalculateNormals();\n\n三个要点：\n· 用顶点坐标（x/z）做相位，相邻顶点错峰摆动，才像「一波波扫过」\n· 幅度按「离根部距离」加权，根部不动、顶部大摆\n· 性能允许时用顶点 Shader（GPU）算，几十万草叶也不卡；C# 更新适合少量物体\n\n二、生长动画：从小苗到大树\n\n生长可以做成「分帧加长」：\n· 方法一（Scale）：Scale 从 0 长到 1，加一点「先快后慢」的缓动，简单但生硬\n· 方法二（L 系统逐步生成）：把 L 系统（procgen-7）的迭代分帧执行，每帧多长一节，树枝「长出来」的感觉\n· 方法三（顶点动画 Shader）：用 Shader 按时间让枝条从根部到尖端渐次出现——配合溶解/位移，最细腻\n\n三、程序动画 vs 关键帧动画：怎么选\n\n· 关键帧 / 骨骼动画：动作是「设计过的」，适合角色走路、打击这类「需要表演感」的\n· 程序动画：动作是「算出来的」，适合「数量大、要随机、要响应环境」的——整片草、一墙藤蔓、绳索晃动\n· 混合：角色走路用骨骼动画，头发 / 尾巴用程序动画叠加（两头都占）\n\n四、常见坑\n· C# 每帧改 vertices 会同步到 CPU 拖累性能 —— 大量顶点交给 Shader / Job\n· 忘了 RecalculateNormals，光不对（阴影发黑）\n· 正弦相位没带位置，整片草同步摇摆 —— 相位 = 时间 + 顶点位置\n\n一句话总结：程序动画用「公式 + 顶点位置」算动作——正弦摆动管草和旗帜、分帧生长管长树；数量大交给 GPU Shader，C# 只伺候少量对象。' },
    { type: 'quiz', question: '想让一片草地每根草都「错峰摇摆」而不是整片同时摆，关键做法是？', options: ['把正弦的相位加上每个顶点的位置坐标，让相邻顶点波峰错开', '把每根草做成单独动画', '把 Time 固定住'], answer: 0, tip: '相位 = 时间 + 顶点位置，是程序动画让「整体有波动感」的命门——所有草用同一个时间算就会整齐划一地摆，加位置做相位才像风吹过。' },
  ],
  'procgen-19': [
    { type: 'text', title: '无限世界：Chunk、流式加载与种子', body: '「跑多远都是世界」——无限世界（Infinite World）的套路就一句话：把世界切成 Chunk，跟着玩家走，只生成看得见的。\n\n一、Chunk：世界的基本单位\n\n别直接生成「一个巨大的世界」，把它切成固定大小的块：\n\nconst int CHUNK = 32;                        // 每块 32×32\nVector2Int GetChunkCoord(Vector3 pos)\n{\n    int cx = Mathf.FloorToInt(pos.x / CHUNK);\n    int cz = Mathf.FloorToInt(pos.z / CHUNK);\n    return new Vector2Int(cx, cz);\n}\n\n所有坐标换算成「块坐标 + 块内坐标」。\n\n二、世界种子：全图只有一个「根源」\n\n无限世界不能每块用不同的随机——会不连续。正确做法：整个世界用同一个种子，每个块的噪声采样用「世界坐标」（而不是块内坐标）：\n\nfloat h = Fbm(seed + worldX * 0.05f, seed + worldZ * 0.05f, 5);\n\n用世界坐标采样，相邻块的边界天然衔接——这是无限世界「无缝」的命门。Random.InitState 要按「种子 + 世界坐标」来，保证同一世界坐标永远拿到同一个结果。\n\n三、流式加载 / 卸载\n\n每帧检查玩家所在的块，加载周围一圈（比如 5×5 范围），卸载远离玩家的块：\n\nvoid UpdateLoad()\n{\n    Vector2Int pc = GetChunkCoord(player.position);\n    for (int x = pc.x - 2; x <= pc.x + 2; x++)\n        for (int z = pc.z - 2; z <= pc.z + 2; z++)\n            if (!chunks.ContainsKey(key)) LoadChunk(x, z);\n    // 卸载超出范围的块\n}\n\n关键参数：加载半径（多大一圈）、卸载距离（多远删掉）。\n\n四、缓存与持久化\n\n· 内存缓存：已加载的块存 Dictionary，玩家逛回来秒开（procgen-16）\n· 持久化：离开过的地方，玩家改了地形，下次回来要还原 —— 只存「被修改过的块」的增量，其余用种子即时重算\n\n五、常见坑\n· 块内坐标用随机生成，块边界断裂 —— 必须用世界坐标采样噪声\n· 块生成造成卡顿 —— 分帧 / 多线程加载（procgen-16）\n· 只加载不卸载，跑远了内存爆 —— 远离就卸载\n· 玩家来回踱步，块反复加载卸载 —— 卸载时设「软卸载」（隔一会儿再删）\n\n一句话总结：无限世界 = Chunk 切块 + 世界种子 + 世界坐标采样保无缝 + 跟随玩家流式加载卸载——跑多远世界都有，内存永远只有眼前一圈。' },
    { type: 'quiz', question: '无限世界里相邻两个 Chunk 的地形要「无缝拼接」、边界不裂开，关键做法是？', options: ['每个 Chunk 用自己的随机种子独立生成', '噪声采样用「世界坐标」而不是块内坐标，配合同一个世界种子，边界天然连续', '生成后手动把边界顶点拉平'], answer: 1, tip: '无缝的命门是「全局采样」：所有块都拿世界坐标去同一个噪声函数里采样，相邻块边界的高度自然连续；每块独立随机必然断层。' },
  ],
  'procgen-20': [
    { type: 'text', title: '程序化综合动手练：生成一座能玩的小城', body: '动手练：把本课全部家当串起来——用程序生成一座「小城」：地形起伏、街区成网、建筑成排、玩家能从头逛到尾不出 bug。\n\n🎯 任务目标：生成一块 100×100 的区域内：FBM 地形（有山有水）、一片居民区（道路网格 + 建筑）、确保从出生点到城中心可达，生成过程不卡顿。\n\n📦 前置要求：学完本课「噪声进阶」「地形实战」「城市生成」「生成验证」四节，会 FBM、Flood Fill。\n\n📋 动手步骤：\n\n1. 搭骨架：新建脚本 CityGenerator.cs，暴露参数（size、seed、建筑密度），Start 里调 Generate()。\n\n2. 地形：写 Fbm 函数（procgen-8 的代码），用固定 seed 初始化，生成高度图；按高度分水 / 沙 / 草 / 岩（procgen-9），生成网格（Mesh）。\n\n3. 验证地形可玩：挑一块水面少、坡度缓的区域当出生点，用 Flood Fill 检查出生点能到达大部分陆地。\n\n4. 城市：在「草地高度带」里生成道路网格（主干道 + 次干道 + 抖动，procgen-11），路网把区域切成街区，每个街区按密度放建筑（楼高随繁荣度）。\n\n5. 连接：把出生点、城中心、一块高地观景点用通道 / 道路连通，用 Flood Fill 验证三处互相可达。\n\n6. 撒细节：沿路放路灯 / 树，远离道路的草地撒草、石头；数量控制住（对象池 / 合并，procgen-16）。\n\n7. 性能：生成放协程分帧（每帧做一小段），加内存缓存（seed + 坐标）。\n\n8. 验证与收尾：打印「统计日志」——多少栋楼、多少棵树、可达面积占比；跑一遍出生点到城中心，确认全程能走通。\n\n✅ 验收清单：\n能 □ 地形有山有水、生态分层（水 / 沙 / 草 / 岩）肉眼可辨\n能 □ 道路成网、街区规整、建筑不穿模不重叠\n能 □ 出生点 → 城中心全程可达（Flood Fill 验证通过）\n能 □ 换一个 seed，城市样子明显不同，但都同样「能玩」\n能 □ 生成过程不卡顿（分帧生效，帧率不掉）\n能 □ 数据全部由 seed 驱动，同 seed 结果一致（可复现）\n\n🕳 常见卡壳：\n坑 1：城市生成完地形「飞了」——高度图和网格的顶点顺序对不上，先画小图（8×8）验证网格逻辑。\n坑 2：建筑叠在一起——街区划分后每栋楼的地块有重叠，加「已占位」检查。\n坑 3：明明验证了连通，跑起来还是到不了城中心——验证用的「可达」和真实移动（跳跃高度、斜坡）不一致，用角色的真实参数测。' },
    { type: 'quiz', question: '在这个城市生成动手练里，最后一步「换一个 seed 也要能玩」验证的是什么？', options: ['生成算法的稳定性——不同种子能产出不同的、但都可玩的关卡，而不是偶尔出 bug', '验证种子能不能复现同一张地图', '验证游戏运行速度快'], answer: 0, tip: '「多 seed 可玩」是程序化生成的关键验收项：一张图碰巧能玩不算本事，换几个种子都稳定可玩才说明算法、验证、修复逻辑是健全的。' },
  ],
}
