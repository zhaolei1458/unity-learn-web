// 输入系统进阶扩充：触屏手势 / Action 组合与增强 / 输入架构与重构
export const lessons = [
  { id: 'inputadv-4', title: '触屏手势' },
  { id: 'inputadv-5', title: 'Action 组合与增强' },
  { id: 'inputadv-6', title: '输入架构与重构' },
  { id: 'inputadv-7', title: '触屏手势深入' },
  { id: 'inputadv-8', title: '输入与 UI 交互' },
  { id: 'inputadv-9', title: '组合按键' },
  { id: 'inputadv-10', title: '输入缓冲与判定' },
  { id: 'inputadv-11', title: '虚拟摇杆深入' },
  { id: 'inputadv-12', title: '手柄反馈' },
  { id: 'inputadv-13', title: '无障碍输入' },
  { id: 'inputadv-14', title: '输入调试' },
  { id: 'inputadv-15', title: '本地多玩家' },
  { id: 'inputadv-16', title: '移动端输入优化' },
  { id: 'inputadv-17', title: '输入事件系统' },
  { id: 'inputadv-18', title: '玩家偏好' },
  { id: 'inputadv-19', title: '输入架构实战' },
  { id: 'inputadv-20', title: '动手练：完整输入系统' },
]

export const cards = {
  'inputadv-4': [
    { type: 'text', title: '触屏手势：滑动、长按、双击自己写', body: '手机上除了虚拟按键，还有「手势」——滑动闪避、长按蓄力、双击翻滚。这一节教你从触摸数据里认出各种手势。\n\n一、触摸的生命周期\n一次触摸有三个阶段：按下（began）→ 移动（moved）→ 抬起（ended）。判断手势，全靠「按下的位置 / 时间 / 移动量」三个量：\n· 滑动 —— 按下到抬起，位移超过阈值（如 50 像素）\n· 长按 —— 按下持续超过阈值（如 0.5 秒）\n· 双击 —— 两次点击间隔小于阈值（如 0.3 秒）\n\n二、用 Input System 读触摸\n用 Touchscreen 指针读主触摸：\n\nif (Touchscreen.current != null)\n{\n    var touch = Touchscreen.current.primaryTouch;\n    if (touch.press.wasPressedThisFrame)\n        startPos = touch.position.ReadValue();   // 记下按下点\n    if (touch.press.wasReleasedThisFrame)\n    {\n        Vector2 endPos = touch.position.ReadValue();\n        Vector2 delta = endPos - startPos;\n        if (delta.magnitude > 50f)              // 位移够大 → 滑动\n            OnSwipe(delta.normalized);\n    }\n}\n\n三、写一个手势检测器\n建议把识别逻辑收进一个 GestureDetector，按状态机走：\n\n· 按下：记录起点和时间，标记「可能是长按 / 滑动」\n· 移动：位移超过阈值 → 判定为滑动，锁定方向\n· 抬起：若没滑动过，且按住时长超阈值 → 长按；若两次间隔够近 → 双击\n\n四、手势与虚拟键配合\n· 场景设计：移动用摇杆、跳跃是按钮、滑动闪避、长按蓄力——各司其职\n· 关键坑：手势识别「要快」。滑动往往手指一动就该触发，别等抬起才判；长按则可以在「按住中」每帧加蓄力值\n· 方向区分：滑动是横是竖，用 atan2(delta.y, delta.x) 转成角度判断象限\n\n五、三个高频坑\n· 多指混淆 —— 每根手指有独立 fingerId，用 primaryTouch 只吃第一根；要双指就按 id 各自跟踪\n· 和 UI 打架 —— 点在按钮上不该触发滑动：先用 EventSystem 判断是否点中 UI，再决定要不要走手势\n· 真机延迟 —— 编辑器触摸模拟和真机手感差很多，手势阈值要上真机调\n\n一句话总结：手势 = 按下 / 移动 / 抬起三段数据 + 位移、时长、间隔三个阈值；滑动要快、长按要累计、双击要比间隔，识别逻辑收进一个检测器。' },
    { type: 'quiz', question: '识别「滑动」手势，最核心的判断依据是什么？', options: ['手指按下的时间超过 1 秒', '手指从按下到抬起，位移超过阈值，并记录方向', '手指连续快速点了两下'], answer: 1, tip: '滑动看「位移」：起点到终点距离超过阈值才叫滑，方向用它定。时间长是长按、两下是双击——三个手势判据各管各的。' },
  ],
  'inputadv-5': [
    { type: 'text', title: 'Action 组合与增强：一个动作吃下复杂输入', body: '前面的动作都是「一个键 = 一个动作」。这节升级：一个动作吃下方向组合、修饰键、甚至输入处理——全在 Input Action 资产里配，不用写逻辑代码。\n\n一、Composite 组合：把一堆键合成一个值\n移动需要「上 / 下 / 左 / 右 / 摇杆」合成一个 2D 方向。做法：在 Input Action 资产里选中 Move 动作 → 点右侧的 + → Add Composite Binding → 选 Vector 2。\n\n它会生成几个子绑定：Up、Down、Left、Right，分别绑上 W/S/A/D 或摇杆轴。\n\n读法还是读一个动作：\nVector2 dir = controls.Player.Move.ReadValue<Vector2>();\n\n好处：一个动作一个值，死区、归一化引擎统一处理；不用在代码里拼四个按键。\n\n二、Modifier 修饰键：Shift 一下就是重击\n想让「攻击」在普通时轻击、按住 Shift 时重击：\n\n给攻击动作加绑定 → 选 With Modifiers → 填 Modifier（Shift）和一个按键（攻击键）\n\n这样只有「Shift + 攻击键」同时按下才触发这条绑定。不用写一行判断 Shift 的代码。\n\n三、Action 处理器（Processors）：改造输入值\n处理器对读进来的值做处理，挂在绑定上，按顺序执行：\n· Deadzone 死区 —— 摇杆中心一小圈忽略，防角色自己飘\n· Invert 反转 —— 摇杆方向反一下\n· Scale 缩放 —— 灵敏度：乘一个系数\n· Axis Deadzone / Normalize —— 轴死区、归一化\n\n在资产里给绑定挂处理器，比在代码里到处乘系数干净得多。\n\n四、复杂映射实战\n· 坦克 —— 两个 Vector 2 Composite：左摇杆开履带、右摇杆转炮塔\n· 组合技能 —— 攻击 + 方向不同 = 不同招式：建多个带不同绑定组合的动作\n· 单键多义 —— 同一个键在不同 Map（地面 / 载具 / 菜单）里是不同动作，切 Map 就换一套\n\n五、常见坑\n· 修饰键别「碰瓷」—— 如果 Shift 同时是疾跑键，普通攻击和重击可能一起触发；把带 Modifier 的绑定和普通绑定拆开，或换修饰键\n· Composite 的子绑定别和别的动作抢同一个键，冲突时优先用「无修饰」那条\n· 处理器顺序有讲究 —— Deadzone 通常在 Scale 前面：先清死区再乘系数\n\n一句话总结：Composite 把多键合成一个值、With Modifiers 做修饰键组合、Processors 改造输入值——复杂映射全在资产里配，代码只读一个干净的值。' },
    { type: 'quiz', question: '想让「攻击键」按住 Shift 时触发重击、不按 Shift 时轻击，且不想写代码判断 Shift，该怎么配？', options: ['给攻击动作加一条「With Modifiers」绑定：Shift 作修饰键 + 攻击键，只有同时按下才触发', '把 Shift 也绑定成攻击，两个键一起按就触发两次', '在 Update 里用 Input.GetKey 判断 Shift 再调不同方法'], answer: 0, tip: 'With Modifiers 是 Input System 的修饰键方案：绑定上声明「需要同时按住的键」，识别逻辑交给引擎，代码只读动作。选项二会让「单独按 Shift」也触发攻击。' },
  ],
  'inputadv-6': [
    { type: 'text', title: '输入架构与重构：让输入成为可替换的插件', body: '游戏一大，输入代码最容易烂：几十个地方直接查 Input.GetKey、换设备就得改十几处、想加个「手柄方案」无从下手。这一节把输入「立规矩」。\n\n一、输入层隔离：三层各管各的\n· 设备层 —— 键盘 / 手柄 / 触屏，交给 Input System 收\n· 逻辑动作层 —— 只定义「想干什么」：Move、Jump、Fire，不碰任何设备\n· 消费层 —— 角色、相机、UI 只读动作，完全不知道玩家用的什么设备\n\n好处：换设备、改键位、加新输入，玩法代码一行不用动；测试还能喂假输入。\n\n二、可换输入方案：接口 + 实现\n定义一套输入接口，做多个实现：\n\npublic interface IInputSource\n{\n    Vector2 GetMove();\n    bool JumpPressed();\n    bool FirePressed();\n}\n\npublic class KeyboardMouseInput : IInputSource { /* 键盘鼠标实现 */ }\npublic class GamepadInput : IInputSource { /* 手柄实现 */ }\npublic class AIInput : IInputSource { /* 机器人测试输入 */ }\n\n角色只认 IInputSource：\npublic class Player : MonoBehaviour\n{\n    private IInputSource input;\n    void Awake() { input = GetInput(); }   // 按设置 / 设备决定用哪个实现\n\n    void Update()\n    {\n        Vector2 move = input.GetMove();\n        // ...\n    }\n}\n\n跑单元测试时喂 AIInput，就能模拟任意操作。\n\n三、玩家偏好：存「方案」不存「键」\n存档里存玩家选了什么方案（键盘 / 手柄 / 自定义），启动时读档 → 创建对应输入源。自定义键位就回到第 2 节的重映射 + PlayerPrefs。\n\n四、从老 Input 迁移到新 Input\n步骤：\n1. Package Manager 装 Input System 包\n2. Project Settings → Active Input Handling 先选 Both（新旧并存，过渡期不出乱子）\n3. 逐个替换：Input.GetAxis → Action 的 ReadValue；Input.GetKeyDown → 事件或 wasPressedThisFrame\n4. 全换完，把 Active Input Handling 改成 Input System (New) 关掉老 Input\n\n五个常见坑：\n· 新旧并存只是过渡，别拖——两套都开有重复开销\n· Action 的事件要 OnEnable 订阅、OnDisable 退订，否则场景切换后「幽灵回调」\n· 场景切换后输入引用失效——Awake 里重新取，别缓存在静态字段\n· 魔法字符串（GetKey(\"Fire\")）拼错不报错——换成 Action 后拼错直接编译报错，这是迁移的最大红利\n· 记得做一份「迁移后回归清单」：进游戏、菜单、手柄、触屏各过一遍\n\n一句话总结：输入立规矩 = 三层隔离（设备 / 动作 / 消费）+ 接口实现可替换 + 玩家偏好存方案；从老 Input 迁移，Both 过渡、逐块替换、事件该订阅退订，最后关掉旧的。' },
    { type: 'quiz', question: '输入层隔离最主要的好处是什么？', options: ['让代码看起来更整齐', '换设备、改键位、加新输入时只动输入层，玩法代码不用改', '让输入响应变快'], answer: 1, tip: '隔离的意义在「变更不扩散」：设备和动作解耦后，换手柄、重映射、加触屏方案都只改输入层，角色代码永远只读抽象动作。' },
  ],
  'inputadv-7': [
    { type: 'text', title: '触屏手势深入：双指缩放、旋转、多点触控', body: '上一节（inputadv-4）讲了单指手势。这节上难度：双指缩放、双指旋转、多点触控——地图、拍照、双人协作场景都用得上。\n\n一、多点触控的基础：按 fingerId 跟踪\nTouchscreen 上每根手指有独立 id。别再用 primaryTouch（只跟第一根），要按 id 建字典：\n\nprivate Dictionary<int, Vector2> touchStart = new Dictionary<int, Vector2>();\n\nvoid Update()\n{\n    if (Touchscreen.current == null) return;\n    var touches = Touchscreen.current.touches;       // 全部触点\n    for (int i = 0; i < touches.Count; i++)\n    {\n        var touch = touches[i];\n        if (touch.press.wasPressedThisFrame)\n            touchStart[touch.touchId.ReadValue()] = touch.position.ReadValue();\n        if (touch.press.wasReleasedThisFrame)\n            touchStart.Remove(touch.touchId.ReadValue());\n    }\n}\n\n二、双指缩放（Pinch）\n原理：两根手指的距离变化，就是缩放量。\n· 按下时记录两指距离 baseline\n· 每帧算当前距离和上一帧距离的比值 = 缩放系数，应用到相机或目标 scale\n\nvoid Update()\n{\n    if (GetTwoTouches(out Vector2 a, out Vector2 b))\n    {\n        float current = Vector2.Distance(a, b);\n        if (lastDist > 0)\n        {\n            float ratio = current / lastDist;          // >1 放大，<1 缩小\n            zoom *= ratio;\n            zoom = Mathf.Clamp(zoom, 0.5f, 5f);        // 限制范围\n        }\n        lastDist = current;\n    }\n}\n\n注意：lastDist 要每帧更新，别用固定 baseline，否则缩放手感不跟手。\n\n三、双指旋转（Rotate）\n原理：两根手指连线的角度变化 = 旋转量。\n· 用 atan2(b.y - a.y, b.x - a.x) 算连线角度\n· 角度差 = 当前角度 - 上一帧角度，累加到 rotation\n· 实现思路和缩放一样，把「距离」换成「角度」\n\n四、多点触控的坑\n· 手指数量不定：用字典按 id 管理，别写死「第二根手指是 touches[1]」——中间插入/抬起会乱\n· 单指拖 vs 双指缩：手指数变化那一下要处理——双指变单指要重置状态，防止相机猛跳\n· UI 和手势打架：双指缩放手势同时触发了按钮点击——位移超阈值就不算点击\n\n五、真机验证\n编辑器触摸模拟和真机差异大，多点手势一定要真机测：模拟器上双指用 Alt+Ctrl+左键拖动，手感完全不对，阈值、跟手度全部以真机为准。\n\n一句话总结：多点触控按 fingerId 用字典跟踪；双指缩放看「两指距离变化」、旋转看「连线角度变化」，都用「相对上一帧」的增量；手指数量变化要重置状态，最后真机验证手感。' },
    { type: 'quiz', question: '双指缩放时，为什么用「当前距离 ÷ 上一帧距离」做缩放系数，而不是用「当前距离 ÷ 按下时的距离」？', options: ['上一帧距离更准确', '用「相对上一帧的增量」缩放更跟手，不会因为初始距离不同导致缩放速度不一致', '按下时的距离算不准'], answer: 1, tip: '固定 baseline 会导致「起始距离越远，缩放越迟钝」。用每帧增量，缩放比例始终跟手指动作同步，手感才统一、跟手。' },
  ],
  'inputadv-8': [
    { type: 'text', title: '输入与 UI 交互：点击穿透、拖拽与防误触', body: '输入和 UI 打架是移动/网页游戏的高频 Bug：点按钮角色跟着动、拖拽不跟手、误触乱触发。这节一次讲清。\n\n一、点击穿透：点到 UI 不该触发游戏操作\n最常见的问题：点按钮，角色也跟着攻击。因为「玩家点击」同时被 UI 和游戏输入收到了。解决：检测是否点中 UI。\n· Input System + UI：用 EventSystem 的 IsPointerOverGameObject\n\nif (EventSystem.current != null &&\n    EventSystem.current.IsPointerOverGameObject())\n    return;    // 点在 UI 上，游戏输入忽略\n\n触屏版带指针 id：IsPointerOverGameObject(touchId)\n\n二、拖拽 UI（IDragHandler）\nUI 上做拖拽（背包物品、地图标记），Unity UI 提供现成接口：\n\npublic class DragItem : MonoBehaviour, IDragHandler, IBeginDragHandler, IEndDragHandler\n{\n    public void OnBeginDrag(PointerEventData data) { /* 记起点、播放拿起音效 */ }\n    public void OnDrag(PointerEventData data)\n    {\n        transform.position += (Vector3)data.delta;   // 跟手\n    }\n    public void OnEndDrag(PointerEventData data) { /* 检测落点、播放放下音效 */ }\n}\n\n· 关键：data.delta 是「本帧移动量」，直接加到位置就跟手\n· 若要限制拖拽范围：算完位置后 Clamp 在容器 RectTransform 内\n\n三、拖拽中的输入冲突\n· 拖拽时别触发点击：拖拽超过阈值（如 10 像素）就标记「这是拖不是点」，OnClick 不响应\n· 拖拽越过别的 UI：每帧把被拖物 SetAsLastSibling 放最上层，视觉不穿插\n\n四、防误触（误触/连点）\n· 误触：玩家没想点，手指碰到就触发\n· 阈值法：按下后位移超过阈值才算「有效点击」，小于阈值是「碰了一下」，忽略\n· 连点：点击事件加冷却，两次点击间隔小于 N 毫秒忽略\n· 真机手感：移动端点击判定要「松手才算」，按下就触发容易误触（想滑动却点了按钮）\n\n五、常见坑\n· 忘了 IsPointerOverGameObject——UI 点完按钮角色跟着动了，排查半天\n· 把 data.delta 加成世界坐标——UI 用屏幕/UI 坐标，别直接转世界坐标再加\n· 事件目标不是自己——拖拽检测用 IDragHandler 挂在被拖物上，别全局监听再找目标\n\n一句话总结：点击穿透用 IsPointerOverGameObject 挡；拖拽用 IBeginDrag/OnDrag/IEndDrag 接口跟手；点击用「位移阈值 + 冷却」防误触，松手才判定。' },
    { type: 'quiz', question: '玩家点屏幕上的按钮时，底下的角色也朝那个方向攻击了。最可能缺了什么？', options: ['角色的攻击伤害太低', 'UI 点击前没有用 IsPointerOverGameObject 拦截，游戏输入也收到了这次点击', '按钮没有挂 Button 组件'], answer: 1, tip: 'UI 和游戏输入同时收到点击——「点击穿透」。用 EventSystem.IsPointerOverGameObject 判断点在不在 UI 上，在就不下发游戏输入。' },
  ],
  'inputadv-9': [
    { type: 'text', title: '组合按键：连招、组合键与序列输入', body: '三种「组合」分清楚：同时按（Chord）、序列（Sequence）、连招（Combo）。这节讲后两者的实现。\n\n一、三种「组合」分清楚\n· 同时按（Chord）：Shift + 攻击 = 重击（inputadv-5 的 With Modifiers）\n· 序列（Sequence）：按顺序——A 然后 B 然后 C，触发连招\n· 连招（Combo）：一整套序列 + 时间窗口 + 命中判定，格斗游戏核心\n\n二、序列输入（Sequence）的实现\n核心思路：缓存「按键名 + 时间」，按时间顺序比对目标序列。\n· 玩家按下一个键 → 把 key 和 Time.time 推进队列\n· 比对：队列末尾是否匹配目标序列的「最新一段」\n· 时间窗：相邻两次按键间隔超过阈值，清空队列（手速不够，连招断）\n\npublic void OnKeyPressed(string key)\n{\n    float now = Time.time;\n    if (buffer.Count > 0 && now - buffer.Peek().time > 0.4f)\n        buffer.Clear();                       // 间隔太久，断连\n\n    buffer.Enqueue(new KeyPress(key, now));\n    TryMatchCombo();\n}\n\nvoid TryMatchCombo()\n{\n    // 把队列尾部和 combo 序列比对，全对上就触发\n}\n\n三、连招系统进阶\n· 用「方向 + 按键」做连招：格斗游戏经典——前 + 拳、下 + 脚\n· 每个招式有一个「输入窗口」：在这段时间内按下下一个键才能接续，超时断连\n· 命中才能接：有的连招要求第一段「打中」才允许接第二段（击中确认）\n· 视觉反馈：输入对了给「飘字/闪光」提示，玩家才知道输入被吃进去了\n\n四、输入队列的常见坑\n· 队列越长越容易误触发——输入窗口别太宽，0.3~0.5 秒内有效即可\n· 玩家连打按键把队列塞满——限定队列长度，太满丢最老的\n· 序列和普通按键冲突——「A 单按是轻击、A 然后 A 是二连击」：触发连招后要消费掉这些输入，别让连招的那次按键再触发一次普通攻击\n\n五、实战小套路\n· 连招前先做「输入是否被 UI 挡住」的检查（inputadv-8 的点击穿透）\n· 所有连招做成数据表（组合、窗口、结果），别散在代码里写死\n· 关键反馈：连招成功要有音效 + 特效 + 镜头小动，玩家一眼看懂「我连上了」\n\n一句话总结：同时按用 Modifier、按顺序用序列输入（缓存按键 + 时间窗 + 比对）、完整连招要加命中判定和消费机制；输入窗口留 0.3~0.5 秒，超时断连。' },
    { type: 'quiz', question: '想让玩家「先按 A、0.4 秒内按 B」触发一个二段招，0.4 秒内没按就取消。核心判断依据是？', options: ['两次按键的时间间隔是否在窗口内', '两次按键的音量大小', 'B 键是否按得够重'], answer: 0, tip: '序列输入的核心是时间窗：相邻两次按键间隔在 0.4 秒内才有效，超时清空重来。时间决定「连得上连不上」，不是力度或音量。' },
  ],
  'inputadv-10': [
    { type: 'text', title: '输入缓冲与判定：格斗手感的三把钥匙', body: '格斗/动作游戏「手感」的秘密，一半在输入系统里。这节讲三把钥匙：输入缓冲、预输入、帧判定。\n\n一、输入缓冲（Input Buffer）\n玩家按了键，系统先别急着消费，存进一个缓冲队列，等「正确时机」再释放。好处：\n· 连招中：上一招还没播完，下一招的键已经按下——缓冲住，等上招结束自动接上\n· 玩家提前按了：不会丢输入，感受是「我的手很快」\n实现：一个队列，存 (动作, 时间)。每帧把过期的弹出（如超过 0.15 秒），当前可接的招从队头取。\n\n二、预输入（Pre-input）\n玩家「感觉快要该按了」提前 0.1~0.2 秒按下，系统也在窗口内接受。典型场景：\n· 落地瞬间想起跳：玩家在空中提前按跳，落地缓冲窗口内的「跳」立即生效——手感就「跟手」了\n· 硬直结束想接招：硬直最后一帧提前输入，结束立刻出招\n预输入是「允许玩家略早」，输入缓冲是「允许玩家略晚」，两者合起来把输入窗口放宽，手感大幅提升。\n\n三、帧判定（Frame Window）\n很多判定用「帧」计时（1 秒 60 帧）：\n· 输入窗口帧数：如「跳跃预输入窗口 8 帧」≈ 0.13 秒\n· 判定帧：攻击「判定产生」在哪一帧，直接影响连招手感\n· 关键是别在 Update 里一帧一帧数（帧率不稳定），用 Time.deltaTime 累计成「时间窗口」，逻辑才和帧率无关\n\n四、时间窗口的软编码\npublic class InputWindow\n{\n    public float window = 0.15f;          // 秒\n    private float enterTime;\n\n    public void NoteInput()  { enterTime = Time.time; }\n\n    public bool IsWithinWindow()\n    {\n        return Time.time - enterTime <= window;\n    }\n}\n\n五、三把钥匙的配合\n· 手感好 = 玩家「以为」自己操作精准，其实系统放宽了窗口\n· 竞速游戏：窗口窄（0.05 秒），要真本事\n· 休闲/动作游戏：窗口宽（0.2 秒），谁都打得帅\n· 每个动作的窗口单独调：跳跃预输入 0.15 秒、攻击取消 0.2 秒、防御架招 0.05 秒\n\n六、常见坑\n· 预输入窗口太长——角色「自己动」，玩家觉得不跟手；窗口要短，只补「手感」不抢「控制」\n· 缓冲队列无限长——过期不清理，两秒前的按键突然触发；每帧弹出过期的\n· 用帧数计时在手机 30 帧上变短一半——用秒 + Time.deltaTime，别数帧\n\n一句话总结：缓冲让输入「略晚不丢」、预输入让「略早有效」、时间窗用秒算（别数帧）；窗口宽度按游戏类型定——竞速窄、动作宽，手感就是「玩家以为精准、系统放宽窗口」。' },
    { type: 'quiz', question: '玩家在空中按了「跳」，角色落地后那一瞬间居然真的跳起来了。这是哪个机制的效果？', options: ['输入缓冲：落地窗口内保留的跳跃输入被释放', '自动瞄准', '按键防抖'], answer: 0, tip: '这就是输入缓冲/预输入：跳跃键在落地前被按下，缓冲到落地时刻生效。玩家感受是「跟手」，机制是「窗口内保留输入」。' },
  ],
  'inputadv-11': [
    { type: 'text', title: '虚拟摇杆深入：从能用到大厂手感', body: '移动游戏的手感，一大半在虚拟摇杆上。这节从「能用」做到「顺手」。\n\n一、摇杆的基本结构\n· 底（Background）：固定圆盘\n· 头（Knob）：手指拖动的圆点\n· 输入：头相对底心的偏移向量，归一化后是移动方向\n\n拖拽用 Input System 或 UI 的 IDragHandler：\n\npublic class VirtualJoystick : MonoBehaviour, IDragHandler, IPointerUpHandler\n{\n    public RectTransform background;     // 底\n    public RectTransform knob;           // 头\n    public float radius = 80f;           // 拖拽半径\n    public Vector2 output;               // 输出方向（-1~1）\n\n    public void OnDrag(PointerEventData data)\n    {\n        Vector2 pos = (Vector2)background.position - data.position;\n        output = Vector2.ClampMagnitude(pos, radius) / radius;  // 限制在圆内并归一化\n        knob.position = background.position - output * radius;\n    }\n    public void OnPointerUp(PointerEventData data)\n    {\n        output = Vector2.zero;\n        knob.position = background.position;\n    }\n}\n\n二、死区（Dead Zone）\n摇杆推一点点，角色不该动——手指在圆心的微小抖动要有死区忽略：\n· 线性死区：output.magnitude < 0.15 就当 0\n· 平滑死区：超过死区后，映射从 0 开始（不是从死区值跳变）\n\n三、灵敏度与曲线\n· 轻推精细、推满更猛：输出加「曲线」——output = output^0.6 或按距离分段\n· 灵敏度 = 输入到输出的放大系数，太高飘、太低肉，实机调\n· 可做「动态灵敏度」：战斗中输出快一点，探索慢一点\n\n四、动态摇杆（Dynamic）\n固定摇杆的痛：手指按哪里，摇杆就在哪里——想变方向要「抬手指重新按」。\n动态摇杆：手指按下那一点就是摇杆中心，拖起来就是方向：\n· 按下位置 → 摇杆出生位置（只显示在你手指下）\n· 优点：想换方向抬手即换，拇指不用在屏幕上找位置，操作流畅\n· 实现：OnPointerDown 把 background 挪到手指位置，再走普通拖拽逻辑\n\n五、边界与死角\n· 拖拽半径内松开：回弹归零（多数游戏）\n· 拖到边缘不松手：Knob 顶在圆边缘，方向保持（ClampMagnitude 就是干这个的）\n· 斜 45 度方向：归一化后是 (0.707, 0.707)，别输出 (1, 1) 那种超长向量\n\n六、常见坑\n· 忘了归一化——对角移动速度变快（斜向超 1）；先 ClampMagnitude 再归一\n· 死区设 0——手指轻抖角色乱动，真机必测\n· 摇杆和 UI 事件冲突——摇杆区域要挡住按钮点击（inputadv-8 的穿透检查反过来用）\n\n一句话总结：摇杆 = 底 + 头 + 圆内偏移向量；死区忽略微抖、曲线调手感、动态摇杆让拇指「抬手即换向」；对角线要归一化，否则斜着跑得比直着快。' },
    { type: 'component', title: '虚拟摇杆三个调校项', items: [
      { name: '死区 Dead Zone', desc: '忽略圆心微抖，防角色自飘' },
      { name: '灵敏度/曲线', desc: '轻推精细、推满更猛' },
      { name: '动态摇杆', desc: '手指落点为圆心，抬手即换向' },
    ] },
    { type: 'quiz', question: '动态摇杆相比固定摇杆，最大的体验提升是什么？', options: ['角色跑得更快', '手指按下哪里摇杆就出现在哪里，想换方向抬手即换，不用在屏幕上找位置', '画面更清晰'], answer: 1, tip: '固定摇杆要拇指精确按到摇杆位；动态摇杆以手指落点为圆心，换向只需抬手重新按，拇指操作流畅度大幅提升。' },
  ],
  'inputadv-12': [
    { type: 'text', title: '手柄反馈：震动、扳机与「手感」的最后一公里', body: '手柄的体验不只是摇杆和按键，还有「反馈」——玩家被打了、开枪了、赛车撞了，手柄要「震给你看」。\n\n一、震动（Vibration）\nInput System 里用 Gamepad.SetMotorSpeeds 控制左右马达：\n\npublic void Vibrate(float low, float high)\n{\n    Gamepad pad = Gamepad.current;\n    if (pad != null)\n        pad.SetMotorSpeeds(low, high);    // 低频马达(左) + 高频马达(右)\n}\npublic void StopVibration()\n{\n    Gamepad pad = Gamepad.current;\n    if (pad != null)\n        pad.SetMotorSpeeds(0f, 0f);\n}\n\n· 低频马达：大而沉的感觉——爆炸、撞击、坠落\n· 高频马达：小而锐的感觉——枪声、命中的「咔嗒」\n· 记得用完归零！忘了关，手柄一直嗡嗡响，玩家拔线的心都有\n\n二、震动节奏（脉冲）\n连续震动要有「节奏」，别一直嗡嗡：\n· 单发脉冲：命中那一下 0.1 秒\n· 连续脉冲：受伤的「咚咚咚」\n· 渐强渐弱：Boss 出场，震动从小到大地推过来\n实现：用协程控制时长和强度：\n\nIEnumerator Pulse(float strength, float duration)\n{\n    Vibrate(strength, strength);\n    yield return new WaitForSeconds(duration);\n    StopVibration();\n}\n\n三、触觉反馈与高级功能\n· 扳机力反馈（Triggers）：Xbox 手柄扳机有独立马达，可模拟「拉弓的张力」「扣扳机的咔哒」\n· 灯条（Lightbar）：颜色可编程，同步血量/状态\n· 触摸板（DualShock）：可做触摸输入\n· 摇杆 L3/R3：按压也是按钮，别忽略\n\n四、什么时候该震\n· 命中/受伤：玩家被打到，短震\n· 动作完成：处决、落地、加速，轻震一下做「确认」\n· 预警：低血量、Boss 即将出手，持续微弱震感\n· 千万别：全程狂震、无意义震动——震感阈值高，用多了就「麻」了\n\n五、跨平台注意\n· 不同手柄 API 不同：Input System 统一了 SetMotorSpeeds，但强度范围各厂不同，包一层封装\n· 真机必测：编辑器和真机的震动体感完全不同\n· 手柄拔插：Gamepad.current 会变 null，操作前判空\n\n一句话总结：震动用 SetMotorSpeeds 控左右马达、用协程做节奏脉冲、用完归零；低频给沉重、高频给锐利；扳机、灯条、触摸板是加分项；别滥用震动，真机必测。' },
    { type: 'quiz', question: '玩家被击飞、手柄「嗡嗡嗡」个不停停不下来，很可能是哪里出了问题？', options: ['手柄坏了', '震动调用了但没人 Stop/归零，马达一直在转', '震动强度不够大'], answer: 1, tip: 'SetMotorSpeeds 不会自己停——必须在震动结束时归零（或超时协程兜底）。忘了归零是手柄震动最常见的 Bug。' },
  ],
  'inputadv-13': [
    { type: 'text', title: '无障碍输入：让更多人能玩你的游戏', body: '无障碍（Accessibility）不是加分项，是「玩家能不能玩」的问题——色盲玩家、手部受限玩家、老年玩家、不同语言的玩家，都该玩得动。\n\n一、简化操作（Input Reduction）\n有些玩家做不了「快」或「多键同时」的操作，给「简化模式」：\n· 自动操作：自动连击、自动跳跃、自动瞄准\n· 减少按键：把需要按 2 个键的操作合并成 1 个键\n· 放慢输入需求：长按可以替代快速连按（按住蓄力替代 5 连点）\n\n二、辅助选项（Assist Options）\n· 自动瞄准（Aim Assist）：射击游戏标配，准星自动吸附，缩小有效判定\n· 减速（Slow Mode）：QTE、快速反应环节放慢速度或加宽输入窗口\n· 单键模式（One-Button Mode）：整个游戏一个键就能操作，专为手部受限玩家设计\n· 跳过操作：需要「精确时机」的环节给「跳过/降低难度」选项\n\n三、可访问性细节\n· 色盲：不要只靠颜色区分（红/绿），加形状、图标、文字辅助\n· 键位全可改：每个玩家都有自己顺手的键位（inputadv-18 会做保存）\n· 字幕：所有语音配字幕，声音信息不丢失\n· 灵敏度可调：摇杆/鼠标灵敏度给选项，不同玩家差异巨大\n· 界面文字大小、对比度可调\n\n四、实现上的注意\n· 选项要「可记忆」：辅助设置存 PlayerPrefs，重开还在\n· 默认值要保守：默认开着辅助总比默认关着好，或首次启动询问\n· 别把辅助做成「削弱」：辅助选项是「换一种方式玩」，不是「简单的作弊」——要有成就感\n\n五、测试方法\n· 用手柄、键鼠、触屏各测一遍\n· 试试「单手操作」：把鼠标拿走或把手柄放一边，能不能玩\n· 关掉声音玩一遍：信息还完整吗（字幕、图标提示）\n· 找真正的目标玩家测：无障碍设计是「替玩家想」，让玩家说话\n\n六、常见坑\n· 只做「难度选项」就算无障碍——真正的无障碍是「操作可达 + 信息可达」\n· 辅助开关藏太深——设置面板第一页就该有「无障碍」入口\n· 字幕和语音不同步——逐句对时间轴，宁慢勿快\n\n一句话总结：无障碍 = 操作可达（简化/自动/单键）+ 信息可达（字幕/图标/颜色替代）+ 可记忆（设置存档）；辅助选项让玩家「换种方式赢」，而不是替玩家赢。' },
    { type: 'quiz', question: '色盲玩家分不清红/绿两种敌人，但画面只用了颜色区分。最该做的改进是？', options: ['把红敌人改大一点', '在颜色之外再加形状、图标或文字区分', '把两种敌人改成一样的颜色'], answer: 1, tip: '无障碍原则：信息不能只靠一种通道（颜色）传达。加形状/图标/文字，色盲玩家也能区分——这是「信息可达」。' },
  ],
  'inputadv-14': [
    { type: 'text', title: '输入调试：输入「失灵」了？按图索骥', body: '输入是「看不见的」——玩家说「按键没反应」，问题可能在设备、绑定、映射、代码、UI 五层任意一处。这节教你怎么逐层排查。\n\n一、分层排查思路\n1. 设备层：设备被识别吗？（键鼠/手柄/触屏有没有被系统认出来）\n2. 绑定层：输入动作资产里，这条绑定生效吗？（键位对不对、有没有被其他绑定抢）\n3. 映射层：动作值读到了吗？（调试脚本打印 ReadValue）\n4. 代码层：我自己的逻辑对吗？（事件有没有订阅、条件对不对）\n5. UI 层：是不是被 UI 挡了？（inputadv-8 的点击穿透）\n\n二、Unity 自带工具\n· Input System 的 Input Debugger：Window → Analysis → Input Debugger。能看到：\n  - 所有设备的当前状态（哪个按键正被按下）\n  - 各 Action 的实时值\n  - 事件队列（每次输入事件的来源）\n· 场景里加 Input System UI Input Module + EventSystem，UI 的点击才有响应\n\n三、自己写调试面板\n写一个 DebugInput 脚本，把关键输入打印/显示在屏幕：\n\npublic class DebugInput : MonoBehaviour\n{\n    void OnGUI()\n    {\n        GUILayout.Label("Move: " + controls.Player.Move.ReadValue<Vector2>());\n        GUILayout.Label("Jump pressed: " + controls.Player.Jump.WasPressedThisFrame());\n        GUILayout.Label("Gamepad: " + (Gamepad.current != null));\n    }\n}\n\n跑真机时直接看到「输入到底来没来」。\n\n四、日志与追踪\n· 关键动作加 Debug.Log：重映射、方案切换、UI 拦截，记下「发生了什么」\n· 用条件编译：#if UNITY_EDITOR 里的调试代码发布时自动剔除\n· 事件追踪：给每个输入动作包一层「转发器」，统一打日志\n\n五、常见问题定位\n· 按键完全没反应：先查设备层（Input Debugger 里按一下，有没有亮）→ 查绑定层（动作资产的绑定对不对）\n· 手柄没反应：Gamepad.current 是 null 吗？连接了没有、被别的窗口抢了焦点没有\n· 有时候灵有时不灵：八成是冲突——两个绑定抢同一个键、或 UI 和游戏输入打架\n· 换了场景失效：输入组件没在 Awake 里重建，或事件没重新订阅\n\n六、常见坑\n· 忘了看 Input Debugger——它一打开，设备/绑定/事件全现形，比瞎猜快十倍\n· 调试日志发布时还在——Debug.Log 生产环境要关掉（或用日志系统分级）\n· 只测编辑器不测真机——真机设备差异、焦点问题都是编辑器里测不出的\n\n一句话总结：输入排查从下往上走——设备（Input Debugger 看亮不亮）→ 绑定（动作资产）→ 映射（ReadValue）→ 代码（逻辑/订阅）→ UI（穿透）；写个调试面板 + 日志，问题一抓一个准。' },
    { type: 'quiz', question: '玩家说「手柄 A 键没反应」，第一步最该做什么？', options: ['直接改代码重新发布', '打开 Input Debugger，按 A 键看设备有没有识别到这次输入', '把 A 键映射到键盘'], answer: 1, tip: '先查设备层：Input Debugger 里按 A，如果设备识别到、按键亮起，问题在绑定/代码层；如果设备根本没识别，问题在设备层。逐层定位，别跳过。' },
  ],
  'inputadv-15': [
    { type: 'text', title: '本地多玩家：一台电脑/一个屏幕，各玩各的', body: '本地多人（沙发合作/对战）是客厅游戏的核心。难点：怎么把「多个玩家」和「多套输入」对上号，还能分屏显示。\n\n一、多手柄识别\nInput System 里每台手柄是一个 Gamepad 设备，有独立 deviceId：\n· 拿设备列表：Gamepad.all 返回所有手柄\n· 玩家进游戏：按「任意键加入」（Player 1 按 X、Player 2 按 X……），把「玩家 ↔ 设备」绑定\n· 设备热插拔：手柄拔了，对应玩家的输入断了；重新插回要能重连\n\n二、PlayerInput 组件（官方方案）\nInput System 内置 PlayerInput：\n· 每个玩家一个 PlayerInput，配一个独立的 Input Action 资产\n· 属性：Default Map、Behavior（Send Messages / Invoke Unity Events / Invoke C# Events）\n· 自动配对：多个 PlayerInput 会自动「一人一个设备」，手柄不够还能自动切键鼠\n\n三、分屏输入\n分屏的难点不在输入，在「每个玩家的画面」：\n· 每个玩家一个 Camera，各自挂一个独立的输入动作对象\n· 竖分屏/横分屏/四格：用 Camera.rect 切区域\n· 关键：每个玩家的输入只驱动「自己的」角色，别串——每个玩家一个独立的 Player 对象 + 一个独立输入\n\n四、本地对战的输入架构\npublic class LocalPlayer : MonoBehaviour\n{\n    public int playerId;\n    private PlayerInput playerInput;      // 每个玩家独立\n\n    void Awake()\n    {\n        playerInput = GetComponent<PlayerInput>();\n    }\n    void Update()\n    {\n        Vector2 move = playerInput.actions["Move"].ReadValue<Vector2>();\n        // 只动自己的角色\n    }\n}\n\n五、手柄/键鼠混合\n· 玩家 1 用手柄、玩家 2 用键盘——PlayerInput 的「配对」要允许不同类型设备\n· 键盘做单人时，多人模式下键位要分成 P1/P2 两组（WASD + IJKL），或用 2 个键鼠设备\n\n六、常见坑\n· 所有玩家读同一个「当前手柄」——Gamepad.current 只返回「最近用的那个」，多人要按设备索引读\n· 分屏没分干净——两个 Camera 的 rect 重叠或留黑边，各区域要对齐\n· 手柄拔了角色失灵——没做「失联 → 暂停/重连」处理\n\n一句话总结：本地多人 = 每玩家一个独立输入对象（PlayerInput）+ 设备自动/手动配对 + 分屏各占一块 Camera；别用 Gamepad.current（只会取最近用的），按设备索引各读各的。' },
    { type: 'quiz', question: '本地双人对战时，两个手柄输入经常串到同一个角色身上。根因最可能是？', options: ['两个角色离得太近', '两个玩家都用 Gamepad.current 读输入，它只返回「最近用过的」那台', '手柄电量不足'], answer: 1, tip: 'Gamepad.current 永远只指「最近操作的那台手柄」——两人同时操作时它会跳来跳去。多人必须按设备索引/deviceId 固定绑定，各读各的。' },
  ],
  'inputadv-16': [
    { type: 'text', title: '移动端输入优化：让手机玩得「不别扭」', body: '手机和 PC 的输入完全是两回事：没有鼠标、屏幕就那么大、手指还挡画面。移动端优化，就是把这三点化解掉。\n\n一、触屏手感三件事\n· 跟手度：手指一动，角色就该动——输入要「立即读、立即用」，别等下一帧\n· 灵敏度：摇杆/滑屏的灵敏度给玩家可调（玩家差异极大）\n· 阻尼/惯性：滑屏（相机）带一点惯性更顺滑，但要可关（有玩家晕）\n\n二、UI 适配（Safe Area）\n全面屏手机刘海、圆角会挡 UI。Safe Area（安全区）是系统给出的「不被遮挡区域」：\n· Canvas → 挂 SafeArea 脚本，把 RectTransform 限制在安全区内\n· 常用做法：代码取 Screen.safeArea，缩进 UI\n· 关键操作按钮（攻击、跳跃）别放在屏幕边缘被刘海挡住\n\n三、防误触\n· 手指按键区域：按钮别太小，至少 44×44 pt（苹果人机交互指南建议）\n· 间隔：相邻按钮留间距，防「想按 A 碰到 B」\n· 延迟判定：点击/滑动用「松手确认」，滑动会按到按钮时，位移超阈值就不触发点击\n· 常用按键靠近拇指：右手玩家的攻击键在右下角，别放屏幕中间\n\n四、点击区域的「热区」\n· 视觉小、点击区域大：小图标套一个更大的透明点击区域，好点\n· 斜方向好点：方向键/摇杆区做大，边缘留余量\n· 别指望手指精准：指尖触点其实比看起来大，热区要宽容\n\n五、性能与输入\n· 输入处理别卡帧：Update 里别做重活，触摸检测要和帧率解耦\n· 低端机 30 帧：输入窗口用秒算（inputadv-10），别用帧数，否则手感 30 帧和 60 帧差一倍\n\n六、真机测试清单\n· 真机 + 模拟器各测一遍（模拟器的触摸手感不准）\n· 左手横屏右手横屏都测（按键位置左右手不同）\n· 全屏手势（从边缘滑入）和游戏手势冲突——手机系统的返回手势和游戏内滑动手势要避开\n\n一句话总结：移动端 = 跟手（立即读立即用）+ 安全区（别被刘海挡）+ 防误触（热区要大、松手确认）+ 灵敏度可调 + 输入用秒算；真机测两种握持方向。' },
    { type: 'quiz', question: '手机全面屏上，游戏右上角的「暂停」按钮一半被刘海挡住了。最标准的解法是？', options: ['把按钮挪到屏幕正中间', '用 Safe Area：把 UI 限制在系统安全区内，避开刘海/圆角', '把按钮缩小一半'], answer: 1, tip: 'Safe Area 是系统给的不被遮挡区域。UI 挂 SafeArea 脚本自动缩进到安全区内，是全面屏适配的标准做法——不是把按钮随便挪走。' },
  ],
  'inputadv-17': [
    { type: 'text', title: '输入事件系统：把输入变成「广播」', body: '前面的输入都是「轮询」——每帧问一遍「跳了吗」。游戏一大，这种写法会把输入逻辑散得到处都是。事件系统（Event-driven）是把输入「广播」出去，谁关心谁订阅。\n\n一、轮询 vs 事件\n· 轮询：每帧检查，拿到就用。简单直接，但「想用输入的每个地方都要自己查」，耦合高\n· 事件：输入发生了，发一个事件，订阅者响应。解耦，谁关心谁听\n· 适用：轮询适合「持续状态」（移动方向，每帧都在用）；事件适合「瞬发动作」（跳跃、开火，按下那一下触发）\n\n二、C# 事件/委托做输入事件\npublic class InputEvents\n{\n    public static System.Action OnJump;\n    public static System.Action OnFire;\n}\n\n// 输入层（唯一知道设备的地方）\nvoid Update()\n{\n    if (controls.Player.Jump.WasPressedThisFrame())\n        InputEvents.OnJump?.Invoke();\n}\n\n// 角色层（不知道设备，只订阅）\nvoid OnEnable() { InputEvents.OnJump += HandleJump; }\nvoid OnDisable() { InputEvents.OnJump -= HandleJump; }\nvoid HandleJump() { /* 跳 */ }\n\n好处：加新功能只需要订阅事件，不用改输入层；输入层换设备，角色层无感知。\n\n三、参数事件\n只发「跳了」不够，有时要带数据：方向、力度、玩家 ID。\npublic static System.Action<Vector2, float> OnMove;   // 方向 + 力度\n发射时带参数，订阅者按需取。用强类型委托比「object 万能参数」安全，编译期就查错。\n\n四、消息中心（Message Bus）\n事件多了会乱，做个统一的消息中心：\n· 注册表：Dictionary<string, Action> 或「事件类型 → 订阅列表」\n· 发布（Publish）/ 订阅（Subscribe）两个接口\n· 好处：任何系统都能发消息、订阅消息，解耦更彻底；代价：类型安全弱一点、查「谁在听」要翻代码\n\n五、订阅生命周期（最关键的坑）\n事件订阅「必须成对」：\n· OnEnable 订阅、OnDisable 退订——否则对象销毁了还被事件引用，变成「幽灵回调」或内存泄漏\n· 单例事件全局存：场景切换时清空或重新绑定\n· 常见 Bug：订阅了没退订，同一次输入被触发 N 次（每重建一次角色多一份回调）\n\n六、常见坑\n· 该用轮询用了事件——「移动方向」每帧都要值，用轮询/ReadValue 更方便；事件适合「瞬发」\n· 事件回调里做重活——回调在输入帧里同步执行，别在里面加载资源、阻塞\n· 忘了退订——场景切换后旧对象还能收到输入，一帧触发几十次\n\n一句话总结：轮询适合持续状态、事件适合瞬发动作；输入层发广播、玩法层订阅；订阅必须成对（OnEnable/OnDisable），否则幽灵回调 + 内存泄漏。' },
    { type: 'quiz', question: '角色组件销毁后，仍然每帧被调用 N 次输入回调，越重建越卡。最可能是？', options: ['事件订阅了但没退订，旧实例还被事件系统引用', '输入层没启用', 'Update 里循环太多'], answer: 0, tip: '订阅必须成对：OnEnable 订阅、OnDisable 退订。不退订，销毁的实例仍被事件引用，每次输入都会回调它——越重建越多的典型「幽灵回调」。' },
  ],
  'inputadv-18': [
    { type: 'text', title: '玩家偏好：键位和灵敏度，让玩家自己说了算', body: '每个玩家的手不一样：有人喜欢 WASD、有人喜欢方向键，有人鼠标灵敏度高、有人低。好的游戏让玩家「自定义」并「记住」。\n\n一、键位重映射回顾\ninputadv-2 讲过重映射的 API（Binding Override）。这节讲「怎么存、怎么恢复」：\n· 玩家改了键 → 存进 PlayerPrefs（按 action + binding 的名字）\n· 启动时读档 → 应用覆盖\n· 默认值要有「恢复默认」按钮\n\n二、持久化方案\n· PlayerPrefs（简单够用）：存「动作名:绑定索引 → 新路径」，如 "Move:1" = "<Keyboard>/a"\n· JSON 存档（结构化）：玩家整套配置存成一个 JSON：方案名、各动作绑定、灵敏度、摇杆死区\n· 云端/账户（大项目）：跨设备同步\n\n三、灵敏度与偏好一起存\n把「输入偏好」做成一个类，整体存/读：\npublic class InputPreferences\n{\n    public float mouseSensitivity = 1f;\n    public float joystickSensitivity = 1f;\n    public float joystickDeadZone = 0.15f;\n    public bool invertY = false;\n    public Dictionary<string, string> keyOverrides = new Dictionary<string, string>();\n}\n存 JSON 字符串到 PlayerPrefs，读档时反序列化。\n\n四、自定义方案（Preset）\n给玩家几种现成方案，再加自定义：\n· 预设：默认（WASD）、方向键、手柄、单手（简化）——inputadv-13 的无障碍\n· 自定义：在预设基础上改，改完存成新方案\n· 方案切换：菜单里一键切换，改完立即生效、立即存\n\n五、启动流程\n1. 读档：拿保存的方案/覆盖\n2. 应用：把 override 写进 InputActionAsset（用 LoadBindingOverridesFromJson 之类）\n3. 校验：重映射过、但目标键被别的动作占了？提示冲突，让玩家改\n4. 界面：设置页显示「当前键位」，让玩家看着改\n\n六、常见坑\n· 键位没存——改完重启打回原形；每次改完立即 Save\n· 存档损坏/版本不符——反序列化失败要能回退到默认，别崩\n· 冲突不提示——玩家把「跳跃」设成 W，和「前进」冲突了，两个一起触发；重映射时检查冲突\n· 忘了「恢复默认」——玩家的救命按钮，必须有\n\n一句话总结：输入偏好 = 键位覆盖 + 灵敏度 + 死区等，存成一套配置（PlayerPrefs/JSON），启动读档应用；提供预设方案 + 恢复默认 + 冲突检测。' },
    { type: 'quiz', question: '玩家把「跳跃」改绑到 W 键，重启游戏后却变回默认。最可能的原因？', options: ['改键后没保存到 PlayerPrefs（或保存失败）', 'W 键太常用', '跳跃不需要键位'], answer: 0, tip: '重映射只改了内存里的绑定，不保存就丢。每次改键后要立即写 PlayerPrefs（或 JSON 存档），启动时读档应用覆盖。' },
  ],
  'inputadv-19': [
    { type: 'text', title: '输入架构实战：一条「输入层」通吃整个游戏', body: '前几节零件都有了：Input Action、重映射、多设备、事件、偏好。这节把它们装进一个完整的「输入层」——管理、切换、保存、扩展，一套通吃。\n\n一、分层总览\n· 输入源层：Input System 的设备收数据（键鼠/手柄/触屏），统一成 InputActionAsset\n· 输入管理层（InputManager）：单例，管动作资产的启用/禁用、方案切换、设备配对\n· 事件层：把动作转成 C# 事件/委托广播（inputadv-17）\n· 偏好层：读/存玩家自定义（键位覆盖 + 灵敏度，inputadv-18）\n· 消费层：角色、UI、相机，只订阅事件、只读动作\n\n二、InputManager 的核心职责\npublic class InputManager : MonoBehaviour\n{\n    public static InputManager Instance;\n    public InputActionAsset actions;\n\n    void Awake()\n    {\n        Instance = this;\n        LoadPreferences();                 // 读档\n        actions.Enable();\n    }\n\n    public void SwitchMap(string mapName)  // 切换方案（地面/载具/菜单）\n    {\n        foreach (var map in actions.actionMaps)\n            map.Enable(map.name == mapName);\n    }\n    void LoadPreferences() { /* 应用 keyOverrides、灵敏度、死区 */ }\n    public void SavePreferences() { /* 序列化存盘 */ }\n}\n\n三、切换与启用管理\n· 不同游戏状态用不同 Map：默认（Default）/ 菜单（UI）/ 载具（Vehicle）\n· 切换时机：进菜单 → SwitchMap("UI")，回游戏 → SwitchMap("Default")\n· 注意：同一动作多个 Map 里定义，切换要「只开一个」，避免冲突\n\n四、设备配对\n· 单机：自动用「当前设备」\n· 本地多人：每玩家绑定设备（inputadv-15）\n· 管理类里提供 GetPlayerDevice(playerId)，消费层不关心设备\n\n五、扩展新动作\n加一个新操作（比如「翻滚」）只动三步：\n1. InputActionAsset 里加动作 + 绑定\n2. 事件层加一个事件类型\n3. 消费层订阅它\n其余层零改动——这就是「扩展性」的意义。\n\n六、架构的好处与代价\n好处：\n· 玩法代码不再出现任何设备名（GetKey、Gamepad 都不出现）\n· 换设备/加方案只动输入层\n· 测试能喂假输入（inputadv-6 的 IInputSource 思路，用 mock 输入层）\n代价：多一层抽象，小项目用起来重。判断标准：超过 10 个输入动作、要换设备、要自定义键位——就值得。\n\n一句话总结：完整输入层 = 设备层收数据 + 管理层管切换/配对 + 事件层广播 + 偏好层存取 + 消费层只订阅；加新动作改三处，设备方案变化只动输入层。' },
    { type: 'component', title: '输入层五层架构', items: [
      { name: '输入源层', desc: '键鼠/手柄/触屏统一成 InputActionAsset' },
      { name: '管理层', desc: '单例管启用/切换 Map/设备配对' },
      { name: '事件层', desc: '动作转 C# 事件广播，解耦' },
      { name: '偏好层', desc: '键位覆盖 + 灵敏度存取' },
      { name: '消费层', desc: '角色/UI 只订阅事件，不碰设备' },
    ] },
    { type: 'quiz', question: '输入架构里，为什么「消费层」（角色、相机）永远不该出现设备名（如 Gamepad、Keyboard）？', options: ['写设备名会编译报错', '一旦出现设备名，换设备/加方案就得改玩法代码，架构的「解耦」就破了', '设备名太长不好记'], answer: 1, tip: '解耦的意义在「变更不扩散」：消费层只认抽象动作/事件，设备怎么来都无所谓。出现设备名 = 耦合回来了，改键位/换设备就得跟着改。' },
  ],
  'inputadv-20': [
    { type: 'text', title: '动手练：做一个完整输入系统', body: '把 inputadv 1~19 节拧成一套：给一个「俯视角射击小游戏」做一套完整输入层——支持键鼠/手柄/触屏、可重映射、可保存、可切换方案、带 UI 拦截和事件解耦。\n\n🎯 任务目标：实现一个输入层，满足：移动（摇杆/WASD/手柄左摇杆）、射击、翻滚、暂停；键位可在设置里改并保存（PlayerPrefs）；提供「默认/自定义」两个方案；UI 上点击不触发游戏操作；输入通过事件广播，角色代码不含任何设备名；可切换「地面/菜单」两套动作方案。\n\n📦 前置要求：学完本课 1~19 节，会用 Input Action 资产、Input System 事件、重映射 API。\n\n📋 动手步骤：\n1. 建 InputActionAsset：两个 Map——Gameplay（Move / Fire / Dodge / Pause）和 UI（Navigate / Confirm / Cancel）\n2. 写 InputManager 单例：Enable 动作资产、SwitchMap 切方案、Load/Save 偏好\n3. 定义 InputPreferences：键位覆盖字典 + 灵敏度 + 摇杆死区，用 JSON 序列化存 PlayerPrefs\n4. 写事件层：把动作转成 C# 事件（OnMove、OnFire、OnDodge、OnPause），订阅成对（OnEnable/OnDisable）\n5. 角色代码只订阅事件：Player 组件里 OnMove 事件驱动移动、OnFire 开火，代码里不出现 Gamepad/Keyboard\n6. 接触屏：给移动加虚拟摇杆（inputadv-11），输出喂进同一个 Move 动作；Fire 给一个屏幕按钮\n7. UI 拦截：角色输入前检查 IsPointerOverGameObject（inputadv-8），点在 UI 上不下发\n8. 重映射设置页：显示当前键位，玩家点「改」→ 按新键 → 保存覆盖 + 检查冲突（inputadv-18）\n9. 方案切换：设置页提供「恢复默认」；保存一套「自定义」方案，启动时加载\n10. 手柄：接 Gamepad 绑定（左摇杆 Move、右扳机 Fire、B 键翻滚），支持热插拔判空\n\n✅ 验收清单：\n能 □ 键鼠、手柄、触屏三种设备都能玩，切设备不用改代码\n能 □ 设置里改键位，重启游戏后键位还在；「恢复默认」能还原\n能 □ 点 UI 按钮不会误触角色射击/翻滚\n能 □ 角色代码里搜不到 Gamepad / Keyboard / GetKey 等设备名\n能 □ 进菜单切到 UI 方案，回游戏切回 Gameplay，动作不串\n能 □ 手柄拔了再接，还能继续玩（判空 + 重连）\n\n🕳 常见卡壳：\n坑 1：切方案串动作——两个 Map 都开了，同一动作两个来源；SwitchMap 要「只开目标 Map」\n坑 2：重映射不生效——改的是内存绑定，没存也没在启动时 LoadBindingOverrides；每次改动立即 Save\n坑 3：事件订阅后角色被销毁还收到输入——OnEnable/OnDisable 没成对，旧实例幽灵回调\n坑 4：真机手柄没反应——Gamepad.current 在拔出时会变 null，操作前判空\n\n一句话总结：一套输入层 = InputActionAsset 管绑定 + InputManager 管切换/存档 + 事件层解耦 + 偏好层记忆 + UI 拦截防穿透；验收标准就是「三种设备都能玩、键位能记住、角色代码不碰设备名」。' },
    { type: 'quiz', question: '验收标准里有一条：「角色代码里搜不到 Gamepad / Keyboard 等设备名」。这条检验的是？', options: ['代码注释写得好', '输入层解耦是否彻底：设备相关细节都被挡在输入层，玩法代码只认抽象动作/事件', '角色不吃输入'], answer: 1, tip: '这正是解耦的验收：设备名只允许出现在输入层。玩法代码里出现设备名 = 耦合泄漏，换设备就要改玩法代码——架构就失败了。' },
  ],
}
