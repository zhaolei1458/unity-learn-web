export const lessons = [
  { id: 'editor-5', title: '自定义属性绘制器' },
  { id: 'editor-6', title: '编辑器自动化' },
  { id: 'editor-7', title: '资产导入处理' },
  { id: 'editor-8', title: 'Package Manager 与资源生态' },
  { id: 'editor-9', title: 'ScriptableObject 深入' },
  { id: 'editor-10', title: '自定义窗口实战' },
  { id: 'editor-11', title: '场景工具与 Gizmos' },
  { id: 'editor-12', title: '一键构建工具' },
  { id: 'editor-13', title: '代码生成工具' },
  { id: 'editor-14', title: '编辑器性能' },
  { id: 'editor-15', title: '资产导入实战' },
  { id: 'editor-16', title: '序列化与撤销' },
  { id: 'editor-17', title: '批量操作' },
  { id: 'editor-18', title: '菜单系统进阶' },
  { id: 'editor-19', title: '编辑器测试' },
  { id: 'editor-20', title: '编辑器工具综合动手练' },
]

export const cards = {
  'editor-5': [
    { type: 'text', title: '自定义属性绘制器', body: '前面我们用 [CustomEditor] 接管了「整个组件」的检查器。这一节讲更精细的工具——PropertyDrawer（属性绘制器）：它只管「一个序列化字段」怎么画。\n\n区别先分清：\n· CustomEditor —— 管整个组件：重画整个面板、加按钮、加标题。\n· PropertyDrawer —— 管一个字段：比如把一个 int 画成滑块、把一个结构体画成一排联动控件。多个脚本共用同一个自定义类型时，PropertyDrawer 的复用价值最高。\n\n经典场景：做一个 RangedFloat（带最小/最大的范围值），想让它显示成一根「能拖两端的范围滑块」。\n\n第一步，定义数据结构（普通脚本）：\n\n[System.Serializable]\npublic class RangedFloat\n{\n    public float min = 0f;\n    public float max = 1f;\n}\n\n第二步，写属性绘制器（必须放 Editor 文件夹）：\n\nusing UnityEditor;\nusing UnityEngine;\n\n[CustomPropertyDrawer(typeof(RangedFloat))]\npublic class RangedFloatDrawer : PropertyDrawer\n{\n    public override void OnGUI(Rect position, SerializedProperty property, GUIContent label)\n    {\n        SerializedProperty minProp = property.FindPropertyRelative(\"min\");\n        SerializedProperty maxProp = property.FindPropertyRelative(\"max\");\n        float min = minProp.floatValue;\n        float max = maxProp.floatValue;\n        Rect r = EditorGUI.PrefixLabel(position, label);\n        EditorGUI.MinMaxSlider(r, ref min, ref max, 0f, 100f);\n        minProp.floatValue = min;\n        maxProp.floatValue = max;\n    }\n}\n\n逐块拆解：\n· [CustomPropertyDrawer(typeof(RangedFloat))] —— 声明「我这个绘制器负责 RangedFloat 类型」。\n· OnGUI(Rect position, SerializedProperty property, GUIContent label) —— 每个该类型的字段被画时都会调它。\n· FindPropertyRelative(\"min\") —— 拿到结构体里叫 min 的子字段。\n· EditorGUI.MinMaxSlider —— 现成的双端滑块，直接改 min/max。\n\n给字段贴 [Tooltip]、[Range]、[Header] 等特性，PropertyDrawer 也能读到——你还能自定义「特性 + 绘制器」组合，比如 [HealthColor] 让血量字段按数值变色。\n\n几个必须避开的坑：\n· OnGUI 每帧都会跑，别在里面 Instantiate、别做耗时操作。\n· 结构体里的字段要加 [System.Serializable]，或者直接 public，否则序列化不进去，drawer 根本不会被调用。\n· 改完属性记得 ApplyModifiedProperties()，否则改动可能不被记录。\n· 想画成两行甚至自适应高度，重写 GetPropertyHeight() 并处理 position.height。\n\n一句话总结：PropertyDrawer 只画一个字段，配 [CustomPropertyDrawer] 声明负责谁，配合 FindPropertyRelative 读写子字段——自定义类型配个专属绘制器，所有用到它的脚本全都变好看。' },
    { type: 'component', title: 'PropertyDrawer 常用 API', items: [
      { name: 'CustomPropertyDrawer', desc: '声明此绘制器负责哪个类型' },
      { name: 'OnGUI', desc: '每帧绘制该字段的入口，参数带 Rect 和 SerializedProperty' },
      { name: 'FindPropertyRelative', desc: '拿到结构体里的子字段继续读写' },
      { name: 'GetPropertyHeight', desc: '需要异形高度时重写，控制占用空间' },
      { name: 'ApplyModifiedProperties', desc: '把修改写回序列化对象，忘了改会丢' },
    ] },
    { type: 'quiz', question: '想让一个自定义的序列化类型 RangedFloat（min/max 两个 float）显示成一根双端范围滑块，最合适的做法？', options: ['用 CustomEditor 接管整个组件重画', '写一个 PropertyDrawer 加 [CustomPropertyDrawer(typeof(RangedFloat))]，重画这个字段', '写一个 MenuItem 菜单手动处理'], answer: 1, tip: 'PropertyDrawer 正是「专画某一种字段」的工具：CustomPropertyDrawer 声明负责的类型，OnGUI 里用 FindPropertyRelative 拿到 min/max，MinMaxSlider 一画就成，所有脚本用到这个类型都自动变好看。' },
  ],
  'editor-6': [
    { type: 'text', title: '编辑器自动化', body: '编辑器的重复劳动——批量改名字、批量设参数、打包三平台——都该交给「自动化」：菜单点一下，全自动干完。这一节讲编辑器自动化的三板斧。\n\n一、MenuItem 自动化命令。凡是「固定步骤的重复操作」都能做成菜单。常见范式：\n· 批量重命名 —— 选一批物体，脚本统一改名字（加前缀、编号）。\n· 批量设属性 —— 一键把场景里所有 Light 的强度统一、把所有材质改成某个 Shader。\n· 批量生成 —— 从配置表批量建预制体、批量创建资产（第 4 节学过 ScriptableObject 工厂）。\n\n[MenuItem(\"Tools/批量为选中物体加序号\")]\nstatic void RenameSelected()\n{\n    for (int i = 0; i < Selection.gameObjects.Length; i++)\n        Selection.gameObjects[i].name = \"Obj_\" + i;\n}\n\n注意 [MenuItem] 的第三个参数还能控制「菜单优先级」（决定菜单位置），Validate 方法（同名 + 前缀 Validate）能控制菜单可用状态。\n\n二、命令行批处理（Batch Mode）。给自动化装上「无人值守」——不开 Unity 界面直接跑：\n\nUnity.exe -batchmode -projectPath D:/Game -executeMethod BuildScript.PerformBuild -quit -logFile build.log\n\n要点：\n· -batchmode 无界面模式，-quit 跑完退出，-executeMethod 执行静态方法。\n· 批处理里不能弹对话框、不能依赖编辑器窗口，代码要写成纯逻辑。\n· 结果看 -logFile 的日志和进程退出码。\n· 打包脚本里用 BuildPipeline.BuildPlayer 指定场景列表、平台、输出路径。\n\nstatic void PerformBuild()\n{\n    string[] scenes = { \"Assets/Scenes/Main.unity\" };\n    BuildPlayerOptions opt = new BuildPlayerOptions\n    {\n        scenes = scenes,\n        locationPathName = \"Build/win/Game.exe\",\n        target = BuildTarget.StandaloneWindows64,\n        options = BuildOptions.None\n    };\n    BuildReport report = BuildPipeline.BuildPlayer(opt);\n    if (report.summary.result == BuildResult.Succeeded)\n        UnityEngine.Debug.Log(\"打包成功\");\n}\n\n这就是 CI/CD（持续集成）的接缝：GitHub Actions、Jenkins、GitLab CI 都能起一个带 Unity 的环境，定时/推代码触发这个命令，自动打包上传。\n\n三、Editor 脚本常用模式。\n· EditorApplication.update —— 编辑器每帧回调，适合做「实时预览」工具（编辑器模式下实时改场景）。\n· EditorApplication.delayCall —— 当前帧末尾执行一次，适合「菜单点了再等一帧」的稳妥操作。\n· Undo.RecordObject —— 改场景前先记录，Ctrl+Z 能撤销，专业工具必备。\n· AssetDatabase 全家 —— 建文件夹、建资产、刷新、定位。\n\n一句话总结：菜单化重复操作、Batch Mode 无人值守、BuildPipeline 打包、EditorApplication 做实时工具——编辑器自动化就是把这些模式拼起来，让机器替你干体力活。' },
    { type: 'component', title: 'Editor 脚本常用模式', items: [
      { name: 'MenuItem', desc: '菜单命令入口，+Validate 控制可用状态' },
      { name: 'BuildPipeline.BuildPlayer', desc: '代码里打包的入口，指定场景/平台/输出' },
      { name: 'Batch Mode 批处理', desc: '-batchmode -executeMethod 无人值守跑脚本' },
      { name: 'EditorApplication.update', desc: '编辑器每帧回调，做实时预览' },
      { name: 'Undo.RecordObject', desc: '改场景前记录，撤销有救' },
    ] },
    { type: 'quiz', question: '想在不开 Unity 界面的情况下，由 CI 机器自动执行打包 Windows 版本，入口应该是什么？', options: ['写一个静态方法，用 Unity.exe 的 -batchmode -executeMethod 参数在命令行调用它', '让 CI 机器打开 Unity 编辑器手动点 Build', '写一个 DllImport 调用显卡驱动'], answer: 0, tip: '-batchmode -quit -executeMethod 方法名 是 Unity 命令行批处理的标配：无界面运行、执行静态方法、跑完退出——CI 打包全靠这个接缝。' },
  ],
  'editor-7': [
    { type: 'text', title: '资产导入处理', body: '资源导入是 Unity 里「最偷偷摸摸」的一道工序：你往 Assets 里丢一张图，Unity 自动导入、生成 meta 文件、给你一堆默认导入设置。默认设置未必适合你的项目——UI 图要 Sprite、移动端贴图要压缩、模型要生成碰撞体。手动一张张调？几百张贴图能调疯。AssetPostprocessor 就是为这生的：导入时自动处理。\n\n一、AssetPostprocessor 是什么。一个编辑器类，你重写它的回调，Unity 在每次导入资产的前后自动调用。最常用的回调：\n· OnPreprocessTexture —— 贴图导入前，改导入设置。\n· OnPreprocessModel —— 模型导入前，改模型设置。\n· OnPostprocessAllAssets —— 一批导入完成后，做批量操作。\n\n二、按命名规则自动设置。经典玩法：路径里带什么关键字，就套什么设置。\n\npublic class AutoImportSetting : AssetPostprocessor\n{\n    void OnPreprocessTexture()\n    {\n        TextureImporter importer = assetImporter as TextureImporter;\n        string path = importer.assetPath;\n\n        if (path.Contains(\"UI_\"))\n        {\n            importer.textureType = TextureImporterType.Sprite;\n            importer.mipmapEnabled = false;\n            importer.spriteImportMode = SpriteImportMode.Single;\n        }\n        else if (path.Contains(\"Lightmap_\"))\n        {\n            importer.textureType = TextureImporterType.Lightmap;\n        }\n        else\n        {\n            importer.maxTextureSize = 1024;\n            importer.compressionQuality = 50;\n        }\n    }\n}\n\n从此「UI_开头的图 = Sprite + 关 Mipmap、Lightmap_开头 = 光照贴图」就成了团队铁律，谁丢进 Assets 都会被自动套上，肉眼可见地省掉一批人工。\n\n三、模型导入的常用自动化：\n· 名字含 _col 的模型自动生成碰撞体（importer.addCollider = true）。\n· 动画模型的动画导入设置统一（AnimationType、Avatar）。\n· 自动压缩到目标平台格式，移动端打包时免去手工压一遍。\n\n四、OnPostprocessAllAssets 的批量场景：\n· 导入完成自动校验：非法命名直接报错提醒。\n· 自动生成 AssetBundle 变体清单、自动打图集（Sprite Atlas）。\n· 根据导入的新资源自动刷新某个配置表。\n\n必须注意的三个坑：\n· 回调里改动要「轻」——导入本身就很慢，回调再重会雪上加霜；OnPostprocessAllAssets 每次导入都会跑，别做重活。\n· 别在回调里触发「改 → 再导入 → 再回调」的死循环，改设置后不要再动会触发重新导入的资源。\n· 判路径、判类型用 assetImporter.assetPath，别写死绝对路径（项目换位置就废了）。\n\n一句话总结：AssetPostprocessor 是导入管线的「钩子」——OnPreprocessTexture/Model 改设置、OnPostprocessAllAssets 做批量，按命名规则自动套参数，导入规范从此不用靠自觉。' },
    { type: 'component', title: 'AssetPostprocessor 常用回调', items: [
      { name: 'OnPreprocessTexture', desc: '贴图导入前改设置：类型/压缩/Mipmap' },
      { name: 'OnPreprocessModel', desc: '模型导入前：碰撞体/动画/缩放' },
      { name: 'OnPostprocessAllAssets', desc: '整批导入完成后批量处理' },
      { name: 'assetImporter.assetPath', desc: '当前导入资源的路径，判断规则的依据' },
      { name: 'TextureImporter', desc: '改贴图设置的总入口' },
    ] },
    { type: 'quiz', question: '想让所有名字以 UI_ 开头的贴图，导入时自动变成 Sprite 且关掉 Mipmap，用什么最省事？', options: ['写个工具一张张手动处理', '在 AssetPostprocessor 的 OnPreprocessTexture 里按路径判断自动设置', '在 Project Settings 里加一条全局规则'], answer: 1, tip: 'AssetPostprocessor 的 OnPreprocessTexture 在贴图导入前执行，用 assetPath.Contains 判断命名规则，改 TextureImporter 的类型和 Mipmap，从此导入即规范。' },
  ],
  'editor-8': [
    { type: 'text', title: 'Package Manager 与资源生态', body: '这一节讲 Unity 的「软件商店」Package Manager，以及外面的 Asset Store 资源生态——怎么装官方功能、怎么找第三方资源。\n\n一、Package Manager 是什么。\nUnity 把「功能扩展」做成了包（Package）的形式，Package Manager（Window → Package Manager）是统一的管理窗口：装、卸、更新、查版本全在这。类比手机的 App Store——想装个新功能，进去搜、点安装，完事。\n\n二、怎么装官方功能包。\n1. 打开 Window → Package Manager。\n2. 左上角包来源下拉框，切到 Unity Registry（官方注册表）——官方包都在这里。\n3. 搜索框输入包名（比如 Input System），选中，右下角点 Install。\n4. 装完看提示：有的包要重启编辑器，有的会弹「启用新输入系统」的确认，照做即可。\n装完去哪找它：Project 视图右键 Create 菜单里多了新选项（比如 2D Animation 装完有骨架相关菜单）、Window 菜单多了新窗口、组件的 Add Component 搜索框能搜到新组件。\n\n三、常用的官方包（装上即用）。\n· Input System —— 新一代输入系统，替代旧的 Input Manager，手柄、多设备统一管理\n· Cinemachine —— 智能相机：跟随、过场、防抖全自动\n· Timeline —— 过场动画、关卡演出，像剪辑软件一样拖时间轴\n· UI Toolkit —— 新一代 UI 系统，比旧 UGUI 更现代\n· TextMeshPro —— 高质量文字渲染，做 UI 文字首选\n· 2D Animation / 2D IK —— 2D 骨骼动画（d2d-6 提过）\n· Multiplayer / Netcode —— 联机框架\n\n四、内置包 vs 第三方包。\n· 内置模块（Built-in）：跟着编辑器走的功能模块，一般不用装。\n· Unity Registry：官方维护、UPM（Unity Package Manager）分发的包，能一键更新。\n· 第三方包：从 Git URL 装（Package Manager → Add package from git URL）、从本地路径装，或从 Asset Store 导入。各有各的渠道，装完都要小心版本兼容。\n· 版本管理：每个包有自己的版本号。升级包是「改炸弹」——大版本升级可能改 API、破坏已有代码。团队项目务必所有人装同一个版本，升级前先看官方 changelog（更新日志）。\n\n五、Asset Store 商店怎么逛怎么装。\nAsset Store（Window → Asset Store，或浏览器 assetstore.unity.com）是第三方资源商店：模型、贴图、插件、完整模板，免费付费都有。\n流程：\n1. 搜索/筛选想要的资源，点 Download（免费）或购买。\n2. 下载后回到 Unity 编辑器，Package Manager 里切到 My Assets 标签，能看到已拥有的资源，点 Import。\n3. Import 时弹窗让你勾「导入哪些资源」，不用的取消勾选，点 Import。\n也可以直接下载 .unitypackage 文件，用 Assets → Import Package → Custom Package 选择导入。\n导入后注意：Console 里飘红是常态——先看报错（多半是版本不兼容），解决不了查资源文档或回退版本。\n\n六、装包后常用在哪（快速自查）。\n· Create 菜单 / Window 菜单 / Add Component 能搜到新东西 → 装成功了\n· 菜单里没出现 → 包可能装错来源（装到别的项目了）或需要重启编辑器\n· 团队统一包版本：Package Manager 里所有人装同一版本，混着用必出问题\n\n一句话总结：官方功能走 Package Manager 的 Unity Registry 一键装，第三方资源走 Asset Store 导入，装包先看版本兼容、升级先看 changelog——资源生态的门道就这三条。' },
    { type: 'component', title: '常用官方功能包', items: [
      { name: 'Input System', desc: '新一代输入系统，替代旧 Input Manager' },
      { name: 'Cinemachine', desc: '智能相机：跟随、过场、防抖' },
      { name: 'Timeline', desc: '过场动画与关卡演出，时间轴剪辑' },
      { name: 'UI Toolkit', desc: '新一代 UI 系统，比 UGUI 更现代' },
      { name: 'TextMeshPro', desc: '高质量文字渲染，UI 文字首选' },
      { name: '2D Animation', desc: '2D 骨骼动画（d2d-6 提过）' },
    ] },
    { type: 'quiz', question: '想在 Unity 里装官方的 Input System 新输入系统包，正确的入口是？', options: ['浏览器去 Asset Store 搜 Input System 下载', 'Window → Package Manager，包来源切到 Unity Registry，搜索并 Install', '从 GitHub 下载源码手动拷进 Assets 文件夹'], answer: 1, tip: '官方功能包都在 Package Manager 的 Unity Registry 里，搜索安装一键完成、还能后续更新；Asset Store 是第三方资源商店，手动拷源码则丢掉版本管理和依赖解析。' },
  ],
  'editor-9': [
    { type: 'text', title: 'ScriptableObject 深入', body: 'editor-4 讲过 ScriptableObject（SO）能当「数据资产」。这一节往深里挖：SO 为什么是配置神器、怎么用它跨场景共享数据、怎么让团队都能安全地配置。\n\n一、SO 到底是什么。一个普通类，用 [CreateAssetMenu] 装饰后，就能在 Project 里「右键 → Create → 你的菜单」生成一个 .asset 文件。\n\n[CreateAssetMenu(menuName = \"Config/EnemyData\", fileName = \"EnemyData\")]\npublic class EnemyData : ScriptableObject\n{\n    public string displayName;\n    public int hp;\n    public float speed;\n    public Sprite icon;\n}\n\n它和 MonoBehaviour 的本质区别：SO 是「数据」，挂在项目里，不挂在场景物体上。一个 .asset 文件可以被无数个场景、无数个物体引用——同一份配置，全项目通用。\n\n二、SO 的三大用武之地。\n· 数据资产 —— 怪物数值表、技能表、卡牌库、关卡配置：建一个文件夹专放 .asset，配表的人不用碰代码。\n· 跨场景共享 —— 场景切换不销毁的「共享数据」。没有 SO 的话，跨场景传数据要 DontDestroyOnLoad 挂个单例；有了 SO，直接用引用。\n· 事件通道 / 全局变量 —— 把「广播事件」做成 SO 资产，任何脚本都能发、能听，模块之间解耦（比如一个 GameEvent SO：别处 raise，这里 listen）。\n\n三、跨场景共享的正确姿势。\npublic class GameState : ScriptableObject\n{\n    public int score;\n    public int level;\n}\n\n// 场景 A 的脚本\n[SerializeField] GameState state;\nstate.score += 10;   // 改的是 .asset 里的数据\n\n// 场景 B 的脚本拖同一个 .asset 引用\nDebug.Log(state.score);   // 读到场景 A 改的值\n\n关键坑：SO 是「编辑器里持久」的——编辑器里跑游戏改了值，Stop 之后改动会「留在资产上」。所以 SO 适合「配置」不适合「存档」：运行时临时值要记得 Reset，或把值放在场景对象上、SO 只存「初始值」模板。\n\n四、运行时数据的安全做法。推荐模式「模板 + 运行时实例」：\n· EnemyData（SO 模板）：存配置，只读。\n· RuntimeEnemy（普通类/MonoBehaviour）：把 SO 的数据拷一份当「运行时状态」，改的是实例不是模板。\n这样既享受 SO 的配置便利，又不会污染模板数据。\n\n五、让配置「团队友好」。\n· 给字段加 [Header]、[Tooltip]，配表的人才知道每个字段干嘛的。\n· 用 [Range] 限制数值范围，用枚举代替自由字符串，防填错。\n· 重写 OnValidate() 做自动校验：填了负数自动报警。\n· 用 CustomEditor/PropertyDrawer（editor-5）把配置面板画得清爽，配表效率直接翻倍。\n\n六、常见坑。\n· SO 里放「运行时状态」不重置 —— 跑完 Play 数据被污染，配表的人一脸懵。\n· 用 SO 存存档 —— SO 是项目资产，不是玩家存档，存档走 PlayerPrefs/文件/云。\n· 代码里 new ScriptableObject —— 用 CreateInstance 或直接建资产，别 new。\n· 引用拖错 —— 同名 .asset 好几个，拖错一个全项目数值错乱，命名规范要定好。\n\n一句话总结：ScriptableObject 是「配置即资产」——一份 .asset 全项目共享、跨场景复用、团队配表不碰代码；记住「配置用 SO、运行时数据单独拷一份、存档别用 SO」，三句话用好它。' },
    { type: 'component', title: 'SO 三大用武之地', items: [
      { name: '数据资产', desc: '怪物数值/技能/卡牌/关卡配置，配表不碰代码' },
      { name: '跨场景共享', desc: '场景切换不销毁的共享数据，靠引用传递' },
      { name: '事件通道', desc: 'GameEvent 式广播，模块解耦' },
      { name: '模板+实例', desc: 'SO 存模板，运行时数据拷到实例，防污染' },
      { name: 'OnValidate 校验', desc: '配表自动报警，防填错' },
    ] },
    { type: 'quiz', question: '想用 ScriptableObject 跨场景共享一个分数，结果跑完 Play 模式分数停留在上次的值，污染了资产。正确做法？', options: ['不用管，反正游戏能跑', 'SO 只存「初始配置模板」，运行时把数据拷到普通实例上改；SO 别存运行时可变状态', '把 SO 字段改成私有就安全了'], answer: 1, tip: 'SO 在编辑器里是「持久」的，运行时改动会留在资产上。标准模式是「模板 + 运行时实例」：SO 当只读配置，运行时把配置拷出来放实例里改，这样既复用配置又不污染资产。' },
  ],
  'editor-10': [
    { type: 'text', title: '自定义窗口实战', body: 'editor-2 讲过一个最简单的窗口。这一节做一个「能干活」的完整工具窗口：选一个现实需求（这里做「资源浏览器 + 批量改名」），从布局到交互一步到位。\n\n一、窗口骨架。用 EditorWindow.CreateWindow 打开，OnGUI 画界面，静态方法用 [MenuItem] 挂到菜单：\n\npublic class AssetBrowser : EditorWindow\n{\n    [MenuItem(\"Tools/资源浏览器\")]\n    static void Open()\n    {\n        GetWindow<AssetBrowser>(\"资源浏览器\");\n    }\n\n    void OnGUI()\n    {\n        // 在这里画界面\n    }\n}\n\n二、第一步：列出资源。用 AssetDatabase 扫一个目录：\n\nstring[] paths = AssetDatabase.FindAssets(\"t:Texture\", new[] { \"Assets/Textures\" })\n    .Select(AssetDatabase.GUIDToAssetPath).ToArray();\n\n// 或者直接列目录文件\nstring[] files = Directory.GetFiles(\"Assets/Textures\", \"*.png\", SearchOption.AllDirectories);\n\n然后放进一个「可滚动列表」：EditorGUILayout.BeginScrollView() 包住循环，每个资源一行，显示缩略图（AssetPreview.GetAssetPreview）+ 名字。\n\n三、交互：选中。\n· 点一行 → 高亮 + EditorGUIUtility.PingObject / Selection.activeObject 在 Project 里定位它。\n· 多选 → 用 List<bool> 或 Dictionary 记录每行选中态。\n\n四、功能：批量改名。选中若干资源，点按钮，弹输入框：\n\nif (GUILayout.Button(\"批量加前缀\"))\n{\n    string prefix = EditorInputDialog.Show(\"输入前缀\");   // 自己写个简单输入框\n    foreach (string sel in selectedPaths)\n    {\n        string name = Path.GetFileNameWithoutExtension(sel);\n        string dir = Path.GetDirectoryName(sel);\n        string newPath = Path.Combine(dir, prefix + name + Path.GetExtension(sel));\n        AssetDatabase.RenameAsset(sel, prefix + name);   // 改名\n    }\n    AssetDatabase.SaveAssets();\n    AssetDatabase.Refresh();\n}\n\n注意 AssetDatabase.RenameAsset 只接受文件名（不带扩展名和路径），路径要用 AssetDatabase.MoveAsset 或自己拼。\n\n五、窗口体验的加分项。\n· 搜索框 —— 顶部一个 TextField，实时过滤列表。\n· 排序 —— 按名字/类型/大小排。\n· 批量操作条 —— 底部固定一排按钮（改名/删除/导出），不用到处找。\n· 进度反馈 —— 批量几百个资源时用 EditorUtility.DisplayProgressBar 显示进度条，别让窗口看起来「死了」。\n· 记住窗口状态 —— 用 EditorPrefs 记住上次打开的目录、上次的搜索词，关掉重开不丢。\n\n六、常见坑。\n· OnGUI 里做耗时操作 —— 扫描几百个资源逐帧全做，编辑器卡死。放批量操作里做 + 进度条。\n· 忘了 Refresh —— 改完资产不调 AssetDatabase.Refresh()，Project 面板还显示旧状态。\n· 路径用绝对路径 —— 项目换了位置就废，统一用 Assets 开头的相对路径。\n· 直接在列表里 DeleteAsset —— 记得先确认框，删除不可逆（除非进了回收站）。\n\n七、给自己的工具列清单。\n□ 菜单能打开窗口\n□ 能列出目标目录资源\n□ 能选中/多选、能搜索\n□ 有一个能干的批量操作\n□ 有进度反馈\n□ 有防呆（确认框、错误提示）\n\n一句话总结：自定义窗口 = 骨架（EditorWindow + MenuItem）+ 列表（AssetDatabase 扫描 + 滚动视图）+ 交互（选中/搜索/批量操作）+ 体验（进度条/确认框/记住状态）——四层搭起来，就是一个能真用的工具窗口。' },
    { type: 'component', title: '工具窗口四大件', items: [
      { name: 'EditorWindow', desc: '窗口骨架，GetWindow 打开' },
      { name: 'AssetDatabase', desc: '扫资源、改名、刷新，工具的操作对象' },
      { name: '滚动列表 + 选中', desc: 'BeginScrollView 包循环，PingObject 定位' },
      { name: '进度反馈', desc: 'DisplayProgressBar 防「假死」' },
      { name: 'EditorPrefs 记忆', desc: '记住目录/搜索词，重开不丢' },
    ] },
    { type: 'quiz', question: '自定义窗口里批量改名 500 个资源，界面卡得像死机，最可能的优化手段？', options: ['改用批处理脚本，别在窗口里做', '在批量操作里用 EditorUtility.DisplayProgressBar 显示进度并配合 EditorApplication.Step 刷新，让编辑器有响应', '让操作在另一个线程里跑完再回来'], answer: 1, tip: 'EditorWindow 的 OnGUI 跑在主线程，长循环会卡住整个编辑器。用 DisplayProgressBar 分步刷新进度、每处理几个调用一次重绘，用户能看见进度就不觉得「死」。编辑器 API 大多不能跨线程，别想着开线程。' },
  ],
  'editor-11': [
    { type: 'text', title: '场景工具与 Gizmos', body: '做关卡、摆场景、对位置的时候，编辑器里那根蓝线、那个圆圈、那条箭头——都是「Gizmos」。这一节讲怎么用 Gizmos 在场景里画辅助线，让工具「看得见」。\n\n一、Gizmos 是什么。Gizmos 是只在编辑器场景视图（Scene）里绘制的调试图形：连线、方框、球、文字。它们不进游戏画面，是给开发者看的「辅助线」。\n\n二、两个入口。\n· OnDrawGizmos —— 挂在 MonoBehaviour 上，每帧在场景里画。\n· OnDrawGizmosSelected —— 只在物体被选中时画，避免满屏都是线。\n\npublic class Waypoint : MonoBehaviour\n{\n    void OnDrawGizmos()\n    {\n        Gizmos.color = Color.yellow;\n        Gizmos.DrawWireSphere(transform.position, 1f);\n    }\n}\n\n常见绘制：\n· Gizmos.DrawWireSphere / DrawSphere —— 范围、碰撞区域\n· Gizmos.DrawLine / DrawRay —— 射线、连线、巡逻路线\n· Gizmos.DrawWireCube / DrawCube —— 包围盒、可交互范围\n· Handles.Label —— 在位置显示文字（巡逻点编号）\n· Handles.DrawWireArc —— 扇形、视野范围\n\n三、画「巡逻路线」的完整例子。\n\nvoid OnDrawGizmos()\n{\n    // 画路径点之间的连线\n    for (int i = 0; i < waypoints.Length; i++)\n    {\n        Gizmos.color = Color.cyan;\n        Gizmos.DrawWireSphere(waypoints[i].position, 0.3f);\n        if (i + 1 < waypoints.Length)\n            Gizmos.DrawLine(waypoints[i].position, waypoints[i + 1].position);\n    }\n    // 用 Handles 给每个点标号\n    for (int i = 0; i < waypoints.Length; i++)\n        Handles.Label(waypoints[i].position + Vector3.up * 0.5f, i.ToString());\n}\n\n画完拖一拖路径点，线实时跟着变——这就是「场景工具」的雏形：关卡设计师能在场景里直接看到并调整。\n\n四、进阶：让 Gizmos 可交互。\n· Handles.PositionHandle —— 画一个可拖动的位置手柄，拖的时候返回新位置。\n· 用 HandleUtility 检测鼠标——实现「点一下路径加一个点」的绘制工具。\n· 场景绘制工具要用 Handles（不是 Gizmos）：Gizmos 只显示，Handles 能交互。\n\n五、Gizmos 的性能与组织。\n· 只在选中时画（OnDrawGizmosSelected），几百个物体时省大量绘制。\n· 按功能开关：用 Gizmos.matrix 或自定义 static bool 控制某类辅助线开关，场景不花。\n· 场景顶部有个 Gizmos 按钮，可以逐类型开关——给物体挂 Gizmo 分类（如路径、范围、触发器），调试时各开关。\n\n六、常见坑。\n· 满屏 Gizmos —— 所有物体都画大球大线，场景变成毛线团。多用 Selected、多用分类开关。\n· Gizmos.color 忘了设 —— 上一次的颜色残留，线突然变红。每次画前先设色。\n· 在 OnDrawGizmos 里做耗时计算 —— 每帧跑，卡编辑器。复杂的预计算缓存起来。\n· 画了但看不到 —— 场景视图右上角 Gizmos 按钮被关了，或物体被选中态切了。\n\n一句话总结：Gizmos 是场景里的「看得见的注释」——OnDrawGizmos 画线画球标文字，OnDrawGizmosSelected 只在选中时画，Handles 能交互拖动；多用 Selected 和分类开关，别让辅助线变成毛线团。' },
    { type: 'component', title: 'Gizmos 常用绘制', items: [
      { name: 'OnDrawGizmos', desc: '每帧在场景里画辅助图形' },
      { name: 'OnDrawGizmosSelected', desc: '只在选中时画，省性能防眼花' },
      { name: 'DrawLine / DrawWireSphere', desc: '连线、范围、碰撞区域的招牌' },
      { name: 'Handles.Label', desc: '在场景里显示文字（编号/提示）' },
      { name: 'Handles 手柄', desc: 'PositionHandle 等可拖动手柄，可交互' },
    ] },
    { type: 'quiz', question: '场景里挂了几百个带 Gizmos 的物体，辅助线画得满屏都是、乱成一团，怎么治？', options: ['把所有 Gizmos 代码删掉', '尽量用 OnDrawGizmosSelected（选中才画）+ 按类型开关控制，别让不相关的也一直画', '把 Gizmos 颜色全改成黑色'], answer: 1, tip: 'Gizmos 画得多会又乱又卡。两个招：默认只在选中时画（Selected 版本）；给不同用途的 Gizmo 分类，用场景顶部 Gizmos 按钮按类型开关，需要哪类开哪类。' },
  ],
  'editor-12': [
    { type: 'text', title: '一键构建工具', body: '打包不该是「菜单 Build → 点几下 → 等半天 → 手动翻文件」的流程。这一节做一个「一键构建工具」：菜单点一下，按配置自动打多平台，还能挂到 CI 上无人值守。\n\n一、BuildPipeline 是核心。打包的底层就是 BuildPipeline.BuildPlayer：\n\nBuildPlayerOptions opt = new BuildPlayerOptions\n{\n    scenes = new[] { \"Assets/Scenes/Main.unity\", \"Assets/Scenes/Game.unity\" },\n    locationPathName = \"Build/Windows/MyGame.exe\",\n    target = BuildTarget.StandaloneWindows64,\n    options = BuildOptions.None\n};\nBuildReport report = BuildPipeline.BuildPlayer(opt);\n\n三要素：场景列表、输出路径、目标平台。BuildReport 里能读到成功与否、耗时、大小。\n\n二、做成菜单：一键打包。把常用的打包配置固化成一个菜单命令：\n\n[MenuItem(\"Build/Build Windows 一键打包\")]\npublic static void BuildWindows()\n{\n    if (!EditorUtility.DisplayDialog(\"确认\", \"将输出到 Build/Windows？\", \"打包\", \"取消\"))\n        return;\n    BuildPlayerOptions opt = MakeOptions(BuildTarget.StandaloneWindows64, \"Build/Windows/MyGame.exe\");\n    BuildReport report = BuildPipeline.BuildPlayer(opt);\n    if (report.summary.result == BuildResult.Succeeded)\n    {\n        EditorUtility.RevealInFinder(\"Build/Windows\");   // 打完自动打开文件夹\n        Debug.Log($\"打包成功，耗时 {report.summary.totalTime}\");\n    }\n    else\n        Debug.LogError(\"打包失败，见 Build 日志\");\n}\n\n打完自动 RevealInFinder 打开输出目录，少一步人工。\n\n三、构建配置独立成「资产」。不同渠道打包参数不同（场景不同、包名不同、宏不同）——用 ScriptableObject 存构建配置：\n\n[CreateAssetMenu(menuName = \"Build/Config\")]\npublic class BuildConfig : ScriptableObject\n{\n    public string buildName;\n    public BuildTarget target;\n    public string[] scenes;\n    public string[] defineSymbols;   // 宏定义，如 BUILD_QA / BUILD_RELEASE\n}\n\n打包菜单遍历 Assets/BuildConfigs 下的所有配置，一键打全部平台。\n\n四、常用加固：\n· 检查场景是否存在 —— 拼错了路径直接报错，别让 BuildPlayer 报一个晚到的大错。\n· 版本号 —— 打包前自动读/写版本号（PlayerSettings.bundleVersion），进包留档。\n· 打 log —— 每次打包的耗时、产物路径记到文件，出问题能追溯。\n· 自定义宏 —— 正式/测试服走不同 defineSymbols（SERVER_URL 不同），一个菜单打两包。\n\n五、挂到 CI（无人值守）。命令行方式（editor-6 学过）：\n\nUnity.exe -batchmode -quit -projectPath D:/Game \\\n  -executeMethod BuildScript.BuildWindows -logFile build.log\n\n要点：\n· 批处理里不能弹对话框 —— DisplayDialog 会卡住 CI，批处理分支要跳过确认框（用参数或环境变量区分）。\n· -executeMethod 的方法必须是 static，且类不能是 nested。\n· 退出码判断成败 —— CI 流程读 Unity 进程退出码：0 成功，非 0 失败。\n· 指定 -buildTarget 平台 —— 或用方法参数在 BuildPlayerOptions 里定死。\n\n六、常见坑。\n· 场景路径写死还拼错 —— 用 EditorBuildSettings.scenes 自动收集「场景清单」里勾选的场景。\n· 打包时还开着编辑器 —— BuildPlayer 需要独占，先保存场景再打。\n· 不设 ScriptingDefineSymbols 或设错 —— 宏相关代码路径错乱，产物行为不对。\n· 批处理里弹窗 —— CI 卡死等「确认」按钮，永远点不到。\n\n一句话总结：一键构建 = BuildPipeline 打底（场景+平台+输出）+ 菜单固化（Build 菜单一键打 + RevealInFinder）+ 配置资产化（BuildConfig 管多平台）+ CI 接缝（-batchmode -executeMethod，不弹窗、看退出码）——四个环节拼起来，打包从「手动点半小时」变「一条命令」。' },
    { type: 'component', title: '构建工具四件套', items: [
      { name: 'BuildPipeline.BuildPlayer', desc: '打包核心：场景 + 平台 + 输出路径' },
      { name: 'Build 菜单', desc: '一键打包 + 自动打开输出目录' },
      { name: 'BuildConfig 资产', desc: 'SO 存多平台配置，一键打全平台' },
      { name: 'EditorBuildSettings.scenes', desc: '自动收集场景清单，不手拼路径' },
      { name: 'CI 批处理', desc: '-batchmode -executeMethod，不弹窗看退出码' },
    ] },
    { type: 'quiz', question: '一键打包工具里，场景列表怎么维护最省心、不易错？', options: ['在代码里写死一串字符串路径，想加场景就改代码', '用 EditorBuildSettings.scenes 读取「场景清单」里勾选的场景，在 Build Settings 面板可视化维护', '每次打包前手动输入场景路径'], answer: 1, tip: 'Build Settings 面板里的「场景清单」就是官方推荐的维护方式：可视化勾选，代码里用 EditorBuildSettings.scenes 自动读出来当打包场景。写死字符串路径一改场景就忘，必出错。' },
  ],
  'editor-13': [
    { type: 'text', title: '代码生成工具', body: '有些代码「写着重复、改着受罪」——几十个配置类、几十个事件名、一堆枚举。这类活最适合交给「代码生成工具」：点一下菜单，自动把代码写出来。这一节讲代码生成的思路和模板写法。\n\n一、什么时候该生成代码。\n· 大量重复结构 —— 100 个怪物各自一个配置类、50 个成就每个一个枚举，手写又慢又易错。\n· 从数据反推代码 —— 有一张 Excel/CSV 配置表，想让代码里自动有对应的类、字段、常量。\n· 防手打错 —— 事件名、消息名、字符串常量，手打容易拼错，生成出来的保证一致。\n\n二、最朴素的生成：用 StringBuilder 拼。生成一个「自动根据配置表生成枚举」的工具：\n\nstatic void GenMonsterEnum(string csvPath)\n{\n    StringBuilder sb = new StringBuilder();\n    sb.AppendLine(\"// 本文件由工具自动生成，勿手改\");\n    sb.AppendLine(\"public enum MonsterType\");\n    sb.AppendLine(\"{\");\n    foreach (var row in ReadCsv(csvPath))   // 读配置表每一行\n        sb.AppendLine($\"    {row.name} = {row.id},\");\n    sb.AppendLine(\"}\");\n    File.WriteAllText(\"Assets/Generated/MonsterType.cs\", sb.ToString());\n    AssetDatabase.Refresh();\n}\n\n生成的代码第一行写「勿手改」——下次生成会覆盖，手改的部分直接没了。\n\n三、进阶：用模板文件。代码一长，字符串拼接就乱。更好的是「模板文件」：写一个 .txt 模板，留出插值位置：\n\n// MonsterClassTemplate.txt 内容\n// 本文件由工具自动生成，勿手改\nusing UnityEngine;\n\npublic class {NAME} : MonoBehaviour\n{\n    public int hp = {HP};\n    public float speed = {SPEED}f;\n}\n\n生成时读模板、替换占位符、写文件：\n\nstring template = File.ReadAllText(\"Assets/Editor/Templates/MonsterClassTemplate.txt\");\nstring code = template\n    .Replace(\"{NAME}\", name)\n    .Replace(\"{HP}\", hp.ToString())\n    .Replace(\"{SPEED}\", speed.ToString());\nFile.WriteAllText($\"Assets/Scripts/Monsters/{name}.cs\", code);\n\n四、更有「工程味」的工具：\n· 用 Excel 插件（EPPlus 等）读 xlsx，一键把「数值表」生成「配置类 + 数据」。\n· 生成序列化用数据（[Serializable] 的字段列表），字段名和表头一一对应。\n· 生成后自动调用 AssetDatabase.Refresh()，新脚本立刻能被识别。\n· 生成完了顺手写「校验」：生成后把模板脚本编译一遍，编译错误直接弹窗（用 CompilationPipeline）。\n\n五、注意事项（重要）。\n· 目录要建 —— File.WriteAllText 前确认目标文件夹存在，Directory.CreateDirectory 兜底。\n· 覆盖要谨慎 —— 生成文件会覆盖同名文件，工具里加「确认框」防手滑。\n· 生成物进版本库 —— 生成的代码也要进 git，别加 .gitignore 忽略，否则同事拉下来缺文件。\n· 模板和代码同仓 —— 模板文件放项目里（Assets/Editor/Templates），换机器也有。\n· 可重复执行 —— 生成工具要「跑一次和跑十次结果一样」，幂等，别越跑越乱。\n\n六、常见坑。\n· 生成文件手动改 —— 下次生成覆盖，改动全丢。约定「生成目录」专门放生成物，手写代码不放这里。\n· 字符串拼接大代码 —— 可读性差难维护，用模板文件。\n· 忘了 Refresh —— 生成完不刷新，新脚本不出现，编译也看不到。\n· 生成时不带命名空间 —— 生成物和手写代码命名冲突，加统一的 namespace。\n\n一句话总结：代码生成 = 「读配置/模板 → 拼代码 → 写文件 → 刷新」——重复结构靠它消灭手打错误，模板文件让生成逻辑清晰可维护；记住「勿手改生成物、可重复执行、生成物进版本库」三条规矩。' },
    { type: 'component', title: '代码生成要素', items: [
      { name: 'StringBuilder 拼接', desc: '最朴素，适合短代码' },
      { name: '模板文件', desc: '留占位符，生成时替换，适合长代码' },
      { name: '勿手改标记', desc: '生成文件头部声明，防手改被覆盖' },
      { name: 'AssetDatabase.Refresh', desc: '生成后刷新，新脚本立刻识别' },
      { name: '幂等执行', desc: '跑一次和跑十次结果一样' },
    ] },
    { type: 'quiz', question: '用代码生成工具生成的 C# 文件，最该遵守的约定是什么？', options: ['生成的文件要加密保存', '文件头部标注「自动生成勿手改」，且生成物放进专门的目录、进入版本库——下次生成会覆盖手改内容', '生成一次后立刻删除生成器代码'], answer: 1, tip: '生成工具会覆盖同名文件，手改的部分下次生成就没了。规范做法：生成物集中放专用目录、文件头声明「勿手改」、生成物照常进 git——手写代码永远别和生成物混在一个目录。' },
  ],
  'editor-14': [
    { type: 'text', title: '编辑器性能', body: '编辑器卡顿比游戏卡顿更烦人——游戏卡顿能骂优化，编辑器卡顿是「干活的工具」卡，直接影响效率。这一节讲编辑器卡顿的常见来源和治理手段。\n\n一、先分清：卡在「帧里」还是「一次性」。\n· 每帧都跑的（OnGUI、OnDrawGizmos、EditorApplication.update）——卡是持续的，拖累整个编辑器。\n· 一次性大操作（批量导入、批量处理、全量打包）——卡是「假死」，做完就好。\n两种卡法、两套解法。\n\n二、每帧开销的治理。\n· OnGUI 里不做重活 —— 每帧绘制 UI 时别扫描资源、别做字符串拼接大表、别 Instantiate。数据先缓存，OnGUI 只画。\n· OnDrawGizmos 只在选中时画 —— 满场景 Gizmos 卡到爆（editor-11 讲过）。\n· EditorApplication.update 里控制频率 —— 需要每帧做的更新，用「隔 N 帧做一次」或按时间节流。\n· 大列表用虚拟滚动 —— 几百上千个条目全用 GUILayout 画，性能差；用重用的布局（列表分页 / 只画可见部分）。\n\n三、一次性大操作的治理。\n· 分帧处理 —— 把 1000 个资源的处理拆成「每帧处理 20 个」，配合 EditorApplication.update 或 yield return null（用协程）：\n\nIEnumerator ProcessAll(string[] paths)\n{\n    for (int i = 0; i < paths.Length; i++)\n    {\n        ProcessOne(paths[i]);\n        if (i % 20 == 0)\n        {\n            EditorUtility.DisplayProgressBar(\"处理中\", $\"{i}/{paths.Length}\", (float)i / paths.Length);\n            yield return null;   // 让编辑器喘口气\n        }\n    }\n    EditorUtility.ClearProgressBar();\n}\n\n· 加进度条 —— 用户看到进度就不觉得「死了」（editor-10 提过）。\n· 该重就重 —— 有些操作天生重（重新导入所有资源），无法分帧就明确提示「可能要几分钟」。\n\n四、缓存的威力。\n· 结果缓存 —— 扫描过的目录列表、解析过的配置，存 static 变量，别每次重扫。\n· 资产哈希 —— 判断「资源变没变」用哈希，别全量对比内容。\n· 编辑器状态用 EditorPrefs —— 关掉重开还能复用缓存（比如「上次处理到哪了」）。\n· 缓存要能失效 —— 资源一变缓存就得清，设个「手动刷新」按钮最稳妥。\n\n五、别在编辑器里做的事。\n· 同步加载大资源 —— AssetDatabase.LoadAssetAtPath 加载几百 MB 资源会卡；能用引用就用引用。\n· 全量搜索 —— FindAssets 没有条件就全扫，范围尽量收窄（目录 + 类型）。\n· 开一堆没用的窗口 —— Asset Store 预览、性能面板全开着，白吃 CPU。\n· 渲染无关 —— 编辑器里的实时渲染预览（Material preview 等）别开太多。\n\n六、用工具定位。\n· Window → General → Console 的右上角会有性能警告。\n· 用 Profiler（Window → Analysis → Profiler）在 Editor 模式采样，看哪个方法在 OnGUI/update 里耗 CPU。\n· 检查是否有无限循环的 EditorApplication.update 回调忘注销。\n\n七、常见坑。\n· 忘注销 EditorApplication.update —— 工具关掉了回调还在跑，白烧 CPU。\n· OnGUI 里扫描 —— 每帧全目录扫描，卡成幻灯片。\n· 缓存不失效 —— 资源改了还显示旧数据，比卡顿更坑。\n· 进度条忘 Clear —— 界面残留一个甩不掉的进度条。\n\n一句话总结：编辑器性能两本账——每帧开销靠「OnGUI 只画、缓存数据、控制频率」治理，一次性大操作靠「分帧 + 进度条」治理；工具用完注销回调、缓存能刷新、范围收窄，编辑器就顺滑了。' },
    { type: 'component', title: '编辑器性能治理', items: [
      { name: 'OnGUI 只画', desc: '数据先缓存，绘制里不做扫描/拼接' },
      { name: '分帧处理', desc: '大操作拆成每帧一小批 + 进度条' },
      { name: '缓存 + 失效', desc: '结果缓存，资源变了能手动刷新' },
      { name: '注销回调', desc: 'EditorApplication.update 用完就退订' },
      { name: '范围收窄', desc: 'FindAssets 限定目录和类型，别全扫' },
    ] },
    { type: 'quiz', question: '批量处理 1000 个资源时编辑器整个「假死」几分钟，最好的治理手段是？', options: ['让用户等着，反正会做完', '把处理拆成每帧一小批（配合进度条），用协程 yield return null 让编辑器保持响应', '把处理放到另一个线程里跑完再回主线程'], answer: 1, tip: '编辑器 API 大多要求主线程，不能简单丢线程。正统做法是分帧：每帧处理一小批、显示进度条、yield return null 让编辑器重绘——用户能看到进度，编辑器也不「死」。' },
  ],
  'editor-15': [
    { type: 'text', title: '资产导入实战', body: 'editor-7 讲了 AssetPostprocessor 的入门。这一节做一个「完整版」：真实项目里按目录、按命名自动设导入设置，把导入规范固化成代码。\n\n一、需求设定（实战案例）。团队规范：\n· 所有 UI 贴图（放 Assets/UI/）→ 必须是 Sprite、关 Mipmap、不生成图集（自行打图集）。\n· 所有角色贴图（放 Assets/Characters/Textures/）→ 勾上 Read/Write（有的需求）、Alpha 透明、压缩到 ASTC（移动端）。\n· 所有模型（名字带 _col 后缀）→ 自动生成凸碰撞体。\n· 所有音频（Assets/Audio/）→ 按用途设加载类型：BGM 用 Streaming、音效用 Compressed。\n\n二、按目录判断。\n\npublic class AutoImportRules : AssetPostprocessor\n{\n    void OnPreprocessTexture()\n    {\n        TextureImporter importer = assetImporter as TextureImporter;\n        string path = importer.assetPath;\n\n        if (path.StartsWith(\"Assets/UI\"))\n        {\n            importer.textureType = TextureImporterType.Sprite;\n            importer.mipmapEnabled = false;\n        }\n        else if (path.StartsWith(\"Assets/Characters\"))\n        {\n            importer.alphaIsTransparency = true;\n            importer.textureCompression = TextureImporterCompression.Compressed;\n            importer.SetPlatformTextureSettings(new TextureImporterPlatformSettings\n            {\n                name = \"Android\",\n                format = TextureImporterFormat.ASTC_6x6\n            });\n        }\n    }\n\n    void OnPreprocessModel()\n    {\n        ModelImporter model = assetImporter as ModelImporter;\n        if (model.assetPath.Contains(\"_col\"))\n            model.addCollider = true;   // 碰撞体模型自动加碰撞\n    }\n\n    void OnPreprocessAudio()\n    {\n        AudioImporter audio = assetImporter as AudioImporter;\n        string path = audio.assetPath;\n        audio.loadType = path.Contains(\"BGM\") ?\n            AudioImporterLoadType.Streaming : AudioImporterLoadType.CompressedInMemory;\n    }\n}\n\n三、OnPostprocessAllAssets：整批处理。导入完成后做跨资源的事：\n\nstatic void OnPostprocessAllAssets(string[] importedAssets, string[] deletedAssets,\n    string[] movedAssets, string[] movedFromAssetPaths)\n{\n    foreach (string path in importedAssets)\n    {\n        if (!path.EndsWith(\".prefab\")) continue;\n        CheckPrefabRule(path);   // 例如：预制体命名必须带前缀，否则报警\n    }\n}\n\n四、规范化与日志。\n· 记录日志 —— 导入被自动改了设置，在 Console 打一行「UI_xx.png 已设为 Sprite（自动规则）」，团队能追溯。\n· 别乱改 —— 规则之外的文件不动，别把普通贴图乱改。\n· 规则要可配置 —— 目录规则写死在代码里，团队改目录名就要改代码。进阶：把规则做成 ScriptableObject 配置（editor-9 那套），配表的人改配置不改代码。\n\n五、常见坑。\n· 规则冲突 —— 一个文件匹配多个规则（在 UI 又在 Characters），要定优先级，先匹配先生效。\n· 回调里触发再导入 —— 改了设置又改文件导致死循环，用标志位/只读判断。\n· 路径判断写错 —— 用 StartsWith/Contains 前先想清楚，路径大小写、分隔符（Windows 是 \\\\）都要统一。\n· 平台设置覆盖 —— SetPlatformTextureSettings 会覆盖默认，多个平台设置要全配，别漏了 iOS。\n· OnPostprocessAllAssets 里做重活 —— 每次导入都跑，别做全量扫描，收窄到 importedAssets。\n\n六、验收清单。\n□ 丢一张 UI_ 开头的图进 Assets/UI，自动变成 Sprite、无 Mipmap\n□ 丢一个 _col 模型，自动加碰撞体\n□ 丢一段 BGM 音频，自动设成 Streaming\n□ Console 有自动规则的日志，能追溯\n□ 规则之外的资源不受影响\n\n一句话总结：资产导入实战 = 按目录/命名在 OnPreprocess* 回调里设导入设置 + OnPostprocessAllAssets 做跨资源校验 + 日志可追溯；规则固化成代码或 SO 配置，导入规范从此「不用靠自觉」。' },
    { type: 'component', title: '导入规则实战要点', items: [
      { name: '按目录判断', desc: 'StartsWith 区分 UI/角色/音频目录' },
      { name: '按命名判断', desc: '_col 后缀自动加碰撞体' },
      { name: '平台设置', desc: 'SetPlatformTextureSettings 配 Android/iOS' },
      { name: '规则优先级', desc: '多规则冲突时定先后' },
      { name: '日志可追溯', desc: '自动改了什么，Console 留痕' },
    ] },
    { type: 'quiz', question: '一个贴图既满足「UI 目录规则」又满足「角色目录规则」，导入时会怎样？', options: ['两个规则都执行，最后执行的赢', '会冲突且可能互相覆盖，所以要给规则定优先级（先匹配先生效），并避免目录设计重叠', 'Unity 会自动合并两个规则'], answer: 1, tip: '同类型的 OnPreprocess 回调里多个条件命中时，后写的代码会覆盖先写的设置。实战上要把目录规范设计得不重叠，或明确 if-else 优先级，否则导入设置是「掷骰子」。' },
  ],
  'editor-16': [
    { type: 'text', title: '序列化与撤销', body: '编辑器工具的素质分水岭：你做的工具，「Ctrl+Z 能不能撤销」。改坏了场景不能回退的工具，美术和策划用一次就弃。这一节讲 EditorUndo 和序列化。\n\n一、为什么撤销重要。传统编辑器工具直接改场景对象的值，改完「没有进撤销栈」——用户 Ctrl+Z 无反应，只能手动改回去。专业工具的正确姿势：每个修改都通过 Undo 系统登记。\n\n二、Undo.RecordObject：记录修改。改任何对象前先「快照」它：\n\nUndo.RecordObject(target, \"移动路径点\");   // 记下 target 的当前状态，起个名\nUndo.RegisterCreatedObjectUndo(newObj, \"创建路径点\");   // 新建对象也要登记\nUndo.DestroyObjectImmediate(oldObj);       // 删除对象走这个，才能撤销\n\n改完之后用户按 Ctrl+Z，就退回到 RecordObject 那一刻的状态。\n\n三、批量与多对象。改一批对象时，每个都 Record 再改：\n\nforeach (Transform t in selection)\n{\n    Undo.RecordObject(t, \"批量旋转\");\n    t.localRotation *= Quaternion.Euler(0, 45, 0);\n}\n\n注意：RecordObject 要在「改之前」调用，改完再记录等于没记。\n\n四、组件和预制体的撤销。\n· 加组件 —— Undo.AddComponent<T>(go)；删组件走 DestroyObjectImmediate。\n· 改预制体 —— Undo.RecordObject(prefab) 后改；改完考虑 PrefabUtility.SavePrefabAsset。\n· 可撤销的移动 —— 手写 PositionHandle 拖动物体，要包在 Undo 里：拖完统一 RecordObject。\n\n五、序列化的配合。编辑器里大部分「改数据」其实改的是 SerializedObject：\n\nSerializedObject so = new SerializedObject(target);\nso.FindProperty(\"hp\").intValue = 100;\nso.ApplyModifiedProperties();   // 通过 SerializedObject 改的，天然进撤销栈！\n\n用 SerializedObject 修改字段比直接赋值多了两个好处：\n· 自动进撤销栈（改 + ApplyModifiedProperties 组合）。\n· Inspector 会同步刷新。\n所以工具里能用 SerializedObject 就用它，撤销白送。\n\n六、撤销组与性能。\n· Undo.SetCurrentGroupName —— 把一批操作归成一组，一次撤销退一组。\n· 高频操作（拖动中每帧改值）—— 别每帧 RecordObject，拖完再 Record 一次，否则撤销栈爆炸。\n· Undo.CollapseUndoOperations —— 把中间过程合并，用户一步到位。\n\n七、常见坑。\n· 先改后 Record —— 撤销回不到初始状态，白记。\n· 不登记新建/删除 —— 新增的对象删不掉、删掉的对象找不回，撤销不完整。\n· 每帧 Record —— 拖动 100 帧记录 100 次，撤销栈撑爆，卡死。\n· 改预制体不 Record —— 打回预设后改动全丢，无法撤销。\n\n一句话总结：编辑器工具的品质在撤销——改前 Undo.RecordObject、新建删除走 RegisterCreatedObjectUndo/DestroyObjectImmediate、能走 SerializedObject 就走（撤销白送）、高频操作合并成组——做到这些，你的工具才敢给同事用。' },
    { type: 'component', title: 'Undo 常用 API', items: [
      { name: 'Undo.RecordObject', desc: '改对象前快照，Ctrl+Z 可回退' },
      { name: 'RegisterCreatedObjectUndo', desc: '新建对象登记，撤销时能删掉' },
      { name: 'Undo.DestroyObjectImmediate', desc: '删除走这个，撤销时能找回来' },
      { name: 'SerializedObject', desc: '改字段自动进撤销栈，还同步 Inspector' },
      { name: 'SetCurrentGroupName', desc: '一批操作归一组，一次撤销退一组' },
    ] },
    { type: 'quiz', question: '工具里拖动一个物体，每帧都在改它的 position，撤销栈该怎么做？', options: ['每帧都 Undo.RecordObject，保证每步都能撤销', '拖动过程中别每帧 RecordObject——高频操作只在一组操作结束（松开鼠标）时记录一次，配合撤销组', '完全不用 Undo，反正功能能用'], answer: 1, tip: '每帧 RecordObject 会让撤销栈塞满中间态，甚至卡死编辑器。高频改动的正确姿势：拖动的连续修改归为一个撤销组，结束时记录一次（或 CollapseUndoOperations 合并），用户一次撤销回到拖动前。' },
  ],
  'editor-17': [
    { type: 'text', title: '批量操作', body: '编辑器的重复劳动最典型的就是「批量」：一百个资源要改导入设置、五十个物体要换材质、整目录要导出。这一节把批量操作的套路讲全。\n\n一、批量操作的对象。\n· 场景里的物体 —— 选中一堆，批量改名字、设父物体、改组件参数。\n· Project 里的资源 —— 批量改导入设置、重命名、移动、删除、改引用。\n· 数据文件 —— 批量导出 CSV、批量生成配置。\n\n二、批量处理场景物体。\n\n[MenuItem(\"Tools/批量/选中物体加序号\")]\nstatic void RenameSelected()\n{\n    GameObject[] selected = Selection.gameObjects;\n    Undo.RecordObjects(selected, \"批量改名\");   // 多对象一次记录（editor-16）\n    for (int i = 0; i < selected.Length; i++)\n        selected[i].name = $\"Object_{i:000}\";\n}\n\n选中入口用 Selection 系列：Selection.gameObjects / Selection.objects / Selection.activeTransform。\n\n三、批量处理资源。核心是「拿到一批路径」+「逐个改」：\n\nstatic void BatchSetTextureType()\n{\n    string[] guids = AssetDatabase.FindAssets(\"t:Texture\", new[] { \"Assets/Textures\" });\n    foreach (string guid in guids)\n    {\n        string path = AssetDatabase.GUIDToAssetPath(guid);\n        TextureImporter importer = AssetImporter.GetAtPath(path) as TextureImporter;\n        importer.textureType = TextureImporterType.Sprite;\n        importer.SaveAndReimport();   // 改完重导入才生效\n    }\n    AssetDatabase.SaveAssets();\n    AssetDatabase.Refresh();\n}\n\n注意：改资源导入设置后要 SaveAndReimport 生效；改了文件后要 SaveAssets + Refresh 让 Project 面板跟上。\n\n四、批量处理的反模式（别踩）。\n· 用 foreach 改场景里「所有」物体 —— 往往想改的是「选中的」或「某个子集」，全量改是事故。先收窄范围。\n· 不确认就删 —— 批量删除前必须弹确认框，最好支持「预览将要删哪些」。\n· 不设进度 —— 几百个资源逐个操作没有进度条，卡死（editor-14）。\n· 忽略撤销 —— 批量改场景物体不 Undo，改错全盘重来。\n\n五、批量导入导出。\n· 批量导出清单 —— 把资源的名字、GUID、路径导出成 CSV/TXT，供核对、迁移。\n· 批量导入 —— 读一个清单文件，按清单找资源、改名、设参数，实现「配置驱动批量处理」。\n· 迁移工具 —— 旧项目资源批量迁移到新规范目录，跑一遍脚本就行。\n\n六、给批量工具加「防呆」。\n· 预览模式 —— 先跑一遍「只收集不改」的模式，列出将改的清单，确认后再执行。\n· 干跑（Dry Run）—— 打印「会改哪几个」，不真改，供检查。\n· 结果统计 —— 改了多少、跳过多少、失败多少，收尾报个数。\n· 回滚 —— 批量操作前自动做一次「全量 Undo 记录」，操作完提示「可按 Ctrl+Z 回滚」。\n\n七、常见坑。\n· 路径用绝对路径 —— 项目一换机器就废，用相对 Assets 的路径。\n· 忘了 SaveAndReimport —— 改了导入设置不重导入，界面没变化，以为没生效。\n· 同时改文件又改引用 —— 改名后引用全断，要一并处理引用（用 AssetDatabase 的引用查询）。\n· 重复跑不幂等 —— 批量工具跑两遍结果不一样，第二次把第一次的成果又改了，要设计成「幂等」。\n\n一句话总结：批量操作 = 选范围（Selection/FindAssets）+ 逐个处理（记住 SaveAndReimport/Refresh）+ 防呆（预览/确认/进度/Undo）+ 幂等——四件事做到位，批量工具既省时间又不闯祸。' },
    { type: 'component', title: '批量操作四步', items: [
      { name: '收窄范围', desc: 'Selection 选中子集，别全量乱扫' },
      { name: '逐个处理', desc: '改导入设置要 SaveAndReimport' },
      { name: '防呆三件', desc: '预览 / 确认框 / 进度条' },
      { name: 'Undo 记录', desc: '批量改场景物体，撤销有救' },
      { name: '幂等设计', desc: '跑两次结果一致，不重复破坏' },
    ] },
    { type: 'quiz', question: '写批量改资源的工具时，改完 TextureImporter 的 textureType 后，资源没反应。漏了什么？', options: ['改完没保存文件，所以没生效', '改了导入设置后要调 SaveAndReimport() 让资源重导入才生效，最后 SaveAssets + Refresh', 'Unity 有 bug，改不了批量导入'], answer: 1, tip: 'AssetImporter 改的是「导入设置」，必须 SaveAndReimport() 触发重导入才真正生效。同理，直接改文件内容后要 SaveAssets + Refresh 让 Project 面板更新。三件套缺一不可。' },
  ],
  'editor-18': [
    { type: 'text', title: '菜单系统进阶', body: 'MenuItem 那点入门用法 editor-1 讲过，这一节往深里走：层级菜单、快捷键、菜单验证、右键菜单——把菜单做成一个「顺手」的工具入口。\n\n一、层级菜单。菜单名用斜杠就能分层：\n\n[MenuItem(\"Tools/关卡工具/生成/路径点\")]\nstatic void GenPath() { }\n\n[MenuItem(\"Tools/关卡工具/生成/包围盒\")]\nstatic void GenBox() { }\n\n斜杠分层之后，Tools → 关卡工具 → 生成 下拉就出来了。层级别超过两层，太深没人找得到。\n\n二、快捷键。MenuItem 的第三参是优先级（排序用），快捷键要在菜单名里写：\n\n[MenuItem(\"Tools/快速生成 %G\")]   // % = Ctrl（Mac 上是 Cmd）\nstatic void QuickGen() { }\n\n快捷键符号：% 表示 Ctrl/Cmd，# 表示 Shift，& 表示 Alt；可以组合（%#G = Ctrl+Shift+G）。常用、高频的操作才配快捷键，配多了记不住。\n\n三、菜单验证（Validate）。菜单有时要「不可用」——没选中物体时「批量改名」该是灰的：\n\n[MenuItem(\"Tools/批量改名\")]\nstatic void RenameSelected() { }\n\n[MenuItem(\"Tools/批量改名\", true)]   // 第二个参数 true = 验证方法\nstatic bool ValidateRenameSelected()\n{\n    return Selection.gameObjects.Length > 0;   // 没选中就灰掉\n}\n\n验证方法同名 + 第二个参数 true，返回 bool 决定菜单可用性。注意：真正的入口方法记得把 bool 校验再写一遍（菜单被灰掉不代表代码不会被别处调）。\n\n四、右键菜单。\n· 场景/层级右键 —— 在对象上右键出菜单，用 GameObjectMenu 特性：\n\n[MenuItem(\"GameObject/关卡工具/添加路径点\", false, 10)]\nstatic void AddPath(GameObject menuCommand) { }   // 接收被右键的对象\n\n· 资源右键 —— 在 Project 面板资源上右键出菜单，用 AssetDatabase 或 ContextMenu：\n\n[MenuItem(\"Assets/导出选中资源清单\", false, 30)]\nstatic void ExportList()\n{\n    foreach (Object o in Selection.objects) { /* 处理选中的资源 */ }\n}\n\n· 组件右键 —— 挂在组件名上右键出菜单，用 [ContextMenu(\"菜单名\")] 加在类方法上，或写 [MenuItem(\"CONTEXT/组件名/菜单名\")]。\n\n五、菜单的组织与卫生。\n· 统一命名空间 —— 工具菜单统一放 Tools/ 或项目名/ 下，别和 Unity 内置菜单混。\n· 优先级控制排序 —— 第三参设好，常用的排前面。\n· 菜单项过多 —— 分组成子菜单，别一长串平铺。\n· 清理 —— 不用的菜单项删掉，项目里的工具菜单长年累积会变成一坨。\n\n六、常见坑。\n· 快捷键冲突 —— 全局快捷键和 Unity 内置/其他插件的撞了，用 %# 组合减少冲突。\n· 验证方法写错 —— 同名但没加 true 参数，Unity 不认，菜单永远可用。\n· 入口不校验 —— 验证是 UI 层的事，代码入口仍要自己校验（别的脚本也可能调）。\n· 菜单名重复 —— 两个 [MenuItem] 同路径，Unity 报错、二选一。\n\n一句话总结：菜单进阶 = 斜杠分层 + 快捷键（%#&）+ 验证方法（同名+true 灰掉不可用）+ 三种右键入口（GameObjectMenu/Assets/ContextMenu）——菜单是工具的门面，层级、快捷键、可用性三样都顺，团队才爱用。' },
    { type: 'component', title: '菜单进阶四件套', items: [
      { name: '斜杠分层', desc: 'Tools/关卡工具/生成，别超两层' },
      { name: '快捷键', desc: '%=Ctrl #=Shift &=Alt，高频操作才配' },
      { name: '验证方法', desc: '同名 + true 参数，没选中就灰掉' },
      { name: '右键入口', desc: 'GameObjectMenu / Assets / ContextMenu' },
      { name: '统一命名空间', desc: '工具菜单统一 Tools/ 前缀，不混内置' },
    ] },
    { type: 'quiz', question: '想让「批量改名」菜单在没有选中物体时自动变灰不可点，怎么做？', options: ['在菜单方法里判断并 return，什么都不做', '写一个同名且第二个参数为 true 的验证方法返回 bool：没选中返回 false，菜单自动灰掉；入口方法内再校验一次', '给菜单方法加 [RequireComponent] 特性'], answer: 1, tip: 'MenuItem 的验证模式：同名方法 + 第二个参数 true，返回 bool 控制菜单可用性。但注意灰掉的菜单只是 UI 层保护，入口方法内部仍要自己校验——别的脚本或快捷键还可能直接调用它。' },
  ],
  'editor-19': [
    { type: 'text', title: '编辑器测试', body: '工具写多了会膨胀：改了这个窗口，那个菜单又坏了。给编辑器工具写测试，是「改得起」的底气。这一节讲 Unity 的编辑器测试（Edit Mode Tests）和自动化。\n\n一、什么是 Edit Mode Tests。Unity 的 Test Framework 分两种：\n· Edit Mode Tests —— 在编辑器里跑的测试，不进入 Play 模式，适合测纯逻辑、工具函数、序列化、静态方法。\n· Play Mode Tests —— 进 Play 模式跑，适合测运行时行为。\n编辑器工具大多测纯逻辑，用 Edit Mode Tests 就够了。\n\n二、装测试框架。Window → Package Manager → 装 Test Framework（官方包）。然后在 Assets 下建 Tests 文件夹，就能写测试了。\n\n三、写第一个测试。测一个工具函数：\n\nusing NUnit.Framework;\nusing UnityEngine.TestTools;\n\npublic class BatchRenameTests\n{\n    [Test]\n    public void BuildNewName_加前缀_正确拼接()\n    {\n        string result = BatchRename.BuildNewName(\"Enemy_01\", \"NPC\");\n        Assert.AreEqual(\"NPC_Enemy_01\", result);\n    }\n\n    [Test]\n    public void BuildNewName_空前缀_原名不变()\n    {\n        Assert.AreEqual(\"Enemy_01\", BatchRename.BuildNewName(\"Enemy_01\", \"\"));\n    }\n}\n\n要点：\n· [Test] 标测试方法，Assert 断言结果。\n· 被测逻辑要「可测」—— 把纯计算抽成静态方法/纯函数，别都埋在 OnGUI 里。\n· 测试方法命名用「行为描述」—— 一眼看懂测什么。\n\n四、测「会动 Assets」的工具。操作资源的工具，测试要小心别把真项目搞乱：\n· 用临时目录 —— 测试里先 AssetDatabase.CreateFolder 建一个 Temp 目录，测完删掉。\n· 测完清理 —— finally 里 AssetDatabase.DeleteAsset，测试不留下垃圾。\n· 别测真实资源 —— 别拿「正式美术资源」做测试输入，会破坏/污染。\n\n五、窗口测试。窗口本身难自动化，但可以测「窗口逻辑」：\n· 把窗口里「点击按钮后执行的逻辑」抽成公开方法，测试直接调方法（不弹窗口）。\n· 测「数据 → UI 数据」的转换：输入一批资源路径，断言输出列表对不对。\n· UI 布局靠人工看一眼，逻辑交给测试。\n\n六、在 CI 上跑测试。和打包一样走命令行：\n\nUnity.exe -batchmode -projectPath D:/Game -runTests \\\n  -testPlatform EditMode -testResults result.xml -logFile test.log\n\n· 用 -runTests 跑测试，-testPlatform EditMode，-testResults 输出结果文件。\n· CI 读 result.xml 判断红绿，挂到 Git 提交/合并流程上。\n· 每改一次编辑器工具，跑一遍全量编辑器测试，回归不靠人肉。\n\n七、常见坑。\n· 测试依赖场景状态 —— 编辑器测试默认没场景，别依赖「某个场景已加载」。\n· 测试改了共享资源 —— 跑完不还原，污染项目；用临时资源 + 清理。\n· 只测「快乐路径」—— 边界（空输入、非法路径）不测，工具一遇到怪输入就崩。\n· 测试里弹窗 —— 编辑器测试环境下弹窗会卡住，避免在可测逻辑里弹窗。\n\n一句话总结：编辑器测试用 Edit Mode Tests——把工具逻辑抽成纯函数、[Test] 断言、测临时资源并清理、命令行 -runTests 挂 CI；逻辑可测、环境干净、回归自动，工具才敢放心改。' },
    { type: 'component', title: '编辑器测试要点', items: [
      { name: 'Edit Mode Tests', desc: '编辑器内跑测试，测纯逻辑/工具函数' },
      { name: '逻辑可测', desc: '抽成纯函数/静态方法，别埋在 OnGUI' },
      { name: '临时资源 + 清理', desc: '测试建临时目录，跑完删干净' },
      { name: '边界用例', desc: '空输入/非法路径都测，防崩' },
      { name: '-runTests 挂 CI', desc: '命令行跑测试，结果写 XML 判红绿' },
    ] },
    { type: 'quiz', question: '编辑器测试里要测一个「批量改名」工具，最安全的做法是？', options: ['直接在真实项目资源上跑测试，方便', '被测逻辑抽成纯函数，测试用假数据调用它；涉及真实资产的操作用临时目录并在 finally 清理', '不写测试，靠人工点一遍菜单'], answer: 1, tip: '编辑器测试三原则：逻辑抽纯函数（可测）、测试环境独立（临时资源）、测完清理（finally 删除）。直接动真实资源既污染项目又可能破坏数据，绝对不做。' },
  ],
  'editor-20': [
    { type: 'text', title: '编辑器工具综合动手练', body: '学完 19 节，动手做一个「自己每天都要用」的工具——这一节引导你做「一键场景检查器」：扫一遍当前场景，列出所有命名不规范的物体、没挂必要组件的物体、引用为空的字段，一键修。做完你会发现：编辑器工具是给「懒人」最好的礼物。\n\n一、需求定清楚。工具名：场景体检。要干的活：\n1. 列出场景里所有 GameObject，检查命名规范（必须有前缀）。\n2. 检查关键物体是否挂了必要组件（如 Player 必须有 CharacterController）。\n3. 检查 MonoBehaviour 字段里有没有空引用（serialized 引用为 null 的）。\n4. 一键把不规范名字改成规范（预览 + 确认 + 可撤销）。\n\n二、搭骨架。\n\npublic class SceneInspector : EditorWindow\n{\n    [MenuItem(\"Tools/场景体检\")]\n    static void Open() => GetWindow<SceneInspector>(\"场景体检\");\n\n    List<string> issues = new List<string>();   // 检查结果\n    Vector2 scroll;\n\n    void OnGUI()\n    {\n        if (GUILayout.Button(\"开始体检\"))\n            ScanScene();\n        scroll = EditorGUILayout.BeginScrollView(scroll);\n        foreach (string issue in issues)\n            EditorGUILayout.HelpBox(issue, MessageType.Warning);\n        EditorGUILayout.EndScrollView();\n    }\n}\n\n三、第一步：扫描命名。\n\nvoid ScanScene()\n{\n    issues.Clear();\n    foreach (GameObject go in Object.FindObjectsOfType<GameObject>(true))\n    {\n        if (!go.name.StartsWith(\"Env_\") && !go.name.StartsWith(\"Player\") &&\n            !go.name.StartsWith(\"UI_\") && !go.name.StartsWith(\"FX_\"))\n            issues.Add($\"命名不规范：{go.name}\");\n    }\n}\n\n规范前缀写在一个数组里，方便以后加。\n\n四、第二步：检查必要组件。\n\nforeach (GameObject go in Object.FindObjectsOfType<GameObject>(true))\n{\n    if (go.name.StartsWith(\"Player\") && go.GetComponent<CharacterController>() == null)\n        issues.Add($\"Player 缺 CharacterController：{go.name}\");\n}\n\n五、第三步：检查空引用。用 SerializedObject 遍历字段，值是 null 的标记出来：\n\nSerializedObject so = new SerializedObject(component);\nSerializedProperty iter = so.GetIterator();\nwhile (iter.NextVisible(true))\n{\n    if (iter.propertyType == SerializedPropertyType.ObjectReference &&\n        iter.objectReferenceValue == null && iter.name != \"m_Script\")\n        issues.Add($\"空引用：{go.name}.{iter.name}\");\n}\n\n六、第四步：一键修复（带防呆）。\n\n[MenuItem(\"Tools/场景体检/一键修命名\")]\nstatic void FixNames()\n{\n    var renames = CollectBadNames();   // 先收集\n    if (!EditorUtility.DisplayDialog(\"确认\", $\"将改名 {renames.Count} 个物体，可撤销\", \"改名\", \"取消\"))\n        return;\n    foreach (var (go, newName) in renames)\n    {\n        Undo.RecordObject(go, \"修复命名\");\n        go.name = newName;\n    }\n}\n\n防呆三件套：先预览（Collect）、确认框、Undo 可撤销。\n\n七、给工具加分。\n· 一键定位 —— 点一条问题，Selection.activeGameObject + PingObject 定位到对应物体。\n· 一键修复 —— 能自动修的直接修（改名、加组件），不能修的打日志。\n· 保存检查结果 —— 导出成 txt 报告，发给策划核对。\n· 只查当前场景 —— 别去扫别的场景，操作范围锁死。\n\n八、验收清单（对照打勾）。\n□ 菜单能打开窗口\n□ 能扫出命名不规范/缺组件/空引用三类问题\n□ 点问题能定位到场景物体\n□ 一键修复有确认框、可撤销\n□ 修复只影响当前场景，不碰别的\n□ 检查过程不卡编辑器（用进度条/缓存）\n\n九、做完想一想。\n· 把「检查规则」做成 SO 配置，策划也能配规则。\n· 加上「批量修空引用」，半自动处理脏数据。\n· 接到「导入时自动跑体检」（editor-15 的 OnPostprocessAllAssets），资源一进就报警。\n\n一句话总结：一个能用的编辑器工具 = 需求清单 + 扫描逻辑 + 定位交互 + 防呆修复 + 验收清单——照着这一节一步步做，你自己的第一个「生产力工具」就完工了，以后每次点它都省下十分钟。' },
    { type: 'component', title: '动手练验收清单', items: [
      { name: '扫描三类问题', desc: '命名 / 缺组件 / 空引用' },
      { name: '定位交互', desc: '点问题定位到场景物体' },
      { name: '一键修复', desc: '确认框 + Undo 可撤销' },
      { name: '范围锁死', desc: '只查当前场景' },
      { name: '不卡编辑器', desc: '进度条 / 缓存' },
    ] },
    { type: 'quiz', question: '场景体检工具做「一键修复命名」前，最该做哪件事防止误伤？', options: ['直接全部改名，反正工具是给自己用的', '先预览将改名的清单，弹确认框，且用 Undo.RecordObject 保证可撤销', '把改名逻辑放到另一个线程'], answer: 1, tip: '批量修改工具的防呆三件套：先收集预览、弹确认框、Undo 可撤销。没有确认和撤销的批量工具，一次误改就是事故——工具越省事越要防呆。' },
  ],
}
