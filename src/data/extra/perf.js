export const lessons = [
  { id: 'perf-5', title: '内存与 GC' },
  { id: 'perf-6', title: '渲染优化深入' },
  { id: 'perf-7', title: '帧率优化实战' },
  { id: 'perf-8', title: '动手练：给弹幕加对象池' },
  { id: 'perf-9', title: 'Profiler 深入' },
  { id: 'perf-10', title: '内存管理深入' },
  { id: 'perf-11', title: '渲染性能实战' },
  { id: 'perf-12', title: '物理性能' },
  { id: 'perf-13', title: 'UI 性能' },
  { id: 'perf-14', title: '加载与资源' },
  { id: 'perf-15', title: '音频性能' },
  { id: 'perf-16', title: '移动端性能' },
  { id: 'perf-17', title: '帧率与流畅度' },
  { id: 'perf-18', title: '性能优化完整流程' },
  { id: 'perf-19', title: '常见性能坑' },
  { id: 'perf-20', title: '性能综合动手练：弹幕体检' },
]

export const cards = {
  'perf-5': [
    { type: 'text', title: '托管堆、GC 与垃圾制造机', body: '游戏卡顿有一种特殊的「卡法」：平时好好的，每隔几秒突然顿一下，帧率暴跌又马上恢复。十有八九是 GC（垃圾回收）在作祟。\n\n先补概念：C# 的对象（new 出来的 class 实例）都住在「托管堆」上。堆不是无限的，一边 new 一边扔，迟早堆满。GC（Garbage Collector，垃圾回收器）负责扫堆、把没人用的对象清掉腾地方。\n\n问题就在「扫」：GC 运行时，程序会暂停一小会儿——游戏就卡这一下。对象越多、越碎，GC 扫得越久、卡得越明显。\n\n所以性能优化的铁律第一条：别在运行时频繁产生垃圾（GC 垃圾 = 不再被引用、等待回收的对象）。\n\n头号凶手：每帧 new。\n\nvoid Update()\n{\n    // 每帧都造一个临时 List，用完整块扔掉 —— 垃圾制造机\n    List<Enemy> visible = new List<Enemy>();\n    // ...\n}\n\n每帧 new 一个 List，一秒 60 个垃圾对象，堆很快告急，GC 频繁来扫。解法：把 List 提到外面，用 Clear() 复用：\n\nprivate List<Enemy> visible = new List<Enemy>();\n\nvoid Update()\n{\n    visible.Clear();   // 清空不释放，内存留着下次用\n    // ...\n}\n\n二八定律：字符串拼接。\n\n字符串是不可变的——每次 + 都是造一个全新的字符串，旧的扔了。在 Update 里拼 UI 文本，一帧好几个垃圾：\n\nuiText.text = \"得分:\" + score + \" 时间:\" + time;   // 每帧造 2 个新字符串\n\n要拼很多段时，用 StringBuilder（可变的字符容器，不产生垃圾）：\n\nusing System.Text;\nprivate StringBuilder sb = new StringBuilder(64);\n\nvoid Update()\n{\n    sb.Length = 0;               // 清空复用\n    sb.Append(\"得分:\").Append(score);\n    uiText.text = sb.ToString(); // 只在最后 ToString 一次\n}\n\n对象缓存：别每次现找。\n\n· GetComponent 每次都要遍历组件列表，频繁调用也有开销。把结果存进字段，用一次取一次：\n\nprivate Rigidbody rb;\nvoid Awake() { rb = GetComponent<Rigidbody>(); }  // 只取一次\n\n· Find / FindObjectOfType / FindGameObjectWithTag 是出了名的贵，每帧找等于每帧全场景搜索。要么缓存，要么启动时找一次。\n· Camera.main 其实也做搜索——缓存成字段。\n\n还有一个概念要分清：值类型 vs 引用类型。\n· int、float、Vector3、struct 是值类型，分配在栈上，不产生 GC 垃圾\n· class、string、数组、List 是引用类型，分配在托管堆\n· 值类型被装箱（boxing，比如往 object / 接口里塞）才会产生垃圾：\n\nobject o = 42;   // int 装箱成对象，产生垃圾，要避免\n\n用 Profiler 看真相：\n打开 Profiler → CPU 标签，勾上 GC Alloc 列，跑一下游戏，看哪个函数旁边有红色分配数字——那列数字就是它每帧造的垃圾。\n\n一句话总结：GC 卡顿 = 垃圾太多 + GC 来扫；少 new、拼字符串用 StringBuilder、缓存 GetComponent/Find 的结果、警惕装箱，垃圾少了，卡顿自然没了。' },
    { type: 'quiz', question: '游戏「每隔几秒卡顿一下、平时流畅」，最可能是什么原因？', options: ['显卡过热', 'GC 频繁触发导致短暂暂停', '硬盘读取速度慢'], answer: 1, tip: '堆里的垃圾对象攒够了，GC 来清扫，扫的瞬间程序暂停——表现就是周期性卡顿。解决方向是减少运行时垃圾产生（少 new、用 StringBuilder 等）。' },
    { type: 'quiz', question: '下面哪个写法最容易在 Update 里产生 GC 垃圾？', options: ['每次用 StringBuilder 拼文本', '每帧 new 一个 List 装敌人，用完丢弃', '把 GetComponent 结果缓存进字段'], answer: 1, tip: '每帧 new 对象 = 每帧造垃圾，堆很快告急，GC 频繁来扫。StringBuilder 复用不造垃圾，缓存组件也避免了重复查找。' },
  ],
  'perf-6': [
    { type: 'text', title: '合批原理：一次画更多', body: '上一节讲过 Draw Call 是「一帧里 CPU 向 GPU 提交绘制命令的次数」，命令越多 CPU 越忙。这一节把「怎么少提交」的原理讲透——核心一个字：批（Batch）。\n\n原理：把多个物体的绘制合并成一次提交。GPU 一次能画很多三角形，CPU 最贵的是「切换状态」——换材质、换贴图、换 Shader 都要通知 GPU。如果一批物体用同一个材质、同一种网格，CPU 可以一口气说「这一大堆一起画」，提交一次就画完。\n\nUnity 有四种「批」，分清楚它们各自的脾气：\n\n一、动态合批（Dynamic Batching）\n运行时由引擎自动判断、自动合并。省心，但条件苛刻：\n· 必须同材质（贴图、参数全一样）\n· 每个网格顶点数要少（一般 225~300 个以内，具体看平台）\n· 有光照影响、带蒙皮的网格常被排除\n· 每帧都在变形的物体也能批（这是它的长处）\n\n二、静态合批（Static Batching）\n把不动的物体预先合并成一个大网格，运行时一次提交。做法：把物体勾上 Static（至少勾 Batching Static）。\n· 效果好：不限制顶点数\n· 代价：合并后的大网格占内存（多个物体的网格拼成一个大网格），内存占用会上升\n\n三、SRP Batcher（SRP 批处理器）\nURP/HDRP 才有的新一代批处理。它不是把网格拼起来，而是让 GPU 复用材质状态，减少「切换材质」的开销。它比动态合批条件宽松得多，还能批带 MaterialPropertyBlock 的物体。\n· 前提：Shader 兼容（URP 自带的 Shader 都兼容，自定义的要确认）\n\n四、GPU Instancing（GPU 实例化）\n同一网格 + 同一材质，只传一次网格数据，把几千个位置/旋转/颜色一起喂给 GPU，让它一次画几千份。\n\n最适合：草、石头、子弹、粒子这类「一个模型复制几千份」的场景。\n\n一个很容易踩的坑：同材质 ≠ 同一个材质实例。\n\n给 50 个方块各设一次颜色，Unity 会悄悄复制出 50 份材质实例，合批直接失效。想批量改颜色又保持合批，用 MaterialPropertyBlock：\n\nMaterialPropertyBlock mpb = new MaterialPropertyBlock();\nmpb.SetColor(\"_Color\", red);\nrenderer.SetPropertyBlock(mpb);   // 颜色各不相同，但仍共享同一材质，能批\n\n配套的一招：纹理图集（Texture Atlas）。\n\n2D 和 UI 里，几十张小图是几十个纹理，合不了批。把几百张小图拼成一张大图（图集），所有元素用同一张贴图，Draw Call 立刻降下来。Unity 的 Sprite Atlas 就是干这个的。3D 里类似的思路是 Texture Array（纹理数组）。\n\n一句话总结：合批的前提是同材质，手段有动态、静态、SRP、Instancing 四种，配色用 MaterialPropertyBlock，贴图拼图集——记住了，Draw Call 就压得住。' },
    { type: 'component', title: '四种批处理手段', items: [
      { name: 'Dynamic Batching', desc: '动态合批：引擎自动，条件苛刻（同材质+少顶点）' },
      { name: 'Static Batching', desc: '静态合批：预合并静态物体，效果好但费内存' },
      { name: 'SRP Batcher', desc: 'URP/HDRP 专属，复用材质状态减少切换' },
      { name: 'GPU Instancing', desc: '同网格海量实例一次绘制，草/石头/粒子首选' },
    ] },
    { type: 'quiz', question: '1000 棵完全相同的树模型要铺满山坡，想一次提交画完，最合适的方案？', options: ['动态合批', 'GPU Instancing', 'SRP Batcher'], answer: 1, tip: '同网格+同材质的海量实例，GPU Instancing 是专为这种场景设计的——网格只传一次，位置变换打包喂给 GPU，一次画完。' },
  ],
  'perf-7': [
    { type: 'text', title: '帧率优化实战流程', body: '前面学的都是「零件」，这一节教一套「打法」——真枪实弹地调优一个卡顿的游戏，按什么顺序下手。\n\n第一步：定目标。\n先算清楚预算：目标 60 帧 = 每帧 16.7 毫秒；目标 30 帧 = 33.3 毫秒。游戏是手机还是 PC，定一个现实的目标。\n\n第二步：定位，用 Profiler 而不是猜。\nWindow → Analysis → Profiler，点 Record 跑一小段。重点看三块：\n\n· CPU Usage —— 谁是 CPU 上的大户？展开看函数：是脚本逻辑、物理（Physics）、动画（Animation）、还是粒子（Particles）？哪个函数头顶的数字最大？\n· Rendering / GPU —— GPU 忙不忙？屏幕上的东西是不是画太重（overdraw、阴影、后处理）？\n· Memory —— 内存是不是持续上涨？（持续涨 = 泄漏或垃圾堆积）\n\n一个判断口诀：CPU 忙 GPU 闲 → 优化 CPU 侧；GPU 忙 CPU 闲 → 优化渲染侧；两头都忙 → 降档整体规格。\n\n第三步：对症下药。\n\nCPU 侧常见元凶和对应解法：\n· 脚本里每帧 Find / GetComponent / LINQ —— 缓存、改用事件驱动\n· 每帧 new 垃圾 → GC 卡顿 —— 对象池、StringBuilder（perf-5）\n· 物理太重 —— 减少刚体数量、用 CharacterController 替代、调物理步进\n· 动画太多 —— 没在看的角色降帧更新（Animator.cullingMode 设成基于距离剔除）\n\nGPU 侧常见元凶：\n· Overdraw（透明物体叠加太多）—— 粒子别叠加太多层、UI 少用全屏模糊\n· 阴影太多 —— 限制阴影距离、少用实时阴影\n· 后处理太重 —— 辉光、景深按需开，能关就关\n· 贴图太大 —— 超过 2048 的检查，压缩格式用对（Android 用 ASTC/ETC2）\n\n第四步：上 LOD 和距离裁剪。\n\n远处看不清的就别按最高规格画（perf-4 讲过）。把 LOD 切换距离拉开：\n\n· LOD 0（高模）：占屏幕比例大于 40% 才显示\n· LOD 1（中模）：大于 20%\n· LOD 2（低模）：大于 5%\n· Culled：低于 5% 直接不画\n\n粒子数也同理：远距离的粒子系统直接降 Max Particles 或停发（ParticleSystem.Stop(true, StopEmittingAndClear)）。\n\n第五步：实测调优，改一项测一项。\n\n流程固定：改一个点 → 跑 Profiler → 看那一项的数字下来没 → 下不来就回退、换个思路。别一次改五个，改完卡没卡你都不知道是谁的功劳。\n\n最后补一个常用开关：Quality Settings（质量设置）。\nProject Settings → Quality 里把「阴影质量、抗锯齿、贴图质量」逐档调低，是最快的整体降档手段——适合保底，不能替代定点优化。\n\n一句话总结：定目标 → Profiler 定位 → 对症下药 → LOD/距离裁剪 → 改一项测一项，这套流程走完，帧数就有保障了。' },
    { type: 'quiz', question: '游戏「CPU 占用很高、GPU 占用很低」，下面哪个优化方向最对症？', options: ['关掉阴影和后处理', '降低贴图分辨率', '检查脚本里有没有每帧 Find/GetComponent/LINQ 之类的重活'], answer: 2, tip: 'CPU 忙说明瓶颈在逻辑侧（脚本、物理、动画、GC），先查脚本每帧重活和 GC 垃圾；GPU 侧的阴影、贴图、后处理此时不是主因。' },
  ],
  'perf-8': [
    { type: 'text', title: '动手练：给弹幕加对象池', body: '🎯 任务目标：先写一个每 0.2 秒 Instantiate/Destroy 一颗子弹的弹幕发射器，再改造成对象池（预建 30 颗、取用/回收），对比两版「真正生成的对象总数」和流畅度，理解 GC 卡顿从哪来。\n\n📦 前置要求：先学完本课 perf-5（内存与 GC）、perf-6（渲染优化深入）、perf-7（帧率优化实战）。\n\n📋 动手步骤：\n1. 建一个空场景，在原点放一个空物体命名 Shooter，当发射点。\n2. 做子弹预制体：新建一个 Sphere 命名 Bullet，Scale 设 (0.2,0.2,0.2)，给子弹挂 Bullet.cs（向前飞），然后从 Hierarchy 拖到 Assets 面板做成预制体，再从场景里删掉（场景里只留 Shooter）。\n    using UnityEngine;\n    public class Bullet : MonoBehaviour\n    {\n        public float speed = 10f;\n        void Update() { transform.Translate(Vector3.forward * speed * Time.deltaTime); }\n    }\n3. 写「先实现版」发射器 Shooter.cs（直接 Instantiate / Destroy）：\n    using UnityEngine;\n    public class Shooter : MonoBehaviour\n    {\n        public GameObject bulletPrefab;\n        public float interval = 0.2f;\n        public int totalSpawned;   // 发射次数（这个数两版都会涨）\n        private float timer;\n\n        void Update()\n        {\n            timer += Time.deltaTime;\n            if (timer >= interval)\n            {\n                timer = 0f;\n                totalSpawned++;\n                GameObject go = Instantiate(bulletPrefab, transform.position, transform.rotation);\n                Destroy(go, 3f);   // 3 秒后销毁\n            }\n        }\n    }\n4. 把 Shooter 挂到 Shooter 物体上，Bullet 预制体拖进 bulletPrefab。跑起来做「第一版记录」：\n   · Game 视图右上角点 Stats，盯帧率；Hierarchy 里子弹一会儿多一会儿被删。\n   · 打开 Window → Analysis → Profiler，CPU 标签勾上 GC Alloc 列——Instantiate/Destroy 那几帧的红色分配数字一直在跳，这就是垃圾在堆积。\n   · 在 Inspector 盯 totalSpawned，它一直往上涨（真正 Instantiate 的次数也在涨）。\n5. 做对象池。新建脚本 ObjectPool.cs，挂到一个空物体 Pool 上：\n    using System.Collections.Generic;\n    using UnityEngine;\n    public class ObjectPool : MonoBehaviour\n    {\n        public GameObject bulletPrefab;\n        public int prewarm = 30;   // 预建 30 颗\n        private Queue<GameObject> pool = new Queue<GameObject>();\n\n        void Awake()\n        {\n            for (int i = 0; i < prewarm; i++)\n            {\n                GameObject go = Instantiate(bulletPrefab, transform);\n                go.SetActive(false);\n                pool.Enqueue(go);\n            }\n        }\n\n        public GameObject Get()\n        {\n            if (pool.Count == 0)   // 池空了补一颗，别取不到报错\n            {\n                GameObject extra = Instantiate(bulletPrefab, transform);\n                pool.Enqueue(extra);\n            }\n            GameObject obj = pool.Dequeue();\n            obj.SetActive(true);\n            return obj;\n        }\n\n        public void Return(GameObject obj)\n        {\n            obj.SetActive(false);\n            pool.Enqueue(obj);\n        }\n    }\n6. 把 Bullet.cs 改成「池版本」：到期回收而不是销毁，用 Launch 在取出时重置：\n    using UnityEngine;\n    public class Bullet : MonoBehaviour\n    {\n        public float speed = 10f;\n        public float life = 3f;\n        private ObjectPool pool;   // 从哪来，回哪去\n        private float timer;\n\n        public void Launch(ObjectPool owner)\n        {\n            pool = owner;\n            timer = 0f;\n        }\n\n        void Update()\n        {\n            transform.Translate(Vector3.forward * speed * Time.deltaTime);\n            timer += Time.deltaTime;\n            if (timer >= life) pool.Return(gameObject);   // 回池，不 Destroy\n        }\n    }\n7. 把 Shooter.cs 改成「池版本」：从池里取、复位位置、交给 Launch：\n    using UnityEngine;\n    public class Shooter : MonoBehaviour\n    {\n        public ObjectPool pool;\n        public float interval = 0.2f;\n        public int totalSpawned;\n        private float timer;\n\n        void Update()\n        {\n            timer += Time.deltaTime;\n            if (timer >= interval)\n            {\n                timer = 0f;\n                totalSpawned++;\n                GameObject go = pool.Get();   // 从池里取，不是 new\n                go.transform.SetPositionAndRotation(transform.position, transform.rotation);\n                go.GetComponent<Bullet>().Launch(pool);\n            }\n        }\n    }\n8. 挂载调整：把 Shooter 的脚本换成池版本，把 Pool 物体拖进 pool 槽位；Bullet 预制体上的 Bullet.cs 换成池版本。再跑同样时长，做「第二版记录」并对比：\n   · 池版本真正 Instantiate 的对象总数固定：预建 30 颗，同时存在的子弹最多 15 颗（3 秒寿命 ÷ 0.2 秒发射间隔），30 颗绰绰有余，不再持续造新对象。\n   · Profiler 的 GC Alloc 基本不再涨，帧率曲线比先实现版平稳。\n\n✅ 验收清单：\n· 能 □ 先实现版每 0.2 秒发射一颗子弹、3 秒后销毁，totalSpawned 持续上涨\n· 能 □ 在 Profiler 的 GC Alloc 里看到先实现版的垃圾分配在持续堆积\n· 能 □ 对象池版改造后，真正 Instantiate 的对象总数保持恒定（≈30 预建数量）\n· 能 □ 对象池版 GC Alloc 明显下降，帧率更稳\n· 能 □ 能说清卡顿来源：Instantiate/Destroy 产生垃圾 → 堆满触发 GC 清扫 → 清扫瞬间掉帧\n\n🕳 常见卡壳：\n· 池版本子弹从上一颗的旧位置起飞 → Get 之后忘了 SetPositionAndRotation 复位。取出即重置位置旋转，是对象池的标准动作。\n· 子弹到期不回收、越积越多 → 检查 Bullet 有没有调 pool.Return，以及 Shooter 取出时有没有调 Launch（没调就是 pool 为 null 报错）。\n· 池老是扩容、又变成一直 Instantiate → 预建太少。同时存在数 = 寿命 ÷ 发射间隔（3 ÷ 0.2 = 15），prewarm 设 30 就够。\n· 对比只凭感觉 → 用数据说话：两版各跑同一时长，GC Alloc 和帧率曲线截图对比。' },
    { type: 'quiz', question: '对象池相比「每 0.2 秒 Instantiate、3 秒后 Destroy」的最大收益是？', options: ['子弹飞得更快', '复用物体，避免频繁创建/销毁产生的 GC 垃圾，帧率更稳', '完全不占用任何内存'], answer: 1, tip: 'Instantiate/Destroy 每次都在托管堆上造新对象、留垃圾，攒够了 GC 来扫就掉帧；对象池预建一批、取用回收，对象数恒定，垃圾不再持续产生。' },
  ],
  'perf-9': [
    { type: 'text', title: 'Profiler 深入：把卡顿一层层剥开', body: '前面用 Profiler 看「谁占的时间多」，这一节往深挖：Profiler 每个模块是干嘛的、怎么用火焰图快速定位、卡顿怎么顺着调用链追到根因。\n\n一、Profiler 界面各模块\n\nWindow → Analysis → Profiler，选中 CPU Usage（默认），左边一排模块开关对应：\n\n· Scripts：C# 脚本逻辑。这个数字高，问题在代码（每帧重活、GC）\n· Physics：物理引擎。高 → 刚体太多、碰撞太复杂、Fixed Timestep 太小\n· Animation：动画系统。高 → 角色太多没剔除、Animator 每帧全量更新\n· Rendering：渲染提交。高 → Draw Call 多、CPU 端提交压力大\n· Particles：粒子系统。高 → 粒子发射太多、屏幕粒子数爆表\n· UI：UGUI 布局与重建。高 → Canvas 频繁脏（Rebuild）\n· Audio：音频。高 → 空间音频太多、采样率太高\n· Network / Others：网络 / 杂项\n\n看哪块数字最大，问题就在哪块——这是定位的第一步。\n\n二、定位卡顿：Hierarchy 视图 + 顶部计时条\n\n1. 打开 Hierarchy（Profiler 底部）视图，看每个系统的时间轴：哪一段有红色/黄色长条，那段就是卡顿区\n2. 顶部还有一条总帧耗时（ms）的竖线：竖线越高，帧越贵；16.7ms 是 60 帧预算，超了就是掉帧\n3. 卡顿时选中那一帧，下方会显示该帧各函数的耗时明细\n\n三、火焰图（Hierarchy 展开）：顺着调用链追根因\n\n点击卡顿帧，展开顶部函数，一层层往下：\n\n· PlayerLoop → Update → EnemyManager.Update → Pathfinding：看到 Pathfinding 占大头，根因就是寻路\n· 每一行的耗时 = 自身耗时 + 子函数耗时。看「自身」列能区分：是这函数自己重，还是它调的子函数重\n\n四、关键列：GC Alloc 与 Calls\n\n· GC Alloc 列（勾选显示）：每帧分配了多少字节，红色越深说明垃圾越多——GC 卡顿的罪证\n· Calls：调用次数。次数异常高（比如每帧调 500 次 Find）说明在循环里干蠢事\n\n五、定位卡顿的标准流程\n\n1. 打开 Profiler → Record，跑一段容易卡的操作\n2. 找到最高的一帧，看哪个模块（Physics/Scripts/Rendering）时间最长\n3. 展开该模块，顺着调用链找到占时最大的具体函数\n4. 读 GC Alloc：确认是不是垃圾堆积\n5. 修完再跑同一段，对比同一帧的前后耗时\n\n坑位提醒：\n· Profile 要跑「真实负载」，编辑器里 Profiler 本身有开销，数字看相对变化别抠绝对值\n· 编辑器模式下渲染管线跟真机有差异，真机问题用 Device Profiler / Profiler 连真机看\n· 一帧卡好几十毫秒，先找那帧，别盯着平均帧率——平均会骗人\n\n一句话总结：Profiler 看模块 → Hierarchy 找卡顿帧 → 火焰图追根因 → GC Alloc 验垃圾，一条链走到根，卡顿无处可藏。' },
    { type: 'quiz', question: 'Profiler 里发现 Physics 模块占用最大，下一步最该查什么？', options: ['检查脚本里有没有每帧 Find', '查场景里刚体数量、碰撞体复杂度、Fixed Timestep 设置', '压缩贴图'], answer: 1, tip: '模块定位到 Physics，说明瓶颈在物理引擎：刚体太多、碰撞计算过重、物理步进太频繁。对应优化是减刚体、简化碰撞、调 Fixed Timestep。' },
  ],
  'perf-10': [
    { type: 'text', title: '内存管理深入：让堆稳如泰山', body: 'perf-5 讲了 GC 原理，这一节往深走：怎么监控堆、怎么让分配归零、怎么排查「内存只涨不降」的泄漏。\n\n一、监控托管堆：GC Alloc 和 Heap 曲线\n\n· CPU Profiler 的 GC Alloc 列：每帧分配字节数。理想状态是 0——一帧都不该有垃圾\n· Memory Profiler / Profiler 的 Memory 标签：看 Total Allocated 曲线。平稳是健康，爬坡是泄漏\n\n二、避免每帧分配：四大惯犯\n\n1. 字符串拼接（perf-5 讲过）：Update 里拼文本用 StringBuilder 或缓存 UI 文本：\n\nsb.Length = 0;\nsb.Append(\"HP: \").Append(hp);\nuiText.text = sb.ToString();\n\n2. 每帧 new 集合：List/Dictionary/数组提到字段外用 Clear() 复用：\n\nprivate List<Enemy> near = new List<Enemy>();\nvoid Update() { near.Clear(); Physics.OverlapSphereNonAlloc(pos, r, near); }\n\n注意：Unity 的 NonAlloc 系列（OverlapSphereNonAlloc / RaycastNonAlloc）就是为「零分配」设计的——结果写进你复用的数组，不新建。\n\n3. LINQ：Where/Select/OrderBy 内部会生成委托和迭代器，循环里用全是垃圾。换成 for/foreach + 条件判断：\n\n// 别这么写：\nvar targets = enemies.Where(e => e.IsAlive).OrderBy(e => e.Dist);\n// 用数组排序或手动循环选最优\n\n4. 装箱：int/Vector3 塞进 object/接口会产生装箱垃圾。UI 拼接数字尤其容易踩（string 拼 int 会装箱）：\n\nuiText.text = score.ToString();   // 先 ToString 再拼，避免 int 装箱进字符串\n\n三、对象复用：宁可复用烂，不可每帧新\n\n· 变换缓存：GetComponent 结果、Transform 引用，Start/Awake 里取一次\n· 中间数组：用一次丢一次的临时数组，提成字段复用\n· 协程和回调：闭包捕获变量会生成对象，每帧 StartCoroutine 会持续造垃圾——优先用 Update 轮询或缓存协程实例\n\n四、内存泄漏排查：Heap 只涨不降\n\n泄漏 = 对象没被释放但也没被用，GC 永远收不走它（还有引用）。常见源头：\n\n· 事件/委托忘退订：静态事件订阅后 OnDisable 没退订，敌人销毁了但委托还引用它，整个对象树全泄漏。写完事件必查成对：\n\nprivate void OnEnable() { Event.OnPlayerDied += Handler; }\nprivate void OnDisable() { Event.OnPlayerDied -= Handler; }\n\n· 场景没卸载干净：加载的场景离开后没 UnloadUnusedAssets，残留资源驻留内存\n· 缓存了已销毁对象：字段里存的对象被 Destroy 了但引用还在，要置 null\n\n用 Memory Profiler 拍快照对比：切场景前后 Heap 应回到基线；只涨不降就是泄漏，快照 diff 里多出来的对象就是可疑对象。\n\n五、两个配置项\n\n· Project Settings → Player → Scripting GC 设置：\n  - Incremental GC（分步 GC）：把一次大 GC 拆成几小步摊到多帧，卡顿被摊薄（对流畅度帮助很大，缺点是 GC 总时间略增）\n  - 移动端通常建议开启\n· 大对象堆（LOH）分段：超过 85KB 的大对象进 LOH，清理成本高。避免反复分配超大数组，需要时一次到位\n\n一句话总结：GC Alloc 归零是目标——NonAlloc API + 复用集合 + 禁 LINQ + 防装箱四招压分配；事件成对退订 + 场景卸载 + 快照 diff 三招查泄漏；配上 Incremental GC，堆就稳了。' },
    { type: 'quiz', question: '排查「内存只涨不降」的泄漏，最直接的手段是？', options: ['重新编译脚本', '用 Memory Profiler 拍快照，对比场景切换前后的 Heap，diff 里多出来的对象就是可疑目标', '把画质调低'], answer: 1, tip: '泄漏的本质是「有引用、没释放」；快照对比能直接看到哪个对象在场景切换后没被清掉——比瞎猜准得多。' },
  ],
  'perf-11': [
    { type: 'text', title: '渲染性能实战：用 Frame Debugger 查合批', body: 'perf-6 讲了合批原理，这一节教「实际查」和「实际压」：合批到底成没成、材质怎么共享、纹理怎么压、Shader 变体怎么剪。\n\n一、Frame Debugger：合批的验尸报告\n\nWindow → Analysis → Frame Debugger，打开后点播放暂停，逐帧回放渲染：\n\n· 左侧是渲染事件列表，每条一个「绘制调用」（一个 Batch）\n· 点开一条，看右边 Details：\n  - Batch Count / 该批的三角形数 / 顶点数\n  - Mesh：一个网格被多物体复用，说明合批成功（同网格多实例）\n  - Material：一个材质批量渲染，说明材质共享成功\n· 两条记录用同一个材质 + 不同网格，说明「材质一样但网格不同，没合批」——查网格是否带蒙皮/顶点数超标\n\n判断合批的标准：屏幕上一个区域本该十几条记录，压成一条，就是成功。\n\n二、材质共享：一个材质打天下\n\n合批第一前提是同材质。实战三个检查：\n· 检查物体 Inspector 的 Mesh Renderer → Materials：同款材质列表里是不是拖了多份（复制出来的）\n· 改颜色别用 renderer.material（会自动实例化材质、破坏共享），用 renderer.sharedMaterial 或 MaterialPropertyBlock（perf-6 讲过）：\n\nrenderer.sharedMaterial.color = red;   // 共享材质：所有用它的一起变\n\n· 预制体共用同一个材质 asset：在 Project 面板建一个材质，多物体都拖它，别每物体新建\n\n三、纹理压缩：显存和带宽的命脉\n\n· 每个平台压缩格式不同：\n  - Android：ASTC（首选）/ ETC2\n  - iOS：ASTC\n  - PC：BC7 / BC1\n· Import Settings 里选对平台格式，RGBA32（未压缩）一张 2048 就是 16MB，ASTC 压缩后通常 1~3MB\n· 2 的幂尺寸（Power of Two）：非 2 幂纹理在部分平台无法压缩/合批，统一裁到 2 的幂\n· 图集（Atlas）：UI 和 2D 用 Sprite Atlas 把散图并成一张大图，一张贴图一个批次\n· 超过需要的分辨率检查：远处看不清的大纹理，降分辨率肉眼无差别\n\n四、Shader 变体裁剪：一个 Shader 的隐形成本\n\nShader 的每种特性组合（关键词开关）都是一个变体。材质面板一堆打勾项，编译出来可能几百个变体，打包体积和加载时间都膨胀：\n\n· Shader 面板点「+」展开关键词，勾掉的特性都算\n· Project Settings → Graphics → Shader Stripping：按平台剪掉用不到的变体\n· 自定义 Shader：多档特性（特性开关用 shader_feature/multi_compile）可以剪\n· 检查 Prefab 用的 Shader：统一替换成 URP 自带 Shader（它们通常已做变体优化）\n\n五、实战检查清单\n\n· 同材质物体多吗？少 → 合批无望，先统一材质\n· Frame Debugger 里 Batch 数高吗？高 → 查是材质不同、网格不同还是顶点超标\n· 贴图都压缩了吗？没压 → 选平台格式重导\n· Shader 变体多得离谱吗？多 → 开 Stripping\n· 静态物体会动的物体，静态合批开对了吗？\n\n一句话总结：Frame Debugger 当验尸报告查合批，材质共享是合批命脉，纹理压缩管显存带宽，Shader 变体裁剪管包体和加载——四样查一遍，渲染性能就「有数」了。' },
    { type: 'quiz', question: '用 Frame Debugger 看到两个物体明明是同一个材质，却分成了两条绘制记录，最可能的原因是？', options: ['两个物体用的网格不同，无法合并', '贴图没压缩', '抗锯齿开太高'], answer: 0, tip: '合批要求同材质 + 同网格（或可合并条件）；同材质不同网格通常合并不了。查网格是否一致、是否带蒙皮、顶点数是否超上限。' },
  ],
  'perf-12': [
    { type: 'text', title: '物理性能：刚体多了，世界就卡', body: '物理引擎每帧要算刚体运动、碰撞检测、关节约束，东西一多 CPU 直接吃不消。这一节把物理开销的五个开关拧明白。\n\n一、物理物体数量：少一个是一个\n\n· 场景里刚体（Rigidbody）数量是物理开销的第一大头。能不加就不加：\n  - 静止的地面/墙，挂 Mesh Collider 就够，别挂 Rigidbody\n  - 道具类小物体：数量大时用「静态 + 触发器」代替满场刚体\n  - 只检测不参与物理的，用 Collider.isTrigger（不参与刚体动力学）\n· 不可见的远距离物体：远离玩家的物理物体，考虑禁用其 Rigidbody/Collider\n\n二、碰撞检测优化：省一半的配对计算\n\n物理引擎要两两检测碰撞，物体越多配对爆炸（N²）。三招压低：\n\n· 简化碰撞体：Mesh Collider 换成盒/球/胶囊（近似），碰撞计算便宜一个量级\n· 用碰撞层：Project Settings → Physics → Layer Collision Matrix，让「子弹 vs 子弹」「玩家 vs 玩家」这类永不碰撞的层互相不打：\n\nPhysics.IgnoreLayerCollision(LayerMask.NameToLayer(\"Player\"), LayerMask.NameToLayer(\"Enemy\"), true);\n\n· 用离散模式（Discrete）而非连续（Continuous）：Continuous 是给高速穿模用的，普通项目离散就够，便宜很多\n\n三、Fixed Timestep：物理的「帧率」\n\n物理更新按固定时间步走，默认 0.02 秒（50Hz）。\n\n· Project Settings → Time → Fixed Timestep：调大（0.03）物理变少更省，但可能穿模/反应变钝；调小物理更精细但开销翻倍\n· 别轻易改：先看它是不是瓶颈再动\n· Physics.autoSimulation：想完全控制物理更新时机（比如暂停/步进）可以手动 Simulate\n\n四、Sleep 休眠：不动就别算\n\n刚体静止下来后，引擎让它进入 Sleep（休眠）——不再每帧计算。这是最省钱的物理优化：\n\n· 保持默认开启，别用代码动不动唤醒：\n\nrb.WakeUp();   // 谨慎调用，经常唤醒等于不睡\n\n· 物体被反复推来推去醒不过来，检查是不是有代码每帧给它施力/改位置\n· Sleep Threshold（休眠阈值）调大一点，慢速移动的小物体会更快入睡\n\n五、实战检查顺序\n\n1. Profiler 确认瓶颈在 Physics 模块（perf-9 讲过）\n2. 数场景刚体：减到必需数量\n3. Mesh Collider 全换简单碰撞体\n4. 层碰撞矩阵去掉无意义的碰撞对\n5. Fixed Timestep 只在确认必要时调\n6. 确认没有代码每帧打扰刚体休眠\n\n坑位提醒：\n· 触发器（Trigger）也走物理检测，数量多了同样开销大\n· 角色移动优先用 CharacterController（便宜、不带刚体动力学），别给玩家挂刚体+力移动\n· 高速子弹的 Continuous 碰撞是开销大户，必要时只在子弹上开\n\n一句话总结：减刚体数量、简化碰撞体、用碰撞矩阵滤配对、固定步进按需调、让刚体好好睡觉——物理开销从这五处入手，效果立竿见影。' },
    { type: 'quiz', question: '场景里大量「子弹 vs 子弹」「玩家 vs 友军」永远不该碰撞，最省物理开销的做法是？', options: ['给每个物体加更多碰撞体', '在 Layer Collision Matrix 里把这两层互碰关掉', '把 Fixed Timestep 调小'], answer: 1, tip: '物理引擎是两两配对检测，去掉不可能碰撞的层对，直接少一半甚至更多的配对计算——比任何单个物体优化都立竿见影。' },
  ],
  'perf-13': [
    { type: 'text', title: 'UI 性能：一帧重建，全界面重画', body: 'UI 卡有个经典特征：改了血条，整个界面都顿一下。根子在 Canvas 重建（Rebuild）——UGUI 一有脏标记，整块 Canvas 重新布局、重绘网格。这一节把 UI 开销按下去。\n\n一、Rebuild 原理：脏了就全重画\n\nUGUI 的 Canvas 是批渲染的，任何元素改变位置/颜色/文字，都会把所在 Canvas 标记为「脏」，下一帧整个 Canvas 重新生成网格。问题就出在「整个」：一个血条动，全屏 UI 全重画。\n\n二、Canvas 分割：让小改动不出格\n\n核心思路：把会频繁变化的 UI 和静态 UI 分开，放到不同 Canvas：\n\n· 静态 Canvas：背景、标题、常驻按钮——基本不动\n· 动态 Canvas：血条、伤害数字、冷却图标——经常变\n\n动态元素改动时，只有自己的小 Canvas 重画，静态的大界面纹丝不动。注意 Canvas 是嵌套的也有效：子 Canvas 的改动只影响自身。\n\n· 一个误区：不是 Canvas 越多越好，每个 Canvas 都是一次独立的合批提交；平衡点在「独立变化的部分各占一个 Canvas」。\n\n三、避免频繁改 UI 属性\n\n· 血条别每帧改：HP 没变化就不要动 Image.fillAmount——值相同也照样触发脏标记。先判断再赋值：\n\nif (Mathf.Abs(hpRatio - currentFill) > 0.01f)\n{\n    hpFill.fillAmount = hpRatio;\n}\n\n· 文字别每帧拼：Text 更新 = 重排版 = 重绘，是 UI 里最贵的。只在内容变化时更新（perf-10 的 StringBuilder + 变化检测）\n· 属性修改尽量合并：一次更新改多个属性，比分散多次改更省（每改一次就是一次脏标记）\n\n四、图集：UI 合批的命根\n\n· UI 元素要共享同一张图集才能合批（perf-6 讲过）。Project → Create → Sprite Atlas，把 UI 图都拖进去\n· 跨图集的相邻元素无法合批，尽量让同屏元素在同一个图集里\n· 用 Image 时注意是否带 Raycast Target 勾选：不需要接收点击的 Image 把 Raycast Target 关掉——省掉一串射线检测\n\n五、Text 缓存：别让字体渲染拖垮帧率\n\n· 动态字体（Dynamic Font）首次渲染某个字符要生成字形，量大会卡。把常用字符的字体在启动时预热：\n\nText.text = \"0123456789:\";   // 预热数字和冒号\n\n· 大段文字的滚动/聊天框：用 TextMeshPro 的缓存优化，或把滚动的文字固定在独立 Canvas 避免连坐\n· 少用 Rich Text 的复杂标签，解析也有开销\n\n六、UI Profiler 查证\n\nWindow → Analysis → Profiler → UI 模块（或 Canvas 模块）：\n\n· 看 Rebuild 次数和耗时：数字大说明 Canvas 频繁重建\n· 逐个隐藏/显示元素，对比 Rebuild 数字，找到「罪魁」是哪个元素在反复触发\n\n一句话总结：变化的 UI 单独分 Canvas、属性改前先比较、图集统一素材、Text 预热缓存、Profiler 查 Rebuild——五招下去，改血条不再拖全界面。' },
    { type: 'quiz', question: '一个血条每帧都改 fillAmount，导致全屏 UI 频繁重建卡顿，最有效的修法是？', options: ['把血条放到独立的小 Canvas 里', '把画质调低', '给血条换更粗的边框'], answer: 0, tip: 'Rebuild 按 Canvas 整体触发：血条动会重画整个所在 Canvas。把频繁变化的血条隔离到独立 Canvas，静态大界面就不再被连坐重画。' },
  ],
  'perf-14': [
    { type: 'text', title: '加载与资源：卡顿常出在「加载那一瞬间」', body: '加载资源时卡一下，是另一个经典卡顿源。这一节讲加载的正确姿势：什么时候用 Resources、什么时候上 Addressable、异步加载怎么用、场景和资源怎么释放。\n\n一、加载方式：同步是元凶\n\n· Resources.Load 是同步的：加载一个大模型/贴图时，主线程阻塞，画面直接冻结到加载完——这就是「点开背包卡两秒」的原因。\n· 异步加载 Resources.LoadAsync 不阻塞主线程，加载时还能转圈/播动画：\n\nIEnumerator LoadModel()\n{\n    ResourceRequest req = Resources.LoadAsync<GameObject>(\"Enemies/Boss\");\n    yield return req;\n    GameObject boss = Instantiate(req.asset);\n}\n\n· Addressables 更进一层：支持依赖管理、远程分包、引用计数自动释放，是正式项目的主流选择：\n\nAddressables.LoadAssetAsync<GameObject>(\"Boss\");\n\n· 但注意：异步 ≠ 免性能。加载后的 Instantiate/解码贴图仍在主线程，量大了照样卡。\n\n二、加载时机：别在战斗中间加载\n\n· 关键资源提前加载：进 BOSS 房前把 BOSS 模型预加载（Background 或切场景时加载），而不是 BOSS 出场瞬间现加载\n· 用加载界面兜底：正式切换大场景时，先切到一个简单 Loading 场景异步加载，加载完再切目标场景\n· Addressables 的预下载：远程资源提前下载缓存，玩到那儿不用等网络\n\n三、场景卸载：离开的场景要「倒干净」\n\nSceneManager.UnloadSceneAsync 卸载场景后，场景里的资源不会自动全清：\n\nSceneManager.UnloadSceneAsync(scene);\nResources.UnloadUnusedAssets();   // 清掉不再被引用的资源（建议切场景后调用，有开销）\n\n· 没清理的残留资源会累积占内存（perf-10 的泄漏排查里就包含它）\n· Addressables 会自动引用计数：Release 掉不再用的句柄，资源自动卸载\n\n四、资源释放：引用计数是自保\n\n· Resources：Resources.UnloadUnusedAssets + 避免长期持有 Asset 引用\n· Addressables：每次 LoadAssetAsync 得到句柄，用完必须 Addressables.Release(handle)，否则资源永远不卸载\n· 常规代码：Scene 对象销毁后，字段里的引用记得置 null（虽然 GC 会兜底，但主动释放更可控）\n\n五、常驻资源 vs 按需加载\n\n· 常驻（一直在内存）：全局 UI 图集、常用字体、主界面素材——启动时一次加载，整个生命周期复用\n· 按需（用到的加载）：关卡专属模型、BGM、特效——进关卡加载，出关卡释放\n· 划分原则：看「使用频率」和「加载成本」——高频低成本的常驻，低频高成本的就按需\n\n一句话总结：同步加载换异步、关键资源提前载、场景离开必卸载、Addressable 用后必 Release、常驻/按需分清楚——加载卡顿就没了，内存也稳了。' },
    { type: 'quiz', question: '点开背包时游戏卡顿 2 秒，最可能的元凶是？', options: ['背包物品的贴图用了同步 Resources.Load', '游戏渲染分辨率太高', '音频音量太大'], answer: 0, tip: 'Resources.Load 同步加载时主线程阻塞，画面冻结到加载完。换成 Resources.LoadAsync 异步加载，或提前加载/Addressable，卡顿就没了。' },
  ],
  'perf-15': [
    { type: 'text', title: '音频性能：声儿不卡，帧率也别掉', body: '音频也会偷性能：内存占着、解压算着、空间音效一个个算位置。这节把音频的五个开销点理清，让声音丰富又不拖帧。\n\n一、音频内存：一大段波形很占地方\n\n· 音频加载类型（Load Type）决定内存占用：\n  - Decompress On Load（加载即解压）：播放最流畅，但内存占用最大——整段都放进内存\n  - Compressed In Memory（内存压缩）：内存省，但每帧要解压，CPU 换内存\n  - Streaming（流式）：边读边播，内存极小，适合 BGM（长音乐）\n· 策略：短音效（打击、脚步）用 Decompress On Load 求低延迟；长 BGM 用 Streaming 省内存\n· 采样率（Sample Rate）：44100 对大多数音效是浪费，BGM 才用高采样率\n\n二、压缩格式：选对格式省一半\n\n· Vorbis（.ogg）：默认格式，通用、体积小，适合大多数音乐和音效\n· ADPCM：压缩比低一点但 CPU 开销极小，适合需要大量同时播放的短音效\n· PCM（.wav 未压缩）：不压缩，只在要求无损/极低延迟时用\n· 变调、变速处理（pitch）会解压整个 clip，压缩格式下的变调有额外开销\n\n三、动态加载：音频别一次全塞内存\n\n· 全场景音频一进场景就全加载，内存报表。用「需要时加载」：\n  - Addressable 加载音效，用完 Release\n  - 或者按区域加载：走进某个区域才加载该区域的 BGM/环境音\n· 复用 AudioSource：一个 AudioSource 用 PlayOneShot 播多个短音效，比每音效挂一个 AudioSource 省：\n\nsfxSource.PlayOneShot(hitClip);   // 同一个 AudioSource 连播不同音效\n\n· 对象池播放器：子弹命中音、脚步音这类高频音效，用对象池循环复用播放器组件\n\n四、空间音效数量：3D 声音的隐性开销\n\nAudioSource.spatialBlend = 1 的空间音效，每帧要算距离衰减、多普勒效应，数量多了是 CPU 负担：\n\n· 同屏空间音效控制在合理数量（一般十几个以内），远处的直接切换成 2D 或停播\n· AudioSource 的 Spatial Blend 为 1 的才计算 3D；没必要的别开\n· 用 Audio Source 的数量做全局限制：播放时先检查当前正在播放的 AudioSource 数量\n\n五、AudioMixer：分组管理 + 省 CPU\n\n· AudioMixer 把声音分组（BGM/SFX/环境），调音量互不影响——比每个声音单独调 volume 好维护\n· Mixer 的 Snapshot 可以一键切氛围（进战斗音乐变紧张）\n· 用 Mixer 的 Effect 时注意量：过多 effect 处理也是开销\n\n六、查证与坑位\n\n· Profiler → Audio 模块：看 audio 峰值占用，定位是不是音频拖帧\n· 常见坑：\n  - 一个音效创建一堆 AudioSource 组件不回收 → 对象池化\n  - 每帧改 pitch/volume（触发重采样）→ 变化时再改\n  - 大量长 BGM 用 Decompress 全内存常驻 → 换 Streaming\n\n一句话总结：BGM 走 Streaming、短音效 Decompress、同源复用 AudioSource、空间音效限量、AudioMixer 分组管理——五处一拧，音频既丰富又不偷帧。' },
    { type: 'quiz', question: '游戏里 BGM（长音乐）应该用哪种 Load Type 最省内存？', options: ['Decompress On Load', 'Streaming（流式）', 'Compressed In Memory'], answer: 1, tip: '长音乐用 Streaming 边读边播，内存占用极小；Decompress 会把整段音乐解压进内存，很占地方；Compressed In Memory 则每帧解压费 CPU。' },
  ],
  'perf-16': [
    { type: 'text', title: '移动端性能：手机卡不是玄学', body: 'PC 上 100 帧，手机上 30 帧还发热烫手——移动端和桌面端是两码事。这节把手机的性能规则讲透：发热、省电、画质、帧率、适配。\n\n一、发热降频：手机的性能天花板是「热」\n\n手机玩久了发热，CPU/GPU 会主动降频保护自己——帧率从 60 掉到 30，不是引擎问题，是芯片怕烫。\n\n· 发热源头：长时间满负荷（高分辨率 + 全特效 + 无帧率上限）\n· 对策：性能预算要留余量，别把 GPU 顶着 100% 跑，留 20~30% 余量给温度\n· 表现观察：散热好的平板、满电、空调房，帧率就是更稳——测性能要标注环境\n\n二、目标帧率：60 不是唯一答案\n\n· 30 帧是手机游戏主流目标：视觉够流畅，功耗只有 60 帧的一半左右\n· Application.targetFrameRate 设成你的目标，别让 GPU 白干活：\n\nApplication.targetFrameRate = 30;   // 锁 30 帧，省电降热\n\n· 高端机可开高帧模式（60/120），低端机保 30——做成设置项让玩家选\n· Quality Settings 里的 VSync 在手机上默认不强制，靠 targetFrameRate 控制\n\n三、省电策略：功耗就是寿命\n\n· 渲染分辨率 < 屏幕分辨率：真机 Screen.width 是物理分辨率，直接按它渲染很费。用一个可调的分辨率倍率：\n\nint targetW = Screen.width / 2;   // 半分辨率渲染，省一大截功耗\nScreen.SetResolution(targetW, targetW * Screen.height / Screen.width, true);\n\n· 后处理关/降档：辉光、景深、抗锯齿在手机上最贵，做成画质档位\n· 帧率上限 + 分辨率降档 + 阴影距离调小，三样是移动端省电铁三角\n\n四、分辨率与画质档位\n\n· 做画质档：低 / 中 / 高 三档，每档对应一套 Quality Settings（阴影、抗锯齿、贴图质量、后处理开关）\n· 自动检测：启动时根据设备（内存、CPU 核数、GPU 型号）给一个建议档位，玩家可改\n· 适配判断用 SystemInfo 而不是猜机型：\n\nint gpuMem = SystemInfo.graphicsMemorySize;\nif (gpuMem < 2048) SetQuality(QualityLevel.Low);   // 显存小于 2G 默认低画质\n\n五、低端机适配：先保住「能玩」\n\n· 低端机第一要务是稳定 30 帧，画质其次\n· 必要的降级手段：\n  - 关实时阴影，换烘焙贴图（perf-4 的 LOD/剔除照用）\n  - 粒子数量和大小降档\n  - 动态合批/静态合批确认开启，Draw Call 压低\n  - 关后处理或大幅降分辨率\n· 纹理内存：低端机内存小，贴图压缩格式和分辨率在导入设置里按低端机型考虑\n\n六、真机测试：编辑器不等于手机\n\n· 手机上跑 Profiler：Build 勾 Development Build + Autoconnect Profiler，真机连编辑器看真实数据\n· 注意：真机 vs 编辑器差异巨大（GPU 差几个量级），编辑器流畅 ≠ 真机流畅\n· 测发热要「连续玩 30 分钟以上」，看热不热、掉不掉帧——短期测试看不出降频\n\n一句话总结：留功耗余量防发热降频、按设备锁目标帧率、渲染分辨率可调、画质分档自动推荐、低端机先保 30 帧——手机端的优化，本质是「省着用」的艺术。' },
    { type: 'quiz', question: '真机玩半小时后帧率从 60 掉到 30，最可能的原因是什么？', options: ['游戏代码有内存泄漏', '长时间满负荷运行导致发热，芯片降频保护', '玩家手机内存不足'], answer: 1, tip: '降频是移动端经典现象：芯片发热到阈值自动降频，性能断崖式下跌。对策是留性能余量、锁目标帧率、降分辨率——别把 GPU 顶满跑。' },
  ],
  'perf-17': [
    { type: 'text', title: '帧率与流畅度：稳住比冲高更重要', body: '平均帧率高没用——游戏体验看的是「稳不稳」：突然掉一帧，玩家就察觉。这一节讲帧率从「数字」到「手感」的关键点：目标、稳定性、垂直同步、时间尺度、掉帧检测。\n\n一、帧率目标：先定预算\n\n· 60 帧 → 每帧预算 16.7 毫秒；30 帧 → 33.3 毫秒\n· 预算分配：逻辑（Update）多少、渲染多少、物理多少——心里有个账\n· 目标帧率要「敢锁」：GPU 有余力时让它跑满 60，跑不动就主动锁 30 而不是「尽力跑 45」——不稳定的 45 比稳定的 30 体验更差\n\nApplication.targetFrameRate = 60;\n\n二、稳定帧率：波动比低帧更伤体验\n\n· 平均 50 但忽高忽低，玩家觉得「卡」；稳定 30，玩家觉得「流畅」\n· 掉帧的原因大多是「突发峰值」：GC 清扫、大规模加载、瞬时大量特效。压峰值才是稳帧的关键：\n  - GC 分帧（perf-10 的 Incremental GC）\n  - 加载错峰（perf-14）\n  - 特效限流（perf-19）\n· 用「耗时上限」思维：保证每帧在预算内，比偶尔超预算更重要\n\n三、垂直同步（VSync）：撕裂 vs 卡顿\n\n· VSync 把渲染和显示器刷新同步，防止画面撕裂，但也会把帧率锁死在显示器刷新率\n· 帧率高于显示器刷新率时，VSync 开启是浪费（超过 60Hz 显示不出来）；低于刷新率时 VSync 可能引入微卡顿\n· 移动端一般用 targetFrameRate 控帧率，PC 端让玩家在设置里选 VSync 开/关\n\n四、时间尺度：Time 系统的三个坑\n\n· Time.deltaTime：帧间时间，物理/动画/脚本都靠它。时间尺度被改（暂停、子弹时间）时：\n  - Time.timeScale = 0 时 Update 照跑，但 deltaTime 归零——依赖它移动的代码会停\n  - 别用 Time.time 做计时（它受 timeScale 影响），想「真时间」用 Time.realtimeSinceStartup\n· 掉帧瞬间 deltaTime 变大，把「按固定速度移动」的代码写成：\n\ntransform.position += dir * speed * Time.deltaTime;   // 帧长就多走，帧短就少走，速度恒定\n\n五、掉帧检测：别等玩家骂了才知道\n\n写一个简单的帧耗时监测，异常时自动降档：\n\nprivate float[] frameTimes = new float[60];\nprivate int idx;\n\nvoid Update()\n{\n    frameTimes[idx] = Time.deltaTime;\n    idx = (idx + 1) % frameTimes.Length;\n\n    float avg = 0f;\n    foreach (float t in frameTimes) avg += t;\n    avg /= frameTimes.Length;\n\n    if (avg > 0.033f)   // 最近 1 秒平均帧耗时超 33ms → 连续掉帧\n    {\n        ReduceQuality();   // 自动降画质/锁更低的帧率，先把「能玩」保住\n    }\n}\n\n· 也可以接 Profiler 的 Frame 数据，或记录卡顿帧的栈（Profiler 的 Samples）事后分析\n\n六、实测方法\n\n· 测「最坏场景」：满屏敌人、全特效、长时间运行——别只测干净场景\n· 帧率曲线的波峰波谷都要看，别只看平均数\n· 记录帧率曲线（截图/录制），优化前后对比，用数据说话（perf-18 会讲完整流程）\n\n一句话总结：定预算、锁目标帧率、压峰值求稳定、VSync 按需开关、deltaTime 正确使用、做掉帧自动降档——帧率的功夫在「稳」不在「高」。' },
    { type: 'quiz', question: '游戏平均 50 帧但忽高忽低，和稳定 30 帧比，哪个体验更好？', options: ['平均 50 更好，数字高', '稳定 30 更好，帧率波动比低帧率更伤体验', '一样好'], answer: 1, tip: '玩家的「卡」感来自帧率突变：稳定 30 的每帧都流畅，忽高忽低的 50 帧里，峰值卡顿一下就穿帮了。稳帧先压峰值（GC、加载、特效）。' },
  ],
  'perf-18': [
    { type: 'text', title: '性能优化完整流程：定位、优化、复测', body: '前十几节教了无数技巧，这一节串成一条「生产线」：从定位到优化到复测，一套可重复的流程，再加优先级判断和优化清单。\n\n一、核心循环：定位 → 优化 → 复测\n\n不要「一把梭」地乱改。标准循环：\n\n1. 定位：Profiler 找到瓶颈在哪（哪个模块、哪个函数）\n2. 假设：猜一个最可能的优化手段\n3. 优化：只改这一处\n4. 复测：跑同一场景同一操作，对比数据\n5. 决策：有效就留，无效就回退换思路\n\n一次只改一个点——改五个点，复测时你根本不知道是谁的功劳（也是 perf-7 强调的纪律）。\n\n二、优化优先级：先压大头\n\n· 第一优先：占时最大的模块。Profiler 里 Physics 占 60% 就去动物理，别先抠脚本里省 1% 的地方\n· 性价比排序（一般情况）：\n  1. 合批 / Draw Call（perf-6、perf-11）——改一处立竿见影\n  2. GC 垃圾（perf-5、perf-10）——掉帧元凶，清掉最值\n  3. 物理开销（perf-12）——刚体数量控制\n  4. 资源加载（perf-14）——切场景/进战斗的卡顿\n  5. 渲染规格（perf-16 画质档）——最后的整体降档\n· 反向确认：优化后 Profiler 里那个模块的数字要「真的降下来」，别只看整体帧率（别的瓶颈会盖住）\n\n三、优化清单：照单检查\n\nCPU 脚本侧：\n· [ ] Update 里有每帧 Find / GetComponent / LINQ / Instantiate 吗\n· [ ] 每帧有字符串拼接 / 装箱吗\n· [ ] 集合每帧 new 吗，用了 NonAlloc API 吗\n· [ ] 事件有没有成对退订（泄漏）\n\n渲染侧：\n· [ ] Draw Call 压下来了吗（Frame Debugger 查）\n· [ ] 材质共享了吗，颜色用 MaterialPropertyBlock 吗\n· [ ] 贴图压缩、图集了吗\n· [ ] LOD / 剔除 / 阴影距离合理吗\n\n物理 / 动画：\n· [ ] 刚体数量可控吗，碰撞体简化了吗\n· [ ] 动画剔除（cullingMode）开了吗\n\n资源 / 加载：\n· [ ] 关键资源提前加载了吗\n· [ ] 场景卸载 + 资源释放了吗\n\n四、优化过程的纪律\n\n· 记录基线：优化前先跑一遍，记录帧耗时 / GC Alloc / Draw Call 三个数字\n· 一次一改：改完复测，对比基线\n· 留档：每个改动记下「改了什么、数字变化多少」，几轮下来你就有了一份自己的优化记录\n· 别过度优化：到目标帧率就收手，时间花在刀刃上\n\n五、收尾验收\n\n· 全流程走完，目标：\n  - 目标帧率达成且稳定（最坏场景也不掉档）\n  - GC Alloc 接近零\n  - Draw Call 在目标范围\n  - 发热 / 内存曲线平稳\n· 在「最坏场景」下验收，而不是空场景——空场景流畅是假象\n\n一句话总结：定位 → 优化 → 复测循环，先压大头再抠细节，优化前记基线、一次只改一处、用数据验证，清单走一遍，性能就「有账可查」了。' },
    { type: 'quiz', question: '性能优化时改了 5 处（合批、GC、物理、加载、画质），复测帧率上去了，但不知道是谁的功劳。正确的做法是？', options: ['继续保持，反正有效果', '回退重来，一次只改一处，改完复测对比基线', '再加两处优化'], answer: 1, tip: '「一次只改一个点」是优化的铁律：改了 5 处你无法归因，也无法判断哪处该留哪处该弃。回退到基线，逐项验证，才叫科学优化。' },
  ],
  'perf-19': [
    { type: 'text', title: '常见性能坑：六处一踩一个准', body: '很多卡顿不是「没优化」，是「踩了坑」。这一节把最常见的六个坑摆出来，每个都给「症状 + 病根 + 药方」，让公子以后一眼认出。\n\n一、坑一：字符串拼接（每帧拼 UI）\n\n· 症状：打开带实时数字的 UI（血条、计时、分数）帧率就掉\n· 病根：字符串不可变，每次 + 都生成新对象，每帧造一堆垃圾 → GC 卡顿\n· 药方：用 StringBuilder 复用，或缓存结果只在变化时更新（perf-10）\n\n二、坑二：每帧 Instantiate / Destroy\n\n· 症状：子弹多、敌人多时，过一会儿就周期性卡一下\n· 病根：Instantiate/Destroy 在堆上频繁创建销毁对象，垃圾堆积触发 GC 清扫\n· 药方：对象池（perf-5、perf-8）——预建、取用、回收，垃圾清零\n\n三、坑三：每帧 Find / GetComponent / Camera.main\n\n· 症状：物体一多 CPU 占用飙升，但逻辑看起来很简单\n· 病根：这些 API 每次都全场景/全组件搜索，每帧调用就是每帧搜索\n· 药方：缓存成字段（Awake 里取一次）；真需要搜索的用事件/引用传递代替\n\n四、坑四：粒子太多\n\n· 症状：放技能/爆炸瞬间掉帧，其他时候流畅\n· 病根：粒子数爆表 + Overdraw（透明粒子叠加，perf-6 提过），GPU 直接被刷爆\n· 药方：\n  - Max Particles 压到够用就行\n  - 用冒烟、半透明材质时注意叠加层数\n  - 远处的粒子降 Max Particles 或停发\n  - 用对象池管理粒子系统，别每技能 new 一套\n\n五、坑五：阴影太重\n\n· 症状：场景里阴影多时帧率明显下降，尤其移动端\n· 病根：实时阴影要额外渲染一次深度，阴影距离越大、光源越多开销越大\n· 药方：\n  - 阴影距离（Shadow Distance）调近\n  - 一个方向光就够，多的实时光改烘焙\n  - 关掉不必要物体的 Cast/Receive Shadow\n  - 移动端优先烘焙静态阴影\n\n六、坑六：透明物体过多\n\n· 症状：透明物体多的地方掉帧（水、玻璃、粒子）\n· 病根：透明物体不能合批，还要排序（从远到近），又禁了深度写入，Overdraw 也高\n· 药方：\n  - 能不用透明就不用（玻璃框改成贴图模拟）\n  - 透明物体数量控制，别叠太多层\n  - 半透明贴图的分辨率别过高\n\n七、查坑心法\n\n· 卡顿时打开 Profiler：GC Alloc 高 → 坑一二三；Rendering 高 → 坑四五六\n· 掉帧与「具体动作」强相关（放技能卡、开 UI 卡、远处有树卡），就从对应坑入手\n· 一帧卡 + 平时好 = 峰值型问题（GC/加载/特效），持续卡 = 持续型问题（渲染/物理）\n\n一句话总结：字符串拼接、每帧 Instantiate、每帧 Find、粒子过多、阴影、透明物体——六坑认得，Profile 一开对症下药，卡顿十之八九都能治。' },
    { type: 'quiz', question: '放技能的瞬间掉帧，其他时候流畅，最可能是哪个坑？', options: ['每帧 Find', '粒子过多 + 透明叠加导致的瞬时 GPU 压力', '字符串拼接'], answer: 1, tip: '技能释放 = 瞬时大量粒子 + 透明特效叠加，GPU 瞬时压力爆表。压 Max Particles、控制透明层数、对象池管理粒子系统即可。' },
  ],
  'perf-20': [
    { type: 'text', title: '动手练：给弹幕游戏做一次完整性能体检', body: '🎯 任务目标：给一个「弹幕射击小游戏」做一次完整的性能优化：先用 Profiler 定位瓶颈，再按优先级优化，最后复测对比——走完「定位 → 优化 → 复测」全流程，交一份「优化记录」。\n\n📦 前置要求：先学完本课 perf-9（Profiler 深入）、perf-10（内存管理）、perf-11（渲染实战）、perf-12（物理）、perf-13（UI）、perf-18（优化流程）等。\n\n📋 动手步骤：\n1. 搭一个小游戏场景（或用一个已有的简单项目）：玩家（Capsule + 移动脚本）+ 大量敌人（Cube 随机生成 30 个）+ 子弹发射（每 0.1 秒一颗，3 秒后销毁）+ 一个带血条/分数/倒计时的 UI。怎么乱怎么写，故意留坑：每帧 Find、每帧字符串拼接、每帧 Instantiate 子弹、材质每物体一份、无对象池。\n2. 记基线：跑同一段固定操作（比如 20 秒自动战斗），记录三个数字：\n   · 平均帧耗时（ms）和最低帧耗时\n   · Profiler 的 GC Alloc（平均每帧字节）\n   · Frame Debugger 里的 Draw Call 数\n   记到纸上（等下对比用）。\n3. Profiler 定位：看 CPU 模块分布，找到占时最大的模块；展开看是哪个函数；看 GC Alloc 哪帧最红。把「定位结论」写下来：比如「Scripts 占 60%，Update 里 BulletSpawner 每帧 Instantiate 是最大头」。\n4. 按优先级优化（一次一个，改完复测）：\n   优化一：子弹加对象池（perf-5/perf-8 的套路，预建 20 颗，取用/回收），复测 GC Alloc——应该接近归零。\n   优化二：缓存每帧 Find（玩家引用、血条 Text 引用都提到 Awake 缓存），复测脚本耗时。\n   优化三：UI 文本用 StringBuilder + 变化检测（perf-10），复测 GC Alloc 再降。\n   优化四：材质共享——所有 Cube 用同一个材质，颜色用 MaterialPropertyBlock（perf-6/perf-11），复测 Draw Call。\n   优化五：敌人在远处的降更新 / 感知冷却（perf-18 的清单过一遍）。\n5. 复测与记录：每优化一处，跑同样的 20 秒，记录三个数字，和上一步对比。最后写一张「优化记录」表：\n   · 基线：平均帧耗时 / 最低帧耗时 / GC Alloc / Draw Call\n   · +对象池：...\n   · +缓存：...\n   · +材质共享：...\n   · 最终：...\n\n✅ 验收清单：\n· 能 □ 基线数据完整记录（三个数字都记了）\n· 能 □ Profiler 定位结论写清楚了（哪个模块、哪个函数是瓶颈）\n· 能 □ 优化一处复测一次，每次数据有变化、能说清是哪处的功劳\n· 能 □ 最终 GC Alloc 明显下降（接近 0）\n· 能 □ 最终 Draw Call 明显下降\n· 能 □ 平均帧耗时下降，最低帧（最坏一帧）明显改善\n\n🕳 常见卡壳：\n· 优化后数据没变化 → 检查是不是改的地方根本不是瓶颈（回到步骤 3 重新定位），或改完忘了重新 Build/Play\n· 只降了平均没降最低帧 → 最低帧一般是峰值问题（GC/加载/特效），回到 Profiler 找最红那一帧\n· 内存没动过也想查 → 用 Memory Profiler 拍快照对比场景切换前后\n· 编辑器跑的和手机不一样 → 有条件的话真机连 Profiler 复测一遍\n\n一句话总结：摆一个「故意埋坑」的小游戏，按「定位 → 优化 → 复测」循环走一遍，用数据（帧耗时 / GC Alloc / Draw Call）说话——本课全部技巧，最后在这一节汇成一套能复用的体检流程。' },
    { type: 'quiz', question: '按流程优化后平均帧耗时没降，但最低帧（最坏的一帧）明显改善了，说明什么？', options: ['优化没效果', '峰值型问题（GC/加载/特效）被压住了，整体体验其实变稳了', '应该把优化全部回退'], answer: 1, tip: '最低帧反映的是「最坏的一帧」——GC 清扫、瞬时加载这类峰值。峰值被压住 = 卡顿感消失，即使平均没大变，体验也明显变好。' },
  ],
}
