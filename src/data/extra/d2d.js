export const lessons = [
  { id: 'd2d-5', title: '2D 碰撞体深入' },
  { id: 'd2d-6', title: '2D 动画' },
  { id: 'd2d-7', title: '2D 游戏手感' },
  { id: 'd2d-8', title: '动手练：2D 接水果小游戏' },
  { id: 'd2d-9', title: '2D 相机与视口' },
  { id: 'd2d-10', title: '2D 关卡搭建' },
  { id: 'd2d-11', title: '2D 敌人与 AI' },
  { id: 'd2d-12', title: '2D 收集与道具' },
  { id: 'd2d-13', title: '2D 音效与特效' },
  { id: 'd2d-14', title: '2D 射击' },
  { id: 'd2d-15', title: '2D 平台跳跃' },
  { id: 'd2d-16', title: '2D UI 与计分' },
  { id: 'd2d-17', title: '2D 游戏状态' },
  { id: 'd2d-18', title: '2D 性能优化' },
  { id: 'd2d-19', title: '2D 发布适配' },
  { id: 'd2d-20', title: '2D 综合动手练' },
]

export const cards = {
  'd2d-5': [
    { type: 'text', title: '碰撞体形状、偏移与复合碰撞体', body: 'd2d-2 学过四种 Collider2D，这一节把碰撞体的「细节功夫」做足——形状、偏移、复合、物理材质，全都是在实战中踩过的坑。\n\n四种形状怎么选：\n\n· Box Collider 2D —— 方块，最省性能，默认给精灵一个「刚好包住」的矩形\n· Circle Collider 2D —— 圆形，滚动、圆形小物件；和一个点、一条边的判定最便宜\n· Capsule Collider 2D —— 胶囊（竖椭圆），人形角色的身体（头是圆、身体是直），横版人物首选\n· Polygon Collider 2D —— 多边形，能贴精灵轮廓自动生成，适合形状不规则的道具（星星、钻石）；顶点越多越费，能用 Box/Circle 别用 Polygon\n\n偏移 Offset：\n碰撞体在精灵的什么位置，由 Offset 决定。想「碰撞体比画面偏上一点」（比如头部判定），只改 Offset，不挪精灵——这个需求太常见，记牢：改 Offset 不动 Sprite。\n\nSize 微调：\n· 想让判定范围比画面小一点（好躲子弹、不容易卡角），把 Box 的 Size 收窄\n· 想让判定范围比画面大一点（拾取范围），把 Size 放大\n\n复合碰撞体：一个刚体，多块形状。\n\n一个角色身上往往有多个碰撞区域：身体、脚底、手持的武器。正确做法不是挂多个 Rigidbody2D——刚体多了一个乱。标准姿势：\n\n· 父物体（角色）挂一个 Rigidbody2D + 一个 Collider2D（身体）\n· 子物体各自挂 Collider2D（武器、脚、头顶）——注意子物体不能挂 Rigidbody2D\n\n子碰撞体由父刚体统一管理，物理引擎把它们当成「一个刚体上的多块形状」来算碰撞。好处：\n· 只有父物体一个刚体，力、速度都在一处\n· 每块碰撞体独立形状，贴合身体各部位\n· 想加一块（比如尾巴），加个子物体挂碰撞器就行\n\n平台、长条地形也这样：一个父物体挂刚体，底下挂几个子碰撞体拼出形状，比一个大 Polygon 好维护。\n\n物理材质 2D（Physics Material 2D）：\n\n给碰撞体设定「表面手感」的资产：\n· Friction 摩擦 —— 摩擦越大，滑动越难，斜坡上站得稳\n· Bounciness 弹性 —— 弹力越大，撞上去弹得越欢\n\n做弹性挡板：新建一个 Physics Material 2D，Bounciness 调到 0.8，拖到碰撞体的 Material 槽——球撞上去就弹。注意 Bounciness 调高时，往往要把 Friction 调低，不然又弹又黏，动作很怪。\n\n坑位清单：\n· Polygon 顶点太多 → 每帧碰撞检测开销大，超过几十个顶点的物体，改用几个 Box 拼\n· 碰撞体「穿模」/对不上 → 检查 Sprite 的 Pixels Per Unit 和碰撞体的 Offset/Size，单位换算错了碰撞体就会对不上画面\n· 子物体误挂刚体 → 父子各一个刚体，物理引擎会当两个物体，互相打架\n· 忘了加碰撞体 → 精灵图片归图片，没碰撞体就是「幻影」，直接穿墙\n\n一句话总结：形状按需求选、Offset 调位置、复合碰撞体一个刚体多块形状、物理材质定摩擦弹性——碰撞体的手感，细节全在这几处。' },
    { type: 'component', title: '四种 Collider2D 形状', items: [
      { name: 'Box Collider 2D', desc: '方块，最省性能，默认矩形' },
      { name: 'Circle Collider 2D', desc: '圆形，滚动与点线判定最便宜' },
      { name: 'Capsule Collider 2D', desc: '胶囊，人形角色身体首选' },
      { name: 'Polygon Collider 2D', desc: '贴轮廓多边形，形状不规则才用，顶点多则费' },
    ] },
    { type: 'quiz', question: '一个角色身体（矩形）加一把横着的武器（小矩形），想做成一个整体碰撞体，正确的做法？', options: ['两个物体各挂一个 Rigidbody2D', '父物体挂一个 Rigidbody2D，两个子物体各挂 Collider2D（不挂刚体）', '把两个精灵合成一张图，再画一个大 Polygon'], answer: 1, tip: '复合碰撞体：一个刚体管多块形状。子物体只挂碰撞器、不挂刚体，物理引擎把子碰撞体当父刚体的一部分来算——力、速度统一，碰撞贴合。' },
  ],
  'd2d-6': [
    { type: 'text', title: '序列帧动画与 Animator 状态机', body: '2D 角色的动作怎么来？两种主流做法：逐帧动画（换 Sprite）和骨骼动画（2D 骨骼）。这一节讲透 Unity 里最基础的「逐帧 + 状态机」。\n\n逐帧动画的原理：\n角色「跑动」就是连续切换一组跑步姿势的图片——就像翻页动画。准备一组跑动帧（run_0、run_1、run_2……），按时间一张张换，就「跑」起来了。\n\n把一组帧变成动画剪辑：\n\n1. 准备好序列帧图（要么是一整张「图集 + 切片」，要么是散开的几张图）\n2. 全部选中，拖进场景/层级——Unity 会弹窗问「要不要创建动画剪辑」，选 Create Animation（自动生成 Animation Clip 和 Animator Controller）\n3. 用 Animation 窗口（Ctrl+6）微调：把 Sprite 属性关键帧的间隔改成 0.1 秒（默认 60fps 太快，10fps 就是 0.1 秒一帧）\n4. 播放看看节奏，间隔越小动作越细腻、也越吃资源\n\n窗口里其实每一帧就是一个 Sprite 关键帧。\n\nAnimator 2D 状态机：\n\n有了几个剪辑（Idle、Run、Jump），剩下的交给 Animator（anim-2 学过）：\n· Idle —— 待机，循环\n· Run —— 跑步，循环\n· Jump —— 跳跃，一次\n\n过渡和条件照旧办法配：\n\nAnimator anim = GetComponent<Animator>();\nanim.SetBool(\"IsRunning\", true);     // 速度大于 0 时设 true，切到 Run\nanim.SetTrigger(\"Jump\");             // 按跳触发跳跃动画\n\n2D 和 3D 的动画系统是同一套（Animator），差别只在内容：2D 动画的数据就是「换 Sprite」，3D 是「转骨骼」。\n\n节奏与坑：\n· 帧率：序列帧动画默认按 60fps 录，跑起来像快进——把关键帧间隔拉到 0.05~0.1 秒（10~20fps）才像 2D 游戏的节奏\n· 图集切片：一整张「精灵表」要先用 Sprite Editor 切成小片，再用切好的小 Sprite 做动画；切错了片，动画就串图\n· 循环动作：走路、待机这类循环动作，Clip 里勾上 Loop Time（Animation 窗口左下角或导入设置），不然播一遍就停\n· 播放速度：想「加速跑」，anim.speed = 2f 全局倍速；想慢放死亡，anim.speed = 0.5f\n· 别用 Update 手动换 Sprite：自己换图没有插值、没有过渡，还会和 Animator 打架——让 Animator 管，代码只改参数\n\n一句话总结：2D 动画 = 一组序列帧按时间换 Sprite，剪辑做好放进 Animator 状态机，参数驱动切换；帧率节奏、图集切片、Loop Time 三个坑记住就不翻车。' },
    { type: 'text', title: '进阶：2D 骨骼动画与选型', body: '序列帧的缺点是「一帧一张图」，文件多、改动作难。2D 骨骼动画（2D Animation 包）是进阶方案：\n\n· 把角色拆成头、躯干、手臂、腿等部件 Sprite，用骨骼把它们串起来\n· 动骨骼、部件跟着动，不用每帧换图\n· 改一个动作只改骨骼姿势，文件小、动作流畅，还能加 IK（上一节的 IK 知识在 2D 里同样能用）\n\nUnity 官方包：Package Manager 装 2D Animation + 2D IK，就能在编辑器里搭 2D 骨架。\n\n搭建 2D 骨架的流程：装好 2D Animation 包后，选中角色的骨骼 Sprite，用 Skinning Editor 把每个部件绑定到骨骼上（自动生成骨骼层级），然后像 3D 一样摆姿势、做动画。2D IK 包还能给手、脚加 IK 目标——角色去够高处的东西，动作自然不僵硬。\n\n坑位：2D 骨骼对美术资源有要求——部件必须拆成独立的 Sprite（头、身体、手臂分开），一张整体图没法绑骨；另外骨骼多了以后 Skinning 权重调起来也费功夫，简单角色用序列帧更划算。\n\n序列帧 vs 2D 骨骼的选择：\n· 序列帧：风格化强（帧动画特有的「一拍一帧」感觉）、美术流程传统，小项目/像素风首选\n· 2D 骨骼：文件小、动作自然、方便复用，需要角色动作多变时选它\n\n一句话总结：帧数不多用序列帧，动作复杂上 2D 骨骼——两条路 Unity 都支持，按美术资源形态选。' },
    { type: 'quiz', question: '把 8 张跑步序列帧做成动画后，播放起来像快进一样，最快的解决办法？', options: ['把图片全部重新导出', '把动画关键帧间隔从 0.016 秒拉大到 0.05~0.1 秒（降低到 10~20fps）', '给角色加一个 Rigidbody2D'], answer: 1, tip: '序列帧默认按 60fps 记录关键帧，播起来自然飞快。把关键帧间隔拉大到 0.05~0.1 秒（10~20fps），才是 2D 动画的正常节奏。' },
  ],
  'd2d-7': [
    { type: 'text', title: '手感调校：重力、跳跃与宽容度', body: '手感（Game Feel）是 2D 平台游戏的生命。同样的跳跃，参数调好是「畅快」，调坏是「踩棉花」。这一节把「手感」拆成可以调的参数。\n\n手感 = 数值的组合，主要拧这几个旋钮：\n\n一、重力\nGravity Scale（重力倍数）越大，下落越快、跳跃越低。经典调校：\n· 常规 1\n· 平台跳跃类常见 2~3（跳跃干脆，不飘）\n· 要「月球漫步」的关卡，调到 0.5 以下\n\n二、跳起 vs 下落，用两套重力\n真实手感的关键：跳起和下落的重力分开。下落比跳起重，人物「上得慢、落得快」，节奏最舒服：\n\nif (rb.velocity.y < 0)        // 下落中\n    rb.gravityScale = 3f;     // 重力加大，快速砸地\nelse\n    rb.gravityScale = 2f;     // 上升中，重力稍小\n\n三、跳跃初速度\n跳跃高度 ≈ 初速度的平方 ÷ 2 ÷ 重力。想跳得高：要么初速度大、要么重力小。调跳跃别只盯着初速度，记得和重力一起试。\n\n四、按住跳得更高（可变跳跃）\n松开跳跃键就砍掉向上的速度，松手早 = 跳得低：\n\nif (Input.GetButtonUp(\"Jump\") && rb.velocity.y > 0)\n{\n    rb.velocity = new Vector2(rb.velocity.x, rb.velocity.y * 0.5f);  // 松开瞬间减半上升速度\n}\n\n这一条让「轻点跳个小台阶、长按跳大台阶」成为可能，手感立刻高级。\n\n五、土狼时间（Coyote Time）\n从平台边缘走出的一小段时间里（约 0.08~0.12 秒），仍然算「着地」，还能跳——救回无数「明明想跳却没跳出来」的冤枉操作：\n\nif (isGrounded) lastGroundedTime = Time.time;\nbool canJump = isGrounded || (Time.time - lastGroundedTime) < 0.1f;\n\n六、跳跃缓冲（Jump Buffering）\n玩家在落地前 0.1 秒就按了跳，让它在落地瞬间自动补跳。玩家手快不罚，手感和口碑都上来：\n\nif (Input.GetButtonDown(\"Jump\")) jumpBufferTime = Time.time;\nbool wantJump = (Time.time - jumpBufferTime) < 0.1f;\nif (wantJump && canJump) DoJump();\n\n七、二段跳\n跳跃次数计数，落地重置（空中跳两次封顶）：\n\npublic int maxJumps = 2;\nprivate int jumpsLeft;\n\nvoid Update()\n{\n    if (isGrounded) jumpsLeft = maxJumps;   // 着地重置\n    if (Input.GetButtonDown(\"Jump\") && jumpsLeft > 0)\n    {\n        rb.velocity = new Vector2(rb.velocity.x, jumpForce);\n        jumpsLeft--;\n    }\n}\n\n手感优化清单（照着过一遍）：\n· 下落重力 > 上升重力（变重力）——落得快才爽\n· 放开跳跃键减上升速度（可变跳跃）\n· 土狼时间 + 跳跃缓冲各 0.1 秒 —— 平台游戏必备的「宽容度」\n· 水平速度干脆：直接设 velocity，别靠 AddForce 推（d2d-4 讲过，推着走发飘）\n· 转身/急停时给一点点缓冲，别瞬间逆向（手感更跟手）\n· 落地顿帧（Hit Stop）：砸地瞬间画面停 2~3 帧，打击感翻倍\n· 镜头跟随：给相机加 SmoothDamp 延迟，跟着角色滑一点，不要死锁\n\n一句话总结：手感不是玄学，就是重力、初速度、缓冲宽容度、二段跳这些参数的组合——变重力 + 土狼时间 + 跳跃缓冲三件套先上，2D 手感立住一半。' },
    { type: 'quiz', question: '玩家在平台边缘没站稳时，落地前一瞬想跳却总差一步跳不出来，玩家抱怨「按了跳没反应」。下面哪个机制专门解决这个问题？', options: ['重力调大', '土狼时间（Coyote Time）', '把跳跃初速度翻倍'], answer: 1, tip: '土狼时间让角色离开平台边缘后的一小段（约 0.1 秒）仍然算着地、可跳，救回「差一点没跳出来」的冤枉操作；跳跃缓冲则解决「提前按跳」。两个一起上，平台游戏手感才不坑玩家。' },
  ],
  'd2d-8': [
    { type: 'text', title: '动手练：2D 接水果小游戏', body: '🎯 任务目标：用 Sprite、Rigidbody2D 物理和输入，做一个小游戏——水果从天上掉，玩家用篮子接，接到得分、漏掉扣命，命没了弹游戏结束画面。\n\n📦 前置要求：先学完本课 d2d-1（精灵 Sprite）、d2d-2（2D 物理）、d2d-4（2D 角色控制）；把 d2d-5（2D 碰撞体深入）也学了更稳。\n\n📋 动手步骤：\n1. 建 2D 场景。用 2D 模板新建项目（或 File → New Scene 选 2D），确认 Camera 的 Projection 是 Orthographic（正交投影，2D 游戏默认）。\n2. 做水果预制体。新建一个 Sprite（GameObject → 2D Object → Sprite），用系统默认圆形或自己导入一张圆图。给它加 Rigidbody2D（Body Type 保持 Dynamic，Gravity Scale 设 1——受重力下落）和 Circle Collider 2D，Collider 勾上 Is Trigger（用触发器判断接住/漏掉）。调个显眼的颜色，拖进 Assets 做成预制体 Fruit，从场景里删掉。\n3. 做玩家篮子。新建一个 Sprite 拉扁一点（Scale 比如 (3,0.8,1)）当篮子，加 Rigidbody2D 并把 Body Type 改成 Kinematic（不受重力，跟玩家走）+ Box Collider 2D，Tag 设成 Basket。写 PlayerBasket.cs：\n    using UnityEngine;\n    public class PlayerBasket : MonoBehaviour\n    {\n        public float speed = 8f;\n        void Update()\n        {\n            float h = Input.GetAxisRaw(\"Horizontal\");\n            transform.Translate(Vector2.right * h * speed * Time.deltaTime);\n\n            if (Input.GetMouseButton(0))   // 鼠标（可选）：篮子平滑追鼠标 x\n            {\n                Vector3 mouse = Camera.main.ScreenToWorldPoint(Input.mousePosition);\n                mouse.z = 0f;\n                mouse.y = transform.position.y;\n                transform.position = Vector3.MoveTowards(transform.position, mouse, speed * 4f * Time.deltaTime);\n            }\n\n            Vector3 pos = transform.position;\n            pos.x = Mathf.Clamp(pos.x, -8f, 8f);   // 别出屏幕\n            transform.position = pos;\n        }\n    }\n4. 建「漏接底线」。新建一个空物体 KillZone，在场景底部（y ≈ -5）放一个很扁的 Box Collider 2D（Scale 拉宽盖住屏幕），勾 Is Trigger，Tag 设成 KillZone。水果掉到这就算漏接。\n5. 写水果脚本 Fruit.cs，挂到 Fruit 预制体上：\n    using UnityEngine;\n    public class Fruit : MonoBehaviour\n    {\n        void OnTriggerEnter2D(Collider2D other)\n        {\n            if (other.CompareTag(\"Basket\"))\n            {\n                GameManager.instance.AddScore(1);\n                Destroy(gameObject);\n            }\n            else if (other.CompareTag(\"KillZone\"))\n            {\n                GameManager.instance.LoseLife();\n                Destroy(gameObject);\n            }\n        }\n    }\n6. 写 GameManager.cs（计分、扣命、定时生成水果、游戏结束），挂到一个空物体 GameManager 上：\n    using UnityEngine;\n    using UnityEngine.UI;\n    public class GameManager : MonoBehaviour\n    {\n        public static GameManager instance;\n        public GameObject fruitPrefab;\n        public Text scoreText;\n        public Text lifeText;\n        public GameObject gameOverPanel;\n        public int score;\n        public int lives = 3;\n        public float spawnInterval = 1.2f;\n        private float spawnTimer;\n\n        void Awake() { instance = this; }\n\n        void Update()\n        {\n            if (lives <= 0) return;                 // 游戏结束，停止生成\n            spawnTimer += Time.deltaTime;\n            if (spawnTimer >= spawnInterval)\n            {\n                spawnTimer = 0f;\n                Instantiate(fruitPrefab, new Vector3(Random.Range(-7f, 7f), 6f, 0f), Quaternion.identity);\n            }\n        }\n\n        public void AddScore(int amount)\n        {\n            score += amount;\n            scoreText.text = \"得分：\" + score;\n        }\n\n        public void LoseLife()\n        {\n            lives--;\n            lifeText.text = \"命数：\" + lives;\n            if (lives <= 0)\n            {\n                gameOverPanel.SetActive(true);\n                Time.timeScale = 0f;                // 暂停游戏\n            }\n        }\n\n        public void Restart()\n        {\n            Time.timeScale = 1f;\n            score = 0; lives = 3;\n            scoreText.text = \"得分：0\";\n            lifeText.text = \"命数：3\";\n            gameOverPanel.SetActive(false);\n        }\n    }\n7. 搭 UI。Hierarchy 右键 → UI → Canvas，给 Canvas 加 Canvas Scaler（Scale With Screen Size 自适应）。在 Canvas 下建两个 Text（得分、命数）和一个 Panel 当结束面板（里面放一个「游戏结束」Text 和一个 Button）。把 GameManager 的 scoreText / lifeText / gameOverPanel 拖进对应槽位；Button 的 OnClick 绑定 GameManager.Restart（把 GameManager 拖进 OnClick 列表选 Restart）。\n8. 把 Fruit 预制体拖进 GameManager 的 fruitPrefab，点 Play：水果从顶部随机位置落下并加速，移动篮子接住得分涨，漏到底线命数减，命数归零弹结束面板、点按钮重玩。\n\n✅ 验收清单：\n· 能 □ 水果从屏幕顶部不断生成，受重力下落并加速\n· 能 □ 篮子跟随键盘/鼠标左右移动，且不出屏幕\n· 能 □ 水果碰到篮子得分 +1 并消失\n· 能 □ 水果漏到底线命数 -1 并消失\n· 能 □ 命数归零弹出游戏结束面板，游戏暂停\n· 能 □ 点「重玩」能重置得分命数、重新开始\n\n🕳 常见卡壳：\n· 水果穿过篮子不触发 → 三件套逐一检查：Fruit 的 Collider 有没有勾 Is Trigger、Basket 有没有 Collider2D、两边至少一边有 Rigidbody2D；标签名（Basket/KillZone）要和代码里完全一致。\n· 水果不往下掉 → Rigidbody2D 的 Body Type 必须 Dynamic、Gravity Scale 大于 0；设成 Kinematic 或 Static 就不受重力。\n· 结束面板弹出来游戏还在动 → Time.timeScale = 0 没生效。检查是不是别处又把 timescale 改回 1，或代码里用了不受 timescale 影响的逻辑。\n· 篮子跟鼠标「瞬移」很飘 → 用 MoveTowards 平滑追，别直接 transform.position = mouse。' },
    { type: 'quiz', question: '水果碰到篮子要得分、掉到底线要扣命，判断「碰到的是谁」最常用的做法？', options: ['每帧比较水果和篮子的 y 坐标', '给篮子和底线各打一个 Tag，用 OnTriggerEnter2D 比较碰撞物体的 Tag', '每帧用 Find 找篮子算距离'], answer: 1, tip: '物理碰撞回调 + Tag 判断是 2D 小游戏的标准做法：OnTriggerEnter2D 里拿 other.CompareTag 一比对就分清是篮子（得分）还是底线（扣命）；比坐标抗碰撞形状变化，Find 则每帧都贵。' },
  ],
  'd2d-9': [
    { type: 'text', title: '正交相机、跟随与视口', body: '2D 游戏里相机用「正交投影」（Orthographic）——没有近大远小，世界坐标和屏幕一一对应。这一课把相机怎么跟角色、怎么限制边界、什么是视口讲清楚。\n\n一、正交相机的基本设置\n\n选中 Main Camera，Projection 选 Orthographic。关键参数：\n· Size：视口半高（单位是「Unity 世界单位」）。Size = 5 表示屏幕中心到上下边缘是 5 个单位\n· 宽高比决定宽度：宽 = Size × 2 × (屏幕宽/高)。横屏 16:9 比竖屏 9:16 看到的东西宽得多\n· Background：背景色，或挂 Skybox 渲染天空（2D 一般纯色/图）\n\n调 Size 就是在「缩放画面」——Size 小看到的世界少而大，Size 大看到的多而小。\n\n二、相机跟随角色：平滑跟随\n\n最简单的「钉死」跟随：\n\nvoid LateUpdate()\n{\n    transform.position = new Vector3(target.position.x, target.position.y, -10f);\n}\n\nz 固定 -10（相机在角色后 10 个单位）。用 LateUpdate（生命周期知识，prog-19 讲过）——角色在 Update 先动，相机后追，不抖。\n\n要「平滑感」，用 Vector3.Lerp：\n\npublic Transform target;\npublic float smoothTime = 0.15f;\n\nvoid LateUpdate()\n{\n    Vector3 targetPos = new Vector3(target.position.x, target.position.y, -10f);\n    transform.position = Vector3.Lerp(transform.position, targetPos, smoothTime * Time.deltaTime * 10f);\n}\n\nLerp 让相机「追」而不是「瞬移」，镜头有呼吸感，手感更舒服。\n\n三、限制相机边界：别让镜头出地图\n\n地图有边，相机不能跟着角色跑到地图外的虚空。用 Clamp 把相机位置卡在地图范围内：\n\nfloat camHalfWidth = cam.orthographicSize * cam.aspect;   // 半宽\nfloat camHalfHeight = cam.orthographicSize;               // 半高\n\nfloat x = Mathf.Clamp(target.position.x, mapLeft + camHalfWidth, mapRight - camHalfWidth);\nfloat y = Mathf.Clamp(target.position.y, mapBottom + camHalfHeight, mapTop - camHalfHeight);\n\ntransform.position = new Vector3(x, y, -10f);\n\n原理：相机的边缘不能越过地图边缘——位置范围 = 地图界 ± 半视口。Size 变了、分辨率变了，半宽半高自动算，别写死。\n\n四、视口 Viewport：屏幕相对坐标\n\n视口坐标把屏幕归一化成 0~1：(0,0) 左下角、(1,1) 右上角。用处：\n· 判断物体是否在屏幕内：\n\nVector3 viewPos = cam.WorldToViewportPoint(transform.position);\nif (viewPos.x < 0 || viewPos.x > 1 || viewPos.y < 0 || viewPos.y > 1)\n{\n    // 物体出了屏幕范围\n}\n\n· 屏幕坐标换算世界坐标：ScreenToWorldPoint（d2d-8 接水果用过），UI 定位常配 ViewportToWorldPoint\n\n五、常见坑\n\n· Size 调了没变化 → 相机 Projection 忘了切 Orthographic，还是 Perspective\n· 相机跟随「瞬移抖动」 → 在 Update 里跟随，角色还没动相机先动了。挪去 LateUpdate\n· 镜头拉到地图外 → 忘了 Clamp，或边界算错半视口。用上面公式，别写死数字\n· 相机 z 写成 0 → 相机裁剪面把物体裁没/渲染异常。z 留 -10 左右\n\n一句话总结：正交相机用 Size 定「看多大范围」，跟随角色用 LateUpdate + Lerp 平滑追，用 Clamp 把镜头卡在地图界内；视口坐标是 0~1 的屏幕比例，判断出屏、坐标换算都靠它。' },
    { type: 'component', title: '2D 相机关键概念', items: [
      { name: 'Orthographic 正交', desc: '2D 相机标配，无近大远小' },
      { name: 'Size 半高', desc: '视口高度的一半，调它等于缩放画面' },
      { name: 'LateUpdate 跟随', desc: '角色先动、相机后追，不抖' },
      { name: 'Lerp 平滑', desc: '相机「追」而不「瞬移」' },
      { name: 'Clamp 边界', desc: '地图界 ± 半视口，镜头不出图' },
      { name: 'Viewport 视口', desc: '0~1 的屏幕比例坐标' },
    ] },
    { type: 'quiz', question: '相机跟随角色后，镜头把角色跑到地图边缘外、看到地图外的一片虚空。怎么修？', options: ['把相机 Size 调大一点', '给相机位置加 Clamp，限制在地图界 ± 半视口内', '让角色别跑那么快'], answer: 1, tip: '镜头出地图是因为相机位置没限制。用 Clamp 把相机位置卡在「地图界 ± 半视口」内，角色到边缘镜头就停下；调 Size 只能改视野大小，解决不了越界。' },
  ],
  'd2d-10': [
    { type: 'text', title: '瓦片地图进阶与关卡组织', body: 'd2d-3 学过瓦片地图基础，这一课进阶：多层地图、碰撞瓦片、关卡怎么组织。搭一个像样的 2D 关卡，靠的是「图层思维」。\n\n一、瓦片地图的多层结构\n\n一个关卡 = 多层 Tilemap 叠起来。以平台跳跃为例，从后往前叠：\n· 背景层：远景、天空，装饰不参与玩法\n· 地形层：主角站的平台、地面，有碰撞\n· 前景层：树木、前景装饰，挡在角色前面（可后补）\n\n层级面板里：右键 → 2D Object → Tilemap 建多个 Tilemap，用 Order in Layer 控制前后（数值小在里层、大在外层），或把 Tilemap 挂成父子（父空物体，子多个 Tilemap）统一管理。给每一层命名：Background、Ground、Foreground——名字乱，三个月后你自己都找不到层。\n\n二、碰撞瓦片：让地形「踩得住」\n\nTilemap Collider 2D + Composite Collider 2D 是平台地形标配：\n· 给地形 Tilemap 挂 Tilemap Collider 2D：每块瓦片自动生成碰撞\n· 再挂 Composite Collider 2D 并把 Tilemap Collider 的 Used By Composite 勾上：多块瓦片的碰撞合并成一个整体，边缘不硌手、性能也好\n\n注意 Rigidbody：Composite 需要一个 Rigidbody2D（Body Type 选 Static）。勾上后踩平台不抖、不留缝。\n\n三、瓦片设置：碰撞瓦片怎么选\n\n做「能踩的砖」和「穿过的背景」：\n· 地形层用「带碰撞的瓦片」：挂 Tilemap Collider 的层\n· 背景层用普通 Sprite 或「无碰撞 Tilemap」：别给背景挂碰撞，白耗性能\n\n想某些瓦片能踩、某些不能（悬崖装饰）：建两个 Tilemap——碰撞层（Ground）和无碰撞层（Backdrop），别在一层里混。\n\n四、关卡组织：Prefab 化 + 对象分组\n\n大关卡拆成小模块，每个模块做成 Prefab（道具组、敌人组、装饰组），拼关卡 = 拖预制体摆位置：\n· 场景根节点分层：Background / Level / Enemies / Pickups / UI，每类一个空父物体\n· 关卡模块（一段地形、一组障碍）存成 Prefab，复用、拖到不同位置\n· 一次性关卡进度别挂 Prefab 上，写场景里的管理器\n\n好处：修改一处（改了砖块 Prefab）全关卡生效；查错看层级一目了然。\n\n五、绘制瓦片的效率习惯\n\n· 先规划再画：画之前心里先定「这层干嘛」「这层有没有碰撞」，别画完再返工\n· 用 Palette 管理瓦片：Window → 2D → Tile Palette 建瓦片盘，往地上一刷就是一个模块\n· 瓦片要「对齐网格」：刷之前把 Grid 的 Cell Size 和瓦片尺寸对上（一般是 1 或 0.5），否则缝对不上、角色卡缝\n\n六、常见坑\n\n· 角色掉进两块瓦片的缝里 → 碰撞瓦片没合体：勾 Used By Composite 让边缘无缝\n· 背景瓦片也碰撞 → 角色被看不见的墙挡住：背景层别挂碰撞，或单独无碰撞层\n· 层叠顺序乱 → 树跑到角色前面挡视线：检查 Order in Layer，前景层最大\n· 刷瓦片对不齐 → 瓦片导入 Pixels Per Unit 和 Grid 尺寸不匹配：对好单位再刷\n\n一句话总结：关卡 = 多层瓦片叠出来——背景无碰撞、地形合并碰撞、前景挡视线；碰撞用 Tilemap Collider + Composite 合并，关卡拆模块做 Prefab、按层分父物体，工程再大也清爽。' },
    { type: 'component', title: '瓦片关卡关键概念', items: [
      { name: '多层 Tilemap', desc: '背景 / 地形 / 前景按层叠，Order in Layer 控前后' },
      { name: 'Tilemap Collider 2D', desc: '每块瓦片自动生成碰撞' },
      { name: 'Composite Collider 2D', desc: '合并多块瓦片为一个整体，边缘无缝' },
      { name: 'Used By Composite', desc: 'Tilemap Collider 的合并开关' },
      { name: 'Prefab 模块', desc: '关卡拆模块存预制体，拼关卡拖就行' },
      { name: 'Tile Palette', desc: '瓦片盘，刷瓦片利器' },
    ] },
    { type: 'quiz', question: '做平台关卡时角色老卡进两块砖的缝隙里，边缘手感很硌。最对症的修法？', options: ['把砖的碰撞体调大一点', '给地形 Tilemap 挂 Composite Collider 2D，勾 Used By Composite，让多块瓦片合并成一个整体', '给角色加一个更大的碰撞体'], answer: 1, tip: '缝隙是「每块瓦片独立碰撞体」造成的。Tilemap Collider + Composite Collider（Used By Composite）会把整片地形合成一个连续碰撞体，边缘无缝、手感顺滑——这是平台地形标准做法。' },
  ],
  'd2d-11': [
    { type: 'text', title: '敌人巡逻、追击与简单行为', body: '敌人的大脑，说穿了就是三句话：发现玩家了吗？没有→巡逻；有→追；追到了→攻击。这一课把最简单的敌人 AI 拆开，从巡逻到追击到攻击，一行行写出来。\n\n一、敌人状态机：用枚举管行为\n\npublic enum EnemyState { Patrol, Chase, Attack }\n\npublic class EnemyAI : MonoBehaviour\n{\n    public EnemyState state = EnemyState.Patrol;\n}\n\n枚举（prog-8 学过）让敌人行为清清楚楚，不像一堆布尔变量绕来绕去。\n\n二、巡逻：两点之间来回走\n\n给敌人两个巡逻点，来回走：\n\npublic Transform pointA, pointB;\nprivate Transform currentTarget;\n\nvoid Patrol()\n{\n    if (currentTarget == null) currentTarget = pointA;\n\n    transform.position = Vector2.MoveTowards(transform.position, currentTarget.position, speed * Time.deltaTime);\n\n    if (Vector2.Distance(transform.position, currentTarget.position) < 0.1f)\n        currentTarget = currentTarget == pointA ? pointB : pointA;   // 走到了换目标\n}\n\n· Vector2.MoveTowards 匀速走向目标\n· 走到就切换目标点 → 来回巡逻\n· 记得翻 Sprite 朝向：往左走就 Flip X\n\n三、追击：发现玩家就追\n\n在 Update 里测距离：玩家进入「警戒范围」→ 切追击：\n\npublic float chaseRange = 5f;\npublic Transform player;\n\nvoid Update()\n{\n    float dist = Vector2.Distance(transform.position, player.position);\n\n    if (state == EnemyState.Patrol && dist < chaseRange)\n        state = EnemyState.Chase;\n    else if (state == EnemyState.Chase && dist > chaseRange * 1.2f)   // 超出范围松口\n        state = EnemyState.Patrol;\n}\n\nChase 状态里用 MoveTowards 朝玩家走（或 Rigidbody2D 加力），跑出警戒范围 1.2 倍再回巡逻——留个「跟丢」的缓冲，不然敌人会在边界疯狂抖。\n\n四、攻击：追到了就咬\n\n再测一个「攻击距离」，追到就停手攻击：\n\npublic float attackRange = 1.2f;\n\nif (dist < attackRange)\n{\n    state = EnemyState.Attack;\n    Attack();   // 挥一刀、喷一口，用触发器或射线判命中\n}\nelse\n{\n    state = EnemyState.Chase;   // 拉开又继续追\n}\n\nAttack 里做实际伤害：往攻击范围打一个触发器（武器区域）或发射一个弹体（见 d2d-14 射击课），命中玩家扣血。\n\n五、用协程做攻击间隔（prog-3/11）\n\n敌人不能无脑每秒咬十几口，用协程限频：\n\nprivate bool canAttack = true;\n\nvoid Attack()\n{\n    if (!canAttack) return;\n    canAttack = false;\n    StartCoroutine(AttackCooldown());\n}\n\nIEnumerator AttackCooldown()\n{\n    yield return new WaitForSeconds(1f);\n    canAttack = true;\n}\n\n六、常见坑\n\n· 敌人疯狂左右抖 → 追击/巡逻边界用「硬切换」加一点点死区（chaseRange × 1.2）或倒计时\n· 敌人在 Update 里每帧 Vector2.Distance 算两次以上 → 算一次存下来复用，几百个敌人也别每帧狂算\n· 只有巡逻没有追击 → state 切换条件写错，或 player 引用没赋值（空引用就是最常见的「不动」原因）\n· 攻击太快/太慢 → 攻击间隔用协程 + 冷却标志，别在 Update 里裸调\n\n一句话总结：敌人 = 状态机（巡逻/追击/攻击）+ 距离判断切换 + 协程控攻速；MoveTowards 走路、警戒范围进追击、攻击范围开打，三个范围一个状态，最朴素的 AI 就活了。' },
    { type: 'component', title: '敌人 AI 关键概念', items: [
      { name: '状态机', desc: '枚举管 Patrol / Chase / Attack' },
      { name: 'MoveTowards', desc: '匀速走向目标点' },
      { name: '警戒范围', desc: '进范围切追击，出范围×1.2 松口' },
      { name: '攻击范围', desc: '追到就停手开打' },
      { name: '协程攻速', desc: 'WaitForSeconds 控攻击间隔' },
    ] },
    { type: 'quiz', question: '敌人进入警戒范围开始追，追到攻击距离又立刻开始打，玩家一拉开立刻又追，敌人整体很「神经质」。想让它反应更自然，优先改哪？', options: ['把警戒范围加大', '攻击和追击用两个独立距离，并给追击加「超出范围×1.2 才放弃」的缓冲', '把敌人在 Update 里的逻辑全删了重写'], answer: 1, tip: '敌人「神经质」往往是边界切换太硬、距离设置单薄。追击距离、攻击距离分开设置，放弃追击用稍大的缓冲范围，敌人才有「跟丢」的合理行为，不会在边界疯狂抖动。' },
  ],
  'd2d-12': [
    { type: 'text', title: '触发器收集与道具效果', body: '金币、钥匙、血包、加速——收集与道具是 2D 游戏最甜的一环：玩家捡到东西，屏幕刷一下，声音响一声，就满足了。这一课讲触发器收集、道具效果、以及「让收集有反馈」。\n\n一、触发器收集：碰到就算捡到\n\n道具挂触发器，玩家碰到就触发 OnTriggerEnter2D（至少一边有 Rigidbody2D、Collider 勾 Is Trigger）：\n\npublic class Coin : MonoBehaviour\n{\n    public int value = 1;\n\n    void OnTriggerEnter2D(Collider2D other)\n    {\n        if (other.CompareTag("Player"))\n        {\n            GameManager.instance.AddCoin(value);   // 通知计分\n            Destroy(gameObject);                    // 消失\n        }\n    }\n}\n\n要点：触发器收集不阻挡移动——玩家从金币上「穿过去」，触发的一瞬间执行逻辑。Tag 判断是不是玩家，比 GetComponent<Player> 快且更不易错。\n\n二、收集系统的解耦：GameManager 收口\n\n金币、钥匙、血包各干各的，但「攒了多少」需要一个总账。标准做法是单例 GameManager（prog-4 学过）收口：\n\npublic class GameManager : MonoBehaviour\n{\n    public static GameManager instance;\n    public int coinCount;\n    public int keyCount;\n\n    void Awake() { instance = this; }\n\n    public void AddCoin(int amount)\n    {\n        coinCount += amount;\n        UIManager.instance.UpdateCoin(coinCount);   // 通知 UI 刷新\n    }\n}\n\n道具不直接碰 UI——它只告诉 GameManager，GameManager 再统一通知 UI。改动料只动 GameManager，UI 和道具互不知情。\n\n三、道具效果：分类型处理\n\n道具分几类，用枚举区分（prog-8）或在子类里 override：\n\npublic enum ItemType { Coin, Key, Heal, SpeedUp }\n\npublic class Item : MonoBehaviour\n{\n    public ItemType type;\n    public int value = 1;\n    public float duration = 3f;   // 时效型道具持续时间\n\n    void OnTriggerEnter2D(Collider2D other)\n    {\n        if (!other.CompareTag("Player")) return;\n\n        switch (type)\n        {\n            case ItemType.Coin: GameManager.instance.AddCoin(value); break;\n            case ItemType.Key:  GameManager.instance.AddKey(value);  break;\n            case ItemType.Heal: Player.instance.Heal(value);         break;\n            case ItemType.SpeedUp: StartCoroutine(SpeedUp());        break;\n        }\n        Destroy(gameObject);\n    }\n\n    IEnumerator SpeedUp()\n    {\n        Player.instance.speed *= 2f;\n        yield return new WaitForSeconds(duration);\n        Player.instance.speed /= 2f;\n    }\n}\n\n· 即时型（金币/钥匙）：立刻生效、立刻消失\n· 状态型（加速）：先变强，一段时间后自动还原——协程管持续时间（prog-3）\n· 类型用枚举，switch 分派，比一串 if 清爽\n\n四、让收集有反馈：三件套\n\n收集的「爽感」= 视觉 + 音效 + 数值：\n· 消失动画：别直接 Destroy，先播个被吸走的缩放/旋转动画再销毁（协程延后销毁）\n· 音效：PlayOneShot 播一个清脆的「叮」（d2d-13 音效课详讲）\n· 数值跳动：UI 上金币数 +1 的动画，或屏幕角落飘一个「+10」\n\n哪怕只是让金币转个身（transform.Rotate），收集都「活」了。\n\n五、常见坑\n\n· 触发器不触发 → 三件套查一遍：Collider 勾 Is Trigger 没、道具和玩家都有 Collider2D 没、至少一边 Rigidbody2D 没\n· 金币穿过玩家没反应 → Tag 没对上（"Player" 大小写必须和 Inspector 里一致）或 Tag 压根没打\n· 道具只有第一次能捡 → Destroy 删了但没删对，或逻辑在别的物体上。检查是不是在 OnTriggerEnter2D 里把玩家删了\n· 加速道具失效后不还原 → 协程没跑到：物体被销毁协程停、duration 设成 0。检查还原逻辑写在协程里没\n\n一句话总结：触发器 + Tag 判断捡道具，GameManager 单例收口总分账，枚举分类型 switch 分派效果，再加消失动画、音效、数值反馈三件套——收集系统从逻辑到手感就全了。' },
    { type: 'component', title: '收集与道具关键概念', items: [
      { name: '触发器收集', desc: 'Is Trigger + OnTriggerEnter2D' },
      { name: 'Tag 判断', desc: 'CompareTag("Player") 分辨捡拾者' },
      { name: 'GameManager 收口', desc: '单例统一计分、通知 UI' },
      { name: '枚举分派', desc: 'ItemType + switch 分类处理' },
      { name: '协程持续', desc: '状态型道具到期自动还原' },
      { name: '反馈三件套', desc: '消失动画 + 音效 + 数值跳动' },
    ] },
    { type: 'quiz', question: '玩家碰到加速道具，速度翻倍，3 秒后要自动还原。持续时间最省心的实现？', options: ['在 Update 里每帧检查时间', '用协程：速度翻倍 → yield WaitForSeconds(duration) → 速度还原', '加一个倒计时 UI 让玩家自己看'], answer: 1, tip: '协程天生就是干「延时然后自动执行」的：把「变强」和「还原」写进一个 IEnumerator，yield 等待 duration，到期自动走还原——不用每帧轮询计时，代码也最直白。' },
  ],
  'd2d-13': [
    { type: 'text', title: '2D 粒子、音效与反馈', body: '音效和特效是 2D 游戏的「调味料」——同一个跳跃，配上轻快的音效和一圈小粒子，手感立刻不一样。这一课讲 AudioSource 播放、2D 粒子系统、以及怎么让反馈「准」。\n\n一、播放音效的三种姿势\n\n1. 最简单：一个 AudioSource 挂在物体上，播放时 Play()：\n\nvoid OnTriggerEnter2D(Collider2D other)\n{\n    GetComponent<AudioSource>().Play();   // 播自己的 AudioSource\n}\n\n2. 一次性音效（拾取、爆炸这类短音）：用 PlayOneShot，多个音效可以叠播：\n\npublic AudioSource sfxSource;\nsfxSource.PlayOneShot(collectClip);      // 播放时不打断别的\n\n3. 全局音效管理器：所有音效走一个单例 AudioManager（prog-4 单例），避免每处都挂 AudioSource：\n\npublic class AudioManager : MonoBehaviour\n{\n    public static AudioManager instance;\n    public AudioSource sfxSource;\n    public AudioClip coinClip;\n    public AudioClip hurtClip;\n\n    void Awake() { instance = this; }\n\n    public void PlayCoin() { sfxSource.PlayOneShot(coinClip); }\n    public void PlayHurt() { sfxSource.PlayOneShot(hurtClip); }\n}\n\n// 用法：捡金币时\nAudioManager.instance.PlayCoin();\n\n好处：音频资源集中管理、音量统一控制（调一处全局音量）、一个 AudioSource 循环复用。\n\n二、2D 音效设置要点\n\n· 2D 模式：AudioSource 的 Spatial Blend 拉到 0（2D），声音不随距离变化，全场都能听到——拾取、UI 点击用这个\n· 3D 模式（Spatial Blend=1）：声音随距离衰减，脚步声、远处的爆炸用——横版近大远小也用它\n· 循环 Loop：背景音乐勾 Loop，音效不勾\n· 音量 Volume：BGM 0.5~0.8、音效 0.8~1.0，别全拉满，耳朵会炸\n\n三、2D 粒子特效：像样的一圈光\n\nURP/内置管线都用 Particle System，配 2D 粒子三步：\n1. 右键 → Effects → Particle System，建粒子物体\n2. 改材质：Particle System 默认材质是 3D 用的，2D 要换——Renderer → Material 选一张默认圆点/精灵图\n3. 调参数：\n· Start Lifetime 0.2~0.5：粒子存活短，一闪而过\n· Start Speed 0~3：向外散开一点\n· Start Size 0.1~0.3：小颗粒\n· Emission Rate Over Time 10~30：密度\n· Shape 选 Circle：从圆形壳发射，爆炸/拾取效果像样\n\n粒子系统跑完会自动停，不用手动删。\n\n四、代码里触发特效\n\n特效 Prefab 做好后，代码里 Instantiate + 自动销毁：\n\npublic GameObject coinEffectPrefab;\n\nvoid PlayCoinEffect(Vector3 pos)\n{\n    GameObject fx = Instantiate(coinEffectPrefab, pos, Quaternion.identity);\n    Destroy(fx, 1f);   // 1 秒后销毁，防止残留\n}\n\n记得加延时销毁，不然特效物体散落场景里不回收。\n\n五、反馈要「准」：音效特效跟事件走\n\n好的反馈是「动作发生的同一瞬间」给反馈。用 prog-15 的事件系统串：收集时广播事件，音效管理器、特效管理器各自订阅响应——加新反馈不动收集逻辑。\n\n六、常见坑\n\n· 音效没声音 → AudioSource 有没有挂、AudioClip 有没有拖、是不是 3D 模式（距离太远衰减听不见）、AudioListener 有没有（相机上要有）\n· 粒子发白/糊 → 2D 粒子的材质没换，默认材质是 3D 的。Renderer 换 Sprite 材质\n· 特效残留 → Instantiate 后忘了 Destroy，特效物体满场景都是。延时销毁\n· 音效重叠轰炸 → 短音用 PlayOneShot（可叠播），循环音/长音用 Play()；别每个音效都 new AudioSource\n\n一句话总结：短音 PlayOneShot、BGM 循环 Loop、全局音效用单例 AudioManager 收口；2D 粒子把 Renderer 换成 Sprite 材质、调 Lifetime/Size 出效果；音效特效跟事件走，收集瞬间同步反馈——调味料到位，游戏就「活」了。' },
    { type: 'component', title: '音效特效关键概念', items: [
      { name: 'Play / PlayOneShot', desc: '单播 / 可叠播的一次性音效' },
      { name: 'Spatial Blend', desc: '0 是 2D 全场可听，1 是 3D 距离衰减' },
      { name: 'AudioManager 单例', desc: '音效集中管理、音量统一' },
      { name: '2D 粒子', desc: 'Renderer 换 Sprite 材质，2D 才不糊' },
      { name: '延时销毁', desc: 'Instantiate 特效后 Destroy(fx, 1f)' },
      { name: '事件触发反馈', desc: '音效特效订阅事件，同步播' },
    ] },
    { type: 'quiz', question: '想给「拾取音效」加一个全局音量开关，以后所有音效都要统一管理，最合适？', options: ['在每个物体的 AudioSource 里手动调音量', '做一个单例 AudioManager，所有音效都走它的 PlayOneShot，音量统一控制', '把音效文件全部重录一遍'], answer: 1, tip: '音效集中到单例 AudioManager 是最优解：一个 AudioSource 循环复用、音量统一调（全局静音开关就是一行）、音频资源集中管理。分散在每个物体上，改个全局音量要满场景找。' },
  ],
  'd2d-14': [
    { type: 'text', title: '子弹发射、命中检测与敌人死亡', body: '射击系统四步走：造子弹（Instantiate）、子弹飞（速度）、子弹命中（碰撞）、敌人死（销毁+反馈）。这一课把每一步的标准姿势和坑位写清楚。\n\n一、子弹预制体\n\n建一个子弹：Sprite（小圆/椭圆） + Rigidbody2D（Body Type 设 Dynamic，Gravity Scale 设 0——碰撞又不受重力）+ Box/Circle Collider 2D（勾 Is Trigger，用触发器判断命中）。拖进 Assets 做成 Prefab 叫 Bullet。\n\n二、发射子弹：Instantiate + 设速度\n\n玩家（枪口）脚本：\n\npublic class Shooter : MonoBehaviour\n{\n    public GameObject bulletPrefab;\n    public Transform firePoint;      // 枪口位置，一般是玩家的子物体\n    public float bulletSpeed = 12f;\n    public float fireCooldown = 0.2f;\n    private float lastFireTime;\n\n    void Update()\n    {\n        if (Input.GetMouseButtonDown(0) && Time.time >= lastFireTime + fireCooldown)\n        {\n            Fire();\n            lastFireTime = Time.time;   // 冷却：限制射速\n        }\n    }\n\n    void Fire()\n    {\n        GameObject bullet = Instantiate(bulletPrefab, firePoint.position, Quaternion.identity);\n        Rigidbody2D rb = bullet.GetComponent<Rigidbody2D>();\n        rb.velocity = firePoint.right * bulletSpeed;   // 朝枪口方向飞\n    }\n}\n\n要点：\n· 发射点在 firePoint（子物体），不是玩家中心——子弹从「枪口」出，不从「胸口」出\n· 子弹速度用 velocity 直接设，别用 AddForce（推着走发飘，d2d-4 讲过）\n· 冷却用 Time.time 比协程轻（高频操作少开销）\n\n三、子弹飞行方向：朝向鼠标/朝向目标\n\n朝鼠标射击：先算鼠标世界坐标，再算方向：\n\nVector3 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);\nmousePos.z = 0f;\nVector2 dir = (mousePos - transform.position).normalized;   // 单位方向\nrb.velocity = dir * bulletSpeed;\n\n朝固定方向（横版朝右）：velocity = firePoint.right * bulletSpeed（跟随枪口朝向）。\n\n四、子弹命中检测：OnTriggerEnter2D\n\n子弹脚本判断撞到什么：\n\npublic class Bullet : MonoBehaviour\n{\n    public int damage = 1;\n\n    void OnTriggerEnter2D(Collider2D other)\n    {\n        if (other.CompareTag("Enemy"))\n        {\n            other.GetComponent<Enemy>()?.TakeDamage(damage);   // 打敌人扣血\n            Destroy(gameObject);                               // 子弹消失\n        }\n        else if (other.CompareTag("Wall"))\n        {\n            Destroy(gameObject);   // 撞墙也消失\n        }\n    }\n}\n\n· Tag 判断目标：敌人打敌人逻辑、墙打墙逻辑，分开处理\n· ?. 防空：万一没挂 Enemy 组件不报错\n· 打中就消失：避免子弹穿透\n\n五、敌人死亡：血量归零销毁\n\nEnemy 脚本（d2d-11 有完整 AI，这里只写受伤死亡）：\n\npublic class Enemy : MonoBehaviour\n{\n    public int hp = 3;\n\n    public void TakeDamage(int amount)\n    {\n        hp -= amount;\n        if (hp <= 0) Die();\n    }\n\n    void Die()\n    {\n        AudioManager.instance.PlayHurt();                              // 音效\n        Instantiate(deathEffect, transform.position, Quaternion.identity);   // 特效\n        Destroy(gameObject);\n    }\n}\n\n死亡三件套：音效 + 特效 + 销毁，缺一个都像「纸片人消失了」。\n\n六、常见坑\n\n· 子弹穿墙不消失 → 子弹太快，一帧跨过了碰撞体：把 Collider 调大一点、或放慢子弹、或开连续碰撞检测\n· 子弹从胸口出 → firePoint 没挂/没摆对位置，枪口在玩家中心\n· 射速失控狂射 → 忘了冷却，或冷却用协程没走到。Time.time + fireCooldown 最稳\n· 子弹不飞 → velocity 设了没生效：Body Type 是 Kinematic，或方向向量是 (0,0)\n· 子弹打敌人没反应 → Tag 没打/打错、Enemy 没挂 TakeDamage、触发器条件不对\n\n一句话总结：射击 = 子弹预制体（Dynamic+零重力+触发器）+ 枪口 Instantiate + velocity 设速度 + OnTriggerEnter2D 判 Tag 命中 + 敌人血量归零销毁；冷却限射速、打中就消失，四步走完射击系统成型。' },
    { type: 'component', title: '射击系统关键概念', items: [
      { name: '子弹预制体', desc: 'Dynamic + Gravity Scale 0 + Is Trigger' },
      { name: 'firePoint 枪口', desc: '子物体定位发射点，别从胸口出' },
      { name: 'velocity 设速', desc: '直接设速度，不用 AddForce' },
      { name: '冷却限射速', desc: 'Time.time + fireCooldown' },
      { name: 'Tag 判命中', desc: 'OnTriggerEnter2D + CompareTag' },
      { name: '死亡三件套', desc: '音效 + 特效 + 销毁' },
    ] },
    { type: 'quiz', question: '子弹发射后穿过了敌人的碰撞体直接飞走、没触发命中。最常见的原因是？', options: ['子弹速度太快，一帧就跨过了碰撞体', '敌人血量太高', '子弹的 Sprite 太小'], answer: 0, tip: '高速物体的经典问题：一帧移动距离超过碰撞体厚度，物理检测直接跳过。对策：调大子弹 Collider、放慢子弹、或开启连续碰撞检测——让子弹「扫过的路径」也有检测。' },
  ],
  'd2d-15': [
    { type: 'text', title: '重力、跳跃、平台碰撞与二段跳', body: '平台跳跃（Platformer）的手感，d2d-7 讲过手感调校（重力、土狼时间、跳跃缓冲、二段跳），这一课把「重力、跳跃、平台碰撞、二段跳」的核心实现串成一套能跑的完整代码。\n\n一、重力：Rigidbody2D 的重力开关\n\n平台角色核心：Rigidbody2D（Body Type Dynamic，受重力）+ Collider2D（身体）。重力由 Gravity Scale 控制（d2d-7 讲过）：\n· 默认 1：正常下落\n· 平台跳跃常见 2~3：下落干脆不飘\n· 下落比上升重（两套重力）：\n\nvoid Update()\n{\n    float target = rb.velocity.y < 0 ? fallGravity : riseGravity;\n    rb.gravityScale = target;\n}\n\n二、跳跃：地面检测 + 初速度\n\n跳跃 = 检测能不能跳（着地）→ 给一个向上的初速度：\n\npublic float jumpForce = 8f;\nprivate bool isGrounded;\n\nvoid Update()\n{\n    if (Input.GetButtonDown("Jump") && isGrounded)\n    {\n        rb.velocity = new Vector2(rb.velocity.x, jumpForce);   // 直接设速度，别 AddForce\n    }\n}\n\n地面检测最常用：发射一条向下的短射线（Raycast2D）：\n\nprivate void CheckGround()\n{\n    isGrounded = Physics2D.Raycast(\n        transform.position,\n        Vector2.down,\n        groundCheckDistance,   // 略大于脚底到身体中心\n        groundLayer            // 只检测地形层\n    );\n}\n\nLayer 过滤：地面检测只检测 groundLayer，不然踩到敌人也算着地、二段跳会被打断。\n\n三、移动与跳跃的配合\n\n横版移动 + 跳跃是同一套速度系统：\n\nvoid Update()\n{\n    float h = Input.GetAxisRaw("Horizontal");\n    rb.velocity = new Vector2(h * moveSpeed, rb.velocity.y);   // 保留 y 速度（跳跃），只改 x\n}\n\n关键：改 velocity 时保留 rb.velocity.y——直接覆盖会把跳跃速度清零，角色「跳不起来」。\n\n四、平台碰撞：三类「平台」区分\n\n· 普通地面：Box Collider，踩得住、也能从底下顶到（solid）\n· 单向平台（能从下往上跳、从上往下落）：给平台加 Platform Effector 2D 组件，勾 Use One Way\n· 空中浮台：同单向平台，角色跳上来不穿\n\n单向平台标准实现：给平台挂 Platform Effector 2D（勾 Use One Way），角色 Collider 的 Used By Effector 勾上——踩得住、能从底下跳上来。想「按 S 落下」，在按 S 时临时关闭角色与平台的一帧碰撞（或调 Effector）。\n\n五、二段跳：跳两次封顶（d2d-7 的完整版）\n\npublic int maxJumps = 2;\nprivate int jumpsLeft;\n\nvoid Update()\n{\n    if (isGrounded) jumpsLeft = maxJumps;   // 着地重置\n\n    if (Input.GetButtonDown("Jump") && jumpsLeft > 0)\n    {\n        rb.velocity = new Vector2(rb.velocity.x, jumpForce);\n        jumpsLeft--;\n    }\n}\n\n要点：着地才重置计数、跳跃才消耗；空中按跳最多消耗完 maxJumps 次。想区分「一段跳/二段跳动画」，在赋值前判断 jumpsLeft。\n\n六、手感增强（d2d-7 复习，直接可用）\n\n· 土狼时间：离开平台边缘 0.1 秒内还能跳——记 lastGroundedTime，判断 (Time.time - lastGroundedTime) < 0.1f\n· 跳跃缓冲：落地前 0.1 秒按跳，落地瞬间自动跳——记 jumpBufferTime\n· 松开跳减上升速度（可变跳跃）：GetButtonUp("Jump") 时 velocity.y *= 0.5f\n\n七、常见坑\n\n· 角色跳不起来 → 跳跃初速度设的是 AddForce，改成 velocity 直接设；或刚体被墙挡住\n· 角色在空中还能一直跳 → 地面检测没生效：isGrounded 没更新（CheckGround 每帧调了没）、射线没设 Layer 过滤\n· 跳到空中 y 速度被清零 → 移动赋值把 y 覆盖了：保留 rb.velocity.y\n· 二段跳被敌人打断 → 地面检测把敌人也当「地面」：Layer 过滤只检地形\n\n一句话总结：平台跳跃 = 重力（Dynamic+gravityScale）+ 地面检测（Raycast2D 过滤地形层）+ 跳跃（直接设 velocity.y）+ 移动保留 y 速度 + 单向平台（Platform Effector）+ 二段跳计数；土狼时间、跳跃缓冲、可变跳跃三件套再上，手感直接起飞。' },
    { type: 'component', title: '平台跳跃关键概念', items: [
      { name: 'Gravity Scale', desc: '重力倍数，下落干脆看它' },
      { name: 'Raycast2D 地面检测', desc: '向下短射线 + Layer 过滤' },
      { name: '保留 y 速度', desc: '移动只改 x，别把跳跃清零' },
      { name: 'Platform Effector', desc: '单向平台：能踩、能从下跳上来' },
      { name: '二段跳计数', desc: '着地重置、跳跃消耗' },
      { name: '土狼时间/跳跃缓冲', desc: '0.1 秒宽容度，手感神器' },
    ] },
    { type: 'quiz', question: '角色在空中二段跳，但一跳起来就感觉被「压下去」，查下来是移动代码每次把速度整个覆盖了。问题在哪？', options: ['跳跃初速度太小', '移动赋值把 y 速度清零了：应该保留 rb.velocity.y，只改 x', '角色太重，调大 Gravity Scale'], answer: 1, tip: '平台跳跃的黄金规则：改水平速度时保留垂直速度。rb.velocity = new Vector2(h * speed, rb.velocity.y)——把 y 覆盖成 0，跳跃初速度当场被清掉，角色自然「跳不起来」。' },
  ],
  'd2d-16': [
    { type: 'text', title: '血条、金币数与 UI 更新', body: 'UI 是玩家的「仪表盘」：血量、金币、得分、关卡进度。这一课讲血条、金币数、UI 更新的标准做法——重点在「UI 怎么跟数据同步」。\n\n一、UI 搭建基础：Canvas 三件套\n\n创建 UI：Hierarchy 右键 → UI → Canvas。Canvas 下：\n· Canvas Scaler：UI 自适应屏幕。UI Scale Mode 选 Scale With Screen Size，Reference Resolution 设 1920×1080（横屏）或 1080×1920（竖屏）——UI 在各种分辨率下不变形\n· Canvas 下建 UI 元素：Text、Image、Button、Slider 都放 Canvas 里\n\nCanvas 模式：Overlay（默认，UI 永远在最上层，最常用）／Screen Space Camera（UI 在相机前固定距离）／World Space（UI 在 3D 世界，2D 很少用）。\n\n二、金币数：Text 显示数值\n\n建一个 UI Text 显示「金币：0」，写一个 UIManager 专门刷 UI：\n\npublic class UIManager : MonoBehaviour\n{\n    public static UIManager instance;\n    public Text coinText;\n\n    void Awake() { instance = this; }\n\n    public void UpdateCoin(int count)\n    {\n        coinText.text = "金币：" + count;\n    }\n}\n\n游戏逻辑（GameManager 捡金币时，d2d-12 讲过）调用 UIManager 刷新，UI 不直接碰游戏数据——数据归 GameManager、显示归 UIManager，各管一摊。\n\n三、血条：Image 填充 + Slider\n\n两种主流血条：\n\n1. Image 填充（Fill Amount）：建一个 Image（块状），把它的 Image Type 设 Filled、Fill Method 设 Horizontal，用 Fill Amount 当血量比例：\n\npublic Image healthFill;\n\npublic void UpdateHealth(float hp, float maxHp)\n{\n    healthFill.fillAmount = hp / maxHp;   // 0~1，Image 按比例填充\n}\n\n2. Slider（更标准）：建 UI → Slider，Slider 的 Fill Area 里放填充块，代码设 value：\n\npublic Slider healthSlider;\n\npublic void UpdateHealth(float hp, float maxHp)\n{\n    healthSlider.maxValue = maxHp;\n    healthSlider.value = hp;\n}\n\nSlider 自带背景+填充，血条不需要拖拽，把 Handle 禁用/删掉即可。血条配红绿色：血量高时绿、低时红，脚本里改 image.color。\n\n四、UI 和数据同步：事件驱动（d2d-12 / prog-15 复习）\n\n血条、金币 UI 不该自己每帧去读游戏数据——让数据变的时候「通知」UI。事件驱动：\n\n// 角色：血量变化广播\npublic event System.Action<float, float> OnHpChanged;\n\nvoid TakeDamage(float dmg)\n{\n    hp -= dmg;\n    OnHpChanged?.Invoke(hp, maxHp);   // 广播\n}\n\n// 血条 UI：订阅\nvoid Start()\n{\n    character.OnHpChanged += OnHpChanged;\n    OnHpChanged(character.hp, character.maxHp);   // 先刷一次初始值\n}\n\nvoid OnHpChanged(float hp, float maxHp)\n{\n    healthSlider.maxValue = maxHp;\n    healthSlider.value = hp;\n}\n\nvoid OnDestroy()\n{\n    character.OnHpChanged -= OnHpChanged;   // 退订\n}\n\nUI 只在数据变化时刷新，不空转、不会和别处抢写——这是 UI 与数据的「官方正解」。\n\n五、常见坑\n\n· UI 不跟随缩放 → Canvas Scaler 没配，分辨率一变 UI 就变形。Scale With Screen Size 先设好\n· UI 每帧在 Update 里刷 → 空转白耗。让数据广播、UI 订阅\n· 血条方向反了 → Fill Method 设 Horizontal 时 Origin 或镜像没弄对；Slider 检查 Direction\n· 场景切换 UI 找不到 → UIManager 是场景里的单例，跨场景用 DontDestroyOnLoad 或重新赋值\n\n一句话总结：UI 三件套（Canvas Scaler 自适应 + Text/Image/Slider 元素 + UIManager 统一刷新）；金币用 Text 显示、血条用 Slider/Image Filled 填充；数据变化广播事件、UI 订阅刷新——UI 与数据解耦，才不会被每帧轮询拖垮。' },
    { type: 'component', title: 'UI 计分关键概念', items: [
      { name: 'Canvas Scaler', desc: 'Scale With Screen Size 自适应' },
      { name: 'UIManager', desc: '统一刷 UI，数据归 GameManager' },
      { name: 'Image Filled', desc: 'Fill Amount 做血条填充' },
      { name: 'Slider', desc: '血条标准做法，maxValue + value' },
      { name: '事件驱动', desc: '数据广播、UI 订阅刷新' },
    ] },
    { type: 'quiz', question: '想让血条只在血量变化时刷新、并且 UI 和角色代码解耦，最合适的做法？', options: ['角色在 Update 里每帧把血量塞给血条 UI', '角色广播 OnHpChanged 事件，血条订阅它，收到才刷新', '让血条每帧主动读角色的 hp 字段'], answer: 1, tip: '事件驱动是 UI 同步的正解：数据变化方广播，显示方订阅响应。血条不用每帧轮询、角色也不用知道血条的存在——各自独立，改动不打架。' },
  ],
  'd2d-17': [
    { type: 'text', title: '关卡状态、胜负与重玩', body: '游戏得有「状态」：进行中、胜利、失败、重玩。状态没管好，游戏就会「胜利了还在动」「死了还能继续走」。这一课讲关卡状态、胜负判断、重玩流程。\n\n一、状态枚举 + 单例 GameManager\n\n用一个枚举管游戏状态（prog-8 学过枚举）：\n\npublic enum GameState { Playing, Win, Lose, Paused }\n\npublic class GameManager : MonoBehaviour\n{\n    public static GameManager instance;\n    public GameState state = GameState.Playing;\n\n    void Awake() { instance = this; }\n}\n\n所有「该不该动」的判断都看 state：\n· 玩家脚本：state == Playing 才允许输入/移动\n· 敌人脚本：state != Playing 就停（不追不攻击）\n· UI：state 变了就弹对应面板\n\n二、胜负判断：什么时候判定\n\n胜利判定举例：\n· 收集齐所有金币：coinCount 达到目标 → Win\n· 到达终点：玩家碰到终点触发器 → Win\n· 血量归零：玩家死亡 → Lose\n· 掉出关卡：玩家 y < 下限 → Lose\n\npublic void CheckWin()\n{\n    if (coinCount >= targetCoins)\n    {\n        SetState(GameState.Win);\n    }\n}\n\nvoid Update()\n{\n    if (transform.position.y < killY)\n    {\n        GameManager.instance.PlayerDied();   // 掉出地图算输\n    }\n}\n\n三、切状态：统一入口\n\n所有状态切换都走 SetState 一个口，方便在切换瞬间统一处理（暂停、显示面板、锁输入）：\n\npublic void SetState(GameState newState)\n{\n    state = newState;\n\n    if (newState == GameState.Win)      winPanel.SetActive(true);\n    else if (newState == GameState.Lose) losePanel.SetActive(true);\n\n    player.gameObject.SetActive(state == GameState.Playing);   // 锁输入\n}\n\n统一入口的好处：加新状态（暂停、通关动画）只改 SetState，别处都不用动。\n\n四、暂停：Time.timeScale（d2d-8 用过）\n\n暂停游戏 = 让所有物理和时间逻辑停摆：\n\nvoid Pause()\n{\n    Time.timeScale = 0f;      // 全局时间暂停\n    state = GameState.Paused;\n    pausePanel.SetActive(true);\n}\n\nvoid Resume()\n{\n    Time.timeScale = 1f;\n    state = GameState.Playing;\n    pausePanel.SetActive(false);\n}\n\n注意：timeScale=0 会停掉 WaitForSeconds、Rigidbody 物理、动画（默认），但 UI 按钮、自定义计数不受它管——暂停菜单的按钮能点、协程里 Realtime 版照样走（prog-11 讲过）。记得在 OnDestroy / 退出场景时把 timeScale 恢复成 1。\n\n五、重玩：重置一切\n\n重玩 = 回到初始状态：分数清零、玩家回出生点、敌人复位、面板关掉。最简单可靠的重玩：重载场景——所有物体从头初始化，不怕漏重置：\n\npublic void Restart()\n{\n    SceneManager.LoadScene(SceneManager.GetActiveScene().name);\n}\n\n需要 using UnityEngine.SceneManagement。重载场景把「重置一切」直接交给引擎，最省心。注意：DontDestroyOnLoad 的物体会残留——重玩类游戏别把 GameManager 设成跨场景常驻（或重载时手动处理）。\n\n六、UI 绑定按钮\n\nWin/Lose 面板里放 Button，Button 的 OnClick 绑 GameManager 的 Restart / NextLevel 方法（检查器拖拽绑定，d2d-8 做过）。按钮回调必须传「无参方法」，Restart 无参正合适。\n\n七、常见坑\n\n· 胜利/失败后面还在动 → 玩家/敌人脚本没查 state，直接禁用了才干净（SetActive(false)）\n· timeScale=0 后 UI 按钮点了没反应 → UI 响应不受 timeScale 管，一般没问题；如果按钮动画卡住，检查是不是依赖 timeScale 的动画\n· 重玩时血条/金币不清零 → Restart 里只关了面板忘了重置数据；重载场景能根治\n· 掉出地图不判定输 → killY 判断没写，或 y 判断符号写错（角色在下方用 <）\n\n一句话总结：GameState 枚举 + GameManager 单例管状态，所有「动不动」看 state；SetState 统一入口切状态、Time.timeScale 暂停、重载场景做最可靠的重玩；胜负判定、暂停、重玩一条龙，游戏就有了「骨架」。' },
    { type: 'component', title: '游戏状态关键概念', items: [
      { name: 'GameState 枚举', desc: 'Playing / Win / Lose / Paused' },
      { name: 'SetState 统一入口', desc: '切状态走一个口，方便统一处理' },
      { name: '胜负判定', desc: '金币集齐 / 终点 / 血空 / 掉出地图' },
      { name: 'Time.timeScale', desc: '全局暂停，记得恢复成 1' },
      { name: '重载场景重玩', desc: 'LoadScene 重置一切，最省心' },
    ] },
    { type: 'quiz', question: '玩家死亡弹失败面板后，敌人还在继续巡逻、子弹还能发射。根因是？', options: ['敌人脚本没检查游戏状态，state 已变 Lose 还照常跑', '失败面板挡不住子弹', '玩家的碰撞体还在'], answer: 0, tip: '状态切到 Lose 后，所有「该停的行为」都必须检查 state（或直接 SetActive 禁用）。只弹面板不管逻辑，游戏逻辑不会自己停——给玩家、敌人、发射脚本都加上 state 检查。' },
  ],
  'd2d-18': [
    { type: 'text', title: '精灵合批、图集与对象池', body: '2D 游戏卡顿，90% 是 DrawCall 太多 + 物件来回创建销毁。这一课讲三个最核心的优化：精灵合批、图集、对象池，外加 DrawCall 怎么看。\n\n一、什么是 DrawCall：一次「画」的指令\n\nGPU 每画一个「不同的物体」就要下一条绘制指令，叫一次 DrawCall。手机上能扛的批次数有限（多了就掉帧）。DrawCall 越少越快。2D 里同样一张图、同一个材质，Unity 可以把它们合并成一次绘制——这叫合批（Batching）。\n\n二、精灵合批：让同一张图一起画\n\nUnity 2D 默认就做合批：同一个 Sprite、同一个材质、用 Sprite Renderer 画的物体，能自动合成一个批次。要做到：\n· 用 Sprite Renderer（不是 Mesh Renderer）画 2D\n· 材质尽量统一（别给每个物体单独调颜色/材质——颜色不同会拆批）\n· 物体尽量用同一个 Sprite Atlas\n\n会「拆批」的因素（记住别踩）：\n· 不同的纹理（每张图一跑就是新批次）\n· 不同的材质（尤其透明和不透明混排）\n· 物体挨个 Sort Order 乱跳（排序改变影响合批）\n· 动态移动大量物体（每帧重新合批，物体太多会失效）\n\n三、图集（Sprite Atlas）：把零散小图合成一张\n\n把几十张小图（金币、砖块、角色帧）打包成一张大图，采样时只引用大图——渲染时它们天然是「同一张图」，就能合批。\n\nUnity 做法：Assets → Create → 2D → Sprite Atlas，把精灵拖进 Objects for Packing，点 Pack Preview 生成。角色帧、小道具、UI 图标各自打一个图集。\n\n四、对象池：不销毁，回收复用\n\n子弹、粒子、金币这类「高频创建又销毁」的物体，Instantiate/Destroy 每次都要申请/释放内存，是 GC 卡顿的元凶。对象池 = 造一批备用，用的时候取、不用了放回：\n\npublic class ObjectPool : MonoBehaviour\n{\n    public static ObjectPool instance;\n    public GameObject bulletPrefab;\n    private Queue<GameObject> pool = new Queue<GameObject>();\n\n    void Awake() { instance = this; }\n\n    public GameObject GetBullet(Vector3 pos)\n    {\n        GameObject b;\n        if (pool.Count > 0)\n        {\n            b = pool.Dequeue();               // 有存货：取一个\n            b.SetActive(true);\n        }\n        else\n        {\n            b = Instantiate(bulletPrefab);    // 没存货：造一个\n        }\n        b.transform.position = pos;\n        return b;\n    }\n\n    public void ReturnBullet(GameObject b)\n    {\n        b.SetActive(false);                    // 回收：隐藏，不是销毁\n        pool.Enqueue(b);\n    }\n}\n\n子弹用完不 Destroy，调 ReturnBullet 放回池里。注意：池里的物体要「初始状态正确」（速度、碰撞都要复位）。\n\n五、其他 2D 性能要点\n\n· 纹理尽量小、用压缩格式（d2d-19 发布适配会讲）\n· 粒子数量控制：Emission Rate 别拉爆，几十个够炫\n· 地面检测、计算每帧少做：缓存组件引用（GetComponent 存字段）、避免每帧 Find\n· 背景用 Tilemap 合批，别铺几千个独立 Sprite\n\n六、怎么看性能：Profiler + Frame Debugger\n\n· Window → Analysis → Profiler：看 CPU 时间花在哪（GC Alloc 高 = 频繁造对象）\n· Window → Analysis → Frame Debugger：一帧帧看 DrawCall 数，合批有没有生效\n· 手机上看：Unity 手机上接 Profiler 或看 FPS 帧率（统计脚本）\n\n七、常见坑\n\n· 合批不生效 → 材质/纹理不统一。全场景统一材质，图集打起来\n· 对象池物体残留效果 → 出池没复位（速度、颜色、碰撞）。Get 时把初始状态写回去\n· 每帧 Instantiate/Destroy → 高频物必用对象池，Profiler 里 GC Alloc 高就是信号\n· 背景碎成几千个 Sprite → Tilemap 画背景，Tilemap 天然合批\n\n一句话总结：DrawCall 越少越快——同一张图/材质合批、零散小图打图集、高频物件走对象池；Profiler 看 GC、Frame Debugger 数 DrawCall，三个优化 + 两个工具，2D 性能就稳了。' },
    { type: 'component', title: '2D 性能优化关键概念', items: [
      { name: 'DrawCall', desc: '绘制指令，越少越快' },
      { name: '精灵合批', desc: '同图同材质自动合并绘制' },
      { name: 'Sprite Atlas 图集', desc: '零散小图合成一张，天然合批' },
      { name: '对象池', desc: '取用归还、循环复用，消除 GC' },
      { name: 'Profiler / Frame Debugger', desc: '看 GC 和 DrawCall 的工具' },
    ] },
    { type: 'quiz', question: '子弹每帧 Instantiate、撞到就 Destroy，Profiler 里 GC Alloc 飙高、游戏一打枪就卡。最对症的优化？', options: ['降低子弹速度', '用对象池：造一批子弹备用，用的时候取、不用了放回，不反复创建销毁', '把子弹改成不受重力'], answer: 1, tip: 'Instantiate/Destroy 反复申请释放内存是 GC 卡顿的元凶。对象池预先备好一批，取用归还、循环复用，从根源消除高频创建销毁——这是子弹、粒子类高频物体的标准优化。' },
  ],
  'd2d-19': [
    { type: 'text', title: '分辨率适配与发布', body: '游戏写好了要发布到手机。这一课讲分辨率适配、竖屏/横屏、手机发布最常见的坑——让你的 2D 游戏在任何屏幕上都「不变形、不漏玩」。\n\n一、正交相机下「看到多少」由什么决定\n\n2D 的正交相机（d2d-9 讲过）：Camera Size 决定「高度方向看到几个单位」，宽度由屏幕宽高比（aspect）决定。所以：\n· 同一台手机转个方向，看到的世界宽度就变了\n· 不同分辨率手机，看到的高度一样、宽度不同\n\n这就是适配的核心问题：画面在换屏幕/转屏时，要保证「核心玩法区域」始终可见。\n\n二、相机适配：按宽高比调 Size\n\n想保证「横向上内容一定看得全」，按宽高比动态调相机的 Size（让画面刚好框住地图宽）：\n\npublic Camera cam;\npublic float targetWidth = 16f;   // 希望横向看到多少世界单位\n\nvoid Awake()\n{\n    cam.orthographicSize = targetWidth / (2f * cam.aspect);\n}\n\n原理：orthographicSize × 2 × aspect = 看到的宽度。设成 targetWidth / (2×aspect)，任何屏幕都刚好看到 targetWidth 宽的世界。\n\n三、画布适配：Canvas Scaler\n\nUI 能不能自适应，全靠 Canvas Scaler（d2d-16 讲过）：\n· UI Scale Mode = Scale With Screen Size\n· Reference Resolution：按目标设备分辨率设（横屏 1920×1080，竖屏 1080×1920）\n· Screen Match Mode：Match Width Or Height——设成 0.5（各占一半）最省心，或按玩法偏好调\n\n这样 UI 元素按比例缩放，手机和平板不糊不乱。\n\n四、竖屏 / 横屏：手机方向设置\n\n· 竖屏（Portrait）：单手持机玩法\n· 横屏（Landscape）：平台跳跃、横版射击\n\n设置：Player Settings → Resolution and Presentation → Default Orientation：\n· Portrait（竖屏固定）\n· Landscape（横屏固定）\n· Auto Rotation（随手机转，一般不推荐，画面适配复杂）\n\n发布手机游戏几乎都「固定一个方向」，避免转屏导致画面错乱。\n\n五、手机发布注意\n\n· 分辨率：Player Settings → Other Settings → Resolution 设置参考分辨率；手机游戏常设成「按设备自适应」+ 固定方向\n· 安全区（刘海屏）：iPhone 刘海会挡住 UI。用 Screen.safeArea 调整 Canvas\n· 触控输入：Input.touch 或 Input.GetMouseButton（Unity 触屏会映射成鼠标，小项目直接用鼠标 API 最简单）；别依赖鼠标悬停（hover），手机没有悬停\n· 性能：纹理压缩、DrawCall 控制（d2d-18）——手机性能弱，PC 上跑得顺不代表手机顺\n\n六、适配验证\n\n· 编辑器里切分辨率：Game 视图左上角下拉选不同分辨率（iPhone X、Android 常见机型）看画面\n· 转屏测试：切 Portrait/Landscape 看核心区域有没有被裁\n· 真机测试：手机装一次，验证真实手感、性能、安全区\n\n七、常见坑\n\n· 手机转屏画面裁掉一半 → 固定方向（Player Settings 锁 Portrait/Landscape），或按宽高比调相机 Size\n· UI 到边缘被刘海挡 → Screen.safeArea 处理安全区\n· PC 跑得好手机卡 → 纹理压缩、图集、对象池（d2d-18）上；手机别用高分辨率大图\n· 鼠标悬停效果在手机不生效 → 手机没有 hover，交互全走点击\n\n一句话总结：2D 适配 = 相机按宽高比调 Size 保证玩法可见 + Canvas Scaler 让 UI 自适应 + 固定横竖屏 + 安全区避开刘海；编辑器多切分辨率、真机测一次，适配就算稳了。' },
    { type: 'component', title: '发布适配关键概念', items: [
      { name: 'aspect 宽高比', desc: '正交相机宽度 = Size×2×aspect' },
      { name: '相机 Size 适配', desc: '按目标宽度反推 Size' },
      { name: 'Canvas Scaler', desc: 'Scale With Screen Size 自适应' },
      { name: '固定方向', desc: 'Portrait / Landscape 锁一个' },
      { name: 'Screen.safeArea', desc: '避开刘海屏遮挡' },
    ] },
    { type: 'quiz', question: '同一个关卡，在横屏手机上「横向看到的世界宽度」总是和设计不一致，角色常跑出屏外。最直接的修法？', options: ['把相机的 Size 按 cam.aspect 动态算，保证横向看到的目标宽度固定', '把角色速度调慢', '把背景画大一点'], answer: 0, tip: '正交相机高度由 Size 固定、宽度由屏幕宽高比决定——不同手机宽度天然不同。按目标宽度反推 Size（orthographicSize = targetWidth / (2 × aspect)），任何屏幕都能框住玩法区域。' },
  ],
  'd2d-20': [
    { type: 'text', title: '综合动手练：横版跳台收集小游戏', body: '🎯 任务目标：把 2D 课的功夫串成一个完整小游戏——「横版跳台收集」：玩家左右移动 + 跳跃（含二段跳），收集金币，碰到敌人扣血（血条 UI），血空游戏结束，收集完所有金币过关，可重玩。用到的知识：d2d-4 角色控制、d2d-15 平台跳跃、d2d-12 收集、d2d-16 UI 计分、d2d-17 游戏状态。\n\n📦 前置要求：先学完 d2d-9（相机跟随）、d2d-12（收集）、d2d-15（平台跳跃）、d2d-16（UI）、d2d-17（游戏状态）。\n\n📋 动手步骤：\n\n第 1 步｜建场景与主角：2D 场景，建一个方块当玩家（Sprite + Rigidbody2D Dynamic + Box Collider 2D + Tag "Player"）。写 PlayerController.cs 实现移动 + 跳跃 + 二段跳 + 土狼时间：\n\npublic class PlayerController : MonoBehaviour\n{\n    public float moveSpeed = 6f;\n    public float jumpForce = 9f;\n    public int maxJumps = 2;\n    private int jumpsLeft;\n    private Rigidbody2D rb;\n    private float lastGroundedTime;\n    public float coyoteTime = 0.1f;\n    public LayerMask groundLayer;\n\n    void Awake() { rb = GetComponent<Rigidbody2D>(); }\n\n    void Update()\n    {\n        float h = Input.GetAxisRaw("Horizontal");\n        rb.velocity = new Vector2(h * moveSpeed, rb.velocity.y);   // 保留 y\n\n        bool grounded = Physics2D.Raycast(transform.position, Vector2.down, 0.6f, groundLayer);\n        if (grounded) lastGroundedTime = Time.time;\n\n        bool canJump = grounded || (Time.time - lastGroundedTime) < coyoteTime;\n        if (Input.GetButtonDown("Jump") && canJump && jumpsLeft > 0)\n        {\n            rb.velocity = new Vector2(rb.velocity.x, jumpForce);\n            jumpsLeft--;\n        }\n        if (grounded) jumpsLeft = maxJumps;\n    }\n}\n\n第 2 步｜地面与平台：地面（一个长条 Collider2D）和一个单向平台（Platform Effector 2D + Used By Effector），把 groundLayer 设成地面所在 Layer。\n\n第 3 步｜相机跟随：写 CameraFollow 脚本（d2d-9 的 LateUpdate + Lerp + 边界 Clamp），让镜头平滑跟着玩家。\n\n第 4 步｜金币：建金币（Sprite + Circle Collider 2D + Is Trigger + Tag "Coin"），写 Coin.cs（d2d-12：OnTriggerEnter2D 判断玩家 → 调 GameManager.AddCoin → 播音效 → Destroy）。\n\n第 5 步｜敌人：建敌人（Sprite + Box Collider 2D + Rigidbody2D Dynamic + Tag "Enemy"），写简单巡逻 AI（d2d-11 的 MoveTowards 两点巡逻），玩家碰到敌人扣血。\n\n第 6 步｜GameManager（单例）：管金币、生命、状态（d2d-17），控制 UI 刷新和面板。\n\n第 7 步｜UI：Canvas + Canvas Scaler（Scale With Screen Size），放金币 Text、生命 Text、Win 面板、Lose 面板（面板里各放一个 Button，OnClick 绑 GameManager.Restart）。写 UIManager 更新金币和生命文本。\n\n第 8 步｜跑起来验收：玩家移动跳跃流畅（二段跳生效、土狼时间救坑）、镜头平滑跟随、金币收集计数、碰到敌人扣血、血空弹失败面板、金币集齐弹胜利面板、点按钮重玩一切归零。\n\n✅ 验收清单：\n能 □ 玩家左右移动 + 跳跃 + 二段跳，土狼时间让边缘跳更宽容\n能 □ 相机在 LateUpdate 平滑跟随，边界不越出地图\n能 □ 金币碰到玩家计数 +1 并消失\n能 □ 敌人两点巡逻，玩家碰到敌人生命 -1\n能 □ 生命归零弹失败面板、金币集齐弹胜利面板，游戏逻辑停止\n能 □ 点「重玩」重载场景，一切归零重新开始\n\n🕳 常见卡壳：\n· 玩家卡墙/穿地 → 检查 Rigidbody2D 的 Body Type、地面 Layer 过滤、Platform Effector 配置\n· 金币穿过玩家不触发 → 金币 Collider 勾 Is Trigger、双方有 Collider2D、至少一边 Rigidbody2D、Tag 对\n· 相机出地图 → CameraFollow 的 Clamp 边界算对（d2d-9 公式），别写死\n· 胜利/失败后还能动 → GameManager 切状态后，玩家/敌人检查 state 或直接禁用\n\n一句话总结：一个完整横版小游戏 = 角色控制 + 平台跳跃 + 相机跟随 + 金币收集 + 敌人巡逻 + 生命值 + UI 计分 + 游戏状态与重玩——把这八件串起来、每件验收过关，2D 综合动手练就真正毕业了。' },
    { type: 'quiz', question: '玩家跳上单向平台后，一直想从平台底下「跳穿上去」，但总是顶住。最可能的配置问题？', options: ['平台没用 Platform Effector 2D 或角色的 Used By Effector 没勾', '玩家跳跃初速度太大', '平台挂的是普通 Box Collider 且角色跳跃高度不够'], answer: 0, tip: '单向平台（能从底下跳上来）必须用 Platform Effector 2D 配 Used By Effector：普通 Box Collider 会把平台当「实心」，从底下顶住。配置对了才能穿上来、又踩得住。' },
  ],
}
