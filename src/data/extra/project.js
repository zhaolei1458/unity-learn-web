// 完整项目实战（project）新增第 5/6/7 节：主菜单与游戏流程、音效与打磨、发布与迭代
export const lessons = [
  { id: 'project-5', title: '主菜单与游戏流程' },
  { id: 'project-6', title: '音效与打磨' },
  { id: 'project-7', title: '发布与迭代' },
  { id: 'project-8', title: '扩展：第二关卡与关卡选择' },
  { id: 'project-9', title: '扩展：敌人与障碍' },
  { id: 'project-10', title: '扩展：道具与升级系统' },
  { id: 'project-11', title: '扩展：存档与继续' },
  { id: 'project-12', title: '扩展：设置面板' },
  { id: 'project-13', title: '扩展：排行榜与成绩' },
  { id: 'project-14', title: '扩展：成就系统' },
  { id: 'project-15', title: '扩展：双人模式' },
  { id: 'project-16', title: '扩展：手机适配' },
  { id: 'project-17', title: '扩展：手柄支持' },
  { id: 'project-18', title: '打磨：扩展整合' },
  { id: 'project-19', title: '扩展：程序化生成关卡' },
  { id: 'project-20', title: '项目综合动手练' },
]

export const cards = {
  'project-5': [
    { type: 'text', title: '主菜单与游戏流程', body: '前面四课把「一关的滚球游戏」做完了，这一课把它变成「一个完整的游戏」——有主菜单、能进关、能返回、数据还能带过去。核心就一句话：把游戏拆成多个场景，再用代码管好场景切换。\n\n场景结构：两个场景起步。\n· MainMenu —— 标题 + 开始按钮 + 退出按钮\n· Game —— 滚球关卡本体\n\n第一步，把两个场景都登记进 Build Settings：\nFile → Build Settings → 把 MainMenu 和 Game 都拖进 Scenes In Build 并勾上。\n\n「漏了这一步」是新手第一大坑：编辑器里点按钮一切正常，打包出来的游戏一点「开始」就报错或黑屏——因为没登记的场景根本不在包里（platform-1 也提过这坑）。\n\n第二步，做主菜单 UI：\nCanvas 上放 Title（标题文字）+ 两个 Button。「开始游戏」按钮在检查器 On Click 列表里加一个空槽，拖入挂脚本的物体，选中方法 SceneLoader.StartGame。\n\n第三步，写场景加载脚本（核心就两个 API）：\n\npublic class SceneLoader : MonoBehaviour {\n    public void StartGame() {\n        SceneManager.LoadScene(\"Game\");   // 同步加载\n    }\n    public void QuitGame() {\n        Application.Quit();               // 只在打包后生效\n    }\n}\n\n第四步，加个「加载进度条」——场景大了以后，直接切换会白屏半天，用异步加载 + 进度条体验好得多：\n\npublic class SceneLoader : MonoBehaviour {\n    public Slider progressBar;\n    public Text percentText;\n\n    public void LoadWithProgress(string sceneName) {\n        StartCoroutine(LoadAsync(sceneName));\n    }\n\n    IEnumerator LoadAsync(string sceneName) {\n        AsyncOperation op = SceneManager.LoadSceneAsync(sceneName);\n        op.allowSceneActivation = false;   // 先不切场景，等进度条走完\n        while (!op.isDone) {\n            float p = Mathf.Clamp01(op.progress / 0.9f);\n            progressBar.value = p;\n            percentText.text = (int)(p * 100) + \"%\";\n            if (op.progress >= 0.9f) {\n                op.allowSceneActivation = true;   // 拉满，切场景\n            }\n            yield return null;\n        }\n    }\n}\n\n这个坑必须讲：LoadSceneAsync 的进度条走到 90% 就卡住，不是 Bug——前 90% 是「资源加载」，最后 10% 是「场景激活」，allowSceneActivation 保持 false 时场景永远不激活、isDone 永远是 false。要等进度条接近满格再放行。\n\n第五步，流程管理：谁管「从哪进、往哪走」？用一个小单例管全局状态：\n\npublic class GameFlow : MonoBehaviour {\n    public static GameFlow Instance;\n    public int levelNumber = 1;\n    void Awake() {\n        if (Instance == null) {\n            Instance = this;\n            DontDestroyOnLoad(gameObject);   // 跨场景存活\n        } else {\n            Destroy(gameObject);\n        }\n    }\n}\n\n场景切换时数据怎么带过去？三条路：\n· static 字段 —— 最简单，关卡编号、玩家名这种轻量数据\n· PlayerPrefs —— 要跨游戏会话保存的（高分、设置，arch-2 学过）\n· DontDestroyOnLoad 的常驻物体 —— 玩家血量、背包这类对象\n\n第六步，返回主菜单：\n胜利画面（project-4 做过）加一个「返回主菜单」按钮，调用 SceneManager.LoadScene(\"MainMenu\")。\n\n第七步，别忘了「游戏结束回菜单再进关」的完整回归：\n· 主菜单 → 开始 → 玩 → 胜利 → 返回菜单 → 再开始。\n· 尤其检查 Time.timeScale：project-4 里胜利时设了 0，回菜单再进关，进关的 Awake/Start 里要记得 Time.timeScale = 1f 复位，否则新游戏一进去就是「冻结」的。\n\n常见坑汇总：\n· 场景没登记进 Build Settings——打包后点按钮没反应/黑屏\n· 进度条卡 90%——allowSceneActivation 没放行\n· 场景切换后旧引用失效——别在脚本里存场景物体的引用，要时现找或挂常驻\n· timeScale 没复位——新一局开局就卡死\n\n一句话总结：多场景 = 场景登记 + SceneManager 切换 + 数据跨场景带过去 + 流程复位；主菜单、加载、返回一条龙跑通，游戏才算「完整」。' },
    { type: 'quiz', question: '用 LoadSceneAsync 做加载进度条，进度走到约 90% 就卡住不动了，最可能的原因和正解是什么？', options: ['场景文件太大，加载本来就慢，多等一会就好', 'allowSceneActivation 设为 false 时场景不会激活、isDone 恒为 false，要在进度到 0.9 后把它设为 true 放行', 'Slider 的 maxValue 设错了，把 Slider 最大值改成 1.0 就行'], answer: 1, tip: '异步加载前 90% 是资源加载，最后 10% 是场景激活。allowSceneActivation = false 会把「激活」卡住，于是 progress 永远停在上限 0.9、isDone 恒 false——这是正常现象，等进度条满格再放行即可。' },
  ],
  'project-6': [
    { type: 'text', title: '音效与打磨', body: '游戏能玩只是「能玩」，这一课把滚球游戏从「功能齐全」磨到「好听的、好看的、手感对的」——音效、特效、手感（Game Feel）三件套。\n\n一、音效：每件事都有声音\n\n基础三件套（project-4 已经放过一枚金币音，这里系统化）：\n· 收集金币 —— 叮一声，清脆短促\n· 胜利 —— 一小段上扬的旋律\n· 菜单按钮 —— 点击、悬停各一个\n\n加声音的姿势：给小球挂 AudioSource，收集时 PlayOneShot。\n\n进阶：用 AudioMixer 把声音分轨（audio-1 学过）。建一个 Mixer：Master 总闸 → Music、SFX 两个子组。背景音乐的 AudioSource 输出到 Music，金币、按钮音效输出到 SFX。\n\n好处立现：音量设置做成两个滑杆，玩家想单独关音乐就单独关——别把所有声音焊死在一个音量上。\n\n音量设置存 PlayerPrefs（arch-2 学过），启动时读回。\n\n二、特效：收集那一瞬间「开花」\n\nproject-4 收集时 Instantiate 一个粒子预制体。进阶版注意性能：\n· 特效生成销毁频繁，用对象池（perf-1 学过）——开局预生成 10 个特效实例，收集时取一个放出来，播完收回\n· 金币收集可以加：迸发的小光点 + 音效 + 得分飘字（UI 上飘起 +1）\n\n三、手感打磨（Game Feel / Juice）：让操作「有反馈」\n\n手感 = 玩家每个操作，游戏都给出及时、明显、有快感的反馈。滚球游戏几个立竿见影的招：\n\n1. 相机抖动 —— 撞到金币时相机轻震一下：\n\npublic class CameraShake : MonoBehaviour {\n    float shakeAmount = 0f;\n    void Update() {\n        if (shakeAmount > 0f) {\n            transform.position += Random.insideUnitSphere * shakeAmount;\n            shakeAmount = Mathf.Lerp(shakeAmount, 0f, Time.deltaTime * 5f);\n        }\n    }\n    public void Shake(float amount = 0.05f) { shakeAmount = amount; }\n}\n\n收集金币时调用：cameraShake.Shake();\n\n2. 相机跟随更顺 —— project-4 的 CameraFollow 是「硬跟」，改成平滑：\ntransform.position = Vector3.Lerp(transform.position, target.position + offset, Time.deltaTime * 5f);\n\n3. 得分飘字 —— 金币处冒一个「+1」，往上飘、淡出\n4. 胜利烟花 —— 全收集那一下，放一组粒子 + 一段胜利音效 + 相机拉近\n\n四、hit stop（命中停顿）—— 高级手感技巧\n\n想做出「撞上那一下整个世界顿住」的打击感，正确做法是：\n\nfloat stopTime = 0.05f;\nTime.timeScale = 0.1f;             // 别设 0，设很小\nStartCoroutine(Resume());\nIEnumerator Resume() {\n    yield return new WaitForSecondsRealtime(stopTime);  // 真实时间计时\n    Time.timeScale = 1f;\n}\n\n注意这里的两个关键：\n· 别把 timeScale 设 0——会连动画、协程、音效一起冻住，难恢复\n· 恢复用 WaitForSecondsRealtime（真实时间），不是 WaitForSeconds——后者会被 timeScale 一起变慢\n\n五、完整验收清单（跑一遍）：\n· 菜单：按钮有点击音、悬停有反馈\n· 游戏：滚有滚声、金币有叮声+飘字+粒子+相机微震\n· 胜利：特效、音效、暂停、返回菜单\n· 音量：音乐、音效两个滑杆独立生效，设置跨重启保留\n· 性能：特效池化后，连吃 20 个金币帧数不掉\n\n一句话总结：音效管「有反应」，特效管「有画面」，手感管「有快感」——三样堆满，游戏立刻从「能玩」变成「好玩」；所有技巧都要配性能意识和验收清单。' },
    { type: 'quiz', question: '做「命中瞬间整个世界顿一下」（hit stop）的打击感，下面哪个做法最稳妥、最不给自己挖坑？', options: ['Time.timeScale = 0，等 0.05 秒再恢复——注意 WaitForSeconds 用的是真实时间，不受影响', 'Time.timeScale 设成很小（如 0.1），恢复用 WaitForSecondsRealtime 按真实时间计时', '把场景里所有 Rigidbody 改成 Kinematic，过几帧再改回来'], answer: 1, tip: 'timeScale=0 会把动画、协程（WaitForSeconds）、音效一起冻住，恢复时机还容易出错；设成极小值配合 WaitForSecondsRealtime（真实时间）是标准 hit stop 做法——既不彻底停摆，又保证能按时恢复。' },
  ],
  'project-7': [
    { type: 'text', title: '发布与迭代', body: '游戏做完整了，最后一步是「见人」——发布、收反馈、迭代。这一课讲发布前后的完整流程和持续迭代的思路。\n\n一、发布前检查清单（发布比做游戏更磨人）：\n· Build Settings —— 所有场景都在列表里且勾选（project-5 的坑，忘了就黑屏）\n· Player Settings —— 公司名、产品名填好，图标换成自己的，启动画面设好（platform-1 学过）\n· 版本号 —— 用一个有意义的版本号，别一直 0.1\n· 隐私政策 —— 上架商店基本都要\n· 性能测试 —— 别只在你的电脑上测：真机、低配机、核显本子都跑一遍（debug-4 的 Profiler 用起来）\n· 完整回归 —— 新玩家视角从头到尾跑一遍，手机、PC 各一遍\n\n二、发布到哪：\n· 最快见人 —— itch.io：网页版（WebGL）或桌面包，免费传，链接一发就能玩\n· 上架商店 —— Steam（PC 走 Steamworks）、App Store、Google Play（platform-4 学过流程）\n· 微信小游戏 / 网页 —— 传播快，但包体限制严\n\n三、收集玩家反馈（发布只是开始）：\n反馈渠道：商店评论、社区/群、问卷、试玩。\n\n但「评论靠不住」——大部分玩家玩到退坑都懒得说话。更可靠的是遥测（Telemetry）：\n· 留存 —— 多少人玩了第 1 关、多少人玩了第 2 关（中途流失的点就是问题点）\n· 关卡通过率 —— 第 3 关通过率突然掉到 30%，那里就有问题\n· 卡点时长 —— 玩家在哪一关卡得最久\n\nUnity 自带 Unity Analytics 或接第三方（GameAnalytics 等）都能埋这些点。\n\n四、数据驱动迭代（最值钱的一环）：\n收到「第一关太难」的反馈，别急着凭感觉削难度——先看数据：\n1. 玩家到底卡在哪一步（第几个金币、哪面墙）\n2. 是「不会操作」还是「难度偏高」\n3. 小步调整：只改一个变量（金币间距、时间限制），发一个版本\n4. 看新数据：通过率上去没有\n\n一次只改一个变量，才能知道「是哪个改动起的效果」——这是迭代的铁律。\n\n五、版本管理（别把项目搞乱）：\n· 语义化版本号 SemVer：x.y.z——修小 Bug 加 z，加小功能加 y，重大变化加 x\n· 每次发布写更新日志（Changelog）：这版改了啥，玩家和队友都看这个\n· git 打 Tag 标记每个发布版，出问题能立刻回到上个版本\n\n六、后续扩展方向（给你的游戏留「下一步」）：\n· 更多关卡 —— 手动做几关，或接 procgen 那套程序化生成（procgen-4 学过）\n· 排行榜 —— 网络同步最高分（net-1 那套客户端/服务器思路）\n· 成就系统、双人模式、皮肤收集\n\n一句话总结：发布不是终点，是迭代的起点——检查清单保底、遥测数据说话、一次改一个变量、小版本快发，好游戏都是这么「磨」出来的。' },
    { type: 'quiz', question: '发布后收到大量「第一关太难」的反馈，最稳妥的迭代动作是什么？', options: ['凭感觉把第一关削得极简单，立刻发新版', '先看遥测数据定位玩家卡在哪一步，只改一个相关变量，小步发版看数据变化', '把第一关整个删掉重做'], answer: 1, tip: '数据驱动的铁律：先定位卡点（通过率/卡点时长），再小步调整（一次只改一个变量），最后用数据验证效果。凭感觉大改或重做，既浪费又不知道是不是改对了。' },
  ],
  'project-8': [
    { type: 'text', title: '扩展：第二关与关卡选择', body: '游戏能跑一个关卡只是「样品」，这节做成「多关卡 + 选关界面」。核心思路：单场景 + 关卡配置数据，加关卡不动代码。\n\n一、两种多关卡方案的对比：\n· 多场景方案 —— 每关一个场景。关卡多了场景文件爆炸、加载切换麻烦、公共代码重复\n· 单场景 + 数据驱动方案（推荐）—— 一个场景，关卡用配置数据描述（金币放哪、时间限制、有几面墙），进关时程序按数据搭出来\n\n二、关卡配置（ScriptableObject，arch-4 复习）：\n\n[CreateAssetMenu(fileName = \"Level\", menuName = \"滚球/关卡配置\")]\npublic class LevelConfig : ScriptableObject {\n    public string levelName;\n    public int coinCount;\n    public float timeLimit = 60f;\n    public int enemyCount;\n    public int obstacleCount;\n    public float coinRange;    // 金币随机生成的范围\n}\n\n每加一关 = 在 Project 里右键新建一个配置资产、填数值。主逻辑零改动——这就是数据驱动的甜头。\n\n三、选关 UI：\n· Canvas 上放一个 ScrollView，里面按「已解锁关卡」生成按钮列表\n· 锁着的关卡按钮显示锁图标且不可点\n· 按钮点击 → 设当前关卡 → 加载游戏场景\n\n四、解锁与进度（PlayerPrefs 存档）：\n\npublic class LevelProgress : MonoBehaviour {\n    public static int UnlockedLevel {\n        get => PlayerPrefs.GetInt(\"UnlockedLevel\", 1);   // 默认第 1 关\n        set => PlayerPrefs.SetInt(\"UnlockedLevel\", value);\n    }\n    public static void OnLevelCleared(int level) {\n        if (level >= UnlockedLevel) UnlockedLevel = level + 1;  // 通关解锁下一关\n    }\n}\n\n通关 → 解锁 → 选关界面刷新 → 玩家可以进新关。\n\n\n\n选关按钮生成（代码版）：\npublic GameObject buttonPrefab; public Transform listRoot;\n\nvoid BuildList() {\n    for (int i = 1; i <= LevelProgress.UnlockedLevel; i++) {\n        var btn = Instantiate(buttonPrefab, listRoot);\n        var i2 = i;   // 闭包要拷贝\n        btn.GetComponentInChildren<Text>().text = "第 " + i + " 关";\n        btn.GetComponent<Button>().onClick.AddListener(() => StartLevel(i2));\n    }\n}\n\n锁着的关卡不生成按钮，或生成后置灰并禁点——玩家点不到的关卡自然进不去。五、进关后的加载流程：\n游戏场景的初始化脚本读当前关卡配置，生成金币/敌人/障碍，设时间限制。\n\n常见坑：\n· 关卡没解锁也能进——选关按钮点击前检查 UnlockedLevel\n· 单场景多关卡，重进时上一关的金币/敌人没清干净——加载关卡前先清理场景动态物体\n· 关卡数据写死代码里，加关要改代码重编译——用 ScriptableObject/JSON\n\n\n\n选关界面的「进度」可以更直观：每关旁边显示获得的星星或最高分，玩家一眼看到自己玩到哪、还有啥没打满。一句话总结：多关卡 = 单场景 + 关卡配置资产 + 选关 UI + 存档解锁；数据驱动让「加一关」变成「新建一个配置文件」，这才是可扩展的游戏结构。' },
    { type: 'quiz', question: '想让「加新关卡」只改配置不动代码，最合适的方案是？', options: ['每加一关复制一个场景、加一套新逻辑', '关卡数据放 ScriptableObject/JSON，单场景按数据生成关卡', '把关卡参数全写死在代码里'], answer: 1, tip: '数据驱动（arch-4）：关卡配置独立成资产，加关=新建配置。写死代码的代价是加关就要改代码、重新编译，扩展性差。' },
  ],
  'project-9': [
    { type: 'text', title: '扩展：敌人与障碍，玩法升级成「躲」', body: '纯收集太单调，这节加敌人和障碍，让滚球从「收集」变成「边躲边收集」。\n\n一、巡逻敌人：\n\npublic class PatrolEnemy : MonoBehaviour {\n    public Transform[] patrolPoints;\n    public float speed = 2f;\n    int index;\n    void Update() {\n        var target = patrolPoints[index];\n        transform.position = Vector3.MoveTowards(transform.position, target.position, speed * Time.deltaTime);\n        if (Vector3.Distance(transform.position, target.position) < 0.1f)\n            index = (index + 1) % patrolPoints.Length;   // 循环巡逻\n    }\n    void OnCollisionEnter(Collision c) {\n        if (c.collider.CompareTag(\"Player\"))\n            c.collider.GetComponent<PlayerHealth>().TakeDamage(1);\n    }\n}\n\n巡逻点在父物体下挂几个子物体摆位置就行。注意：用 Collision（实体碰撞），别用 Trigger——要真的「撞上」才有伤害。\n\n二、玩家血量 + 无敌帧：\n\npublic class PlayerHealth : MonoBehaviour {\n    public int maxHealth = 3;\n    public int health;\n    public float invincibleTime = 1f;\n    float lastHitTime = -999f;\n    void Start() { health = maxHealth; }\n    public void TakeDamage(int dmg) {\n        if (Time.time - lastHitTime < invincibleTime) return;  // 无敌帧\n        lastHitTime = Time.time;\n        health -= dmg;\n        // 受击反馈：闪红、音效、相机微震\n        if (health <= 0) GameOver();\n    }\n}\n\n无敌帧必须做：没有它，撞到敌人的连续帧会「一秒扣光三条命」。\n\n\n\n尖刺和弹跳板的具体写法：\n· 尖刺 —— Collider 勾 Is Trigger，OnTriggerEnter 里给玩家扣血，配合一个旋转/缩放动画更像真刺\n· 弹跳板 —— 触发器碰到就给刚体一个向上冲量：\n\npublic class Bouncer : MonoBehaviour {\n    public float power = 8f;\n    void OnTriggerEnter(Collider other) {\n        if (other.TryGetComponent(out Rigidbody rb))\n            rb.AddForce(Vector3.up * power, ForceMode.Impulse);\n    }\n}\n\n注意用 AddForce + Impulse 走物理通道，别直接改位置——弹起来才有抛物线的真实手感。三、障碍物三兄弟：\n· 尖刺 —— 触发器 + 伤害，碰了扣血\n· 移动平台 —— 正弦往返运动，可以站上去（给小球提供缓冲）\n· 弹跳板 —— 碰到给向上冲量，跳起来躲敌人\n\n移动平台：\n\npublic class MovingPlatform : MonoBehaviour {\n    public float range = 3f, speed = 1f;\n    Vector3 start;\n    void Start() { start = transform.position; }\n    void Update() {\n        transform.position = start + Vector3.right * Mathf.Sin(Time.time * speed) * range;\n    }\n}\n\n四、失败与重开：\n血量归零 → 显示「失败」→ 选项：重试本关 / 回主菜单。重试要重置血量、小球位置、金币状态（别忘了 timeScale 复位，project-5 的坑）。\n\n常见坑：\n· 无敌帧忘了，撞一下秒死——受击后短暂免疫\n· 敌人用 Trigger，球「穿过」敌人没伤害——用 Collision 或 OnTriggerEnter 手动扣血\n· 失败重开没重置血量/场景状态，二次游玩出错\n\n\n\n敌人和障碍都别忘了「视觉提示」——尖刺用醒目的颜色、敌人巡逻路线贴地面画线，玩家看得懂才躲得开，这是关卡引导的一部分（leveldesign-3）。被撞后的反馈要成体系：闪红 + 音效 + 击退，缺一不可，反馈越完整玩家越不觉得「冤枉」。\n\n小贴士：敌人数量从 1 个起步，玩起来顺手了再加——难度曲线别一上来就拉满。一句话总结：敌人（巡逻碰撞）+ 障碍（尖刺/平台/弹跳板）+ 血量无敌帧，滚球从「收集游戏」升级成「躲与收集」；失败重开的场景重置是隐藏大坑。' },
    { type: 'quiz', question: '小球撞到敌人，一帧之内连扣三次血、直接死。最可能缺了什么？', options: ['无敌帧：受击后一段时间内免疫再受击', '敌人太强', '血量上限太低'], answer: 0, tip: '碰撞在连续帧会反复触发，没有无敌帧就等于「一秒扣光」。受击后设免疫窗口（无敌帧）是动作游戏的基础。' },
  ],
  'project-10': [
    { type: 'text', title: '扩展：道具与升级，给玩家成长感', body: '道具是「局内爽感」，升级是「局外成长」。这节两个都做，滚球游戏就有了一层「变强」的循环。\n\n一、道具种类（枚举定义，加新道具=加一个枚举值）：\n\npublic enum PowerUpType { Speed, Magnet, Shield, DoubleScore }\n\n· Speed 加速 —— 5 秒内速度翻倍\n· Magnet 磁铁 —— 附近金币自动吸过来\n· Shield 护盾 —— 抵消一次伤害\n· DoubleScore 双倍 —— 10 秒内金币分数 ×2\n\n二、道具生成与拾取：\n场景里定时/随机刷道具（用一个小生成器），拾取靠触发器：\n\npublic class PowerUpItem : MonoBehaviour {\n    public PowerUpType type;\n    public float lifetime = 15f;\n    void Start() { Destroy(gameObject, lifetime); }   // 没人捡就消失\n    void OnTriggerEnter(Collider other) {\n        if (other.CompareTag(\"Player\")) {\n            other.GetComponent<PlayerPower>().Apply(type);\n            Destroy(gameObject);\n        }\n    }\n}\n\n三、效果管理：计时器到点恢复\n\npublic class PlayerPower : MonoBehaviour {\n    public PlayerController controller;\n    public float speedTimer, doubleTimer;\n    void Update() {\n        if (speedTimer > 0f) {\n            speedTimer -= Time.deltaTime;\n            controller.speed = controller.baseSpeed * 2f;   // 生效中\n        } else {\n            controller.speed = controller.baseSpeed;\n        }\n        if (doubleTimer > 0f) doubleTimer -= Time.deltaTime;\n    }\n    public void Apply(PowerUpType t) {\n        switch (t) {\n            case PowerUpType.Speed: speedTimer = 5f; break;\n            case PowerUpType.DoubleScore: doubleTimer = 10f; break;\n            case PowerUpType.Shield: hasShield = true; break;\n        }\n    }\n}\n\n注意 Update 里每帧检查计时器，到点恢复默认值——道具效果才不会「永久生效」。\n\n\n\n磁铁的实现骨架：\npublic class MagnetPower : MonoBehaviour {\n    public float radius = 5f;\n    void Update() {\n        if (!active) return;\n        foreach (var coin in CoinManager.Instance.ActiveCoins) {\n            Vector3 dir = transform.position - coin.position;\n            if (dir.magnitude < radius) {\n                coin.position = Vector3.MoveTowards(coin.position, transform.position, 10f * Time.deltaTime);\n                if (Vector3.Distance(coin.position, transform.position) < 0.5f)\n                    coin.Collect();   // 吸到身边直接收集\n            }\n        }\n    }\n}\n\nActiveCoins 是场景里还活着的金币列表，比每帧 OverlapSphere 扫全场省得多。四、磁铁实现：\n每帧把周围 N 米内的金币往自己这边拉（或直接判定收集）。用低频扫描 + 缓存避免每帧 OverlapSphere。\n\n五、升级系统（局外成长，金币换）：\n\npublic class ShopSystem : MonoBehaviour {\n    public int speedLevel {\n        get => PlayerPrefs.GetInt(\"SpeedLevel\", 0);\n        set => PlayerPrefs.SetInt(\"SpeedLevel\", value);\n    }\n    public bool TryUpgrade() {\n        int cost = 50 * (speedLevel + 1);        // 越升越贵\n        int gold = PlayerPrefs.GetInt(\"Gold\", 0);\n        if (gold < cost) return false;\n        PlayerPrefs.SetInt(\"Gold\", gold - cost);\n        speedLevel = speedLevel + 1;\n        return true;\n    }\n    public float GetSpeedBonus() => 1f + speedLevel * 0.1f;   // 每级 +10%\n}\n\n升级在结算界面或主菜单做：把「本局赚的金币」加进总金币（PlayerPrefs），再消费升级。\n\n常见坑：\n· 道具效果没计时器，永久生效——每帧检查、到点恢复\n· 磁铁每帧 OverlapSphere 卡——低频扫描\n· 金币只算本局、不存 PlayerPrefs，攒钱升级无从谈起——结算时入账\n\n\n\n道具的「权重」设计别漏：好道具（磁铁、护盾）出现概率低、普通道具高，玩家吃到好道具才有惊喜感。用权重随机：\n\nPowerUpType PickType() {\n    float r = Random.value;\n    if (r < 0.1f) return PowerUpType.Shield;   // 10% 稀有\n    if (r < 0.4f) return PowerUpType.Magnet;\n    return PowerUpType.Speed;                   // 60% 常见\n}\n\n道具图标和文字提示要配对：吃到道具的瞬间，屏幕角落闪一下道具名（「加速中！」），玩家才知道刚才发生了什么——没有反馈的 buff 等于不存在。一句话总结：道具=局内短时强化（计时器管生命周期），升级=局外永久成长（金币换等级、存档保存）；一短一长两个循环叠起来，玩家就有「越来越强」的盼头。' },
    { type: 'quiz', question: '玩家吃了加速道具后，速度永久翻倍、再也回不来。最可能的原因？', options: ['加速计时器没写，效果一直生效', '加速度数值设太大', '道具刷得太频繁'], answer: 0, tip: '道具效果要有生命周期：计时器到点恢复默认值。忘了计时器，就是「永久 buff」——这是道具系统最常见的新手 bug。' },
  ],
  'project-11': [
    { type: 'text', title: '扩展：存档与继续', body: '玩家辛辛苦苦打到第三关，关掉游戏全没了，这体验太糟。这节做存档：记录关卡、金币、血量、检查点，随时能「继续游戏」。\n\n一、存档数据结构（JSON 序列化，arch-2 复习）：\n\n[System.Serializable]\npublic class SaveData {\n    public int level = 1;\n    public int gold;\n    public int health;\n    public int speedLevel;\n    public int unlockedLevel = 1;\n    public float[] playerPos = new float[] { 0f, 0.5f, 0f };   // Vector3 转 float[]\n    public bool[] achievements;      // 成就解锁状态（project-14 用）\n}\n\n二、存档管理器（单例）：\n\npublic class SaveManager : MonoBehaviour {\n    public static SaveManager Instance;\n    public SaveData data = new SaveData();\n    const string Key = \"RollBallSave\";\n    void Awake() { Instance = this; Load(); }\n\n    public void Save() {\n        data.playerPos = new float[] { player.position.x, player.position.y, player.position.z };\n        string json = JsonUtility.ToJson(data);\n        PlayerPrefs.SetString(Key, json);\n        PlayerPrefs.Save();          // 别忘了！不调可能没落盘\n    }\n    public void Load() {\n        if (PlayerPrefs.HasKey(Key))\n            data = JsonUtility.FromJson<SaveData>(PlayerPrefs.GetString(Key));\n    }\n}\n\n三、检查点（Checkpoint）：\n\nvoid OnTriggerEnter(Collider other) {\n    if (other.CompareTag(\"Checkpoint\")) {\n        SaveManager.Instance.Save();        // 过点存档\n        other.GetComponent<Checkpoint>().Activate();  // 亮灯反馈\n    }\n}\n\n玩家死亡后重开，从「最后过的检查点」复活，而不是整关重来——这是动作游戏的体贴。\n\n\n\n新游戏 = 清档：\npublic void NewGame() {\n    PlayerPrefs.DeleteKey("RollBallSave");\n    PlayerPrefs.SetInt("UnlockedLevel", 1);\n    PlayerPrefs.Save();\n    SaveManager.Instance.data = new SaveData();\n    StartGame();\n}\n\n继续游戏 = 读档进关：有档就按 SaveData.level 进对应关，再恢复位置血量。\n\n另外给 SaveData 加个 version 字段（存档版本号），以后结构改了能识别旧档做迁移，不会读崩。四、主菜单三选项：\n· 继续游戏 —— 有档就进最后一关，恢复位置血量\n· 新游戏 —— 清档重来\n· 关卡选择 —— 已解锁关随便进\n\n五、读档恢复：\n进游戏场景后，把 SaveData 填回玩家：位置、血量、金币、升级等级。\n\n常见坑：\n· JsonUtility 不能直接序列化 Vector3——转 float[] 或自定义可序列化类\n· 忘了 PlayerPrefs.Save()，存档只在内存——调 Save() 强制落盘\n· 读档后关卡动态物体状态对不上（金币没重置、敌人位置不对）——关卡初始化时按存档重建\n\n\n\n检查点的摆放有讲究：别太密（每 10 秒一个就没挑战）也别太稀（死一次重打 5 分钟很劝退）——按「玩家操作节奏」放，通常一个关卡 2~4 个检查点合适。\n\n存档的坑再补一个：PlayerPrefs 在真机上延迟落盘，退出游戏瞬间保存可能丢——关键存档点调 PlayerPrefs.Save() 强制写入，或直接写文件（File.WriteAllText 到 Application.persistentDataPath）更稳。\n\n读档失败的兜底：找不到存档或 JSON 解析失败时，弹「存档损坏，是否重开新档」——别让玩家卡在白屏。一句话总结：存档 = 可序列化的 SaveData + SaveManager 读写 + 检查点自动存 + 主菜单继续/新游戏；JSON + PlayerPrefs 是最低门槛的持久化方案。' },
    { type: 'quiz', question: '用 JsonUtility 存存档，发现 Vector3 类型字段丢了没序列化。正解是？', options: ['多调用几次 Save 就好', 'Vector3 不是 JsonUtility 支持的类型，要转成 float[] 或自定义可序列化类', '换用 PlayerPrefs.SetFloat 单独存三个数就行'], answer: 1, tip: 'JsonUtility 只处理它认识的类型，Vector3 得转成 float[] 或写个 [Serializable] 的坐标类。这是 Unity 存档最常见的坑之一。' },
  ],
  'project-12': [
    { type: 'text', title: '扩展：设置面板，音量/画质/键位', body: '一个正经游戏得有「设置」：音量能调、画质能选、键位能改，而且关了游戏再打开还记着。这节全做，用 PlayerPrefs 存（arch-2 复习）。\n\n一、音量（AudioMixer 分轨 + 暴露参数）：\n先建 AudioMixer：Master → Music / SFX 两个子组（audio-1 复习），把音乐和音效的 AudioSource 分别输出到两组。\n\n在 Mixer 的 Music 组上右键音量 → Expose to script，记下参数名（如 MusicVolume）：\n\npublic class SettingsManager : MonoBehaviour {\n    public AudioMixer mixer;\n    void Start() {\n        // 读存档，把 dB 值设回 Mixer\n        mixer.SetFloat(\"MusicVolume\", PlayerPrefs.GetFloat(\"MusicVolume\", 0f));\n        mixer.SetFloat(\"SfxVolume\", PlayerPrefs.GetFloat(\"SfxVolume\", 0f));\n        QualitySettings.SetQualityLevel(PlayerPrefs.GetInt(\"Quality\", 2), true);\n    }\n    public void SetMusic(float db) {\n        mixer.SetFloat(\"MusicVolume\", db);\n        PlayerPrefs.SetFloat(\"MusicVolume\", db);\n    }\n    public void SetSfx(float db) {\n        mixer.SetFloat(\"SfxVolume\", db);\n        PlayerPrefs.SetFloat(\"SfxVolume\", db);\n    }\n}\n\n大坑：Mixer 音量单位是分贝（dB），范围约 -80（静音）~ 0（最大）。UI 滑杆是 0~1 的话，要换算：\n\nfloat sliderValue;   // 0~1\nfloat db = Mathf.Log10(Mathf.Clamp(sliderValue, 0.0001f, 1f)) * 20f;  // 转 dB\n\n二、画质（QualitySettings）：\nEdit → Project Settings → Quality 里配好几档（低/中/高）。代码切换：\n\npublic void SetQuality(int index) {\n    QualitySettings.SetQualityLevel(index, true);\n    PlayerPrefs.SetInt(\"Quality\", index);\n}\n\n下拉框选项索引要和 Quality 列表顺序对得上。\n\n三、键位重映射（新版 Input System）：\nInput System 自带重绑定：\n\nInputAction action;   // 配置好的动作\nvar op = action.PerformInteractiveRebinding()\n    .WithControlsExcluding(\"Mouse\")\n    .OnComplete(cb => {\n        string json = action.SaveBindingOverridesAsJson();  // 存覆盖\n        PlayerPrefs.SetString(\"Bindings\", json);\n        cb.Dispose();\n    })\n    .Start();\n\n启动时读回：\naction.LoadBindingOverridesFromJson(PlayerPrefs.GetString(\"Bindings\"));\n\n\n\n键位重绑的入口 UI：\n设置面板放一个「重绑移动键」按钮，点击后进入等待状态，提示玩家按新键；收到 InputAction 回调后把 JSON 存 PlayerPrefs，下次启动读回即可。\n\n启动恢复的完整姿势：\nvoid Start() {\n    string json = PlayerPrefs.GetString("Bindings", "");\n    if (!string.IsNullOrEmpty(json))\n        action.LoadBindingOverridesFromJson(json);\n}\n\n这样键位改动即时生效、重开保留，设置面板就闭环了。四、UI 组织：设置面板一个 Canvas，音量滑杆、画质下拉、键位按钮，改动即生效即存档。\n\n常见坑：\n· 滑杆 0~1 直接赋给 Mixer，音量曲线全乱——换算 dB\n· 画质索引和 Quality 列表对不上，选「高」实际还是低——下拉选项按列表顺序\n· 键位重绑定没存档，重开还原——Save/Load override JSON\n\n\n\n音量设置的另一个细节：给玩家一个「静音」总开关，存 PlayerPrefs 并在 Start 时应用（AudioListener.volume = 0 一键全静）。画质选项切换是即时生效的，改完让玩家看到画面变化再存档，别「选完不生效」让玩家以为坏了。\n\n设置面板记得放「恢复默认」按钮：一键把音量、画质、键位全部复位——玩家折腾坏了有退路，少一堆客服问题。一句话总结：设置面板 = AudioMixer dB 换算 + QualitySettings 画质 + Input System 键位重绑，全用 PlayerPrefs 持久化；每个改动「即时生效 + 立即存档」是标准做法，配一个「恢复默认」收尾，设置系统就完整闭环了。' },
    { type: 'quiz', question: 'UI 音量滑杆（0~1）直接传给 AudioMixer 的暴露参数，声音忽大忽小很怪。为什么？', options: ['因为 Mixer 参数是分贝（dB），要把 0~1 换算成合适的 dB 范围再传', '因为 AudioSource 音量上限太低', '因为滑杆更新太慢'], answer: 0, tip: 'Mixer 参数单位是 dB（-80~0）。直接把 0~1 线性值塞进去，大部分区间没声音、尾部骤变。用 Mathf.Log10 换算成 dB 曲线才对。' },
  ],
  'project-13': [
    { type: 'text', title: '扩展：排行榜与成绩', body: '「我最高分多少？」是玩家重开游戏的动力之一。这节做成绩记录和排行榜：本地 TopN + 数据展示。\n\n一、成绩记录什么：\n· 总分、收集金币数、用时、关卡\n记录设计要「多维度」——只记一个分数，信息太少。\n\n二、本地排行榜（PlayerPrefs 存 TopN）：\n\npublic class Leaderboard : MonoBehaviour {\n    public int maxEntries = 5;\n    List<int> scores = new List<int>();\n\n    void Start() {\n        for (int i = 0; i < maxEntries; i++)\n            scores.Add(PlayerPrefs.GetInt(\"Score\" + i, 0));\n    }\n    public void Submit(int newScore) {\n        scores.Add(newScore);\n        scores.Sort((a, b) => b.CompareTo(a));        // 降序\n        if (scores.Count > maxEntries) scores.RemoveRange(maxEntries, scores.Count - maxEntries);\n        for (int i = 0; i < scores.Count; i++)\n            PlayerPrefs.SetInt(\"Score\" + i, scores[i]);\n        PlayerPrefs.Save();\n    }\n    public List<int> GetTop() => scores;\n}\n\n\n\n「新纪录」判定：\n在 Submit 前先取旧 Top1：\n\nint oldTop = scores.Count > 0 ? scores[0] : 0;\nif (newScore > oldTop) ShowNewRecord(newScore);\n\n结算界面的流程：通关 → 算成绩（分数/金币/用时）→ Submit 进排行 → 弹「新纪录」→ 刷新榜单 UI。\n\n本地 TopN 是「死数据」：存进 PlayerPrefs 的就是快照，升级游戏也不丢——这正是它适合做本地成绩的原因。三、展示 UI：\n结算界面弹「新纪录！」（分数 > 当前 Top1），主菜单的排行榜面板列出 Top5：名次、分数、关卡、日期。\n\n四、网络排行榜（进阶）：\n· 自建服务器 —— 客户端 POST 分数，服务器存库、返回排行（net-1 的客户端/服务器思路）\n· 现成服务 —— Unity Gaming Services 的 Leaderboards、第三方（如 PlayFab）\n\n网络版要注意：提交失败要重试、要防作弊（分数服务器端校验）、成绩带玩家 ID。\n\n五、和存档的关系：\n排行榜数据和存档分开存：存档是「玩家状态」（进度），排行榜是「成绩历史」。别混在一个结构里。\n\n常见坑：\n· 只记最高分一个数，没有关卡/日期，展示单薄——多字段\n· 本地和网络排行混用逻辑混乱——分清「本机 TopN」和「全网排行」\n· 网络提交失败静默丢弃——失败队列重试\n\n\n\n排行榜的「成绩」和「排名反馈」要分开：成绩是客观数字（第 3 关、120 分），排名是相对（超过几条旧纪录）。给「超过第几名」的反馈，比只列个列表更有成就感——玩家知道自己进步了，才愿意继续冲分。\n\n排行榜数据记得在场景加载时刷新一次，别用缓存的旧榜——成绩改了要立刻看得见。一句话总结：排行榜 = 定成绩维度 + 本地 TopN 存档 + 结算界面上榜；想联机再走服务器，但先把手上的「本机排行榜」做扎实。' },
    { type: 'quiz', question: '只想做「玩家自己机器的最高分前 5 名」，不上传网络。最省事的存储和做法是？', options: ['PlayerPrefs 存一个有序的 TopN 列表，新分插入排序，读出来展示', '必须自己搭服务器', '存到代码里写死'], answer: 0, tip: '本地排行榜就是「有序数组 + PlayerPrefs 持久化」：新分插入、降序排列、截断到 N、存回去。网络排行才需要服务端。' },
  ],
  'project-14': [
    { type: 'text', title: '扩展：成就系统，多一层「目标外的目标」', body: '成就是给玩家的「额外目标」：收集 100 个金币、无伤过一关、5 秒通关……让玩家在主线之外多一份追求，还多一个炫耀的由头。\n\n一、成就定义（ScriptableObject 配置）：\n\n[CreateAssetMenu(fileName = \"Achievement\", menuName = \"滚球/成就\")]\npublic class Achievement : ScriptableObject {\n    public string id;\n    public string title;\n    [TextArea] public string desc;\n    public Sprite icon;\n    public int target;            // 目标值，如 100\n    public bool hidden;           // 隐藏成就，解锁前不显示\n}\n\n成就表做成「一堆配置资产」，加成就 = 新建资产，代码零改动。\n\n二、成就管理器（事件驱动）：\n\npublic class AchievementManager : MonoBehaviour {\n    public static AchievementManager Instance;\n    public List<Achievement> all;\n    Dictionary<string, int> progress = new Dictionary<string, int>();\n    Dictionary<string, bool> unlocked = new Dictionary<string, bool>();\n\n    void Awake() {\n        Instance = this;\n        foreach (var a in all)   // 读存档\n            unlocked[a.id] = PlayerPrefs.GetInt(\"Ach_\" + a.id, 0) == 1;\n    }\n\n    public void AddProgress(string id, int amount) {\n        if (unlocked.ContainsKey(id) && unlocked[id]) return;   // 已解锁不重复\n        progress[id] = progress.GetValueOrDefault(id) + amount;\n        var ach = all.Find(a => a.id == id);\n        if (ach != null && progress[id] >= ach.target)\n            Unlock(id);\n    }\n\n    void Unlock(string id) {\n        unlocked[id] = true;\n        PlayerPrefs.SetInt(\"Ach_\" + id, 1);\n        PlayerPrefs.Save();\n        var ach = all.Find(a => a.id == id);\n        ShowToast(\"成就解锁：\" + ach.title);   // 弹通知\n    }\n}\n\n\n\n无伤过关这种「条件成就」怎么做：\n关键在「标记 + 清零」——受击时把标记置 false，通关时检查：\n\npublic bool noHit;\nvoid OnTriggerEnter(Collider c) { if (c.CompareTag("Enemy")) noHit = false; }\nvoid OnLevelClear() {\n    if (noHit) AchievementManager.Instance.AddProgress("noHit", 1);\n}\n\n注意：标记要在每次开局重置为 true，否则上一局的受击会带进下一局。三、接入事件（解耦）：\n· 金币收集时：AchievementManager.Instance.AddProgress(\"coin\", 1)\n· 关卡通关时：AddProgress(\"clear\", 1)\n· 无伤过关：在失败逻辑里把「无伤标记」清零，通关时检查\n\n四、通知 UI：\n屏幕角落弹出成就卡片：图标 + 名字，1~2 秒后淡出。用协程排队，别同时弹一堆。\n\n五、成就界面：\n主菜单放「成就」入口，列出所有成就：已解锁（亮）、未解锁（灰 + 锁）、隐藏（问号）。\n\n常见坑：\n· 成就不存档，重开清零重弹——解锁状态写 PlayerPrefs\n· 已解锁还重复触发——先查 unlocked\n· 条件写死在代码里难维护——配置 target，事件驱动\n\n\n\n成就通知的体验细节：弹出时给个音效和轻微缩放动画，别让通知挡住关键 UI；同屏最多一条，多条排队。成就列表的排序也有讲究：把「接近达成」的放前面（进度 80% 那种），给玩家「再玩一下就能解锁」的钩子。\n\n成就系统接入「第一次通关」这类里程碑时，注意别和教程冲突——教程关卡不触发成就，防刷，也避免新手一脸懵。一句话总结：成就 = 配置资产定义 + 管理器事件驱动解锁 + 通知 + 界面；解锁状态持久化、已解锁去重、事件接入游戏各处，成就系统就立住了。' },
    { type: 'quiz', question: '成就「收集 100 金币」第一次达成弹出后，第二次又弹了。必须修哪一步？', options: ['解锁后标记已解锁，每次 AddProgress 先查已解锁就返回', '把金币目标改成 1000', '把通知关了'], answer: 0, tip: '成就是「一次性」：解锁状态先查，已解锁直接 return；同时状态要存 PlayerPrefs，否则重开游戏它还会再弹一遍。' },
  ],
  'project-15': [
    { type: 'text', title: '扩展：本地双人，一台电脑分屏对战', body: '一个人玩的是收集，两个人玩的就是竞技。这节加本地双人模式：一台电脑、一块键盘/一个手柄，分屏抢金币。\n\n一、双人玩法设计（最简单有效）：\n同一场地，两个小球各占一个出生点，比赛谁先收集满 N 个金币。金币一局一刷新，抢到才是自己的。\n\n二、分屏：两个相机各管一半屏幕\n\npublic class SplitScreen : MonoBehaviour {\n    public Camera cam1, cam2;\n    void Start() {\n        cam1.rect = new Rect(0f, 0f, 0.5f, 1f);    // 左半屏\n        cam2.rect = new Rect(0.5f, 0f, 0.5f, 1f);  // 右半屏\n    }\n}\n\nViewport Rect 的四个数：x、y（左下角）、宽、高（都是 0~1 比例）。忘了设，两个相机叠一起全屏。\n\n三、输入区分：\n老 Input Manager：Project Settings → Input 里自带轴只有一套（Horizontal/Vertical）。双人需要第二套：\n\npublic class PlayerController2 : MonoBehaviour {\n    public string axisX, axisZ;      // 检查器里配：P1 用 Horizontal/Vertical，P2 用 Horizontal2/Vertical2\n    Rigidbody rb;\n    void Update() {\n        float x = Input.GetAxis(axisX);\n        float z = Input.GetAxis(axisZ);\n        rb.AddForce(new Vector3(x, 0f, z) * speed);\n    }\n}\n\n在 Input Manager 里复制一套轴改名为 Horizontal2/Vertical2，键位设为 IJKL 或小键盘。新版 Input System 用两套 Action Map 或两个 Player Input 组件更干净。\n\n\n\n双人结算逻辑：\n每枚金币被吃时，判断收集者是谁：\n\nif (other.CompareTag("P1")) p1Score++;\nelse if (other.CompareTag("P2")) p2Score++;\nif (p1Score >= target || p2Score >= target)\n    ShowResult(p1Score > p2Score ? "P1 胜利" : "P2 胜利");\n\n金币归属靠 Tag 区分玩家，别用 FindObjectOfType 找玩家——两个玩家两个 Tag，直接对号入座。四、双人 UI：\n· 屏幕两侧各显示一个玩家的分数\n· 计时/金币上限提示居中\n· 结算：谁先满谁赢，或时间到比分数\n\n五、菜单入口：\n主菜单加「双人模式」按钮，切到双人场景；或「单人/双人」开关决定生成几个玩家。\n\n常见坑：\n· 两个相机 Viewport 没设，画面叠一起——各占半屏\n· 两人共用一套输入，一起动——配第二套轴\n· 分屏渲染开销翻倍，低配卡——降低分屏画质、减少特效\n\n\n\n双人模式的相机还有个升级：跟随各自玩家的同时，把两个玩家都框进画面（相机取两个球的中点，按距离拉开）。新手做「固定分屏」最稳，进阶再考虑动态取景。\n\n双人模式建议先做「同屏比分数」，再考虑「争抢同一批金币」——后者更有竞技性，但也更容易出判定纠纷。一句话总结：本地双人 = 分屏相机（Viewport Rect 各半）+ 两套输入 + 抢金币规则 + 双人 UI；一台电脑变成「对战擂台」，是最划算的多人模式。' },
    { type: 'quiz', question: '两个相机都渲染了全屏，画面完全叠在一起。问题出在哪？', options: ['相机没设 Viewport Rect，两个都占满全屏', '相机数量超过 1', '没开 HDR'], answer: 0, tip: '分屏就是给每个相机设 Viewport Rect（左半/右半），各渲染一块屏幕区域。默认 0~1 全屏，不设就叠一起。' },
  ],
  'project-16': [
    { type: 'text', title: '扩展：手机适配，触屏 + 竖屏', body: 'PC 版做好，顺手出手机版？这节把滚球游戏适配到手机：触屏操作、竖屏锁定、UI 自适应。\n\n一、触屏输入：虚拟摇杆\n手机没有方向键，做一块「虚拟摇杆」——手指按下拖动，方向映射成移动：\n\npublic class VirtualJoystick : MonoBehaviour {\n    public RectTransform knob;      // 摇杆头\n    public PlayerController controller;\n    Vector2 origin; bool touching;\n\n    void Update() {\n        if (Input.touchCount > 0) {\n            Touch t = Input.GetTouch(0);\n            if (t.phase == TouchPhase.Began) { origin = t.position; touching = true; }\n            else if (t.phase == TouchPhase.Ended || t.phase == TouchPhase.Canceled) {\n                touching = false; knob.anchoredPosition = Vector2.zero;\n            }\n            if (touching) {\n                Vector2 delta = (t.position - origin);\n                Vector2 clamped = Vector2.ClampMagnitude(delta, 100f);\n                knob.anchoredPosition = clamped;                     // 摇杆头跟着手指\n                Vector3 dir = new Vector3(clamped.x, 0f, clamped.y) / 100f;\n                controller.Move(dir);                                 // 传给玩家\n            }\n        }\n    }\n}\n\n触屏坐标是屏幕像素，要先归一化（除以摇杆半径）再当方向用。\n\n二、竖屏锁定：\n\nScreen.orientation = ScreenOrientation.Portrait;   // 锁竖屏\n\n在启动脚本里设一次即可；横竖屏乱转会毁掉 UI 布局。\n\n三、UI 适配（Canvas Scaler + 安全区）：\n· Canvas 的 Canvas Scaler 设成 Scale With Screen Size，参考分辨率选 1080×1920，Match 调 0.5——UI 自动缩放\n· 锚点布局（art-2 复习）：按钮钉角落，摇杆钉左下\n· 安全区（Safe Area）：刘海屏/挖孔屏会挡住 UI，用 Screen.safeArea 收缩布局：\n\nRect safe = Screen.safeArea;\nRectTransform rt = GetComponent<RectTransform>();\nrt.offsetMin = safe.position;\nrt.offsetMax = safe.position + safe.size - new Vector2(Screen.width, Screen.height);\n\n\n\n触屏的另一个大坑：多点触控。\nInput.GetTouch(0) 只取第一根手指，两根手指一起按会乱。做摇杆时锁死「第一根按下的手指」，后续变化只跟最初那根走：\n\nint fingerId = -1;\nif (t.phase == TouchPhase.Began && fingerId == -1) fingerId = t.fingerId;\nif (t.fingerId == fingerId) { /* 才处理 */ }\n\n性能上手机帧率建议锁 60（Application.targetFrameRate = 60），省电不发烫；实时阴影在手机上先关掉再看帧数。四、性能注意：\n· 手机发热降频 = 掉帧——限制帧率 Application.targetFrameRate = 60\n· 少用实时阴影、多后处理——手机上先关掉再测\n· 用 Profiler 的 Development Build 连真机测（debug-4 复习）\n\n常见坑：\n· 触屏坐标没归一化，方向乱飞——除以摇杆半径\n· 忘了锁竖屏，横竖乱转 UI 错位——启动时设 Portrait\n· UI 被刘海屏挡住——用 safeArea 收缩\n\n\n\nUI 适配再补一个高频坑：Canvas Scaler 的 Match 值影响「宽高哪个优先」——竖屏游戏 Match 偏 1（高度优先），横屏偏 0（宽度优先），调反了在不同比例手机会拉伸变形。安全区代码要在 Canvas 初始化后跑，别忘了。\n\n真机预览的优先级高于一切：模拟器/编辑器看着正常的 UI，真机一看可能就裂——手机适配每改一步都要真机验证。一句话总结：手机适配 = 虚拟摇杆（触屏归一化）+ 竖屏锁定 + Canvas Scaler 缩放 + 安全区；性能按手机标准压，真机验证为准。' },
    { type: 'quiz', question: '手机切横竖屏后 UI 错位、按钮跑出屏幕，最基础的解决是？', options: ['给 Canvas 配 Canvas Scaler（Scale With Screen Size），再配合锚点布局', '把所有 UI 固定像素', '换更大的图'], answer: 0, tip: 'Canvas Scaler 让 UI 按参考分辨率缩放适配不同屏幕，锚点决定贴哪。手机不同机型千差万别，这套是标配。' },
  ],
  'project-17': [
    { type: 'text', title: '扩展：手柄支持，插上就能玩', body: 'PC 玩家一大半用手柄。这节让滚球游戏支持手柄，键盘手柄自由切换，顺带做震动反馈。\n\n一、老 Input Manager 的手柄轴：\n老 Input 的 Horizontal/Vertical 轴默认就映射了手柄左摇杆——所以现有代码通常「插上手柄就能动」。\n\nfloat h = Input.GetAxis(\"Horizontal\");   // 键盘 WASD + 手柄摇杆都吃\n\n如果没反应，检查 Project Settings → Input：对应轴的 Joystick 映射、以及有没有勾上「手柄已连接」。\n\n二、新版 Input System（更推荐）：\n用 Input Actions 配一套动作，同一动作挂多个 Binding（键盘 + 手柄）：\n\n// 生成的 InputActions.cs（在 Inspector 里配好）\npublic class PlayerInput : MonoBehaviour {\n    public void OnMove(InputAction.CallbackContext ctx) {\n        Vector2 v = ctx.ReadValue<Vector2>();\n        rb.AddForce(new Vector3(v.x, 0f, v.y) * speed);\n    }\n}\n\n键盘手柄自动识别：玩家按手柄，系统走手柄 Binding；按键盘，走键盘 Binding。这就是新版 Input 的意义。\n\n三、震动反馈：\n\npublic void Vibrate(float low, float high, float duration) {\n    var pad = Gamepad.current;                    // UnityEngine.InputSystem\n    if (pad == null) return;\n    pad.SetMotorSpeeds(low, high);                // 左右马达\n    StartCoroutine(StopAfter(duration));\n}\nIEnumerator StopAfter(float t) {\n    yield return new WaitForSecondsRealtime(t);\n    if (Gamepad.current != null) Gamepad.current.SetMotorSpeeds(0f, 0f);   // 别忘了停\n}\n\n震动忘停 = 手柄一直抖，玩家第一反应就是「这游戏有毒」。\n\n四、键位表：\n做进设置面板（project-12 的键位重映射扩展）：每个动作列出来，显示当前绑定、支持手柄按键重绑。\n\n\n\n新版 Input 的配置步骤（一次配好，键盘手柄通吃）：\n1. 右键 Create → Input Actions，建一套 Action Map（如 Gameplay）\n2. 加动作 Move（Value → Vector2），给它加两个 Binding：键盘 WASD 和手柄左摇杆\n3. 勾选 Auto-save，回场景给玩家挂 Player Input 组件，Behavior 选 Invoke Unity Events\n4. 在 Inspector 里把 OnMove 事件拖到移动脚本的方法上\n\n之后插键盘走键盘、插手柄走手柄，什么都不用写。五、UI 导航：\n手柄玩家点不了鼠标——UI 按钮支持手柄导航：EventSystem 默认支持，确保按钮在导航链上（UI 编辑器里可调 Navigation）。\n\n常见坑：\n· 老 Input 手柄轴名拼错没反应——检查 Joystick 映射\n· 新版 Input 没配 Action/Binding，手柄无响应——去 Input Actions 里绑\n· 震动忘了停，手柄一直抖——设个协程按时归零\n\n\n\n手柄的「键位提示」也要适配：检测到当前设备是手柄时，UI 上的提示图标自动从「WASD」换成「摇杆」——玩家才不会困惑。\n\n手柄震动别在 Update 里每帧 SetMotorSpeeds——设成一次性的触发即可，每帧设反而可能让马达抽搐。一句话总结：手柄支持 = 键盘手柄双绑定（新版 Input 自动识别）+ 震动反馈（用完要停）+ UI 导航；让玩家「插上就玩」是 PC 游戏的体面。' },
    { type: 'quiz', question: '新版 Input System 下想让键盘和手柄都能控制移动，正确姿势是？', options: ['在同一个 Input Action 上同时绑键盘和 Gamepad 的 Binding，系统按可用设备自动走对应绑定', '手柄玩家必须先手动设置键位', '新版 Input System 不支持手柄'], answer: 0, tip: '一个动作多个 Binding：键盘一组、手柄一组，插什么用什么，自动识别——这就是新版 Input System 的设计初衷。' },
  ],
  'project-18': [
    { type: 'text', title: '打磨：把扩展整合成完整游戏', body: '八节扩展做完，各有各的「零件」。这节把它们拧成一台整机：关卡选择、敌人、道具、存档、设置、成就、排行榜，全都要协作，还要能发布。\n\n一、全局架构（各管各的，靠事件解耦）：\n\nGameManager（单例，跨场景）\n├── SaveManager —— 存档读写\n├── SettingsManager —— 音量/画质/键位\n├── AchievementManager —— 成就\n├── Leaderboard —— 排行榜\n└── 关卡流程：选关 → 加载 → 结算 → 回选关\n\n关键：各子系统之间别互相直接调用成网状——用事件广播（prog-5 复习）。\n\n二、整合核心事件流（以「收集金币」为例）：\n金币被吃 → 发一个 OnCoinCollected 事件 → 同时响应：\n· 加分（得分 UI）\n· 成就进度（AchievementManager）\n· 本局统计（结算用）\n· 音效/特效\n\n金币脚本只广播，不知道也不关心谁在听——加一个新系统想听金币，加一个订阅就行，金币代码不用动。\n\n三、场景与菜单整合：\n主菜单一个界面收纳所有入口：开始（继续/新游戏）、关卡选择、设置、排行榜、成就、双人模式。\n\n四、数据流检查（最常断的环节）：\n· 存档贯穿：关卡解锁、金币、升级、成就、设置，全走 SaveManager/SettingsManager\n· 通关结算：成绩进排行榜、金币入账、解锁下一关、成就判定——一条线跑完\n· 失败结算：扣血量/重试，存档不乱写\n\n五、完整回归清单（发布前逐项跑）：\n□ 新游戏 → 第一关 → 收集/被敌人打 → 胜利/失败 → 结算 → 回主菜单\n□ 继续游戏能恢复到检查点\n□ 设置改动生效且重启保留\n□ 成就解锁一次，界面状态正确\n□ 排行榜上榜正确\n□ 双人/手机/手柄模式可用\n□ 发布构建（Development Build）跑通全流程\n\n常见坑：\n· 各子系统直接互相 new/GetComponent，耦合成一团——事件解耦\n· 存档格式改了旧档读崩——SaveData 加版本号\n· 整合完只测新功能不回归——全清单跑一遍\n\n一句话总结：整合 = 架构分层 + 事件解耦 + 主菜单收纳 + 数据流打通 + 回归清单；把「八个零件」磨成「一台机器」，这游戏才算做完。' },
    { type: 'quiz', question: '金币被吃要同时「加分、推进成就、刷新排行、存进度」，金币脚本里最干净的做法？', options: ['在金币脚本里挨个调用四个系统的方法', '金币只发一个「金币收集」事件，四个系统各自订阅响应', '让四个系统每帧轮询金币数量'], answer: 1, tip: '事件解耦：金币只管广播，谁需要谁订阅。要加第五个响应的系统，加一个订阅就行，金币代码零改动。' },
  ],
  'project-19': [
    { type: 'text', title: '扩展：接入程序化生成关卡', body: '手动搭关卡到第十关就开始腻了。这节用 procgen 那套（随机 + 种子）自动生成关卡，让「无限关卡」成为可能。\n\n一、核心：种子可复现\n\nRandom.InitState(seed);   // 重置随机序列\n\n同一 seed，之后所有 Random 调用都产生同一串「随机数」→ 生成同一关。这就是「可复现关卡」的钥匙。\n\n二、关卡生成器：\n\npublic class LevelGenerator : MonoBehaviour {\n    public GameObject coinPrefab, enemyPrefab, obstaclePrefab;\n    public int seed;\n    public float spawnRange = 8f;\n\n    void Awake() {\n        Random.InitState(seed);\n        Generate();\n    }\n\n    void Generate() {\n        // 金币：随机位置铺 20 个\n        for (int i = 0; i < 20; i++) {\n            Vector3 pos = RandomPoint();\n            if (!IsReachable(pos)) continue;        // 不可达就跳过\n            Instantiate(coinPrefab, pos, Quaternion.identity);\n        }\n        // 敌人、障碍同理，数量由关卡配置决定\n        for (int i = 0; i < 3; i++)\n            Instantiate(enemyPrefab, RandomPoint(), Quaternion.identity);\n    }\n\n    Vector3 RandomPoint() {\n        Vector3 p = new Vector3(Random.Range(-spawnRange, spawnRange), 0.5f, Random.Range(-spawnRange, spawnRange));\n        if (Vector3.Distance(p, Vector3.zero) < 2f) return RandomPoint();   // 别刷在出生点\n        return p;\n    }\n}\n\n\n\n可达性检查的实用做法：\n用 NavMesh.SamplePosition 确认位置在网格上，再向下 Raycast 确认没悬空；更省事的保底策略是「验证+重试」：\n\nVector3 FindReachablePoint() {\n    for (int i = 0; i < 10; i++) {\n        Vector3 p = RandomPoint();\n        if (NavMesh.SamplePosition(p, out var hit, 0.5f, NavMesh.AllAreas))\n            return hit.position;\n    }\n    return Vector3.zero;   // 兜底：出生点附近\n}\n\n万一 10 次都找不到可达点就换种子重生成，保证「至少 N 枚金币可达」。三、防「坑爹关」的检查：\n· 出生点周围别刷——留安全区\n· 金币别刷进墙里/不可达——用 NavMesh 可达性检查或射线落点校验\n· 保底：至少 N 个金币可达，否则换种子重生成\n\n四、种子怎么来：\n· 选关 UI 里「新随机关」→ 随机一个种子\n· 种子存进存档（PlayerPrefs）——重进同一关用同一种子，关卡不变\n· 好友分享：种子号贴出去，对方输入同一个种子，玩同一关（文字版「关卡码」）\n\n五、和 project-8 的关卡配置结合：\nLevelConfig 里加 seed 字段：\n· 手工关：seed 写死 + 预置特殊物体\n· 随机关：seed 随机，生成器负责全布局\n\n常见坑：\n· 忘调 Random.InitState，每次进关布局都不一样——生成前必调\n· 生成到出生点/墙里，玩家开局被卡——出生点保护 + 可达性检查\n· 种子没存档，重开换关——存档记录 seed\n\n\n\n种子分享的另一层价值：关卡「排行榜」可以按种子排——同一关比谁分高，程序化关卡也有了竞技维度。一句话总结：程序化关卡 = 种子可复现 + 生成器按数据铺 + 可达性校验 + 种子存档；配合 project-8 的配置，手工关随机关共存，关卡数量变成无限。' },
    { type: 'quiz', question: '想让「种子 42」每次进游戏都生成一模一样的关卡，关键一步是？', options: ['生成前调用 Random.InitState(42)，同一种子同一随机序列', '把随机数写成固定的常量', '每次手摆一遍'], answer: 0, tip: 'Random.InitState(seed) 重置随机序列：同一 seed → 同一串随机数 → 同一关。这就是程序化关卡「可复现」的根基。' },
  ],
  'project-20': [
    { type: 'text', title: '项目综合动手练：完整游戏 + 扩展整合', body: '最后一节，把整个项目从头到尾完整做一遍：核心闭环 + 扩展整合 + 发布。这是验收课，做完你就拥有一个「完整、可发布」的 Unity 游戏。\n\n一、本轮目标：\n从零搭到发布，验收点就是「完整闭环」：新游戏 → 打关 → 胜/败 → 结算 → 回菜单 → 继续游戏，全链路跑通。\n\n二、分步执行清单（每步配验收）：\n\n第 1 步，核心闭环（project-5/6）：\n· 主菜单 → 关卡 → 胜利/失败 → 结算 → 回菜单\n· 验收：任意环节都能回到主菜单，重进不卡死（timeScale 复位）\n\n第 2 步，至少 3 关（project-8/19）：\n· 2 关手工 + 1 关程序化生成（seed 存档）\n· 验收：选关界面显示解锁进度，随机关可复现\n\n第 3 步，玩法升级（project-9/10）：\n· 敌人/障碍 + 道具\n· 验收：撞敌人扣血带无敌帧，道具效果到点恢复\n\n第 4 步，持久化（project-11）：\n· 检查点存档 + 继续游戏\n· 验收：重开游戏能回到最后检查点\n\n第 5 步，系统整合（project-12/13/14）：\n· 设置 + 排行榜 + 成就\n· 验收：设置生效并保存，成就只解锁一次，排行上榜\n\n第 6 步，平台（project-15/16/17 三选一）：\n· 双人 / 手机适配 / 手柄，挑一个做扎实\n· 验收：所选平台完整可玩\n\n第 7 步，打磨与发布（project-6/7）：\n· 音效、手感、UI 打磨；发布构建 + 真机/目标平台跑通\n· 验收：Development Build 全流程无红字\n\n三、最终总验收清单（全勾才过关）：\n□ 主菜单 → 第一关能进能回，不卡死\n□ 至少 3 关可选，解锁进度正确\n□ 敌人/障碍/道具正常，无敌帧、道具计时生效\n□ 存档：重开能继续到检查点\n□ 设置音量/画质/键位生效并跨重启保留\n□ 至少一个成就解锁并弹出通知\n□ 排行榜有成绩记录\n□ 选择的平台适配（双人/手机/手柄）可玩\n□ 发布构建在目标机器跑通，无致命报错\n\n四、收尾心法：\n做完不是终点——把这份游戏存好，后续想加什么（更多关卡、换玩法、联机），这就是你的「样板工程」。\n\n一句话总结：综合练 = 闭环 + 多关卡 + 玩法 + 存档 + 系统 + 平台 + 发布，七步做完配九条验收；完成它，你就是「独立开发者」了。' },
    { type: 'quiz', question: '综合练习的最终验收，最核心的一条是？', options: ['每个功能都有一点但互相不通', '从主菜单到结算到继续的完整闭环，每个环节都能跑通', '代码写得越多越好'], answer: 1, tip: '功能多而断，不如闭环完整。验收的第一条永远是把「完整流程」跑通——任何一环断掉，都不算完整的游戏。' },
  ],
}
