// UI Toolkit —— 进阶内容（uitoolkit-5/6/7）
export const lessons = [
  { id: 'uitoolkit-5', title: '数据绑定与模板' },
  { id: 'uitoolkit-6', title: '样式动画与主题' },
  { id: 'uitoolkit-7', title: '与 UGUI 对比迁移' },
]

export const cards = {
  'uitoolkit-5': [
    { type: 'text', title: '数据绑定思路', body: '界面是给玩家看的，数据才是游戏里真正要变的东西——血量、金币、物品栏。UI Toolkit 里把「数据」和「界面」连起来的技术，就是数据绑定（Data Binding）。\n\n先说最朴素的思路：手动同步。你用前面学过的 query 找到元素，把数据填进去：\n\nvar label = root.Q<Label>(\"ScoreLabel\");\nlabel.text = score.ToString();\n\n手动同步直白、可控，但有个大坑：数据一变，你得记得到处改界面。血量、金币、背包同时变，漏改一处，界面就和数据对不上——这就是最常见的「UI 显示错乱」bug 来源。\n\n所以 UI Toolkit 提供了「绑定」机制，让界面自动跟着数据走。两种主流：\n· 与 Unity 对象绑定 —— 把 Inspector 面板用的那套序列化绑定搬进运行时 UI。给元素挂 Binding，指定要绑的字段，UI 自动同步\n· 自己写数据源类 —— 实现 INotifyPropertyChanged 这类通知接口，数据一变就通知界面刷新\n\n给新手的第一条建议：小项目、几个静态文字，手动同步就够了；一旦出现「一份数据多处显示」（金币数在右上角和商店都显示），就该上绑定。\n\n一句话总结：手动同步简单但容易漏，数据绑定是让界面自动跟随数据的省心方案——数据一多、一处变多处显示，就该换绑定了。' },
    { type: 'text', title: 'ListView 列表与模板', body: '列表是游戏 UI 最常见的形态：背包格子、任务列表、成就列表。UI Toolkit 做列表用 ListView，而且核心思想是「模板 + 数据」分离——你只管提供列表长什么样和数据是什么，剩下的重复生成交给框架。\n\n先看 ListView 的基本用法，代码里三步：\n\n// 1. 创建 ListView\nvar listView = new ListView();\n\n// 2. 提供数据源\nlistView.itemsSource = itemNames;   // List<string> 之类的数据\n\n// 3. 提供「一项长什么样」\nlistView.makeItem = () => new Label();   // 造一个空项\nlistView.bindItem = (element, index) => {\n    ((Label)element).text = itemNames[index];  // 把数据填进这一项\n};\n\nmakeItem 管「造模板」，bindItem 管「填数据」——框架循环调用，自动生成整列。\n\n想做得更正规，把模板做成 .uxml 文件（Visual Tree Asset 视觉树资产），再在 makeItem 里加载它。这样「列表项长什么样」变成可视化资产，策划和美术都能改，不碰代码。\n\nListView 的隐藏优势：虚拟化。它不会真的把一万个元素都生成出来，只生成「看得见的那几行」，滚动时回收重用——所以背包上万格也不卡。\n\n动态生成元素的两个坑：\n· 忘调 RefreshItems —— 数据变了列表不自动刷新，要手动通知\n· 用错数据源类型 —— itemsSource 要是 List 这类动态集合，别给数组或之后不再变化的对象\n\n一句话总结：ListView = 数据源 + 模板（makeItem）+ 填数据（bindItem）三件套，配合虚拟化，上万条列表也流畅。' },
    { type: 'quiz', question: 'ListView 里，makeItem 和 bindItem 各管什么？', options: ['makeItem 填数据，bindItem 造模板', 'makeItem 造出每一项的模板，bindItem 把对应数据填进这一项', 'makeItem 负责排序，bindItem 负责过滤'], answer: 1, tip: 'makeItem 只负责「造出一个空项」长什么样（模板），bindItem 按索引把数据填进去——造模板和填数据分开，列表才能复用虚拟化。' },
  ],
  'uitoolkit-6': [
    { type: 'text', title: '过渡动画与动画', body: '静态的界面能看，会动的界面才「活」。UI Toolkit 做动效有两档：轻量的过渡（Transition）和更自由的动画。\n\n过渡是给「样式变化」加平滑过渡。在 USS 里写：\n\n.my-button {\n    background-color: gray;\n    transition: background-color 0.2s;\n}\n.my-button:hover {\n    background-color: gold;\n}\n\n鼠标悬停时，背景色不是瞬间变，而是 0.2 秒内从灰平滑滑到金。transition 的写法像 CSS：transition: 属性 时长;，一条可以写多个。\n\n常用的过渡属性：\n· color、background-color —— 文字/背景变色\n· opacity —— 淡入淡出\n· transform —— 位移、缩放（translate、scale）\n\n过渡适合「状态切换」——悬停、按下、选中。想更自由的动画（连续播放、循环、复杂路径），用 C# 侧做：在代码里随时间改元素的样式属性，配合协程或动画系统：\n\nIEnumerator FadeIn(VisualElement el, float duration) {\n    float t = 0f;\n    while (t < duration) {\n        t += Time.deltaTime;\n        el.style.opacity = t / duration;\n        yield return null;\n    }\n    el.style.opacity = 1f;\n}\n\n几个坑：\n· transition 要写在「初始状态」那一侧，写在 hover 里不生效\n· 过渡时长的单位要带 s（秒），别写裸数字\n· 动画性能：整屏大量元素同时做 opacity 动画会掉帧，尽量收敛动画元素的数量\n\n一句话总结：状态切换用 USS transition 一行搞定，连续演出用 C# 动画——动画是点睛，别让界面到处都在跳。' },
    { type: 'text', title: '主题换肤与响应式', body: '主题换肤（Theme）是所有游戏 UI 的刚需：一套界面，切换「明亮/暗黑」「可爱/硬核」多套皮肤。UI Toolkit 的优势就在这——结构和逻辑不动，只换 USS 样式文件。\n\n换肤的核心思路：把「长相」全部隔离在 USS 里，代码里一个词都不写死样式。做法：\n· 每套主题一个 USS 文件：theme-dark.uss、theme-light.uss\n· 换肤 = 运行时换样式表：\n\nvar sheet = Resources.Load<StyleSheet>(\"Themes/theme-dark\");\nroot.styleSheets.Clear();\nroot.styleSheets.Add(sheet);\n\n把根元素的样式表清掉换上新的，整棵 UI 树一次性换肤。\n\n更推荐的做法是「主题变量」。UI Toolkit 有 USS 变量（用 -- 开头定义，var() 引用）：\n\n:root {\n    --bg-color: #1a1a1a;\n    --accent-color: gold;\n}\n\nbutton {\n    background-color: var(--bg-color);\n}\n\n换主题时只改 :root 里那几个变量，所有引用处自动跟着变——换肤成本降到最低。\n\n响应式（Responsive）是另一个话题：不同屏幕尺寸下界面要自适应。UI Toolkit 没有网页那种 @media 断点查询，但两个办法很实用：\n· Flexbox 布局天然自适应 —— 用 flex-grow、百分比宽度，界面自己会伸缩\n· 在 C# 里监听窗口尺寸变化，按分辨率切换布局或样式：\n\nvoid OnResize() {\n    var w = root.resolvedStyle.width;\n    if (w < 800f) root.AddToClassList(\"compact\");\n    else root.RemoveFromClassList(\"compact\");\n}\n\n一句话总结：换肤 = 换 USS 或改 CSS 变量；响应式 = flex 布局兜底 + 代码按尺寸切换样式——两样都是「不动结构、只动样式」的艺术。' },
    { type: 'quiz', question: '想给一套界面加「暗黑/明亮」两套主题，最省事、最不容易出错的做法是？', options: ['在代码里写死每个元素的颜色，主题切换时逐行改', '把颜色定义成 USS 变量，换主题只改 :root 里的变量', '做两份 UXML 文件分别给两套主题用'], answer: 1, tip: '颜色定义在 USS 变量里，换主题只改变量值，所有引用处自动变——一份结构、一份逻辑，换来换去都不乱。' },
  ],
  'uitoolkit-7': [
    { type: 'text', title: 'UGUI 与 UI Toolkit 差异', body: 'UGUI（Canvas 那套）是 Unity 的老牌 UI 系统，你大概率在项目课里学过锚点、Canvas。UI Toolkit 是官方力推的新一代。做项目前得先分清两者，别混着用。\n\n核心差异对照：\n· 架构 —— UGUI 是「Canvas + RectTransform + 锚点坐标」的面向对象结构；UI Toolkit 是「UXML 结构 + USS 样式 + Flexbox 布局」，更像网页\n· 布局 —— UGUI 手动拖锚点、算坐标；UI Toolkit 用 flex-direction、justify-content 这些属性自动排布，窗口自适应\n· 样式 —— UGUI 样式散在每个组件参数里；UI Toolkit 样式集中在 USS 文件，可换肤、可复用\n· 数据 —— UGUI 列表要自己生成一个个子物体；UI Toolkit 有 ListView 加虚拟化\n· 编辑器扩展 —— UGUI 做不了编辑器插件界面；UI Toolkit 是编辑器扩展的官方推荐\n· 生态 —— UGUI 资料多、老项目多、社区积累厚；UI Toolkit 新、官方主推、上手后效率高\n\n各自的擅长场景：\n· UGUI 适合：已经成型的老项目、需要大量绝对定位的 HUD、对性能要求苛刻的移动端存量界面\n· UI Toolkit 适合：新项目的新 UI、需要换肤的主题化界面、列表密集的界面（背包、商店）、想顺便学编辑器扩展\n\n一句话总结：UGUI 老而稳，UI Toolkit 新而香——新项目优先选 UI Toolkit，存量项目继续用 UGUI 别急着搬。' },
    { type: 'text', title: '迁移策略与两套混用', body: '既然两者各有长短，就绕不开两个实际问题：老项目怎么迁？能不能两套混用？\n\n先回答「能不能混用」：能，而且官方支持。UGUI 的 Canvas 和 UI Toolkit 的 UIDocument 可以同屏共存——一个场景里挂一套 Canvas 做 HUD，挂一个 UIDocument 做背包界面，互不干扰，渲染层都放在 UI 相机下。但要注意：\n· 两者的输入事件是分开的，EventSystem 管 UGUI，UI Toolkit 有自己的事件系统，别指望一次点击同时触发两边\n· 别让两者互相嵌套（比如在 UGUI 的 Button 里嵌 UI Toolkit 的元素）——会出各种布局和事件怪问题\n\n再说迁移的稳妥套路，别想一次搬完：\n· 增量迁移 —— 新功能、新界面直接用 UI Toolkit；老界面维持 UGUI，一条条来\n· 先迁好迁的 —— 静态菜单、设置面板、列表类界面，迁起来收益明显；再碰 HUD 这类高频刷新的\n· 迁移时按「界面」为单位，别按「功能」切——一个界面要么全 UGUI 要么全 UI Toolkit\n\n选型建议给个倾向：\n· 全新项目，无历史包袱 —— 直接全 UI Toolkit，官方主推方向\n· 老项目做大改版 —— 用增量迁移，新界面 UI Toolkit + 老界面 UGUI 并行过渡\n· 团队都熟 UGUI、项目快交付 —— 别折腾，UGUI 干完，下个项目再上新\n\n一句话总结：两套能混用但要各管各的；迁移走「新界面先上、老界面渐进」的增量路线——别为了「高级」给快交付的项目添乱。' },
    { type: 'quiz', question: '老项目里想加一个新背包界面，旧 HUD 还稳着。最稳的落地方式是？', options: ['把老 HUD 先重写成 UI Toolkit，再动背包', '背包直接用 UI Toolkit，HUD 维持 UGUI，两套并行增量迁移', '禁止混用，全部推倒重建'], answer: 1, tip: '两套 UI 可以并行共存，增量迁移最稳——新界面直接上新技术，老界面维持原样，按界面一条条迁，风险最小。' },
  ],
}
