// 音频系统扩充：音频分析与可视化 / 声音设计基础 / 动态音乐系统
export const lessons = [
  { id: 'audio-4', title: '音频分析与可视化' },
  { id: 'audio-5', title: '声音设计基础' },
  { id: 'audio-6', title: '动态音乐系统' },
  { id: 'audio-7', title: '动手练：给小游戏配全套音效' },
]

export const cards = {
  'audio-4': [
    { type: 'text', title: '音频分析与可视化：让声音「看得见」', body: '前几节是「放声音」，这一节是「读声音」——从正在播的音频里抠出数据，做音浪条、律动灯光、节拍反馈。\n\n一、两个采样 API\nAudioSource（或 AudioListener）有两个方法，把当前音频瞬间的数据拷进一个 float 数组：\n\nsource.GetOutputData(samples, 0);                       // 时域波形：当前时间点的振幅，值约 -1~1\nsource.GetSpectrumData(samples, 0, FFTWindow.Blackman); // 频域频谱：各频段的能量\n\n· GetOutputData —— 看「整体响度」：值越大越响，做音量检测用它\n· GetSpectrumData —— 看「频率分布」：低音、中音、高音各有多少能量，做可视化用它\n\n二、音量检测（RMS）\n把一帧采到的波形算个均方根，就是「这一刻多响」：\nfloat[] data = new float[256];\nsource.GetOutputData(data, 0);\nfloat sum = 0f;\nfor (int i = 0; i < data.Length; i++) sum += data[i] * data[i];\nfloat rms = Mathf.Sqrt(sum / data.Length);   // 0~1 的响度\n\n原始值跳得厉害，要做平滑：\ncurrent = Mathf.Lerp(current, rms, 0.1f);    // 慢慢跟\n\n三、频谱可视化：音浪条\n1. 用 GetSpectrumData 拿 64~256 个频段的能量\n2. 每个频段对应一根柱子，把能量映射到柱子的 localScale.y\n\nfor (int i = 0; i < 64; i++)\n{\n    float v = spectrum[i] * scale;                 // 能量转高度\n    bars[i].localScale = new Vector3(1f, v, 1f);\n}\n\n显示上的坑：原始频谱里低音猛、高音细，直接映射高音几乎看不见。先做压缩：\nv = Mathf.Pow(v, 0.5f);   // 开方压缩，把高低音都拉到看得见的范围\n\n四、节奏检测入门（能量法）\n最简单的节拍判断：音量突然比「近期平均」高一截，就是一下鼓点。\n\nfloat[] data = new float[256];\nsource.GetOutputData(data, 0);\nfloat rms = ComputeRms(data);              // 复用上面的 RMS\nenergyHistory.Add(rms);\nfloat avg = energyHistory.Average;         // 近几十帧的平均\nif (rms > avg * 1.3f && Time.time > lastBeat + 0.3f)   // 突增 + 冷却\n{\n    lastBeat = Time.time;\n    FlashFloor();                          // 地板闪一下\n}\n\n· 阈值 1.3 可调：想灵敏就调小，怕乱闪就调大\n· 冷却 0.3 秒防止同一拍闪两次\n\n五、常见坑\n· 采样数组别每帧 new，缓存一份复用（GC 警告）\n· 频谱高频段分辨率低、低音占大头，别惊讶\n· 没有 AudioSource 在播时采样全是 0，要做静默保护\n\n一句话总结：GetOutputData 读响度、GetSpectrumData 读频谱，音量做平滑、频谱开方压缩，能量突增超过近期平均 = 一个节拍。' },
    { type: 'quiz', question: '想做「鼓点一到，地板闪烁」的效果，入门最靠谱的检测思路是？', options: ['监听 AudioSource 的 OnMusicBeat 事件', '用 GetOutputData 算实时音量，和近期的平均音量比，突然高出一截就判定为节拍', '读频谱里最高那个频段的数值'], answer: 1, tip: 'Unity 没有现成的节拍事件。能量法最省事：实时音量比短期平均高出阈值（带冷却）就是鼓点。只盯最高频段会把低音鼓漏掉。' },
  ],
  'audio-5': [
    { type: 'text', title: '声音设计基础：好音效不是「找」出来的，是「配」出来的', body: '这节不谈代码，谈「声音从哪来、怎么配」。一个游戏的声场好不好，七分靠设计，三分靠技术。\n\n一、音效的三个来源\n· 录音（Foley / 实采）—— 真实世界的声音：脚步声、开门、布料摩擦。拟音师用道具（沙、纸、鞋）在录音棚里「演」出动作声\n· 合成（Synthesis）—— 程序 / 合成器生成：电子音、UI 哔声、激光。免费工具一堆：Bfxr、sfxr、ChipTone，鼠标一抖就是一个「叮」\n· 素材库 —— 免费 / 付费音效库：Freesound、Kenney、Soniss，按关键词搜\n\n二、合成 vs 录音，怎么选\n· 合成 —— 适合 UI、机械、电子、科幻；无限循环不累、无版权烦恼、参数可调\n· 录音 —— 适合真实世界：脚步、武器、自然；「像真的」是它的命根子\n\n好游戏往往是混着用：真实声做底，合成声做点缀。\n\n三、混音思路：频率分层\n声音的「厚度」来自频率分配：\n· 低频 —— 爆炸、引擎的「轰」：震撼，但小喇叭一压就没了\n· 中频 —— 对话、音效主体：别挤成一团\n· 高频 —— 脚步声、按键的「咔哒」：提神，太多就吵\n\n节奏类游戏为什么「咔哒」声清脆？因为高频负责「被听见」。给不同音效留出各自的频率段，声场才不糊。\n\n四、层次感\n· 远近分层 —— 近处大声、远处小声（就是第 2 节的 3D 衰减）\n· 前景 / 背景 —— 玩家操作声是前景，环境声、音乐是背景，音量拉开\n· 主次 —— 一局游戏里最重要的声音（得分、警报）音量要压得住环境\n\n五、配声音的标准流程\n1. 列清单 —— 按系统列：移动、战斗、UI、环境，每个要什么音效写下来\n2. 找 2~3 个备选 —— 每个音效多备几份，试听挑最顺耳的\n3. 挂进游戏 —— AudioSource + 触发时机（动画事件、碰撞回调）\n4. 实机调混音 —— 音量、声相、衰减，在真实场景里听，别在编辑器里凭想象\n5. 打磨 —— 同一个音效换 pitch（音高）就是新声音；叠一层噪音就有质感\n\n六、常见坑\n· 音量不统一 —— 先定一个基准（约 -12dB），全项目对齐，别有的震耳、有的听不见\n· 一个音效用到底 —— 爆炸 = 低频轰 + 中频碎 + 高频嘶，是「叠」出来的，不是找个采样一放了事\n· 忘掉「静音帧」—— 连续音效之间留气口，声音也要呼吸\n\n一句话总结：录音给真实、合成给自由、素材库补量；频率分层别糊成一锅，层次拉开，再按「列清单 → 试听 → 挂接 → 实调 → 打磨」的流程走一遍。' },
    { type: 'quiz', question: '想让「爆炸」音效听起来厚实有层次，而不是单薄的一声，最该怎么做？', options: ['把爆炸音量拉到最大', '把多层声音叠起来：低频的轰 + 中频的碎 + 高频的嘶，混音调平衡', '用 AudioMixer 把爆炸轨调成 0dB 以上'], answer: 1, tip: '单采样凑合是新手坑。厚实感来自叠层：低频给震撼、中频给质感、高频给清脆，混音平衡好才有层次。音量拉满只会更吵，不会更厚。' },
  ],
  'audio-6': [
    { type: 'text', title: '动态音乐系统：让音乐跟着游戏呼吸', body: '好游戏的 BGM 不是一路播到底——探索时舒缓、战斗时激昂、脱战后回落。这一节做「会变」的音乐系统。\n\n一、分轨混音：一首音乐拆成几层\n把音乐拆成多条音轨（打击乐 / 贝斯 / 和弦 / 旋律），每条轨一个 AudioSource，全部送进同一个 AudioMixer 的 Music 分组。这样哪层响、哪层轻，随时单控。\n\n二、音乐分层（Layering）：层层加码\n· 探索 —— 只放「氛围底垫」（长音 Pad / 弦乐铺底）\n· 预警 —— 底垫 + 低鼓\n· 战斗 —— 再叠贝斯、打击乐、旋律，一层层往上加\n\n每一层进来，紧张感升一档。这就是电影配乐「加一层管乐」的同一招。\n\n三、无缝切换的三种手法\n· 硬切 —— 直接换 AudioClip：接缝容易跳，只有很响亮的瞬间能藏住\n· 淡入淡出 —— Fade Out 旧轨 + Fade In 新轨（复用音效管理那节的 Fade 协程）：顺滑，但中间有一小段「空窗」\n· 交叉淡变（Crossfade）—— 两轨同时一进一出，在中间汇合：最丝滑\n\n四、对齐节拍：切换不「掉拍」\n切歌切在节拍里，才不会节奏错乱。用 AudioSettings.dspTime（音频时钟）做「定时播放」：\n\ndouble startTime = AudioSettings.dspTime + 1.0;   // 一秒后\nnewSource.PlayScheduled(startTime);\noldSource.SetScheduledEndTime(startTime);          // 旧的正好在这时停\n\n这样切换发生在同一时刻，接缝干净。想卡在小节开头，就把 startTime 对齐到「下一个小节」。\n\n五、参数驱动音乐：把游戏状态接进来\n\nint danger = enemyCount;                     // 敌人越多越紧张\nmusicMixer.SetFloat(\"LayerCount\", danger);\n\n然后让不同层的高通滤波、音量跟着这个参数走——或者干脆代码里控制各层 AudioSource 的 volume：\n\nSetLayer(LayerType.Drums, 0f);     // 安静：鼓关掉\nSetLayer(LayerType.Drums, 1f);     // 战斗：鼓拉满\n\n六、设计心法\n· 状态别太碎 —— 音乐是「几档」，不是「连续函数」：探索 / 预警 / 战斗三档够用，别见个变量就切\n· 切换留过渡 —— 一次切换 1~2 秒的渐变，别瞬间跳\n· 音量叠加注意 —— 层多了总音量会顶爆，混音器 Master 压一压\n\n一句话总结：分轨 + 分层让音乐「有梯度」，Crossfade + dspTime 让切换「不掉拍」，把游戏状态映射到各层音量 / 滤波——音乐就活了。' },
    { type: 'quiz', question: '从「探索」切到「战斗」BGM，想让切换顺滑、不跳帧不刺耳，最稳的做法是？', options: ['直接 stop 旧的再 play 新的', '用交叉淡变：两轨同时淡出 / 淡入，并尽量把切换对齐到节拍', '把两首歌塞进同一个 AudioSource 轮流播'], answer: 1, tip: '硬切接缝明显；交叉淡变让新旧在中间汇合最丝滑，配合 dspTime 对齐节拍，切换「不掉拍」。一个 AudioSource 同时只播一个 clip，没法无缝。' },
  ],
  'audio-7': [
    { type: 'text', title: '动手练：给小游戏配全套音效', body: '动手练：把「混音器 + 音效管理 + 音量面板」凑成一个完整的音频系统——给一个滚球小游戏配上背景音乐、收集音效、UI 点击声，用 AudioMixer 把音乐和音效分轨，再做一个滑杆面板分别调音量、存进 PlayerPrefs。\n\n🎯 任务目标：给一个滚球小游戏配齐 BGM + 收集音效 + UI 点击声，用 AudioMixer 的 Music / SFX 两个分组分开控音量，写一个设置面板（两个 Slider → SetFloat → 存 PlayerPrefs），让音乐和音效互不干扰、音量跨重启保留。\n\n📦 前置要求：先学完本课的「声音设计基础」（audio-5）和「动态音乐系统」（audio-6），会用 AudioSource、知道 AudioMixer 分组思路。\n\n📋 动手步骤：\n\n1. 准备小游戏：新建场景，放一个球（Player，挂 Rigidbody + 一个简单的 AddForce 移动脚本），放 3~5 颗金币（Collider 勾 Is Trigger，OnTriggerEnter 判定玩家），加一块地面。最简滚球即成。\n\n2. 准备音频资源：准备三个音频——一段循环 BGM、一个「叮」收集音效、一个「咔哒」UI 点击音效。没有现成的就从素材库下（Kenney 免费包、Freesound），拖进 Project。\n\n3. 建 AudioMixer：Window → Audio → Audio Mixer，创建混合器命名 MainMixer。选中它，右侧 Inspector 里点 + 号创建两个子分组，命名 Music 和 SFX。\n\n4. 暴露音量参数：在 Audio Mixer 窗口里，点开 Music 分组的音量（Volume）滑杆 → 右键 → Expose to script，右侧会多出一个参数；双击改名 MusicVolume。SFX 分组同样操作，暴露 SfxVolume。代码就是靠这两个名字 SetFloat 的。\n\n5. 挂 BGM：新建空物体命名 MusicPlayer，挂 AudioSource：Clip 拖 BGM、勾 Loop、勾 Play On Awake，Output 选 MainMixer → Music 分组。这个音源只负责背景音乐。\n\n6. 写音效管理器 AudioManager.cs（单例 + PlayOneShot）：\nusing UnityEngine;\npublic class AudioManager : MonoBehaviour\n{\n    public static AudioManager Instance;\n    public AudioSource sfxSource;      // 挂 SFX 分组的音源\n    public AudioClip coinClip;\n    public AudioClip clickClip;\n\n    void Awake() { Instance = this; }\n\n    public void PlayCoin()  { sfxSource.PlayOneShot(coinClip); }   // PlayOneShot：可叠加、不打断自己\n    public void PlayClick() { sfxSource.PlayOneShot(clickClip); }\n}\n\n7. 接线收集音效：新建空物体挂 AudioManager 脚本，给它挂一个 AudioSource（Output 选 SFX 分组，关掉 Play On Awake）。在金币的 OnTriggerEnter 里，玩家碰到就调 AudioManager.Instance.PlayCoin()。\n\n8. 接线 UI 点击声：场景里加一个 Button（GameObject → UI → Button）。给一个空物体写个简单方法或在按钮 OnClick 里直接拖 AudioManager 物体，把 PlayClick 加进按钮点击事件。\n\n9. 写音量设置面板 AudioSettings.cs：\nusing UnityEngine;\nusing UnityEngine.Audio;\nusing UnityEngine.UI;\npublic class AudioSettings : MonoBehaviour\n{\n    public AudioMixer mixer;\n    public Slider musicSlider;\n    public Slider sfxSlider;\n    const string MusicKey = \"MusicVolume\";\n    const string SfxKey = \"SfxVolume\";\n\n    void Start()\n    {\n        musicSlider.value = PlayerPrefs.GetFloat(MusicKey, 0.8f);   // 没有存档就默认 0.8\n        sfxSlider.value = PlayerPrefs.GetFloat(SfxKey, 0.8f);\n        ApplyMusic();\n        ApplySfx();\n    }\n\n    public void ApplyMusic() { SetVolume(\"MusicVolume\", musicSlider.value, MusicKey); }\n    public void ApplySfx()   { SetVolume(\"SfxVolume\", sfxSlider.value, SfxKey); }\n\n    void SetVolume(string param, float value, string key)\n    {\n        // 滑杆 0~1 转 dB：人耳对音量感知是对数的，直接传 0~1 会让低音量区间几乎听不出变化\n        float db = value <= 0.0001f ? -80f : Mathf.Log10(value) * 20f;\n        mixer.SetFloat(param, db);\n        PlayerPrefs.SetFloat(key, value);\n        PlayerPrefs.Save();\n    }\n}\n\n10. 搭 UI 面板：在 Canvas 下加一个 Panel（设置面板）+ 两个 Slider + 两个文字标签（音乐音量 / 音效音量）。两个 Slider 的 On Value Changed 事件分别拖 AudioSettings 的 ApplyMusic / ApplySfx。运行。\n\n11. 验证分轨与持久化：运行游戏——拖音乐滑杆，BGM 变、音效不变；拖音效滑杆，音效变、BGM 不变。退出游戏重进，音量保持上次的值。\n\n✅ 验收清单：\n能 □ 背景音乐和音效分别受两个滑杆控制，互不干扰\n能 □ 收集金币有音效（PlayCoin），点按钮有点击声（PlayClick）\n能 □ 音量设置退出游戏重开后还在（PlayerPrefs）\n能 □ 看得出 BGM 音源 Output 指向 Music、音效音源 Output 指向 SFX\n能 □ 能说清为什么滑杆值要转成 dB 再 SetFloat\n\n🕳 常见卡壳：\n坑 1：SetFloat 没反应——混音器分组没 Expose to script，或代码里的参数名和暴露出的名字对不上（MusicVolume / SfxVolume 必须一字不差）。\n坑 2：滑杆拉到底音量不是 0——dB 里 -∞ 才完全静音，滑杆 0 要映射成 -80dB（SetVolume 里 value <= 0.0001f 那分支就是干这个的），直接传 0 只是静得像没声但还在响。\n坑 3：音乐和音效一起变——两个 AudioSource 的 Output 指到同一个分组了。BGM 指 Music、音效指 SFX，别都指 Master。' },
    { type: 'quiz', question: '音量滑杆为什么要把 0~1 的值转成 dB 再 SetFloat，而不是直接把 0~1 传进混音器？', options: ['混音器的 SetFloat 只接受负值，不接受 0~1', '人耳对音量的感知是对数的：直接传 0~1 会让中低段听不出变化，且 0 无法真正静音，转成 dB 才符合听感', 'SetFloat 的参数名必须按 dB 格式命名'], answer: 1, tip: '混音器音量单位就是 dB，人耳对响度的感知近似对数——滑杆 0~1 线性传进去，中低段变化基本听不出来；0 也要映射成 -80dB 才接近静音。转 dB 是标准做法。' },
  ],
}
