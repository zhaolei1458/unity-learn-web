// 进阶内容：prog 进阶编程（prog-6/7/8）
export const lessons = [
  { id: 'prog-6', title: '接口 Interface' },
  { id: 'prog-7', title: '集合与泛型' },
  { id: 'prog-8', title: '枚举与常用语法' },
  { id: 'prog-9', title: '动手练：用继承和接口重构' },
  { id: 'prog-10', title: '现代 C# 语法：lambda / LINQ / async' },
  { id: 'prog-11', title: '协程深入：yield 的各种写法' },
  { id: 'prog-12', title: '命名空间与脚本组织' },
  { id: 'prog-13', title: '异常处理' },
  { id: 'prog-14', title: '字符串进阶' },
  { id: 'prog-15', title: '委托与事件实战' },
  { id: 'prog-16', title: '静态类与工具类' },
  { id: 'prog-17', title: '属性与封装' },
  { id: 'prog-18', title: '序列化深入' },
  { id: 'prog-19', title: '生命周期实战' },
  { id: 'prog-20', title: '编程综合动手练' },
]
export const cards = {
  'prog-6': [
    { type: 'text', title: '接口 Interface', body: '接口（Interface）是 C# 里「约定」的化身——它只说「你必须有这些能力」，不管「你怎么实现」。这是解耦和做出灵活架构的核心武器。\n\n一、接口长什么样\n\ninterface IDamageable\n{\n    void TakeDamage(int amount);   // 约定：必须有这个能力\n}\n\n接口里只写「方法签名」：方法名、参数、返回类型，没有函数体。它规定的是「能做什么」，而不是「怎么做」。\n\n二、实现接口\n\n任何类都能「实现」接口，用冒号跟接口名：\n\npublic class Enemy : MonoBehaviour, IDamageable\n{\n    public void TakeDamage(int amount)\n    {\n        health -= amount;          // 敌人自己的实现\n    }\n}\n\npublic class Player : MonoBehaviour, IDamageable\n{\n    public void TakeDamage(int amount)\n    {\n        health -= amount;          // 玩家自己的实现\n    }\n}\n\nEnemy 和 Player 八竿子打不着，但都实现了 IDamageable——这就有了「共同的身份」。\n\n三、接口多态：把不同类当同一种东西\n\nIDamageable target = GetComponent<IDamageable>();  // 拿到的是「能挨打」的组件\nif (target != null)\n{\n    target.TakeDamage(10);   // 管你是敌是玩家，能挨打就扣血\n}\n\n子弹命中时，不用知道打中的是敌人还是玩家，只要它实现了 IDamageable，就能扣血——这就是「面向接口编程」：依赖约定，不依赖具体类型。\n\n四、接口 vs 抽象类（核心区别）\n\n· 接口：纯约定，只有方法签名；一个类可以实现多个接口；成员默认 public\n· 抽象类：可以有实现好的方法、也可以有字段（数据）；一个类只能继承一个\n\n什么时候用哪个？\n· 能力是「多面手」：一个物体同时是「能挨打 + 能交互 + 能拾取」，用多个接口\n· 血统是「一家人」：各类之间有共享的数据和逻辑，用抽象类\n\n五、Unity 里的经典应用\n\ninterface IInteractable { void Interact(); }\n\n门、箱子、NPC 都实现 IInteractable，玩家按 E 时统一处理：\n\nIInteractable target = hit.collider.GetComponent<IInteractable>();\ntarget?.Interact();\n\n实现接口的脚本在 Add Component 面板上还会带个小标签，一眼能认出。\n\n六、坑\n\n· 接口不能 new：interface 只是约定，不能实例化\n· 忘实现某个方法：编译直接报错，反而帮你查漏\n· 接口名惯例用 I 开头（IDamageable、IInteractable）\n\n一句话总结：接口只约定「有什么能力」，不规定「怎么做」；用接口类型接收不同类的对象，多态展开，代码只依赖约定不依赖具体类——这是解耦的第一步。' },
    { type: 'component', title: '接口关键概念', items: [
      { name: 'interface', desc: '纯约定，只有方法签名，没有实现' },
      { name: '实现接口', desc: '类用冒号跟接口名，逐个实现方法' },
      { name: '接口多态', desc: '用接口类型接收不同实现，统一调用' },
      { name: '抽象类', desc: '可有实现和字段，一个类只能继承一个' },
      { name: 'I 开头命名', desc: '接口命名惯例，如 IDamageable' },
    ] },
    { type: 'quiz', question: '子弹打中敌人要扣血、打中玩家也要扣血，但两者是完全不同的类。最干净的写法是？', options: ['在子弹脚本里用 if 分别判断打中的是敌人还是玩家', '定义 IDamageable 接口，敌人和玩家都实现，子弹统一调 TakeDamage', '把敌人和玩家合并成一个类'], answer: 1, tip: '接口让不同类拥有共同能力，子弹拿到 IDamageable 引用统一调 TakeDamage，不用关心具体是谁——这正是接口多态和解耦的价值。' },
  ],
  'prog-7': [
    { type: 'text', title: '集合与泛型', body: '存一个数用变量，存一组数据就要用「集合」——数组、列表、字典，是游戏逻辑装数据的容器。这一课把最常用的四兄弟和泛型一次讲清。\n\n一、数组 Array：固定大小\n\nint[] scores = new int[5];          // 长度固定 5 个\nscores[0] = 100;                    // 下标从 0 开始\n\nfloat[] speed = { 1f, 2f, 3f };    // 初始化直接给值\n\n数组长度固定、不可增删，适合「数量确定」的场景。用下标 scores[i] 取值。\n\n二、List：动态列表（最常用）\n\nList<int> scores = new List<int>();\nscores.Add(100);       // 末尾追加\nscores.RemoveAt(0);    // 按下标删\nscores.Remove(100);    // 按值删\nscores.Count           // 元素个数\nscores[0]              // 按下标取\n\nList 能随时增删，是游戏里装「可变数量的东西」（敌人、子弹、背包）的首选。\n\n三、Dictionary：键值对\n\nDictionary<string, int> inventory = new Dictionary<string, int>();\ninventory[\"金币\"] = 100;     // 用键存\nint gold = inventory[\"金币\"];  // 用键取\n\nDictionary 存「键 → 值」的对应关系，按键取值极快。存配置、统计计数、背包（物品名 → 数量）都靠它。\n\n注意：取一个不存在的键会抛异常！取之前先判断：\n\nif (inventory.ContainsKey(\"钥匙\")) { ... }\n\n四、HashSet：去重集合\n\nHashSet<string> tags = new HashSet<string>();\ntags.Add(\"金属\");\ntags.Add(\"金属\");   // 重复添加被忽略，集合里只有一个\n\ntags.Contains(\"金属\");   // 判断有没有，极快\n\nHashSet 自动去重、查询极快，适合「记录一堆互不重复的标记」。\n\n五、泛型：尖括号里的类型\n\nList<int>、Dictionary<string, int> 里的 <int> 就是泛型——它让「一套容器」能装任意类型。写的时候在类名后跟 <T>：\n\npublic class Box<T>\n{\n    public T content;\n}\n\nBox<int> intBox = new Box<int>();\nBox<string> strBox = new Box<string>();\n\n同一份代码，装不同类型。集合全是泛型，所以什么类型都能装。\n\n六、foreach 遍历\n\nforeach (int score in scores)\n{\n    Debug.Log(score);\n}\n\nforeach (var pair in inventory)   // var 自动推断类型\n{\n    Debug.Log(pair.Key + \" = \" + pair.Value);\n}\n\nforeach 只读不改最安全；想一边遍历一边增删，用 for 循环加下标，别在 foreach 里改集合（会报错）。\n\n一句话总结：数组定长、List 动态、Dictionary 键值对、HashSet 去重；泛型让容器装任意类型；遍历用 foreach，增删用 for——四件套分清用，数据管理不再乱。' },
    { type: 'component', title: '集合四兄弟', items: [
      { name: 'Array 数组', desc: '定长，数量确定时用' },
      { name: 'List<T>', desc: '动态列表，可增删，最常用' },
      { name: 'Dictionary<K,V>', desc: '键值对，按键快速取值' },
      { name: 'HashSet<T>', desc: '去重集合，查询极快' },
      { name: '泛型 <T>', desc: '一套容器装任意类型' },
      { name: 'foreach', desc: '遍历集合，只读最安全' },
    ] },
    { type: 'quiz', question: '想做一个「物品名 → 数量」的背包，用哪个集合最合适？', options: ['List<string>', 'Dictionary<string, int>', 'HashSet<string>'], answer: 1, tip: '背包是「名字查数量」的键值对关系，正好是 Dictionary 的活：键存物品名、值存数量，按键取值又快又直观。' },
  ],
  'prog-8': [
    { type: 'text', title: '枚举与常用语法', body: '最后一课补几味「常用语法」——枚举、空值处理、三元运算符、属性。都是写游戏代码天天碰的东西。\n\n一、枚举 enum：给「一堆选项」起名\n\npublic enum WeaponType { Sword, Bow, Staff, Fist }\n\n枚举就是给一组固定选项命名，比裸用数字 0/1/2 清晰一万倍。\n\n声明和使用：\n\npublic WeaponType weapon = WeaponType.Sword;\n\nif (weapon == WeaponType.Bow)\n{\n    ShootArrow();\n}\n\n· 枚举默认底层是 int，第一个选项是 0，往后递增\n· 也可以显式指定值：enum WeaponType { Sword = 1, Bow = 2 }——存档时存数字就稳\n\n二、枚举 × switch：状态处理的黄金搭档\n\nswitch (weapon)\n{\n    case WeaponType.Sword: AttackSword(); break;\n    case WeaponType.Bow:   AttackBow();   break;\n    case WeaponType.Staff: AttackStaff(); break;\n    default: AttackFist(); break;\n}\n\n枚举和 switch 是天作之合：枚举列全选项、switch 逐个处理，比一串 if else 清爽（logic-5 讲过 switch）。Unity 的检查器里，枚举字段还会变成下拉菜单。\n\n三、? 可空类型与 ?? 空值合并\n\nint? score = null;         // ? 表示「这个 int 可以是空的」\n\n?? 的意思是「左边是空就用右边」：\n\nint result = score ?? 0;   // score 是 null 就用 0\n\n写防御性代码时常用：\n\npublic string playerName;\nstring show = playerName ?? \"未命名\";\n\n四、三元运算符 ?:\n\nif (hp > 0) state = \"存活\"; else state = \"死亡\";\n\n可以缩写成一行：\n\nstring state = hp > 0 ? \"存活\" : \"死亡\";\n\n格式：条件 ? 成立的值 : 不成立的值。适合「一个条件选两个值」的小场合；太复杂就别硬缩写，可读性优先。\n\n五、属性 Property：带保护的字段\n\n属性是「看起来像字段、用起来像方法」的成员：\n\nprivate int hp;\npublic int HP\n{\n    get { return hp; }                 // 读的时候\n    set { hp = Mathf.Max(0, value); }  // 写的时候，value 是写入的值\n}\n\n好处：可以在读写时加逻辑——血量永远不小于 0，就是靠 setter 卡住的：\n\nHP = -50;   // 实际存的是 0，负数被挡掉\n\n自动属性（不想要额外逻辑时，简写）：\n\npublic int Score { get; set; }     // 自动生成隐藏字段\n\n一句话总结：枚举把选项命名、switch 配枚举逐项处理、? 和 ?? 防空、?: 一行二选一、属性给字段加读写保护——这几味语法配齐，C# 常用招式就齐活了。' },
    { type: 'component', title: '常用语法速查', items: [
      { name: 'enum', desc: '给固定选项命名，如武器类型' },
      { name: 'switch + enum', desc: '枚举逐项处理，黄金搭档' },
      { name: '可空 ?', desc: 'int? 表示可为空' },
      { name: '?? 合并', desc: '左边空就用右边，防空' },
      { name: '三元 ?:', desc: '一行二选一' },
      { name: '属性 get/set', desc: '带读写保护的字段' },
    ] },
    { type: 'quiz', question: '想给「武器」变量定义几种固定类型（剑/弓/法杖/拳头），后续还能在检查器里下拉选择，用哪个最合适？', options: ['int 加注释', 'enum 枚举', 'string 字符串'], answer: 1, tip: '枚举就是「给一组固定选项命名」，比裸用数字/字符串清晰，且检查器会显示成下拉菜单；逻辑上 switch 配枚举逐项处理也最顺手。' },
  ],
  'prog-9': [
    { type: 'text', title: '动手练：用继承和接口重构', body: '🎯 任务目标：写一套「能挨打」的敌人体系——基类 Enemy 管生命值和受伤，两个子类近战 / 远程各自覆写攻击方式，再用接口 IDamageable 让玩家和敌人一样能被伤害。\n\n📦 前置要求：先学完类与对象（prog-1）、继承与多态（prog-2）、接口 Interface（prog-6）。建议在已有小球 / 金币的测试场景里做，方便验证。\n\n📋 动手步骤（每一步新建一个 .cs 文件，文件名与类名完全一致）：\n\n第 1 步｜写接口：右键项目面板 → Create → C# Script，命名 IDamageable，把默认内容整个替换成：\n\npublic interface IDamageable\n{\n    void TakeDamage(int amount);   // 约定：凡是「能挨打」的东西都得有这个能力\n}\n\n第 2 步｜写基类：新建脚本 Enemy。基类放所有敌人共用的东西——生命值、受伤逻辑；Attack 交给子类实现，所以写成 abstract。\n\npublic abstract class Enemy : MonoBehaviour, IDamageable\n{\n    public int health = 100;\n    public float moveSpeed = 2f;\n\n    public virtual void TakeDamage(int amount)   // 公共受伤逻辑\n    {\n        health -= amount;\n        if (health <= 0)\n        {\n            Destroy(gameObject);   // 血空就销毁\n        }\n    }\n\n    public abstract void Attack();   // 子类各自覆写\n}\n\n第 3 步｜写近战子类：新建脚本 MeleeEnemy，继承 Enemy 并 override Attack。\n\npublic class MeleeEnemy : Enemy\n{\n    public float attackRange = 2f;\n\n    public override void Attack()\n    {\n        Debug.Log(\"近战：挥刀砍一下，范围 \" + attackRange);\n        // 真正的实现可以在这里检测范围内的玩家并扣血\n    }\n}\n\n第 4 步｜写远程子类：新建脚本 RangeEnemy，同样继承 Enemy，攻击方式换成「射箭」。\n\npublic class RangeEnemy : Enemy\n{\n    public float fireRate = 1f;\n\n    public override void Attack()\n    {\n        Debug.Log(\"远程：射出一支箭\");\n        // 实现里可以 Instantiate 一支箭的预制体\n    }\n}\n\n第 5 步｜让玩家也实现接口：新建脚本 Player，玩家和敌人八竿子打不着，但实现同一个 IDamageable，就能统一被扣血。\n\npublic class Player : MonoBehaviour, IDamageable\n{\n    public int health = 100;\n\n    public void TakeDamage(int amount)\n    {\n        health -= amount;\n        if (health <= 0)\n        {\n            Debug.Log(\"玩家阵亡\");\n        }\n    }\n}\n\n第 6 步｜写统一扣血入口：新建脚本 DamageZone，挂在一个触发器上。它不关心打中的是谁，只认接口——这就是面向接口编程。\n\npublic class DamageZone : MonoBehaviour\n{\n    public int damage = 10;\n\n    void OnTriggerEnter(Collider other)\n    {\n        IDamageable target = other.GetComponent<IDamageable>();  // 管你是敌是玩家\n        if (target != null)\n        {\n            target.TakeDamage(damage);   // 能挨打就扣血\n        }\n    }\n}\n\n第 7 步｜场景里验证编译：建两个方块，一个挂 MeleeEnemy、一个挂 RangeEnemy；建一个球挂 Player；给 Player 球加一个 Box Collider 并勾 Is Trigger，挂上 DamageZone。等 Unity 编译完，看 Console 有没有红色报错。\n\n第 8 步｜跑起来验证：按 Play，让球滚进方块——两个敌人和玩家都实现了 IDamageable，DamageZone 会统一调用 TakeDamage；血扣到 0，敌人会被 Destroy，Console 输出对应日志。\n\n✅ 验收清单（全部做到算过关）：\n能 □ Enemy 基类定义了生命值和受伤方法，Attack 是 abstract\n能 □ MeleeEnemy 和 RangeEnemy 都继承 Enemy，并用 override 覆写了 Attack\n能 □ 玩家 Player 实现了 IDamageable，和敌人一样能被 TakeDamage\n能 □ 代码在 Unity 里编译通过，Console 没有红色报错\n能 □ 用 IDamageable 接口调用 TakeDamage，敌人和玩家都能被扣血\n\n🕳 常见卡壳：\n· override 编译报错 —— 基类方法必须是 virtual 或 abstract，子类方法必须带 override，且参数、返回类型和基类完全一致（漏写参数或类型不匹配都会报错）。\n· 抽象类挂不上物体 —— Enemy 是 abstract，检查器里 Add Component 找不到它，只能挂它的子类 MeleeEnemy / RangeEnemy。这是正常现象。\n· 一个 .cs 文件里放了多个类，或类名和文件名不一致 —— 会编译报错。一个文件一个公开类，文件名与类名保持一致；接口建议单独建文件。' },
    { type: 'quiz', question: '敌人共享的生命值 / 受伤逻辑放在基类，Attack 让子类各自覆写，同时玩家也要能挨打。下面哪个设计最合理？', options: ['把一切逻辑都写进 IDamageable 接口，敌人和玩家都实现它', 'Enemy 抽象基类放公共逻辑、Attack 用 override 覆写；玩家用 IDamageable 接口', '敌人之间完全不用继承，每个敌人自己写一份受伤逻辑'], answer: 1, tip: '抽象基类擅长「共用一套数据与实现」（生命值、受伤），override 让行为各自不同；接口擅长「跨类约定能力」（玩家和敌人都能挨打）。各司其职。' },
  ],
  'prog-10': [
    { type: 'text', title: '现代 C# 语法：lambda / LINQ / async', body: '现代 C# 语法这一课，把写游戏代码最常用的「现代招式」一次性补齐——lambda、Action/Func、LINQ、async/await、空安全。这些不是花架子，是实际工程里的日常。\n\n一、lambda 箭头函数：匿名的小方法\n\n(x, y) => x + y\n\n左边是参数、右边是表达式，整体就是一个「没有名字的方法」。它最常见的三个去处：\n· 回调 —— 比如给按钮注册点击处理\n· 排序 —— 自定义比较规则\n· LINQ —— 给查询写条件（见第三节）\n\n二、Action / Func：内置的委托\n\n委托就是「方法的类型」。C# 给你现成的两种：\n\nAction：无返回值的方法类型\n\nAction<int> onHurt = (dmg) => Debug.Log(\'受到 \' + dmg + \' 伤害\');\nonHurt(10);   // 调用\n\nFunc<T>：有返回值的方法类型，最后一个泛型参数就是返回值类型\n\nFunc<int, int> doubleIt = (n) => n * 2;\nint r = doubleIt(5);   // 10\n\n事件（Event）的底层就是委托，按钮的 onClick、伤害回调全是这套东西。\n\n三、LINQ：用「查询」的方式处理集合\n\nLINQ 让你像写 SQL 一样筛数据，不用手写一堆 for + if。最常用的四个方法：\n· Where —— 过滤\n· Select —— 投影（取某个字段）\n· OrderBy —— 排序\n· FirstOrDefault —— 取第一个，没有就返回默认值（null）\n\n从 List<敌人> 里挑血量最低的：\n\nusing System.Linq;\n\nvar weakest = enemies\n    .Where(e => e.isAlive)\n    .OrderBy(e => e.hp)\n    .FirstOrDefault();   // 活着且血量最低的那个，没有就 null\n\n一句话：把「遍历 + 条件判断 + 排序」折叠成一个查询表达式，可读性直接上一个台阶。\n\n四、async / await：真正的异步\n\n网络请求、加载场景、读文件这类「要等」的操作，用异步：\n\nasync Task<int> FetchScoreAsync()\n{\n    var result = await webRequest();   // 等结果回来，期间不卡主线程\n    return result;\n}\n\n· async 标记方法是异步方法，返回 Task（无返回）或 Task<T>（有返回）\n· await 挂起等待，拿到结果后继续往下走\n\n跟协程（IEnumerator + yield return）怎么选？\n· 协程：Unity 传统方案，靠 MonoBehaviour 驱动，暂停/恢复方便，但拿不到「返回值」、异常处理弱\n· async/await：返回值顺手、异常好抓（try/catch）、能多任务并行；但要注意物体销毁后别让回调继续跑\n\n简单规则：UI 响应、网络、读文件这类「要结果、要排错」的用 async；逐帧动画、按时间推进的游戏逻辑用协程。\n\n五、空安全：?. 和 ??\n\n?. 是「不为空才调」，?? 是「为空就用默认值」：\n\nonHurt?.Invoke(10);   // 委托为空就不调，不会报 NullReferenceException\nint score = player?.score ?? 0;   // player 或 score 为空就用 0\n\n写防御代码的两件套，配合使用，省掉一大堆 if (x != null)。\n\n六、常见坑\n· lambda 捕获循环变量：for 循环里注册 lambda，拿到的可能是循环结束的值 —— 用局部变量复制一份再捕获\n· async void 别滥用：只在事件处理器里用；普通方法返回 void 的 async，异常会直接崩，要返回 Task 并 try/catch\n· LINQ 别在 Update 里每帧查：LINQ 有开销，几万敌人的集合每帧 Where + OrderBy，帧率直接崩 —— 大集合少查、要每帧读的数据缓存起来\n\n一句话总结：lambda 写匿名方法、Action/Func 给方法当类型、LINQ 用查询处理集合、async/await 等异步、?. 和 ?? 防空——五件套是现代 C# 的日常，配上那三个坑位提醒，就能写出又简洁又稳的代码。' },
    { type: 'component', title: '现代 C# 语法速查', items: [
      { name: 'lambda', desc: '箭头匿名方法，(x,y) => x+y，回调/排序/LINQ 专用' },
      { name: 'Action / Func', desc: '内置委托：Action 无返回、Func<T> 有返回' },
      { name: 'LINQ', desc: 'Where/Select/OrderBy 像写查询一样处理集合' },
      { name: 'async / await', desc: 'Task 异步等待，返回值顺手、异常好抓' },
      { name: '?. 和 ??', desc: '空安全：?.Invoke() 防空、?? 给默认值' },
    ] },
    { type: 'quiz', question: '想从一队敌人里挑出「存活且血量最低」的那个，最简洁的写法是？', options: ['手写 for 循环遍历所有敌人、逐个比较血量', '用 LINQ：enemies.Where(e => e.isAlive).OrderBy(e => e.hp).FirstOrDefault()', '让敌人列表在 Awake 里自动排序好'], answer: 1, tip: 'LINQ 把「过滤 + 排序 + 取首项」一步写完：Where 筛存活、OrderBy 按血量排、FirstOrDefault 取第一个，既简洁又可读；手写 for 也能做，但啰嗦且容易错。' },
  ],
  'prog-11': [
    { type: 'text', title: '协程深入：yield 的各种写法', body: 'prog-3 学了协程基础：IEnumerator 函数 + yield return + StartCoroutine。这一课把它拧到底——yield 能返回的不止 WaitForSeconds 一种，不同的 return 对应不同的「继续时机」，选对了协程才真正顺手。\n\n一、yield return 全家福\n\n· yield return null —— 等一帧。最常用的初始化、下一帧再开始干活，就是它\n· yield return new WaitForSeconds(t) —— 等 t 秒。游戏时间，受 Time.timeScale 影响，做延时、做节奏就它\n· yield return new WaitForSecondsRealtime(t) —— 等真实世界的 t 秒。timeScale 怎么改它都不受影响，暂停菜单的倒计时、死亡回放用它\n· yield return new WaitForEndOfFrame() —— 等这一帧完全渲染完。截图、帧尾的 UI 更新、后处理采样用它\n· yield return new WaitUntil(条件) —— 等到某个条件为 true。例：yield return new WaitUntil(() => health <= 0)\n· yield return new WaitWhile(条件) —— 等到某个条件变 false。和 WaitUntil 是一对，挑顺口的那边写\n\n场景对照一句话：想「下帧再继续」用 null；想「延时 N 秒」用 WaitForSeconds；想「暂停时也走」用 WaitForSecondsRealtime；想「等这一帧结束」用 WaitForEndOfFrame；想「等一个条件成立」用 WaitUntil / WaitWhile。\n\n二、代码示例\n\nIEnumerator DoRoutine()\n{\n    yield return null;                              // 等一帧\n    yield return new WaitForSeconds(1f);            // 等 1 秒游戏时间\n    yield return new WaitForSecondsRealtime(1f);    // 等 1 秒真实时间\n    yield return new WaitForEndOfFrame();           // 等帧完全渲染完\n    yield return new WaitUntil(() => isReady);      // 等 isReady 为 true\n    yield return new WaitWhile(() => isBusy);       // 等 isBusy 变 false\n}\n\n三、停协程的正确姿势\n\nStartCoroutine 会返回一个 Coroutine 对象，留着它就能精确停：\n\nCoroutine routine = StartCoroutine(DoRoutine());\nStopCoroutine(routine);     // 按对象停\n\n也可以按名字停：StopCoroutine("DoRoutine")。注意：两个同名协程叠着跑时，按名字会「全停掉」；用返回的 Coroutine 对象最精准。还有个大前提——协程挂在哪个 MonoBehaviour 上就用哪个 Start/StopCoroutine，物体被 Destroy 的那一瞬间，它身上的协程立刻全停。\n\n四、协程嵌套\n\n一个协程里可以直接 yield 另一个协程，等它跑完再继续：\n\nIEnumerator Outer()\n{\n    yield return StartCoroutine(Inner());   // 等 Inner 跑完\n    Debug.Log("Inner 完成，外协程继续");\n}\n\nIEnumerator Inner()\n{\n    yield return new WaitForSeconds(2f);\n}\n\n这里 StartCoroutine 返回的也是 Coroutine，yield 它等于「等它结束」。嵌套多了会形成调用链，是处理「顺序演出」（开场动画、多段剧情）的经典套路。\n\n五、协程 vs async/await（prog-10 学过）\n\n· 协程：Unity 传统方案，由 MonoBehaviour 驱动，逐帧推进、可随时停、语法简单；但拿不到「返回值」，异常处理弱（没有天然的 try/catch 支持）\n· async/await：能 return 结果、能 try/catch 抓异常、能 Task.WhenAll 并行；但绕开 Unity 生命周期，物体销毁后回调可能还在跑，要自己留意\n\n选型规则：UI 响应、网络、读文件这类「要结果、要排错」的用 async/await；逐帧动画、按时间推进的游戏逻辑用协程。\n\n六、常见坑\n\n· 协程挂在 GameObject 上：协程是 MonoBehaviour 的方法，物体销毁、或 SetActive(false) 后，挂它的协程全停。想「全局不死」的计时逻辑，别挂在临时物体上。\n· timeScale=0 时 WaitForSeconds 不走：暂停菜单把 timeScale 设 0，所有 WaitForSeconds 冻结——要用 WaitForSecondsRealtime。\n· 每帧 new WaitForSeconds 的开销：高频循环里每次都 new 一个对象，攒 GC。同一延时值可以缓存复用：static readonly WaitForSeconds oneSec = new WaitForSeconds(1f);\n· 别在 Update 里狂开协程：每帧 StartCoroutine 成千上万，帧率直接崩。协程是「按需开」，不是「每帧开」；高频逻辑直接写在 Update 里反而更省。\n\n一句话总结：yield 的选择就是「时机」的选择——null 等帧、WaitForSeconds 等游戏时间、Realtime 等真实时间、WaitUntil/While 等条件；用 StartCoroutine 的返回值精确停协程、协程可嵌套、要返回值就上 async/await，坑全记住，协程就玩明白了。' },
    { type: 'component', title: 'yield 速查', items: [
      { name: 'yield return null', desc: '等一帧' },
      { name: 'WaitForSeconds', desc: '等 N 秒游戏时间，受 timeScale 影响' },
      { name: 'WaitForSecondsRealtime', desc: '等真实 N 秒，暂停菜单用它' },
      { name: 'WaitForEndOfFrame', desc: '等这一帧完全渲染完' },
      { name: 'WaitUntil / WaitWhile', desc: '等条件成立 / 等条件变假' },
      { name: 'StopCoroutine', desc: '用 StartCoroutine 的返回值精确停' },
    ] },
    { type: 'quiz', question: '游戏里把 Time.timeScale 设成 0 做暂停菜单，此时哪些协程 yield 还会继续推进？', options: ['WaitForSeconds 和 WaitForEndOfFrame', 'WaitForSecondsRealtime 和 WaitUntil / WaitWhile', '所有 yield 都会正常推进'], answer: 1, tip: 'WaitForSeconds 的计时依赖 timeScale，timeScale=0 时它永远不结束；WaitForSecondsRealtime 走真实时钟，不受影响；WaitUntil / WaitWhile 靠条件判断，也照常工作。暂停菜单的倒计时就用 Realtime。' },
  ],
  'prog-12': [
    { type: 'text', title: '命名空间与脚本组织', body: 'namespace 是什么：给代码「分组」的名字。工程一大、类一多，两个脚本都叫 Player 怎么办？namespace 把同名类隔开：Game.Player 和 UI.Player 互不干扰。Unity 新建脚本默认不带 namespace（挂进全局命名空间），但对工具库、插件，namespace 是标配。\n\n一、声明 namespace\n\nnamespace Game.Weapons\n{\n    public class Sword\n    {\n        public int damage = 10;\n    }\n}\n\nnamespace Game.Armor\n{\n    public class Sword   // 另一个 Sword，和武器的剑同名不冲突\n    {\n        public int defense = 20;\n    }\n}\n\nGame.Weapons.Sword 和 Game.Armor.Sword 是两个不同的类——namespace 就像文件夹，把同名文件分开放。\n\n二、using 引用：少打前缀\n\n在别的文件里用 Sword，不想每次写全名：\n\nusing Game.Weapons;\n\npublic class Player : MonoBehaviour\n{\n    Game.Weapons.Sword sword1 = new Game.Weapons.Sword();  // 不写 using 写全名\n    Sword sword2 = new Sword();                             // 写了 using 直接短名\n}\n\n三、namespace 嵌套\n\nnamespace 可以用点号表示层级：namespace Game.Weapons 等于「Game 里套 Weapons」。也可以一层层套：\n\nnamespace Game\n{\n    namespace Weapons\n    {\n        public class Sword { }\n    }\n}\n\n两种写法等价，点号写法更常见。\n\n四、脚本目录结构：namespace 和文件夹配套\n\n类放在哪个文件夹，就该在哪层 namespace——保持「文件夹 ↔ namespace」一致，找代码跟翻文件夹一样直觉：\n\nAssets/Scripts/\n  Game/Weapons/Sword.cs   → namespace Game.Weapons\n  Game/Weapons/Bow.cs     → namespace Game.Weapons\n  Game/Player/Player.cs   → namespace Game.Player\n\n好处：新同事（包括三个月后的你）看文件夹就猜得到 namespace，全局搜索也不串。\n\n五、常见坑\n\n· using 拼写或层级写错：编译报「找不到类型或命名空间」。对着 namespace 声明逐级查\n· 两个同名类都被 using 进来：编译器报「歧义」。用全名（带前缀）区分，或只 using 其中一个\n· 无 namespace 和带 namespace 混用：带前缀的类用全名调无前缀的类。小项目统一：要么全带、要么全不带\n· 自己类叫 Physics、Debug 等内置类型名：和 UnityEngine 重名，编译期一片混乱。避开内置名\n\n一句话总结：namespace 给代码分组防重名，using 把前缀省掉，文件夹和 namespace 配套管理脚本——工程一大，组织就值钱。' },
    { type: 'component', title: '命名空间关键概念', items: [
      { name: 'namespace', desc: '给代码分组，同名类放进不同组互不冲突' },
      { name: 'using', desc: '声明引用，省掉 namespace 前缀' },
      { name: '点号嵌套', desc: 'namespace Game.Weapons 表示层级' },
      { name: '文件夹配套', desc: '类放哪层文件夹，namespace 就跟到哪层' },
      { name: '全局命名空间', desc: '不写 namespace 的类都在这，注意重名' },
    ] },
    { type: 'quiz', question: '两个不同系统里各有一个叫 Sword 的类，想让它们在一个项目里共存，最合适的做法？', options: ['改文件名，一个叫 Sword1 一个叫 Sword2', '用 namespace 分组，Game.Weapons.Sword 和 Game.Armor.Sword 互不冲突', '把两个类合并到一个文件里'], answer: 1, tip: 'namespace 就是给类分组的「文件夹」：同名类放进不同 namespace 就井水不犯河水，调用时用全名或 using 声明即可；改文件名是治标不治本，合并类是下策。' },
  ],
  'prog-13': [
    { type: 'text', title: '异常处理', body: '异常（Exception）是程序运行时「出岔子」的信号。游戏里最常见的：NullReferenceException（空引用）、数组越界、除零、格式转换失败。异常处理就是「预感这里可能出事 → 提前设兜底 → 出了事接住，不让整个游戏崩掉」。\n\n一、try / catch：把「可能出事」的代码包起来\n\ntry\n{\n    int index = int.Parse(inputText);   // 用户输入的不是数字会抛异常\n    scores[index] = 100;\n}\ncatch (System.FormatException e)        // 只接「格式错误」这一种\n{\n    Debug.LogWarning("输入的不是数字，请重试");\n}\n\n· try 里放「可能出问题」的代码\n· catch 接住异常，让程序继续跑，而不是崩掉\n· 可以 catch 特定类型：FormatException、NullReferenceException、IndexOutOfRangeException……分类接，各给各的对策\n\n二、finally：不管成败都执行\n\ntry\n{\n    SaveGame();\n}\ncatch (System.Exception e)\n{\n    Debug.LogError("存档失败：" + e.Message);\n}\nfinally\n{\n    Debug.Log("保存流程结束");   // 成功失败都走到这\n}\n\nfinally 常用来「收尾」：释放资源、关闭连接、记日志——哪怕 catch 里又出错，finally 也保证跑。\n\n三、主动抛异常：throw\n\n自己的方法「发现问题」，可以用 throw 主动报告：\n\npublic void AddItem(string itemName, int count)\n{\n    if (count < 0)\n        throw new System.ArgumentOutOfRangeException("count", "数量不能是负数");\n    // ...\n}\n\n调用方就能用 try/catch 接住。注意：抛异常是「极端情况」的信号，别拿它当普通流程控制（性能差、代码难读）。\n\n四、Unity 里的特殊习惯\n\n· 开发期调试：不写 try/catch，让异常直接弹到 Console——越早暴露越好，屏蔽掉反而藏 bug\n· 上线容错：对「外部输入」这种不可控环节（网络、用户输入、读文件）包 try/catch，防线上崩\n· NullReferenceException 是头号敌人：多用 ?. 和 ??（prog-10 学过）、启动时检查引用、别依赖「应该不会空」\n\n五、常见坑\n\n· catch 吞异常不留痕迹：catch 完什么都不记，bug 消失了也无从查起。至少 Debug.LogError(e)\n· catch (Exception e) 太宽泛：把错一锅端，真 bug 被误杀。尽量 catch 具体类型\n· 把 try/catch 放进每帧 Update：异常检查有开销，高频路径别拿异常做逻辑判断\n· try 块过大：一大坨全包起来，出问题不知道是哪一行。try 包最小范围\n\n一句话总结：try/catch/finally 给「会出事的代码」装护栏——try 尝试、catch 分类接住、finally 收尾；throw 主动报告问题；开发期让异常裸奔暴露，上线对外部输入包护栏。' },
    { type: 'component', title: '异常处理要点', items: [
      { name: 'try / catch', desc: '包住可能出事的代码，分类接住异常' },
      { name: 'finally', desc: '成败都执行，用来收尾' },
      { name: 'throw', desc: '主动抛异常，报告极端情况' },
      { name: '常见异常', desc: 'NullReference / Format / IndexOutOfRange' },
      { name: '?. 和 ??', desc: '防空引用，NullReference 头号克星' },
    ] },
    { type: 'quiz', question: '用户输入转成数字、写进数组，用户可能输入非数字，希望程序别崩、给个提示。正确的做法？', options: ['不管，让它崩', '用 try/catch 接 FormatException，提示用户重新输入', '把数组换成 List 就不需要处理了'], answer: 1, tip: '外部输入是不可控环节，try/catch 接住 FormatException 就能「出岔子不崩、给用户回话」；换成 List 解决的是越界问题，不是格式错误。' },
  ],
  'prog-14': [
    { type: 'text', title: '字符串进阶', body: '字符串是 C# 里最容易写「慢」的地方。string 是不可变的——每次 + 拼接，其实都新建了一个字符串、丢掉旧的。这一课讲拼接的正确姿势、插值语法和格式化。\n\n一、string 不可变：拼接的真实代价\n\nstring name = "玩家";\nname = name + "打出了暴击";   // 这行干了什么？\n\n实际过程：先造一个新字符串「玩家打出了暴击」，再让 name 指向它，旧的「玩家」变成垃圾等回收。拼一次还行，循环里拼几百次，垃圾堆积、GC 卡顿（Unity 里就是掉帧）。\n\n二、循环拼接用 StringBuilder\n\n要在循环里攒一个长字符串（日志、聊天记录、排行榜文本）：\n\nusing System.Text;\n\nStringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 1000; i++)\n{\n    sb.Append("第 " + i + " 行\\n");\n}\nstring result = sb.ToString();   // 最后一次性转成字符串\n\nStringBuilder 内部是可变的缓冲区，往里 Append 只在末尾追加，不新建、不丢垃圾——循环拼接的救星。\n\n三、字符串插值 $"..."：可读性之王\n\nstring msg = $"玩家 {playerName} 击杀了 {killCount} 个敌人，剩余生命 {hp}";\n\n$ 前缀 + 花括号 {} 把变量直接嵌进字符串，比 + 拼接清爽得多。花括号里的表达式可以很丰富：\n\nDebug.Log($"位置：({transform.position.x:F1}, {transform.position.y:F1})");  // :F1 保留一位小数\n\n四、string.Format：老式占位符\n\nstring msg = string.Format("击杀 {0} 个敌人，得分 {1}", kills, score);\n\n{0} {1} 是占位符，按顺序填参数。插值 $"" 能做的它都能做，只是写法老一些。团队老代码里常见，要能读懂。\n\n五、性能注意\n\n· 小次数拼接（< 5 次）：随便 +，差别可忽略\n· 循环/高频里拼长串：用 StringBuilder\n· 每帧生成字符串是掉帧大户：Debug.Log 传参本身会 ToString，高频 Update 里少 Log；UI 文本刷新也攒着点\n· 插值 $"" 性能和 + 相当：它编译后也是 string.Concat，图的是可读性，不图性能\n\n一句话总结：string 不可变，循环拼接用 StringBuilder；插值 $"" 让字符串可读性飞起，string.Format 是旧写法要看得懂；性能记一句——循环拼长串走 StringBuilder，每帧别狂造字符串。' },
    { type: 'component', title: '字符串工具速查', items: [
      { name: 'string 不可变', desc: '+ 拼接每次新建字符串，攒垃圾' },
      { name: 'StringBuilder', desc: '可变缓冲区，循环拼长串专用' },
      { name: '插值 $"{}"', desc: '变量直接嵌进字符串，可读性之王' },
      { name: 'string.Format', desc: '老式占位符 {0} {1}，要能读懂' },
      { name: ':F1 格式', desc: '保留小数等格式化说明符' },
    ] },
    { type: 'quiz', question: '在 Update 里把 100 个物体的坐标用 + 拼成一行日志，每秒 60 次，会怎样？', options: ['没问题，C# 会自动优化', '每帧都新建多个字符串，垃圾堆积、GC 卡顿', '代码会编译报错'], answer: 1, tip: 'string 不可变，+ 拼接每次都是新建字符串。高频循环里用 StringBuilder 攒、最后一次性 ToString；而且每帧打 100 行日志本身就是性能灾难，少 Log 才是根治。' },
  ],
  'prog-15': [
    { type: 'text', title: '委托与事件实战', body: 'prog-5 学过事件与委托的概念，这一课用一个完整的「伤害事件」把它串成实战：玩家被打 → 广播事件 → 血条、特效、成就系统各自响应，互不牵连。这是事件驱动设计的标准范式。\n\n一、先看完整例子：伤害事件系统\n\n定义事件：在角色类里声明「受伤事件」，谁挨打谁广播：\n\npublic class Character : MonoBehaviour\n{\n    public event System.Action<int> OnDamaged;   // 事件：受伤时广播，参数是伤害值\n\n    public void TakeDamage(int amount)\n    {\n        if (OnDamaged != null)\n            OnDamaged(amount);      // 广播：告诉所有订阅者「我挨打了」\n    }\n}\n\n订阅方：血条、特效、成就各自关注这个事件：\n\npublic class HealthBar : MonoBehaviour\n{\n    public Character character;\n\n    void Start()\n    {\n        character.OnDamaged += UpdateBar;     // 订阅：受伤时我来刷新血条\n    }\n\n    void OnDestroy()\n    {\n        character.OnDamaged -= UpdateBar;     // 反订阅：我销毁了，别找我了\n    }\n\n    void UpdateBar(int damage)\n    {\n        Debug.Log($"血条收到：-{damage}");\n    }\n}\n\n二、这套设计好在哪里\n\n· 解耦：Character 完全不知道血条、特效、成就的存在，它只管「广播」，谁关心谁订阅\n· 扩展：以后想加「震动」「连击计数」，写个新类订阅即可，一行不改角色代码\n· 一处改、多处响应：伤害这个动作一变，所有订阅方自动跟着变\n\n三、event 关键字：安全锁\n\n用 event 修饰后，外部只能 += / -= 订阅退订，不能直接 OnDamaged(10) 乱触发——广播权只在自己手里。这是事件和公开委托的本质区别：事件把「触发权」和「订阅权」分开。\n\n四、Unity 里更顺手的写法：UnityEvent\n\n想在 Inspector 里拖拽绑定（不写代码就把事件接到函数上），用 UnityEvent：\n\nusing UnityEngine.Events;\n\npublic class Character : MonoBehaviour\n{\n    public UnityEvent<int> OnDamaged = new UnityEvent<int>();\n\n    public void TakeDamage(int amount)\n    {\n        OnDamaged.Invoke(amount);\n    }\n}\n\n在 Inspector 里给 OnDamaged 加监听，把血条的 UpdateBar 拖进去。UI 按钮的 onClick 就是这套东西。\n\n五、常见坑\n\n· 忘退订：订阅的物体被销毁，但事件持有者还活着，下次广播会调「幽灵方法」→ 报错或诡异表现。OnDestroy 里 -= 反订阅，养成习惯\n· 空事件直接调用：没人订阅时事件是 null，直接调用会报空引用。用 if (OnDamaged != null) 判空，或 .Invoke 前判空\n· 把事件当委托到处调：外部直接触发别人的事件，打破封装。event 关键字挡住它\n\n一句话总结：事件让「广播」和「订阅」解耦——受伤方只广播，血条/特效/成就各自订阅响应，加新功能不动老代码；event 锁触发权、UnityEvent 支持检查器拖拽、OnDestroy 记得退订。' },
    { type: 'component', title: '事件实战要点', items: [
      { name: '声明事件', desc: 'public event System.Action<int> OnDamaged' },
      { name: '广播', desc: '挨打方触发事件，通知所有订阅者' },
      { name: '订阅 / 退订', desc: '+= 订阅、-= 退订，OnDestroy 必退' },
      { name: 'event 关键字', desc: '外部只能订阅退订，不能乱触发' },
      { name: 'UnityEvent', desc: '支持检查器拖拽绑定，UI onClick 同款' },
    ] },
    { type: 'quiz', question: '角色受伤时，血条要刷新、特效要播放、成就要判断，且以后可能还要加「震动」。最不容易把代码搞乱的设计？', options: ['在 TakeDamage 里依次调用血条、特效、成就的方法', '用事件：Character 广播 OnDamaged，血条/特效/成就各自订阅响应', '让血条在 Update 里每帧检查角色血量'], answer: 1, tip: '事件驱动是解耦利器：角色只广播不关心谁响应，加新功能就加个新订阅者，不改角色一行代码；挨个调用会让角色越来越臃肿、每帧轮询又浪费性能。' },
  ],
  'prog-16': [
    { type: 'text', title: '静态类与工具类', body: 'static（静态）是 C# 里「不挂在具体对象身上」的成员。理解 static 一句话：普通成员属于「某一个实例」，静态成员属于「这个类本身」——全类共享一份。工具类、全局配置、Mathf 全建在这套机制上。\n\n一、静态成员：属于类，不属于实例\n\npublic class GameData\n{\n    public static int score;            // 静态字段：全局唯一一份\n    public string playerName;           // 实例字段：每个对象各一份\n}\n\nGameData.score = 100;                   // 直接用类名访问，不需要 new\nGameData g1 = new GameData();           // 实例还得 new 出来\ng1.playerName = "小明";\n\n· 静态成员用「类名.成员」直接访问，不用 new\n· 所有代码共享同一份静态数据——全局分数、设置、当前关卡这种「只有一个」的东西用它\n\n二、静态类：工具类标准写法\n\nstatic class 不能 new、不能继承、里面全是静态成员：\n\npublic static class MathHelper\n{\n    public static float Clamp01(float v)\n    {\n        if (v < 0f) return 0f;\n        if (v > 1f) return 1f;\n        return v;\n    }\n\n    public static float Normalize(float value, float min, float max)\n    {\n        return (value - min) / (max - min);\n    }\n}\n\n用的时候：\n\nfloat t = MathHelper.Normalize(hp, 0f, maxHp);   // 血量归一化到 0~1\n\n这就是「工具类」：一堆不依赖实例的纯函数，像 Mathf 那样直接类名调用。\n\n三、Mathf 就是最典型的工具类\n\nMathf.Clamp、Mathf.Lerp、Mathf.Abs、Mathf.Max……Unity 的 Mathf 全是静态方法，所以才能直接 Mathf.xxx 调用。自己写游戏里重复的小工具，就照这个模式封装成静态类。\n\n四、静态构造：第一次使用前跑一次\n\nstatic 类（或类里的静态成员）可以在「第一次被用到」时执行一段初始化：\n\npublic static class Config\n{\n    public static readonly float jumpHeight;\n    public static readonly float moveSpeed;\n\n    static Config()          // 静态构造函数：类第一次被访问前自动执行一次\n    {\n        jumpHeight = 1.5f;\n        moveSpeed = 5f;\n    }\n}\n\n· 静态构造没有访问修饰符、不带参数、只执行一次\n· readonly 静态字段只能在构造里赋值——配置类的标准姿势\n\n五、静态 vs 单例（prog-4 学过）\n\n· 单例：保留 MonoBehaviour 的生命周期、Inspector、协程，但要管理实例\n· 静态类：纯粹、轻量、无实例概念，但不能挂组件、没有生命周期函数\n· 选型：纯数据/纯函数 → 静态类；要 MonoBehaviour、要场景内物体 → 单例\n\n六、常见坑\n\n· 滥用静态字段存一堆全局状态：静态全局变量是「隐式耦合」，多到一定程度代码就难查难改。有状态的数据优先实例/单例，静态多放常量、纯函数\n· 静态类里放了实例成员：编译报错——static class 只能装静态成员\n· 场景切换静态数据不清零：static 数据跨场景存活（除非手动重置），关关卡数值残留，注意切换时重置\n· Update 里反复 new 一个工具类对象：工具类本来就不用 new，直接类名调用\n\n一句话总结：static 让成员属于「类」不属于「实例」，静态类是纯函数工具库（像 Mathf 那样用）、静态构造只在首次访问跑一次；纯函数用静态类、要挂场景用单例，全局状态别滥用。' },
    { type: 'component', title: 'static 关键概念', items: [
      { name: '静态成员', desc: '属于类不属于实例，类名.成员直接访问' },
      { name: '静态类', desc: '不能 new、全是静态成员，工具类标准写法' },
      { name: 'Mathf', desc: 'Unity 现成的静态工具类典范' },
      { name: '静态构造', desc: '第一次使用前自动执行一次的初始化' },
      { name: 'readonly 静态', desc: '只能在构造里赋值，配置类标配' },
    ] },
    { type: 'quiz', question: '想在多个脚本里共用一段「把血量归一化到 0~1」的计算，最合适的封装？', options: ['在每个脚本里各写一遍', '写一个静态工具类 MathHelper，方法用 static，直接用类名调用', '写一个 MonoBehaviour，挂到场景空物体上再 Find 它'], answer: 1, tip: '纯函数就该进静态工具类：不需要实例、不需要挂场景，类名.方法直接调用，全工程共享一份。挂场景还得 Find 实例，纯属绕路。' },
  ],
  'prog-17': [
    { type: 'text', title: '属性与封装', body: '属性（Property）是 C# 给「字段加护栏」的标准姿势：外部读起来像字段，内部却可以拦截、校验、计算。这一课把 get/set、自动属性、只读、封装一次讲透。\n\n一、为什么字段要封装\n\npublic int hp;   // 直接暴露字段\n\n直接暴露字段，别处代码想怎么改就怎么改，hp = -999 也拦不住；属性则能在「写入」时拦下来校验。\n\n二、属性语法：get / set\n\nprivate int hp;                         // 真正的数据存在私有字段\n\npublic int HP\n{\n    get { return hp; }                  // 读取时执行\n    set\n    {\n        hp = Mathf.Max(0, value);       // 写入时执行，value 是要写入的值\n    }\n}\n\nHP = -50;   // 写进去之前被 setter 拦住，hp 实际存 0\n\n· set 里的 value 是「调用方要写入的值」\n· get/set 里可以加任意逻辑：校验、计算、触发事件（prog-15 的伤害事件就能在 setter 里广播）\n\n三、自动属性：无脑属性\n\n不需要额外逻辑时，简写：\n\npublic int Score { get; set; }       // 编译器自动生成隐藏字段\n\npublic float Speed { get; private set; }   // 外部可读，只有自己能写\n\n自动属性 = 字段 + 无逻辑的 get/set，是「先留着以后可能要加逻辑」的保险——现在用字段，以后想拦数据还得改一遍外部代码。\n\n四、只读：只读不写\n\n只读属性：只提供 get，没有 set：\n\npublic int MaxHealth { get; }   // 只能在构造函数里赋值\n\n或者用只读字段：\n\npublic readonly int MaxHealth = 100;   // readonly 字段：只能声明处或构造函数赋值\n\n区别：只读属性（没有 set）和 readonly 字段都能实现「初始化后不能改」；属性还能顺手加计算逻辑，readonly 字段是纯粹的「不可变值」。\n\n五、表达式体属性：一行简洁\n\n逻辑只有一行的属性，用 => 缩写：\n\npublic int RemainingPercent => (int)(100f * hp / maxHp);   // 每次读时计算\n\npublic bool IsDead => hp <= 0;\n\n只读、按需计算的属性，这样写最利落。注意：=> 写法是「每次访问都现算」，不是缓存值。\n\n六、常见坑\n\n· 属性里写死循环/开销大的逻辑：getter 里跑遍历、Debug.Log，每次读都执行，会在意想不到的地方爆开销。getter 保持轻量\n· setter 里忘了校验：setter 的意义就是校验，别只写 hp = value 冒充\n· 自动属性想加逻辑时：改成「字段 + 手动属性」，调用处不用改（外部语法一样）——这正是封装的好处\n\n一句话总结：属性 = 给字段加读写护栏——set 校验、get 计算，自动属性先留余地、只读属性防外部改动、表达式体属性一行算完；封装的收益是：内部随便改，外部语法不变。' },
    { type: 'component', title: '属性关键概念', items: [
      { name: 'get / set', desc: '读取与写入时执行的访问器' },
      { name: 'value 关键字', desc: 'setter 里代表「要写入的值」' },
      { name: '自动属性', desc: '{ get; set; } 简写，留余地的保险' },
      { name: '只读', desc: '没有 set 或 readonly，初始化后不能改' },
      { name: '表达式体', desc: '=> 一行缩写，每次访问现算' },
    ] },
    { type: 'quiz', question: '想让血量永远不会被外部设成负数，同时外部读写方式不变，最合适？', options: ['直接用 public int hp，大家自觉点', 'private 字段 + 属性，set 里 Mathf.Max(0, value)', '把 hp 变成 static'], answer: 1, tip: '属性就是干这个的：数据存私有字段，set 里校验拦截负值。外部读写语法跟字段一模一样，但写入被 setter 卡住——这就是封装的意义。' },
  ],
  'prog-18': [
    { type: 'text', title: '序列化深入', body: '序列化 = 把数据「变成可保存/可传输的形态」，反序列化 = 变回来。Unity 里它管两件大事：Inspector 面板上显示你的字段、存档读档。这一课把 [SerializeField]、[Serializable]、JSON 一次讲透。\n\n一、[SerializeField]：私有字段也能进 Inspector\n\npublic 字段默认显示在 Inspector；private 字段默认不显示。想让 private 字段也能在面板上调整：\n\npublic class Player : MonoBehaviour\n{\n    [SerializeField] private float speed = 5f;     // 私有字段，面板可见可调\n    [SerializeField] private int hp;\n}\n\n面板上出现了 speed、hp 两个槽。好处：数据被 private 封装保护（prog-17），又能在面板调。\n\n二、[HideInInspector]：反着来\n\npublic int score;             // public 默认显示\n[HideInInspector] public int uid;   // public 但不想在面板上露出来\n\n三、[Serializable]：把类/结构体序列化\n\n想让自定义的类、结构体在 Inspector 里「折叠展开可编辑」，加 [Serializable]：\n\n[System.Serializable]\npublic class Weapon\n{\n    public string name;\n    public int damage;\n    public float fireRate;\n}\n\npublic class Player : MonoBehaviour\n{\n    [SerializeField] private Weapon weapon;   // 面板上变成一个可展开的组\n}\n\n结构体、含嵌套字段的类、甚至 List<Weapon> 都能这样序列化。注意：必须加 [Serializable]，否则字段全不显示。\n\n四、JsonUtility：JSON 存档\n\nUnity 内置 JsonUtility 把对象转成 JSON 字符串（可存档/可发网络）：\n\n[System.Serializable]\npublic class SaveData\n{\n    public int score;\n    public int level;\n    public string playerName;\n}\n\nSaveData data = new SaveData { score = 100, level = 3, playerName = "小明" };\nstring json = JsonUtility.ToJson(data);    // 转 JSON\nSaveData loaded = JsonUtility.FromJson<SaveData>(json);   // 从 JSON 读回对象\n\n五、JSON 实用细节\n\n· 字段必须是 public 或带 [SerializeField]：JsonUtility 只序列化这两类\n· 支持嵌套、List、数组；不支持 Dictionary：字典要先转成 List 或自定义结构\n· 存 PlayerPrefs：PlayerPrefs.SetString("save", json) 就能持久化存档\n· 注意：JsonUtility 不支持属性（get/set 那种），只认字段——存档类别写成属性\n\n六、常见坑\n\n· 忘了 [Serializable]：Inspector 里自定义类字段一片空白，检查是不是漏了这个特性\n· 字段名改动：改字段名会让已存档的 JSON 对不上（反序列化丢字段）。存档结构定了就别乱改名字，加字段要给默认值\n· 字典序列化不了：用 List<KeyValuePair> 或自己拼个列表结构代替\n· 在属性上挂 [SerializeField]：无效，它只认字段\n\n一句话总结：[SerializeField] 把私有字段送进 Inspector、[Serializable] 让自定义类可折叠编辑、JsonUtility 把对象转 JSON 存档读档；三件套配齐，面板显示和存档读写都拿下了。' },
    { type: 'component', title: '序列化三件套', items: [
      { name: '[SerializeField]', desc: '私有字段也能在 Inspector 显示可调' },
      { name: '[HideInInspector]', desc: 'public 字段不想露出来时用' },
      { name: '[Serializable]', desc: '让类/结构体可折叠编辑、可 JSON' },
      { name: 'JsonUtility', desc: 'ToJson / FromJson 存档读档' },
      { name: 'PlayerPrefs', desc: 'SetString 存 JSON 字符串做存档' },
    ] },
    { type: 'quiz', question: '想让自定义的 Weapon 类在 Inspector 里能折叠编辑、还能被 JsonUtility 存档，缺一不可的写法？', options: ['类加 [Serializable]，字段用 public 或 [SerializeField]', '类加 [SerializeField] 就够了', '把类里的字段全写成属性'], answer: 0, tip: '[Serializable] 让类能被序列化（面板可折叠、JsonUtility 可用），public 或 [SerializeField] 字段才能被序列化进数据。[SerializeField] 是给字段的、[Serializable] 是给类/结构体的，两个都要。' },
  ],
  'prog-19': [
    { type: 'text', title: '生命周期实战', body: 'MonoBehaviour 有一串「自动被调用的函数」，顺序固定、用法不同，这就是生命周期。新脚本默认生成的 Awake / Start / Update 只是其中三个。这一课把完整时序和各自用途讲透。\n\n一、完整时序（一个物体被激活的流程）\n\n物体被实例化/激活时，按这个顺序调用：\n\nAwake → OnEnable → Start → （每帧 Update → LateUpdate → 固定频率 FixedUpdate）→ ... → OnDisable → OnDestroy\n\n二、Awake vs Start：初始化分两步\n\n· Awake：物体被创建瞬间调用一次。哪怕脚本还没启用（enabled=false）也会跑。适合：获取组件引用（GetComponent）、初始化自身数据、设单例 instance\n· Start：在 Awake 之后、第一次 Update 之前调用一次，且要求脚本已启用。适合：需要所有物体都 Awake 完才能做的事（查别的物体、订阅事件）\n\n经典分工：\n\nvoid Awake()\n{\n    rb = GetComponent<Rigidbody>();     // 拿自己身上的引用\n    GameManager.instance = this;         // 单例就位\n}\n\nvoid Start()\n{\n    GameManager.instance.Register(this); // 其他物体都 Awake 完了，可以找它们了\n}\n\n规则：引用自己的组件 → Awake；依赖别的物体的状态 → Start。\n\n三、OnEnable / OnDisable：开关时刻\n\n· OnEnable：物体每次「启用」（包括首次创建）都调用——比 Awake 更频繁\n· OnDisable：物体每次「停用」调用（包括销毁前）\n\n用途：订阅/退订事件（prog-15 讲过的 -= 放这儿最稳）、暂停时停掉协程。物体 SetActive(false) 后 Update 不再跑、但 OnDisable 会跑——清理动作放这对最合适。\n\n四、Update / LateUpdate / FixedUpdate\n\n· Update：每帧调用。管「每帧都要做的事」：读输入、移动逻辑\n· LateUpdate：每帧在 Update 之后调用。管「跟着别人动」：相机跟随角色放这儿，避免抖动（角色先动、相机后动）\n· FixedUpdate：固定频率调用（默认 50 次/秒），不受帧率影响。管「物理」：和 Rigidbody 打交道时用\n\n五、销毁：OnDestroy\n\n物体销毁（包括场景切换）前调用一次。用途：退订事件、保存数据、释放自己创建的东西。注意：场景退出时在编辑器里也会触发（Play 结束），别在这里依赖别的物体。\n\n六、生命周期 × 常见坑\n\n· 在 Awake 里 Find 别场景的物体：场景还没加载完，找不到。用 Start 或等场景就绪\n· 在 Start 里 GetComponent：比 Awake 晚，引用没拿到就报空。拿自己组件一律 Awake\n· SetActive(false) 后 Update 停止：以为还在跑，结果逻辑停摆——停用的物体不执行 Update/协程（但 OnDisable 会跑）\n· OnDestroy 里访问其他物体：可能已经被销毁，会空引用。OnDestroy 只管自己\n· 脚本 enabled=false 时：Update/Start 不跑，但 Awake/OnEnable 仍然跑——初始化照常，驱动逻辑停止\n\n一句话总结：Awake 拿自己的引用、Start 依赖别人、OnEnable/OnDisable 管开关和订阅、Update 每帧逻辑、LateUpdate 相机跟随、FixedUpdate 物理、OnDestroy 收尾——把初始化放对位置，九成生命周期 bug 当场消失。' },
    { type: 'component', title: '生命周期速查', items: [
      { name: 'Awake', desc: '创建即调用，拿自身引用、设单例' },
      { name: 'OnEnable / OnDisable', desc: '开关时刻，订阅退订、停协程' },
      { name: 'Start', desc: '依赖别人的初始化，所有物体 Awake 之后' },
      { name: 'Update / LateUpdate', desc: '每帧逻辑 / 相机跟随' },
      { name: 'FixedUpdate', desc: '固定频率，物理专用' },
      { name: 'OnDestroy', desc: '销毁前收尾：退订、保存' },
    ] },
    { type: 'quiz', question: '相机要「跟随」角色，角色每帧先动、相机后动才不抖。相机跟随代码最该放哪？', options: ['Awake', 'LateUpdate', 'FixedUpdate'], answer: 1, tip: 'LateUpdate 在 Update 之后执行——角色先跑完移动，相机再追上去，天然避免「角色还没动相机就追」的抖动。物理的 FixedUpdate 不适合每帧跟随逻辑。' },
  ],
  'prog-20': [
    { type: 'text', title: '编程综合动手练', body: '🎯 任务目标：把类/继承/事件/协程四样功夫串成一套小脚本系统——做一个「玩家 + 敌人 + 血条」的迷你演示：玩家能用协程做攻击冷却，敌人继承基类、被攻击后广播受伤事件，血条订阅事件自动刷新，再用一个工具类统一计算血量百分比。\n\n📦 前置要求：先学完类与对象（prog-1）、继承与多态（prog-2）、协程（prog-3/11）、事件（prog-15）、静态工具类（prog-16）。建议在空场景里用方块和球摆着测。\n\n📋 动手步骤（每步建一个 .cs）：\n\n第 1 步｜工具类：写一个静态类 MathHelper，封装血量百分比计算（prog-16 讲过纯函数进静态类）：\n\npublic static class MathHelper\n{\n    public static float HealthPercent(float hp, float maxHp)\n    {\n        return Mathf.Clamp01(hp / maxHp);   // 0~1，防除零溢出\n    }\n}\n\n第 2 步｜基类：写抽象基类 Unit（敌人玩家共用生命值、受伤广播事件、死亡处理）：\n\npublic abstract class Unit : MonoBehaviour\n{\n    public float maxHp = 100f;\n    public float hp;\n    public event System.Action<float, float> OnHpChanged;   // 事件：广播当前/最大血量\n\n    protected virtual void Awake()\n    {\n        hp = maxHp;\n    }\n\n    public virtual void TakeDamage(float amount)\n    {\n        hp -= amount;\n        hp = Mathf.Max(0f, hp);\n        OnHpChanged?.Invoke(hp, maxHp);     // 受伤就广播（?. 防空，prog-10 学过）\n        if (hp <= 0f) OnDeath();\n    }\n\n    protected abstract void OnDeath();\n}\n\n第 3 步｜玩家：继承 Unit，用协程做攻击冷却（prog-3/11 的 StartCoroutine + WaitForSeconds）：\n\npublic class Player : Unit\n{\n    public Unit target;\n    public float damage = 20f;\n    public float attackCooldown = 1f;\n    private bool canAttack = true;\n\n    void Update()\n    {\n        if (Input.GetKeyDown(KeyCode.Space) && canAttack && target != null)\n        {\n            target.TakeDamage(damage);\n            canAttack = false;\n            StartCoroutine(CooldownRoutine());\n        }\n    }\n\n    IEnumerator CooldownRoutine()\n    {\n        yield return new WaitForSeconds(attackCooldown);\n        canAttack = true;\n    }\n\n    protected override void OnDeath()\n    {\n        Debug.Log("玩家阵亡");\n    }\n}\n\n第 4 步｜敌人：继承 Unit，用 override 覆写死亡表现（给个销毁的占位）：\n\npublic class Enemy : Unit\n{\n    protected override void OnDeath()\n    {\n        Debug.Log("敌人被消灭");\n        Destroy(gameObject);    // 简单销毁，真实项目可换死亡动画\n    }\n}\n\n第 5 步｜血条：订阅 OnHpChanged 事件，收到广播就刷新文本（事件驱动，prog-15）：\n\nusing UnityEngine.UI;\n\npublic class HealthBar : MonoBehaviour\n{\n    public Unit unit;\n    public Text hpText;\n\n    void Start()\n    {\n        unit.OnHpChanged += OnHpChanged;    // 订阅\n        OnHpChanged(unit.hp, unit.maxHp);    // 先手动刷一次初始值\n    }\n\n    void OnDestroy()\n    {\n        unit.OnHpChanged -= OnHpChanged;    // 退订，别忘了\n    }\n\n    void OnHpChanged(float hp, float maxHp)\n    {\n        hpText.text = $"生命：{hp:F0} / {maxHp:F0}（{MathHelper.HealthPercent(hp, maxHp) * 100f:F0}%）";\n    }\n}\n\n第 6 步｜场景拼装：场景里放一个 Cube 挂 Player（把 target 拖到 Enemy 上），一个 Sphere 挂 Enemy，一个 Canvas 下放 Text 挂 HealthBar 脚本并把 unit 拖到 Player 上。\n\n第 7 步｜跑起来验证：按空格，Player 攻击 Enemy → Enemy 血掉、血条文本刷新 → Enemy 血空被 Destroy；冷却期间连按空格只有第一下生效。\n\n✅ 验收清单：\n能 □ MathHelper 是 static 类，HealthPercent 返回 0~1\n能 □ Unit 是抽象基类：有事件、TakeDamage 广播、OnDeath 抽象\n能 □ Player 和 Enemy 继承 Unit 且 override OnDeath\n能 □ 攻击冷却用协程实现：冷却中连按无效，冷却后恢复\n能 □ HealthBar 通过订阅事件刷新，OnDestroy 退订\n\n🕳 常见卡壳：\n· 事件订阅了没反应 → 检查 Start 里有没有 +=、广播方有没有调用 OnHpChanged?.Invoke、退订写没写\n· 冷却不生效 → canAttack 恢复的代码放对协程位置没，StartCoroutine 有没有真正被调用\n· 抽象方法没覆写编译报错 → OnDeath 前必须有 override，签名和基类一致\n· Destroy 后血条还在刷 → HealthBar 退订了没；Unit 销毁时订阅方要 -=，否则回调调「幽灵」报空引用\n\n一句话总结：一套小系统把四门功夫都用上——继承共享生命值、事件解耦血条、协程管冷却、静态类封装工具；能跑通这套，编程课的核心招式就算真正串起来了。' },
    { type: 'quiz', question: '这套系统里，HealthBar 拿到血量变化最干净的途径是？', options: ['每帧在 Update 里读 unit.hp', '订阅 unit.OnHpChanged 事件，被广播时才刷新', '让 HealthBar 每隔一秒定时刷新一次'], answer: 1, tip: '事件驱动的核心：变化方广播、关心方订阅。血条只在「血量真的变了」时被唤醒刷新，不用每帧轮询，也不用手动挨个调用——这就是 prog-15 的实战用法。' },
  ],
}
