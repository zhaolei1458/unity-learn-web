# Unity 学习工具（Web 版）换电脑运行指南

> 配合 `unity-learn-web.zip`（源码包）使用。新电脑解压后，按下面步骤就能跑起来继续开发。

## 一、需要安装的东西（仅一样）

**Node.js**（JavaScript 运行环境，跑项目必需）

- 下载：https://nodejs.org/zh-cn （选 **LTS 长期支持版**）
- 要求：**v18 或更高**（Vite 5 需要；装最新的 LTS 就没错）
- 装完验证：打开命令行输入
  ```
  node -v
  npm -v
  ```
  能打印出版本号（如 v20.x.x）就算装好了。

> 不需要装 Unity、不需要任何 AI 工具、不需要数据库。这是纯前端项目。

## 二、解压与安装依赖

1. 把 `unity-learn-web.zip` 解压到你想放的地方（比如 `D:\projects\unity-learn-web`）
2. 打开命令行，进到项目目录：
   ```
   cd D:\projects\unity-learn-web
   ```
3. 安装项目依赖（第一次必须，以后不用）：
   ```
   npm install
   ```
   会联网下载依赖包，需要几分钟。看到 `added X packages` 就是装好了。

> 如果 `npm install` 很慢或失败（国内网络），用淘宝镜像：
> ```
> npm install --registry=https://registry.npmmirror.com
> ```

## 三、启动开发（日常用这个）

```
npm run dev
```
看到 `Local: http://localhost:5173/` 后，浏览器打开 http://localhost:5173 就能用了。

改代码后浏览器会自动刷新（热更新），不用手动重启。

## 四、其他常用命令

| 命令 | 作用 |
|---|---|
| `npm run dev` | 本地开发（改代码实时生效） |
| `npm run build` | 构建发布版，产物在 `dist/` 文件夹 |
| `npm run preview` | 预览构建结果 |

## 五、常见问题

- **`npm install` 报错 `ERR!`** → 多半是网络问题，用淘宝镜像重试（见上）。
- **`node -v` 版本太低（v14/v16）** → 升级 Node 到 LTS 版。
- **打开页面空白** → 确认 `npm run dev` 在跑、地址是 5173，且命令行没报错。

## 六、这个项目的目录结构（新电脑想找东西时）

```
src/
├─ data/courses.js        ← 所有课程内容（想加课改这个文件）
├─ store/progress.js      ← 学习进度存储
├─ components/
│  ├─ HomeView.vue        ← 首页（课程卡）
│  ├─ CourseView.vue      ← 课程页（小节列表）
│  ├─ LessonView.vue      ← 内容页（卡片流程）
│  ├─ cards/              ← 卡片组件（讲解/测验/演示/图解）
│  └─ demos/              ← 互动演示组件（3D/2D）
└─ three/scene.js         ← 3D 场景工具
```

加课指南：改 `src/data/courses.js`，在 `courses` 数组加小节标题、在 `lessonCards` 加卡片内容即可，不用动其他代码。
